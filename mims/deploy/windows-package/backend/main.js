/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ([
/* 0 */,
/* 1 */
/***/ ((module) => {

module.exports = require("@nestjs/core");

/***/ }),
/* 2 */
/***/ ((module) => {

module.exports = require("@nestjs/common");

/***/ }),
/* 3 */
/***/ ((module) => {

module.exports = require("@nestjs/swagger");

/***/ }),
/* 4 */
/***/ ((module) => {

module.exports = require("@nestjs/config");

/***/ }),
/* 5 */
/***/ ((module) => {

module.exports = require("helmet");

/***/ }),
/* 6 */
/***/ ((module) => {

module.exports = require("compression");

/***/ }),
/* 7 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AppModule = void 0;
const common_1 = __webpack_require__(2);
const config_1 = __webpack_require__(4);
const throttler_1 = __webpack_require__(8);
const schedule_1 = __webpack_require__(9);
const core_1 = __webpack_require__(1);
const throttler_2 = __webpack_require__(8);
const common_module_1 = __webpack_require__(10);
const performance_interceptor_1 = __webpack_require__(37);
const audit_interceptor_1 = __webpack_require__(39);
const auth_module_1 = __webpack_require__(40);
const patients_module_1 = __webpack_require__(62);
const sync_module_1 = __webpack_require__(82);
const medicines_module_1 = __webpack_require__(85);
const inventory_module_1 = __webpack_require__(91);
const prescriptions_module_1 = __webpack_require__(99);
const issuance_module_1 = __webpack_require__(105);
const pharmacies_module_1 = __webpack_require__(111);
const users_module_1 = __webpack_require__(117);
const transfers_module_1 = __webpack_require__(122);
const reports_module_1 = __webpack_require__(127);
const analytics_module_1 = __webpack_require__(134);
const feature_flags_module_1 = __webpack_require__(138);
const permissions_module_1 = __webpack_require__(53);
const departments_module_1 = __webpack_require__(143);
const sub_departments_module_1 = __webpack_require__(148);
const clinics_module_1 = __webpack_require__(153);
const visits_module_1 = __webpack_require__(74);
const tokens_module_1 = __webpack_require__(160);
const referrals_module_1 = __webpack_require__(163);
const receipts_module_1 = __webpack_require__(170);
const payments_module_1 = __webpack_require__(177);
const patient_visits_module_1 = __webpack_require__(180);
const rooms_module_1 = __webpack_require__(184);
const beds_module_1 = __webpack_require__(190);
const admissions_module_1 = __webpack_require__(76);
const operations_module_1 = __webpack_require__(196);
const lab_tests_module_1 = __webpack_require__(208);
const lab_orders_module_1 = __webpack_require__(213);
const payroll_module_1 = __webpack_require__(223);
const expenditure_module_1 = __webpack_require__(228);
const attendance_module_1 = __webpack_require__(232);
const license_module_1 = __webpack_require__(258);
const database_module_1 = __webpack_require__(52);
const hospitals_module_1 = __webpack_require__(265);
const database_config_1 = __webpack_require__(272);
const app_config_1 = __webpack_require__(273);
let AppModule = class AppModule {
};
exports.AppModule = AppModule;
exports.AppModule = AppModule = __decorate([
    (0, common_1.Module)({
        imports: [
            license_module_1.LicenseModule,
            config_1.ConfigModule.forRoot({
                isGlobal: true,
                load: [database_config_1.default, app_config_1.default],
                envFilePath: ['.env.local', '.env'],
            }),
            throttler_1.ThrottlerModule.forRoot([
                {
                    ttl: 60000,
                    limit: 100,
                },
            ]),
            schedule_1.ScheduleModule.forRoot(),
            database_module_1.DatabaseModule,
            common_module_1.CommonModule,
            auth_module_1.AuthModule,
            patients_module_1.PatientsModule,
            sync_module_1.SyncModule,
            medicines_module_1.MedicinesModule,
            inventory_module_1.InventoryModule,
            prescriptions_module_1.PrescriptionsModule,
            issuance_module_1.IssuanceModule,
            hospitals_module_1.HospitalsModule,
            pharmacies_module_1.PharmaciesModule,
            users_module_1.UsersModule,
            transfers_module_1.TransfersModule,
            reports_module_1.ReportsModule,
            analytics_module_1.AnalyticsModule,
            feature_flags_module_1.FeatureFlagsModule,
            permissions_module_1.PermissionsModule,
            departments_module_1.DepartmentsModule,
            sub_departments_module_1.SubDepartmentsModule,
            clinics_module_1.ClinicsModule,
            visits_module_1.VisitsModule,
            tokens_module_1.TokensModule,
            referrals_module_1.ReferralsModule,
            receipts_module_1.ReceiptsModule,
            payments_module_1.PaymentsModule,
            patient_visits_module_1.PatientVisitsModule,
            rooms_module_1.RoomsModule,
            beds_module_1.BedsModule,
            admissions_module_1.AdmissionsModule,
            operations_module_1.OperationsModule,
            lab_tests_module_1.LabTestsModule,
            lab_orders_module_1.LabOrdersModule,
            attendance_module_1.AttendanceModule,
            payroll_module_1.PayrollModule,
            expenditure_module_1.ExpenditureModule,
        ],
        providers: [
            {
                provide: core_1.APP_GUARD,
                useClass: throttler_2.ThrottlerGuard,
            },
            {
                provide: core_1.APP_INTERCEPTOR,
                useClass: performance_interceptor_1.PerformanceInterceptor,
            },
            {
                provide: core_1.APP_INTERCEPTOR,
                useClass: audit_interceptor_1.AuditInterceptor,
            },
        ],
    })
], AppModule);


/***/ }),
/* 8 */
/***/ ((module) => {

module.exports = require("@nestjs/throttler");

/***/ }),
/* 9 */
/***/ ((module) => {

module.exports = require("@nestjs/schedule");

/***/ }),
/* 10 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CommonModule = void 0;
const common_1 = __webpack_require__(2);
const cache_service_1 = __webpack_require__(11);
const redis_service_1 = __webpack_require__(12);
const audit_service_1 = __webpack_require__(14);
const encryption_service_1 = __webpack_require__(18);
const entity_encryption_service_1 = __webpack_require__(20);
const audit_log_viewer_service_1 = __webpack_require__(21);
const threat_detection_service_1 = __webpack_require__(22);
const alert_service_1 = __webpack_require__(23);
const dashboard_service_1 = __webpack_require__(24);
const export_service_1 = __webpack_require__(25);
const compliance_service_1 = __webpack_require__(26);
const audit_module_1 = __webpack_require__(27);
let CommonModule = class CommonModule {
};
exports.CommonModule = CommonModule;
exports.CommonModule = CommonModule = __decorate([
    (0, common_1.Global)(),
    (0, common_1.Module)({
        imports: [audit_module_1.AuditModule],
        providers: [
            cache_service_1.CacheService,
            redis_service_1.RedisService,
            audit_service_1.AuditService,
            encryption_service_1.EncryptionService,
            entity_encryption_service_1.EntityEncryptionService,
            audit_log_viewer_service_1.AuditLogViewerService,
            threat_detection_service_1.ThreatDetectionService,
            alert_service_1.AlertService,
            dashboard_service_1.DashboardService,
            export_service_1.ExportService,
            compliance_service_1.ComplianceService,
        ],
        exports: [
            cache_service_1.CacheService,
            redis_service_1.RedisService,
            audit_service_1.AuditService,
            encryption_service_1.EncryptionService,
            entity_encryption_service_1.EntityEncryptionService,
            audit_log_viewer_service_1.AuditLogViewerService,
            threat_detection_service_1.ThreatDetectionService,
            alert_service_1.AlertService,
            dashboard_service_1.DashboardService,
            export_service_1.ExportService,
            compliance_service_1.ComplianceService,
        ],
    })
], CommonModule);


/***/ }),
/* 11 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var CacheService_1;
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CacheService = void 0;
const common_1 = __webpack_require__(2);
const redis_service_1 = __webpack_require__(12);
let CacheService = CacheService_1 = class CacheService {
    constructor(redis) {
        this.redis = redis;
        this.logger = new common_1.Logger(CacheService_1.name);
        this.cache = new Map();
        this.defaultTTL = 5 * 60 * 1000;
        this.cacheHits = 0;
        this.cacheMisses = 0;
    }
    async get(key) {
        const redisValue = await this.redis.get(key);
        if (redisValue !== null) {
            this.cacheHits++;
            this.logger.debug(`Redis cache hit for key: ${key}`);
            return redisValue;
        }
        const entry = this.cache.get(key);
        if (!entry) {
            this.cacheMisses++;
            return null;
        }
        if (Date.now() > entry.expiresAt) {
            this.cache.delete(key);
            this.cacheMisses++;
            this.logger.debug(`Cache expired for key: ${key}`);
            return null;
        }
        this.cacheHits++;
        this.logger.debug(`In-memory cache hit for key: ${key}`);
        return entry.data;
    }
    async set(key, data, ttl) {
        const ttlMs = ttl || this.defaultTTL;
        const ttlSeconds = Math.floor(ttlMs / 1000);
        await this.redis.set(key, data, ttlSeconds);
        const expiresAt = Date.now() + ttlMs;
        this.cache.set(key, { data, expiresAt });
        this.logger.debug(`Cache set for key: ${key}, expires in ${ttlMs}ms`);
    }
    async delete(key) {
        await this.redis.del(key);
        const result = this.cache.delete(key);
        if (result) {
            this.logger.debug(`Cache deleted for key: ${key}`);
        }
        return result;
    }
    async deletePattern(pattern) {
        let count = 0;
        const regex = new RegExp(pattern);
        for (const key of this.cache.keys()) {
            if (regex.test(key)) {
                await this.redis.del(key);
                this.cache.delete(key);
                count++;
            }
        }
        if (count > 0) {
            this.logger.debug(`Deleted ${count} cache entries matching pattern: ${pattern}`);
        }
        return count;
    }
    clear() {
        this.cache.clear();
        this.logger.debug('Cache cleared');
    }
    getStats() {
        const hitRate = this.cacheHits + this.cacheMisses > 0
            ? (this.cacheHits / (this.cacheHits + this.cacheMisses) * 100).toFixed(2)
            : 0;
        return {
            size: this.cache.size,
            keys: Array.from(this.cache.keys()),
            hits: this.cacheHits,
            misses: this.cacheMisses,
            hitRate: `${hitRate}%`,
        };
    }
    cleanup() {
        let count = 0;
        const now = Date.now();
        for (const [key, entry] of this.cache.entries()) {
            if (now > entry.expiresAt) {
                this.cache.delete(key);
                count++;
            }
        }
        if (count > 0) {
            this.logger.debug(`Cleaned up ${count} expired cache entries`);
        }
        return count;
    }
};
exports.CacheService = CacheService;
exports.CacheService = CacheService = CacheService_1 = __decorate([
    (0, common_1.Injectable)(),
    __param(0, (0, common_1.Inject)((0, common_1.forwardRef)(() => redis_service_1.RedisService))),
    __metadata("design:paramtypes", [typeof (_a = typeof redis_service_1.RedisService !== "undefined" && redis_service_1.RedisService) === "function" ? _a : Object])
], CacheService);


/***/ }),
/* 12 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var RedisService_1;
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RedisService = void 0;
const common_1 = __webpack_require__(2);
const config_1 = __webpack_require__(4);
let RedisService = RedisService_1 = class RedisService {
    constructor(configService) {
        this.configService = configService;
        this.logger = new common_1.Logger(RedisService_1.name);
        this.isConnected = false;
    }
    async onModuleInit() {
        try {
            const redis = await Promise.resolve().then(() => __webpack_require__(13));
            const redisUrl = this.configService.get('REDIS_URL') || 'redis://localhost:6379';
            this.client = redis.createClient({
                url: redisUrl,
            });
            this.client.on('error', (err) => {
                this.logger.error('Redis connection error:', err);
                this.isConnected = false;
            });
            this.client.on('connect', () => {
                this.logger.log('Redis connected successfully');
                this.isConnected = true;
            });
            await this.client.connect();
        }
        catch (error) {
            this.logger.warn('Redis not available, using in-memory cache fallback');
            this.isConnected = false;
        }
    }
    async get(key) {
        if (!this.isConnected || !this.client) {
            return null;
        }
        try {
            const value = await this.client.get(key);
            return value ? JSON.parse(value) : null;
        }
        catch (error) {
            this.logger.error(`Redis GET error for key ${key}:`, error);
            return null;
        }
    }
    async set(key, value, ttlSeconds) {
        if (!this.isConnected || !this.client) {
            return;
        }
        try {
            const stringValue = JSON.stringify(value);
            if (ttlSeconds) {
                await this.client.setEx(key, ttlSeconds, stringValue);
            }
            else {
                await this.client.set(key, stringValue);
            }
        }
        catch (error) {
            this.logger.error(`Redis SET error for key ${key}:`, error);
        }
    }
    async del(key) {
        if (!this.isConnected || !this.client) {
            return;
        }
        try {
            await this.client.del(key);
        }
        catch (error) {
            this.logger.error(`Redis DEL error for key ${key}:`, error);
        }
    }
    async flushAll() {
        if (!this.isConnected || !this.client) {
            return;
        }
        try {
            await this.client.flushAll();
            this.logger.log('Redis cache flushed');
        }
        catch (error) {
            this.logger.error('Redis FLUSHALL error:', error);
        }
    }
};
exports.RedisService = RedisService;
exports.RedisService = RedisService = RedisService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof config_1.ConfigService !== "undefined" && config_1.ConfigService) === "function" ? _a : Object])
], RedisService);


/***/ }),
/* 13 */
/***/ ((module) => {

module.exports = require("redis");

/***/ }),
/* 14 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuditService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const core_1 = __webpack_require__(1);
const express_1 = __webpack_require__(17);
let AuditService = class AuditService {
    constructor(prisma, request) {
        this.prisma = prisma;
        this.request = request;
    }
    async log(payload) {
        const ipAddress = this.getClientIp();
        const userAgent = this.request.headers['user-agent'] || 'Unknown';
        return await this.prisma.auditLog.create({
            data: {
                userId: payload.userId,
                hospitalId: payload.hospitalId,
                action: payload.action,
                entityType: payload.entityType,
                entityId: payload.entityId,
                beforeState: payload.beforeState || null,
                afterState: payload.afterState || null,
                ipAddress,
                userAgent: userAgent,
                timestamp: new Date(),
            },
        });
    }
    async getEntityHistory(entityType, entityId, limit = 100) {
        return await this.prisma.auditLog.findMany({
            where: {
                entityType,
                entityId,
            },
            orderBy: {
                timestamp: 'desc',
            },
            take: limit,
            include: {
                user: {
                    select: {
                        id: true,
                        email: true,
                        fullName: true,
                    },
                },
            },
        });
    }
    async getUserActivity(userId, hospitalId, limit = 100) {
        return await this.prisma.auditLog.findMany({
            where: {
                userId,
                hospitalId,
            },
            orderBy: {
                timestamp: 'desc',
            },
            take: limit,
            include: {
                user: {
                    select: {
                        id: true,
                        email: true,
                        fullName: true,
                    },
                },
            },
        });
    }
    async getSensitiveOperations(hospitalId, startDate, endDate, limit = 1000) {
        return await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                action: {
                    in: ['CREATE', 'UPDATE', 'DELETE'],
                },
                timestamp: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            orderBy: {
                timestamp: 'desc',
            },
            take: limit,
            include: {
                user: {
                    select: {
                        id: true,
                        email: true,
                        fullName: true,
                        role: true,
                    },
                },
            },
        });
    }
    async getSuspiciousActivity(hospitalId, threshold = 5) {
        const oneHourAgo = new Date(Date.now() - 60 * 60 * 1000);
        return await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                action: 'FAILED_LOGIN',
                timestamp: {
                    gte: oneHourAgo,
                },
            },
            orderBy: {
                timestamp: 'desc',
            },
        });
    }
    getClientIp() {
        const forwarded = this.request.headers['x-forwarded-for'];
        if (typeof forwarded === 'string') {
            return forwarded.split(',')[0].trim();
        }
        return (this.request.headers['x-real-ip'] ||
            this.request.socket.remoteAddress ||
            'Unknown');
    }
};
exports.AuditService = AuditService;
exports.AuditService = AuditService = __decorate([
    (0, common_1.Injectable)(),
    __param(1, (0, common_1.Inject)(core_1.REQUEST)),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof express_1.Request !== "undefined" && express_1.Request) === "function" ? _b : Object])
], AuditService);


/***/ }),
/* 15 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var PrismaService_1;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PrismaService = void 0;
const common_1 = __webpack_require__(2);
const client_1 = __webpack_require__(16);
let PrismaService = PrismaService_1 = class PrismaService extends client_1.PrismaClient {
    constructor() {
        super({
            log: [
                { level: 'query', emit: 'event' },
                { level: 'error', emit: 'stdout' },
                { level: 'warn', emit: 'stdout' },
            ],
            datasources: {
                db: {
                    url: process.env.DATABASE_URL,
                },
            },
        });
        this.logger = new common_1.Logger(PrismaService_1.name);
        if (process.env.NODE_ENV === 'development') {
            this.$on('query', (e) => {
                if (e.duration > 1000) {
                    this.logger.warn(`⚠️ Slow query (${e.duration}ms): ${e.query}`);
                }
                else {
                    this.logger.debug(`Query: ${e.query} (${e.duration}ms)`);
                }
            });
        }
    }
    async onModuleInit() {
        try {
            await this.$connect();
            this.logger.log('✅ Database connected successfully');
            this.logger.log(`📊 Connection pool: See DATABASE_URL for pool settings`);
        }
        catch (error) {
            this.logger.error('❌ Database connection failed', error);
            throw error;
        }
    }
    async onModuleDestroy() {
        await this.$disconnect();
        this.logger.log('Database disconnected');
    }
    async enableRLS(hospitalId) {
        await this.$executeRaw `SELECT set_config('app.current_hospital_id', ${hospitalId}, true)`;
    }
    async cleanDatabase() {
        if (process.env.NODE_ENV === 'production') {
            throw new Error('Cannot clean database in production');
        }
        const models = Reflect.ownKeys(this).filter((key) => typeof key === 'string' && key[0] !== '_' && key[0] !== '$');
        return Promise.all(models.map((modelKey) => {
            if (this[modelKey]?.deleteMany) {
                return this[modelKey].deleteMany();
            }
        }));
    }
};
exports.PrismaService = PrismaService;
exports.PrismaService = PrismaService = PrismaService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [])
], PrismaService);


/***/ }),
/* 16 */
/***/ ((module) => {

module.exports = require("@prisma/client");

/***/ }),
/* 17 */
/***/ ((module) => {

module.exports = require("express");

/***/ }),
/* 18 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.EncryptionService = void 0;
const common_1 = __webpack_require__(2);
const config_1 = __webpack_require__(4);
const crypto = __webpack_require__(19);
let EncryptionService = class EncryptionService {
    constructor(configService) {
        this.configService = configService;
        this.algorithm = 'aes-256-gcm';
        this.saltLength = 16;
        this.ivLength = 16;
        this.tagLength = 16;
        const encryptionKey = this.configService.get('ENCRYPTION_KEY', 'default-dev-key-change-in-production-immediately');
        this.key = crypto.pbkdf2Sync(encryptionKey, 'salt', 100000, 32, 'sha256');
    }
    encrypt(plaintext) {
        const iv = crypto.randomBytes(this.ivLength);
        const cipher = crypto.createCipheriv(this.algorithm, this.key, iv);
        let encrypted = cipher.update(plaintext, 'utf8', 'hex');
        encrypted += cipher.final('hex');
        const authTag = cipher.getAuthTag();
        return `${iv.toString('hex')}:${authTag.toString('hex')}:${encrypted}`;
    }
    decrypt(encryptedData) {
        try {
            const parts = encryptedData.split(':');
            if (parts.length !== 3) {
                throw new Error('Invalid encrypted data format');
            }
            const [ivHex, authTagHex, ciphertextHex] = parts;
            const iv = Buffer.from(ivHex, 'hex');
            const authTag = Buffer.from(authTagHex, 'hex');
            const ciphertext = Buffer.from(ciphertextHex, 'hex');
            if (iv.length !== this.ivLength) {
                throw new Error('Invalid IV length');
            }
            if (authTag.length !== this.tagLength) {
                throw new Error('Invalid auth tag length');
            }
            const decipher = crypto.createDecipheriv(this.algorithm, this.key, iv);
            decipher.setAuthTag(authTag);
            let decrypted = decipher.update(ciphertext).toString('utf8');
            decrypted += decipher.final('utf8');
            return decrypted;
        }
        catch (error) {
            console.error('[ENCRYPTION_ERROR]', error.message);
            throw new Error('Decryption failed - data may have been tampered with');
        }
    }
    hash(data) {
        return crypto.createHash('sha256').update(data).digest('hex');
    }
    verifyHash(plaintext, hash) {
        const computed = this.hash(plaintext);
        return crypto.timingSafeEqual(Buffer.from(computed), Buffer.from(hash));
    }
    generateRandomToken(length = 32) {
        return crypto.randomBytes(length).toString('hex');
    }
};
exports.EncryptionService = EncryptionService;
exports.EncryptionService = EncryptionService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof config_1.ConfigService !== "undefined" && config_1.ConfigService) === "function" ? _a : Object])
], EncryptionService);


/***/ }),
/* 19 */
/***/ ((module) => {

module.exports = require("crypto");

/***/ }),
/* 20 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.EntityEncryptionService = void 0;
const common_1 = __webpack_require__(2);
const encryption_service_1 = __webpack_require__(18);
let EntityEncryptionService = class EntityEncryptionService {
    constructor(encryptionService) {
        this.encryptionService = encryptionService;
    }
    async encryptEntity(entity, encryptedFields) {
        const encrypted = { ...entity };
        for (const field of encryptedFields) {
            if (encrypted[field] && typeof encrypted[field] === 'string') {
                try {
                    encrypted[field] = this.encryptionService.encrypt(encrypted[field]);
                }
                catch (error) {
                    console.error(`[ENCRYPTION_ERROR] Failed to encrypt field: ${field}`, error);
                    throw new Error(`Encryption failed for field: ${field}`);
                }
            }
        }
        return encrypted;
    }
    async decryptEntity(entity, encryptedFields) {
        const decrypted = { ...entity };
        for (const field of encryptedFields) {
            if (decrypted[field] && typeof decrypted[field] === 'string') {
                try {
                    if (decrypted[field].includes(':')) {
                        decrypted[field] = this.encryptionService.decrypt(decrypted[field]);
                    }
                }
                catch (error) {
                    console.error(`[DECRYPTION_ERROR] Failed to decrypt field: ${field}`, error);
                }
            }
        }
        return decrypted;
    }
    async encryptEntities(entities, encryptedFields) {
        return Promise.all(entities.map((entity) => this.encryptEntity(entity, encryptedFields)));
    }
    async decryptEntities(entities, encryptedFields) {
        return Promise.all(entities.map((entity) => this.decryptEntity(entity, encryptedFields)));
    }
    encryptField(value) {
        return this.encryptionService.encrypt(value);
    }
    decryptField(encryptedValue) {
        return this.encryptionService.decrypt(encryptedValue);
    }
    isEncrypted(value) {
        if (typeof value !== 'string')
            return false;
        const parts = value.split(':');
        return parts.length === 3 && parts[0].length === 32 && parts[1].length === 32;
    }
    getEncryptedDataHash(encryptedValue) {
        return this.encryptionService.hash(encryptedValue);
    }
    verifyEncryptedDataIntegrity(encryptedValue, storedHash) {
        const currentHash = this.getEncryptedDataHash(encryptedValue);
        return this.encryptionService.verifyHash(currentHash, storedHash);
    }
};
exports.EntityEncryptionService = EntityEncryptionService;
exports.EntityEncryptionService = EntityEncryptionService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof encryption_service_1.EncryptionService !== "undefined" && encryption_service_1.EncryptionService) === "function" ? _a : Object])
], EntityEncryptionService);


/***/ }),
/* 21 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuditLogViewerService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let AuditLogViewerService = class AuditLogViewerService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async getAuditLogs(filters, pagination) {
        const limit = Math.min(pagination.limit || 50, 500);
        const where = this.buildWhereClause(filters);
        const cursorCondition = pagination.cursor
            ? { id: pagination.cursor }
            : undefined;
        const logs = await this.prisma.auditLog.findMany({
            where,
            skip: pagination.cursor ? 1 : 0,
            cursor: cursorCondition,
            take: limit + 1,
            orderBy: { timestamp: 'desc' },
        });
        const hasMore = logs.length > limit;
        const results = hasMore ? logs.slice(0, -1) : logs;
        const nextCursor = hasMore && results.length > 0
            ? results[results.length - 1].id
            : null;
        const total = await this.prisma.auditLog.count({ where });
        return {
            logs: results,
            nextCursor,
            hasMore,
            total,
        };
    }
    async getAuditLogById(id) {
        return this.prisma.auditLog.findUnique({
            where: { id },
        });
    }
    async getEntityHistory(entityType, entityId) {
        return this.prisma.auditLog.findMany({
            where: {
                entityType,
                entityId,
            },
            orderBy: { timestamp: 'desc' },
        });
    }
    async getUserActivity(userId, hospitalId, days = 30) {
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - days);
        return this.prisma.auditLog.findMany({
            where: {
                userId,
                hospitalId,
                timestamp: { gte: startDate },
            },
            orderBy: { timestamp: 'desc' },
        });
    }
    async getSensitiveOperations(hospitalId, days = 30) {
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - days);
        const sensitiveEntities = [
            'BiometricEnrollment',
            'User',
            'Permission',
            'Role',
            'MedicineInventory',
            'InventoryTransfer',
        ];
        return this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                entityType: { in: sensitiveEntities },
                action: { in: ['CREATE', 'UPDATE', 'DELETE'] },
                timestamp: { gte: startDate },
            },
            orderBy: { timestamp: 'desc' },
        });
    }
    async getSuspiciousActivity(hospitalId, days = 7) {
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - days);
        return this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                action: 'DELETE',
                timestamp: { gte: startDate },
            },
            orderBy: { timestamp: 'desc' },
        });
    }
    async getAuditStatistics(hospitalId, days = 30) {
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - days);
        const logs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                timestamp: { gte: startDate },
            },
            select: {
                action: true,
                userId: true,
                entityType: true,
            },
        });
        const operationsByType = {};
        const operationsByUser = {};
        const operationsByEntity = {};
        for (const log of logs) {
            operationsByType[log.action] = (operationsByType[log.action] || 0) + 1;
            operationsByUser[log.userId] = (operationsByUser[log.userId] || 0) + 1;
            operationsByEntity[log.entityType] =
                (operationsByEntity[log.entityType] || 0) + 1;
        }
        const sensitiveEntities = [
            'BiometricEnrollment',
            'User',
            'Permission',
            'Role',
        ];
        const sensitiveCount = logs.filter((l) => sensitiveEntities.includes(l.entityType) &&
            ['CREATE', 'UPDATE', 'DELETE'].includes(l.action)).length;
        return {
            totalOperations: logs.length,
            operationsByType,
            operationsByUser,
            operationsByEntity,
            sensitiveOperations: sensitiveCount,
        };
    }
    buildWhereClause(filters) {
        const where = {};
        if (filters.userId) {
            where.userId = filters.userId;
        }
        if (filters.hospitalId) {
            where.hospitalId = filters.hospitalId;
        }
        if (filters.entityType) {
            where.entityType = filters.entityType;
        }
        if (filters.action) {
            where.action = filters.action;
        }
        if (filters.startDate || filters.endDate) {
            where.timestamp = {};
            if (filters.startDate) {
                where.timestamp.gte = filters.startDate;
            }
            if (filters.endDate) {
                where.timestamp.lte = filters.endDate;
            }
        }
        if (filters.searchText) {
            where.OR = [
                { userId: { contains: filters.searchText, mode: 'insensitive' } },
                { entityType: { contains: filters.searchText, mode: 'insensitive' } },
                { entityId: { contains: filters.searchText, mode: 'insensitive' } },
                { ipAddress: { contains: filters.searchText, mode: 'insensitive' } },
            ];
        }
        return where;
    }
};
exports.AuditLogViewerService = AuditLogViewerService;
exports.AuditLogViewerService = AuditLogViewerService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], AuditLogViewerService);


/***/ }),
/* 22 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var ThreatDetectionService_1;
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ThreatDetectionService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const audit_log_viewer_service_1 = __webpack_require__(21);
let ThreatDetectionService = ThreatDetectionService_1 = class ThreatDetectionService {
    constructor(prisma, auditLogViewerService) {
        this.prisma = prisma;
        this.auditLogViewerService = auditLogViewerService;
        this.logger = new common_1.Logger(ThreatDetectionService_1.name);
        this.failedLoginAttempts = new Map();
        this.bulkOperationWindow = 60;
    }
    async trackFailedLoginAttempt(userId, hospitalId, ipAddress, userAgent, reason) {
        const key = `${userId}:${hospitalId}`;
        const now = new Date();
        if (!this.failedLoginAttempts.has(key)) {
            this.failedLoginAttempts.set(key, []);
        }
        const attempts = this.failedLoginAttempts.get(key);
        const fiveMinutesAgo = new Date(now.getTime() - 5 * 60 * 1000);
        const recentAttempts = attempts.filter((a) => a.timestamp > fiveMinutesAgo);
        recentAttempts.push({
            userId,
            hospitalId,
            ipAddress,
            userAgent,
            timestamp: now,
            reason,
        });
        this.failedLoginAttempts.set(key, recentAttempts);
        if (recentAttempts.length >= 3) {
            const alert = {
                type: 'FAILED_LOGIN_ATTEMPTS',
                severity: recentAttempts.length >= 5 ? 'CRITICAL' : 'HIGH',
                userId,
                hospitalId,
                description: `${recentAttempts.length} failed login attempts detected for user ${userId}`,
                details: {
                    attemptCount: recentAttempts.length,
                    timeWindowMinutes: 5,
                    ipAddresses: [...new Set(recentAttempts.map((a) => a.ipAddress))],
                    reasons: [...new Set(recentAttempts.map((a) => a.reason))],
                    firstAttempt: recentAttempts[0].timestamp,
                    lastAttempt: recentAttempts[recentAttempts.length - 1].timestamp,
                },
                timestamp: now,
                requiresAction: recentAttempts.length >= 5,
            };
            this.logger.warn(`🚨 THREAT DETECTED: ${alert.description}`);
            return alert;
        }
        return null;
    }
    async detectBulkOperations(hospitalId, userId) {
        const now = new Date();
        const oneMinuteAgo = new Date(now.getTime() - this.bulkOperationWindow * 1000);
        const logs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                ...(userId && { userId }),
                timestamp: { gte: oneMinuteAgo },
            },
            select: {
                userId: true,
                entityType: true,
                action: true,
                timestamp: true,
            },
        });
        const threshold = 100;
        if (logs.length > threshold) {
            const entityTypes = [...new Set(logs.map((l) => l.entityType))];
            const bulkUserId = userId || 'UNKNOWN';
            const alert = {
                type: 'BULK_OPERATIONS',
                severity: logs.length > 500 ? 'CRITICAL' : 'HIGH',
                userId: bulkUserId,
                hospitalId,
                description: `Bulk operations detected: ${logs.length} operations in 60 seconds`,
                details: {
                    operationCount: logs.length,
                    threshold,
                    timeWindowSeconds: this.bulkOperationWindow,
                    entityTypes,
                    timeRange: {
                        start: oneMinuteAgo,
                        end: now,
                    },
                },
                timestamp: now,
                requiresAction: logs.length > 500,
            };
            this.logger.warn(`🚨 THREAT DETECTED: ${alert.description}`);
            return alert;
        }
        return null;
    }
    async detectPermissionEscalation(hospitalId, lookbackMinutes = 30) {
        const alerts = [];
        const cutoffTime = new Date(Date.now() - lookbackMinutes * 60 * 1000);
        const permissionLogs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                entityType: { in: ['Permission', 'Role'] },
                action: 'UPDATE',
                timestamp: { gte: cutoffTime },
            },
            select: {
                userId: true,
                entityId: true,
                entityType: true,
                beforeState: true,
                afterState: true,
                timestamp: true,
            },
        });
        const userPermissionChanges = new Map();
        for (const log of permissionLogs) {
            const key = log.userId;
            if (!userPermissionChanges.has(key)) {
                userPermissionChanges.set(key, []);
            }
            userPermissionChanges.get(key).push(log);
        }
        const now = new Date();
        for (const [userId, changes] of userPermissionChanges.entries()) {
            if (changes.length >= 5) {
                const alert = {
                    type: 'PERMISSION_ESCALATION',
                    severity: changes.length >= 10 ? 'CRITICAL' : 'HIGH',
                    userId,
                    hospitalId,
                    description: `Permission escalation attempt: ${userId} modified ${changes.length} permissions/roles`,
                    details: {
                        changeCount: changes.length,
                        timeWindowMinutes: lookbackMinutes,
                        entityTypes: [...new Set(changes.map((c) => c.entityType))],
                        entities: changes.map((c) => c.entityId),
                        timeRange: {
                            start: cutoffTime,
                            end: now,
                        },
                    },
                    timestamp: now,
                    requiresAction: changes.length >= 10,
                };
                this.logger.warn(`🚨 THREAT DETECTED: ${alert.description}`);
                alerts.push(alert);
            }
        }
        return alerts;
    }
    async detectSuspiciousIP(hospitalId, userId, currentIP, lookbackHours = 24) {
        const cutoffTime = new Date(Date.now() - lookbackHours * 60 * 60 * 1000);
        const logs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                userId,
                timestamp: { gte: cutoffTime },
            },
            select: {
                ipAddress: true,
                timestamp: true,
            },
        });
        const uniqueIPs = [...new Set(logs.map((l) => l.ipAddress).filter(Boolean))];
        const ipThreshold = 5;
        if (uniqueIPs.length >= ipThreshold) {
            const now = new Date();
            const alert = {
                type: 'SUSPICIOUS_IP',
                severity: uniqueIPs.length >= 10 ? 'CRITICAL' : 'MEDIUM',
                userId,
                hospitalId,
                description: `Suspicious activity: ${userId} accessing from ${uniqueIPs.length} different IP addresses`,
                details: {
                    ipCount: uniqueIPs.length,
                    threshold: ipThreshold,
                    ips: uniqueIPs,
                    currentIP,
                    timeWindowHours: lookbackHours,
                },
                timestamp: now,
                requiresAction: uniqueIPs.length >= 10,
            };
            this.logger.warn(`🚨 THREAT DETECTED: ${alert.description}`);
            return alert;
        }
        return null;
    }
    async comprehensiveThreatScan(hospitalId) {
        const alerts = [];
        try {
            const bulkOpAlert = await this.detectBulkOperations(hospitalId);
            if (bulkOpAlert) {
                alerts.push(bulkOpAlert);
            }
            const permissionAlerts = await this.detectPermissionEscalation(hospitalId);
            alerts.push(...permissionAlerts);
            this.logger.log(`Threat scan completed for ${hospitalId}: ${alerts.length} alerts detected`);
        }
        catch (error) {
            this.logger.error(`Threat scan failed for ${hospitalId}: ${error.message}`);
        }
        return alerts;
    }
    async getThreatSummary(hospitalId) {
        try {
            const alerts = await this.prisma.threatAlert.findMany({
                where: {
                    hospitalId,
                    createdAt: { gte: new Date(Date.now() - 24 * 60 * 60 * 1000) },
                },
                select: {
                    severity: true,
                    requiresAction: true,
                },
            });
            const summary = {
                criticalAlerts: alerts.filter((a) => a.severity === 'CRITICAL').length,
                highAlerts: alerts.filter((a) => a.severity === 'HIGH').length,
                mediumAlerts: alerts.filter((a) => a.severity === 'MEDIUM').length,
                lowAlerts: alerts.filter((a) => a.severity === 'LOW').length,
                totalAlerts: alerts.length,
                requiresImmediateAction: alerts.filter((a) => a.requiresAction).length,
            };
            return summary;
        }
        catch (error) {
            this.logger.error(`Failed to get threat summary: ${error.message}`);
            return {
                criticalAlerts: 0,
                highAlerts: 0,
                mediumAlerts: 0,
                lowAlerts: 0,
                totalAlerts: 0,
                requiresImmediateAction: 0,
            };
        }
    }
    resetFailedLoginAttempts(userId, hospitalId) {
        const key = `${userId}:${hospitalId}`;
        this.failedLoginAttempts.delete(key);
    }
    getFailedLoginAttemptCount(userId, hospitalId) {
        const key = `${userId}:${hospitalId}`;
        const attempts = this.failedLoginAttempts.get(key) || [];
        const fiveMinutesAgo = new Date(Date.now() - 5 * 60 * 1000);
        return attempts.filter((a) => a.timestamp > fiveMinutesAgo).length;
    }
};
exports.ThreatDetectionService = ThreatDetectionService;
exports.ThreatDetectionService = ThreatDetectionService = ThreatDetectionService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof audit_log_viewer_service_1.AuditLogViewerService !== "undefined" && audit_log_viewer_service_1.AuditLogViewerService) === "function" ? _b : Object])
], ThreatDetectionService);


/***/ }),
/* 23 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var AlertService_1;
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AlertService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let AlertService = AlertService_1 = class AlertService {
    constructor(prisma) {
        this.prisma = prisma;
        this.logger = new common_1.Logger(AlertService_1.name);
        this.alerts = new Map();
    }
    async createAlert(threat, adminId) {
        const notification = {
            id: this.generateAlertId(),
            hospitalId: threat.hospitalId,
            adminId,
            alertType: threat.type,
            severity: threat.severity,
            title: this.generateAlertTitle(threat.type, threat.severity),
            message: threat.description,
            actionRequired: threat.requiresAction,
            read: false,
            createdAt: threat.timestamp,
        };
        const key = threat.hospitalId;
        if (!this.alerts.has(key)) {
            this.alerts.set(key, []);
        }
        this.alerts.get(key).push(notification);
        try {
            await this.prisma.threatAlert.create({
                data: {
                    hospitalId: threat.hospitalId,
                    userId: threat.userId,
                    alertType: threat.type,
                    severity: threat.severity,
                    description: threat.description,
                    details: threat.details,
                    requiresAction: threat.requiresAction,
                    read: false,
                },
            });
        }
        catch (error) {
            this.logger.error(`Failed to persist alert: ${error.message}`);
        }
        this.logger.log(`Alert created: ${notification.alertType} (${notification.severity}) for ${notification.hospitalId}`);
        return notification;
    }
    async getUnreadAlerts(hospitalId) {
        const alerts = this.alerts.get(hospitalId) || [];
        return alerts.filter((a) => !a.read);
    }
    async getAlerts(hospitalId, options = {}) {
        const limit = Math.min(options.limit || 50, 500);
        const offset = options.offset || 0;
        try {
            const where = { hospitalId };
            if (options.severity) {
                where.severity = options.severity;
            }
            if (options.unreadOnly) {
                where.read = false;
            }
            const alerts = await this.prisma.threatAlert.findMany({
                where,
                orderBy: { createdAt: 'desc' },
                skip: offset,
                take: limit,
            });
            const total = await this.prisma.threatAlert.count({ where });
            return {
                alerts: alerts.map((a) => this.mapToNotification(a)),
                total,
                limit,
                offset,
            };
        }
        catch (error) {
            this.logger.error(`Failed to retrieve alerts: ${error.message}`);
            return {
                alerts: [],
                total: 0,
                limit,
                offset,
            };
        }
    }
    async markAlertAsRead(alertId, hospitalId) {
        const alerts = this.alerts.get(hospitalId) || [];
        const alert = alerts.find((a) => a.id === alertId);
        if (alert) {
            alert.read = true;
        }
        try {
            await this.prisma.threatAlert.update({
                where: { id: alertId },
                data: { read: true },
            });
        }
        catch (error) {
            this.logger.error(`Failed to mark alert as read: ${error.message}`);
        }
    }
    async markAllAlertsAsRead(hospitalId) {
        const alerts = this.alerts.get(hospitalId) || [];
        alerts.forEach((a) => {
            a.read = true;
        });
        try {
            await this.prisma.threatAlert.updateMany({
                where: { hospitalId, read: false },
                data: { read: true },
            });
        }
        catch (error) {
            this.logger.error(`Failed to mark all alerts as read: ${error.message}`);
        }
    }
    async dismissAlert(alertId, hospitalId) {
        const alerts = this.alerts.get(hospitalId) || [];
        const alertIndex = alerts.findIndex((a) => a.id === alertId);
        if (alertIndex >= 0) {
            alerts[alertIndex].dismissedAt = new Date();
        }
        try {
            await this.prisma.threatAlert.update({
                where: { id: alertId },
                data: { dismissedAt: new Date() },
            });
        }
        catch (error) {
            this.logger.error(`Failed to dismiss alert: ${error.message}`);
        }
    }
    async getAlertById(alertId) {
        try {
            const alert = await this.prisma.threatAlert.findUnique({
                where: { id: alertId },
            });
            return alert ? this.mapToNotification(alert) : null;
        }
        catch (error) {
            this.logger.error(`Failed to retrieve alert: ${error.message}`);
            return null;
        }
    }
    async getAlertSummary(hospitalId) {
        try {
            const alerts = await this.prisma.threatAlert.findMany({
                where: { hospitalId, read: false },
                select: {
                    severity: true,
                    requiresAction: true,
                },
            });
            return {
                unreadCount: alerts.length,
                unreadCritical: alerts.filter((a) => a.severity === 'CRITICAL').length,
                unreadHigh: alerts.filter((a) => a.severity === 'HIGH').length,
                unreadMedium: alerts.filter((a) => a.severity === 'MEDIUM').length,
                requiresAction: alerts.filter((a) => a.requiresAction).length,
            };
        }
        catch (error) {
            this.logger.error(`Failed to get alert summary: ${error.message}`);
            return {
                unreadCount: 0,
                unreadCritical: 0,
                unreadHigh: 0,
                unreadMedium: 0,
                requiresAction: 0,
            };
        }
    }
    async clearOldAlerts(daysOld = 30) {
        try {
            const cutoffDate = new Date(Date.now() - daysOld * 24 * 60 * 60 * 1000);
            const result = await this.prisma.threatAlert.deleteMany({
                where: {
                    dismissedAt: { lt: cutoffDate },
                },
            });
            this.logger.log(`Cleared ${result.count} old alerts (older than ${daysOld} days)`);
            return result.count;
        }
        catch (error) {
            this.logger.error(`Failed to clear old alerts: ${error.message}`);
            return 0;
        }
    }
    generateAlertId() {
        return `alert-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    }
    generateAlertTitle(alertType, severity) {
        const typeLabels = {
            FAILED_LOGIN_ATTEMPTS: 'Failed Login Attempts',
            BULK_OPERATIONS: 'Bulk Operations Detected',
            PERMISSION_ESCALATION: 'Permission Escalation Attempt',
            SUSPICIOUS_IP: 'Suspicious IP Activity',
        };
        const label = typeLabels[alertType] || 'Security Alert';
        return `[${severity}] ${label}`;
    }
    mapToNotification(alert) {
        return {
            id: alert.id,
            hospitalId: alert.hospitalId,
            adminId: alert.adminId,
            alertType: alert.alertType,
            severity: alert.severity,
            title: this.generateAlertTitle(alert.alertType, alert.severity),
            message: alert.description,
            actionRequired: alert.requiresAction,
            read: alert.read,
            createdAt: alert.createdAt,
            dismissedAt: alert.dismissedAt,
        };
    }
};
exports.AlertService = AlertService;
exports.AlertService = AlertService = AlertService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], AlertService);


/***/ }),
/* 24 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DashboardService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let DashboardService = class DashboardService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async getSecurityMetrics(hospitalId, hoursBack = 24) {
        const since = new Date(Date.now() - hoursBack * 60 * 60 * 1000);
        const threatAlerts = await this.prisma.threatAlert.findMany({
            where: {
                hospitalId,
                createdAt: { gte: since },
            },
        });
        const auditLogs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                timestamp: { gte: since },
            },
        });
        const threatsBySeverity = {
            CRITICAL: threatAlerts.filter((t) => t.severity === 'CRITICAL').length,
            HIGH: threatAlerts.filter((t) => t.severity === 'HIGH').length,
            MEDIUM: threatAlerts.filter((t) => t.severity === 'MEDIUM').length,
            LOW: threatAlerts.filter((t) => t.severity === 'LOW').length,
        };
        const threatsByType = {
            FAILED_LOGIN_ATTEMPTS: threatAlerts.filter((t) => t.alertType === 'FAILED_LOGIN_ATTEMPTS').length,
            BULK_OPERATIONS: threatAlerts.filter((t) => t.alertType === 'BULK_OPERATIONS').length,
            PERMISSION_ESCALATION: threatAlerts.filter((t) => t.alertType === 'PERMISSION_ESCALATION').length,
            SUSPICIOUS_IP: threatAlerts.filter((t) => t.alertType === 'SUSPICIOUS_IP').length,
        };
        const unreadAlerts = threatAlerts.filter((t) => !t.read).length;
        const auditActionCounts = {
            CREATE: auditLogs.filter((a) => a.action === 'CREATE').length,
            UPDATE: auditLogs.filter((a) => a.action === 'UPDATE').length,
            DELETE: auditLogs.filter((a) => a.action === 'DELETE').length,
        };
        const uniqueUsers = new Set(auditLogs.map((a) => a.userId)).size;
        const severityLevel = threatsBySeverity.CRITICAL > 0
            ? 'CRITICAL'
            : threatsBySeverity.HIGH > 0
                ? 'HIGH'
                : threatsBySeverity.MEDIUM > 0
                    ? 'MEDIUM'
                    : 'LOW';
        return {
            hospitalId,
            timeRange: {
                start: since,
                end: new Date(),
                hoursBack,
            },
            totalThreats: threatAlerts.length,
            totalAlerts: threatAlerts.length,
            unreadAlerts,
            threatsBySeverity,
            threatsByType,
            totalAuditActions: auditLogs.length,
            auditActionsByType: auditActionCounts,
            uniqueUsersActive: uniqueUsers,
            overallSeverityLevel: severityLevel,
            criticalThreatCount: threatsBySeverity.CRITICAL,
            alertsRequiringAction: threatAlerts.filter((t) => t.requiresAction)
                .length,
            dismissedAlerts: threatAlerts.filter((t) => t.dismissedAt !== null)
                .length,
        };
    }
    async getThreatTrend(hospitalId, daysBack = 7) {
        const since = new Date(Date.now() - daysBack * 24 * 60 * 60 * 1000);
        const threatAlerts = await this.prisma.threatAlert.findMany({
            where: {
                hospitalId,
                createdAt: { gte: since },
            },
            orderBy: { createdAt: 'asc' },
        });
        const trendByDay = {};
        const severityByDay = {};
        const typeByDay = {};
        threatAlerts.forEach((threat) => {
            const dayKey = threat.createdAt.toISOString().split('T')[0];
            trendByDay[dayKey] = (trendByDay[dayKey] || 0) + 1;
            if (!severityByDay[dayKey]) {
                severityByDay[dayKey] = {
                    CRITICAL: 0,
                    HIGH: 0,
                    MEDIUM: 0,
                    LOW: 0,
                };
            }
            severityByDay[dayKey][threat.severity]++;
            if (!typeByDay[dayKey]) {
                typeByDay[dayKey] = {
                    FAILED_LOGIN_ATTEMPTS: 0,
                    BULK_OPERATIONS: 0,
                    PERMISSION_ESCALATION: 0,
                    SUSPICIOUS_IP: 0,
                };
            }
            typeByDay[dayKey][threat.alertType]++;
        });
        const dailyValues = Object.values(trendByDay);
        const trendDirection = dailyValues.length >= 2
            ? dailyValues[dailyValues.length - 1] >
                dailyValues[dailyValues.length - 2]
                ? 'INCREASING'
                : 'DECREASING'
            : 'STABLE';
        return {
            hospitalId,
            daysBack,
            period: {
                start: since,
                end: new Date(),
            },
            totalThreatsInPeriod: threatAlerts.length,
            dailyThreatCount: trendByDay,
            dailySeverityDistribution: severityByDay,
            dailyTypeDistribution: typeByDay,
            averageThreatsPerDay: dailyValues.length > 0
                ? dailyValues.reduce((a, b) => a + b, 0) / dailyValues.length
                : 0,
            peakThreatDay: Object.entries(trendByDay).sort(([, a], [, b]) => b - a)[0]?.[0],
            peakThreatCount: Math.max(...dailyValues, 0),
            trendDirection,
        };
    }
    async getComplianceStatus(hospitalId) {
        const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);
        const biometricEnrollmentCount = await this.prisma.biometricEnrollment.count({
            where: { hospitalId },
        });
        const failedAuthAlerts = await this.prisma.threatAlert.count({
            where: {
                hospitalId,
                alertType: 'FAILED_LOGIN_ATTEMPTS',
                createdAt: { gte: thirtyDaysAgo },
            },
        });
        const permissionEscalationAlerts = await this.prisma.threatAlert.count({
            where: {
                hospitalId,
                alertType: 'PERMISSION_ESCALATION',
                createdAt: { gte: thirtyDaysAgo },
            },
        });
        const suspiciousActivityAlerts = await this.prisma.threatAlert.count({
            where: {
                hospitalId,
                alertType: 'SUSPICIOUS_IP',
                createdAt: { gte: thirtyDaysAgo },
            },
        });
        let complianceScore = 100;
        if (biometricEnrollmentCount === 0)
            complianceScore -= 10;
        if (failedAuthAlerts > 10)
            complianceScore -= 30;
        if (suspiciousActivityAlerts > 5)
            complianceScore -= 20;
        complianceScore = Math.max(0, complianceScore);
        const complianceLevel = complianceScore >= 90
            ? 'EXCELLENT'
            : complianceScore >= 75
                ? 'GOOD'
                : complianceScore >= 60
                    ? 'FAIR'
                    : 'POOR';
        return {
            hospitalId,
            complianceScore,
            complianceLevel,
            period: {
                start: thirtyDaysAgo,
                end: new Date(),
            },
            checks: {
                dataEncryption: {
                    compliant: true,
                    details: `${biometricEnrollmentCount} biometric records encrypted`,
                },
                authenticationSecurity: {
                    compliant: failedAuthAlerts <= 10,
                    details: `${failedAuthAlerts} failed login attempts in 30 days`,
                },
                accessControl: {
                    compliant: permissionEscalationAlerts === 0,
                    details: `${permissionEscalationAlerts} permission escalation attempts`,
                },
                suspiciousActivityMonitoring: {
                    compliant: suspiciousActivityAlerts <= 5,
                    details: `${suspiciousActivityAlerts} suspicious IP activities`,
                },
            },
            recommendations: complianceLevel === 'EXCELLENT'
                ? []
                : complianceLevel === 'GOOD'
                    ? ['Review and address failed login attempts', 'Monitor suspicious IP activity']
                    : complianceLevel === 'FAIR'
                        ? [
                            'Implement stricter access controls',
                            'Review user permissions',
                            'Enable multi-factor authentication',
                            'Monitor failed login attempts',
                        ]
                        : [
                            'Ensure all sensitive data is encrypted',
                            'Review and enforce access control policies',
                            'Investigate and address security threats',
                            'Implement stricter authentication requirements',
                            'Enable comprehensive audit logging',
                        ],
        };
    }
    async getEncryptionStatus(hospitalId) {
        const biometricEnrollments = await this.prisma.biometricEnrollment.count({
            where: { hospitalId },
        });
        const lastKeyRotation = new Date('2026-01-15');
        const keyRotationFrequency = 90;
        const daysUntilNextRotation = Math.max(0, keyRotationFrequency -
            Math.floor((Date.now() - lastKeyRotation.getTime()) / (24 * 60 * 60 * 1000)));
        return {
            hospitalId,
            encryptionAlgorithm: 'AES-256-GCM',
            encryptedRecords: biometricEnrollments,
            encryptionCoverage: biometricEnrollments > 0 ? 100 : 0,
            lastKeyRotation,
            nextKeyRotationDate: new Date(lastKeyRotation.getTime() +
                keyRotationFrequency * 24 * 60 * 60 * 1000),
            daysUntilNextRotation,
            keyRotationStatus: daysUntilNextRotation > 30
                ? 'OK'
                : daysUntilNextRotation > 14
                    ? 'WARNING'
                    : 'URGENT',
            encryptedFieldsTypes: [
                'fingerprintData',
                'faceData',
                'irisData',
                'voiceData',
            ],
            complianceRequirements: {
                algorithm: 'AES-256 or stronger',
                keyRotation: '90 days',
                dataClassification: 'Highly Sensitive (PHI)',
            },
        };
    }
    async getAlertDistribution(hospitalId, daysBack = 7) {
        const since = new Date(Date.now() - daysBack * 24 * 60 * 60 * 1000);
        const alerts = await this.prisma.threatAlert.findMany({
            where: {
                hospitalId,
                createdAt: { gte: since },
            },
        });
        return {
            hospitalId,
            period: { start: since, end: new Date() },
            totalAlerts: alerts.length,
            readAlerts: alerts.filter((a) => a.read).length,
            unreadAlerts: alerts.filter((a) => !a.read).length,
            dismissedAlerts: alerts.filter((a) => a.dismissedAt !== null).length,
            requiresActionCount: alerts.filter((a) => a.requiresAction).length,
            bySeverity: {
                CRITICAL: alerts.filter((a) => a.severity === 'CRITICAL').length,
                HIGH: alerts.filter((a) => a.severity === 'HIGH').length,
                MEDIUM: alerts.filter((a) => a.severity === 'MEDIUM').length,
                LOW: alerts.filter((a) => a.severity === 'LOW').length,
            },
            byType: {
                FAILED_LOGIN_ATTEMPTS: alerts.filter((a) => a.alertType === 'FAILED_LOGIN_ATTEMPTS').length,
                BULK_OPERATIONS: alerts.filter((a) => a.alertType === 'BULK_OPERATIONS').length,
                PERMISSION_ESCALATION: alerts.filter((a) => a.alertType === 'PERMISSION_ESCALATION').length,
                SUSPICIOUS_IP: alerts.filter((a) => a.alertType === 'SUSPICIOUS_IP')
                    .length,
            },
        };
    }
    async getAuditActivity(hospitalId, daysBack = 7, limit = 10) {
        const since = new Date(Date.now() - daysBack * 24 * 60 * 60 * 1000);
        const auditLogs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                timestamp: { gte: since },
            },
            orderBy: { timestamp: 'desc' },
            take: limit,
        });
        const actionCounts = {
            CREATE: auditLogs.filter((a) => a.action === 'CREATE').length,
            UPDATE: auditLogs.filter((a) => a.action === 'UPDATE').length,
            DELETE: auditLogs.filter((a) => a.action === 'DELETE').length,
        };
        const entityTypeCounts = {};
        auditLogs.forEach((log) => {
            entityTypeCounts[log.entityType] =
                (entityTypeCounts[log.entityType] || 0) + 1;
        });
        const uniqueUsers = new Set(auditLogs.map((a) => a.userId)).size;
        return {
            hospitalId,
            period: { start: since, end: new Date() },
            totalActions: auditLogs.length,
            actionsByType: actionCounts,
            actionsByEntity: entityTypeCounts,
            uniqueUsersActive: uniqueUsers,
            recentActions: auditLogs.slice(0, 10).map((log) => ({
                action: log.action,
                entityType: log.entityType,
                timestamp: log.timestamp,
                userId: log.userId,
                description: `${log.action} ${log.entityType} (${log.entityId})`,
            })),
        };
    }
};
exports.DashboardService = DashboardService;
exports.DashboardService = DashboardService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], DashboardService);


/***/ }),
/* 25 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExportService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let ExportService = class ExportService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async exportAuditLogsToCSV(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.timestamp = {};
            if (startDate)
                where.timestamp.gte = startDate;
            if (endDate)
                where.timestamp.lte = endDate;
        }
        const auditLogs = await this.prisma.auditLog.findMany({
            where,
            orderBy: { timestamp: 'desc' },
            take: 10000,
        });
        const headers = [
            'ID',
            'Hospital ID',
            'User ID',
            'Action',
            'Entity Type',
            'Entity ID',
            'IP Address',
            'User Agent',
            'Timestamp',
        ];
        const rows = auditLogs.map((log) => [
            log.id,
            log.hospitalId,
            log.userId,
            log.action,
            log.entityType,
            log.entityId,
            log.ipAddress || '',
            (log.userAgent || '').substring(0, 50),
            log.timestamp.toISOString(),
        ]);
        const csvContent = [
            headers.map((h) => `"${h}"`).join(','),
            ...rows.map((row) => row
                .map((cell) => `"${String(cell).replace(/"/g, '""')}"`)
                .join(',')),
        ].join('\n');
        return csvContent;
    }
    async exportAuditLogsToJSON(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.timestamp = {};
            if (startDate)
                where.timestamp.gte = startDate;
            if (endDate)
                where.timestamp.lte = endDate;
        }
        const auditLogs = await this.prisma.auditLog.findMany({
            where,
            orderBy: { timestamp: 'desc' },
            take: 10000,
        });
        const jsonData = {
            exportType: 'AuditLogs',
            hospitalId,
            exportDate: new Date().toISOString(),
            totalRecords: auditLogs.length,
            dateRange: {
                start: startDate?.toISOString() || null,
                end: endDate?.toISOString() || null,
            },
            data: auditLogs.map((log) => ({
                id: log.id,
                hospitalId: log.hospitalId,
                userId: log.userId,
                action: log.action,
                entityType: log.entityType,
                entityId: log.entityId,
                beforeState: log.beforeState,
                afterState: log.afterState,
                ipAddress: log.ipAddress,
                userAgent: log.userAgent,
                timestamp: log.timestamp.toISOString(),
            })),
        };
        return JSON.stringify(jsonData, null, 2);
    }
    async exportAuditLogsToPDF(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.timestamp = {};
            if (startDate)
                where.timestamp.gte = startDate;
            if (endDate)
                where.timestamp.lte = endDate;
        }
        const auditLogs = await this.prisma.auditLog.findMany({
            where,
            orderBy: { timestamp: 'desc' },
            take: 1000,
        });
        let content = '';
        content += '='.repeat(80) + '\n';
        content += 'AUDIT LOG REPORT\n';
        content += `Hospital ID: ${hospitalId}\n`;
        content += `Generated: ${new Date().toISOString()}\n`;
        if (startDate)
            content += `Start Date: ${startDate.toISOString()}\n`;
        if (endDate)
            content += `End Date: ${endDate.toISOString()}\n`;
        content += `Total Records: ${auditLogs.length}\n`;
        content += '='.repeat(80) + '\n\n';
        auditLogs.forEach((log) => {
            content += `ID: ${log.id}\n`;
            content += `User: ${log.userId}\n`;
            content += `Action: ${log.action}\n`;
            content += `Entity: ${log.entityType} (${log.entityId})\n`;
            content += `IP: ${log.ipAddress || 'N/A'}\n`;
            content += `Time: ${log.timestamp.toISOString()}\n`;
            content += '-'.repeat(80) + '\n';
        });
        return content;
    }
    async exportThreatsToCSV(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.createdAt = {};
            if (startDate)
                where.createdAt.gte = startDate;
            if (endDate)
                where.createdAt.lte = endDate;
        }
        const threats = await this.prisma.threatAlert.findMany({
            where,
            orderBy: { createdAt: 'desc' },
            take: 5000,
        });
        const headers = [
            'ID',
            'Hospital ID',
            'Alert Type',
            'Severity',
            'Description',
            'Requires Action',
            'Read',
            'Dismissed',
            'Created At',
            'Updated At',
        ];
        const rows = threats.map((threat) => [
            threat.id,
            threat.hospitalId,
            threat.alertType,
            threat.severity,
            threat.description,
            threat.requiresAction ? 'Yes' : 'No',
            threat.read ? 'Yes' : 'No',
            threat.dismissedAt ? 'Yes' : 'No',
            threat.createdAt.toISOString(),
            threat.updatedAt.toISOString(),
        ]);
        const csvContent = [
            headers.map((h) => `"${h}"`).join(','),
            ...rows.map((row) => row
                .map((cell) => `"${String(cell).replace(/"/g, '""')}"`)
                .join(',')),
        ].join('\n');
        return csvContent;
    }
    async exportThreatsToJSON(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.createdAt = {};
            if (startDate)
                where.createdAt.gte = startDate;
            if (endDate)
                where.createdAt.lte = endDate;
        }
        const threats = await this.prisma.threatAlert.findMany({
            where,
            orderBy: { createdAt: 'desc' },
            take: 5000,
        });
        const jsonData = {
            exportType: 'ThreatAlerts',
            hospitalId,
            exportDate: new Date().toISOString(),
            totalRecords: threats.length,
            dateRange: {
                start: startDate?.toISOString() || null,
                end: endDate?.toISOString() || null,
            },
            data: threats.map((threat) => ({
                id: threat.id,
                hospitalId: threat.hospitalId,
                userId: threat.userId,
                adminId: threat.adminId,
                alertType: threat.alertType,
                severity: threat.severity,
                description: threat.description,
                details: threat.details,
                requiresAction: threat.requiresAction,
                read: threat.read,
                dismissedAt: threat.dismissedAt?.toISOString() || null,
                createdAt: threat.createdAt.toISOString(),
                updatedAt: threat.updatedAt.toISOString(),
            })),
        };
        return JSON.stringify(jsonData, null, 2);
    }
    async exportThreatsToPDF(hospitalId, startDate, endDate) {
        const where = { hospitalId };
        if (startDate || endDate) {
            where.createdAt = {};
            if (startDate)
                where.createdAt.gte = startDate;
            if (endDate)
                where.createdAt.lte = endDate;
        }
        const threats = await this.prisma.threatAlert.findMany({
            where,
            orderBy: { createdAt: 'desc' },
            take: 1000,
        });
        let content = '';
        content += '='.repeat(80) + '\n';
        content += 'THREAT ALERT REPORT\n';
        content += `Hospital ID: ${hospitalId}\n`;
        content += `Generated: ${new Date().toISOString()}\n`;
        if (startDate)
            content += `Start Date: ${startDate.toISOString()}\n`;
        if (endDate)
            content += `End Date: ${endDate.toISOString()}\n`;
        content += `Total Alerts: ${threats.length}\n`;
        content += '='.repeat(80) + '\n\n';
        const bySeverity = {
            CRITICAL: threats.filter((t) => t.severity === 'CRITICAL').length,
            HIGH: threats.filter((t) => t.severity === 'HIGH').length,
            MEDIUM: threats.filter((t) => t.severity === 'MEDIUM').length,
            LOW: threats.filter((t) => t.severity === 'LOW').length,
        };
        content += 'THREAT SUMMARY BY SEVERITY\n';
        content += '-'.repeat(40) + '\n';
        content += `CRITICAL: ${bySeverity.CRITICAL}\n`;
        content += `HIGH: ${bySeverity.HIGH}\n`;
        content += `MEDIUM: ${bySeverity.MEDIUM}\n`;
        content += `LOW: ${bySeverity.LOW}\n`;
        content += '-'.repeat(40) + '\n\n';
        content += 'DETAILED THREAT RECORDS\n';
        content += '='.repeat(80) + '\n\n';
        threats.forEach((threat) => {
            content += `ID: ${threat.id}\n`;
            content += `Type: ${threat.alertType}\n`;
            content += `Severity: ${threat.severity}\n`;
            content += `Description: ${threat.description}\n`;
            content += `Requires Action: ${threat.requiresAction ? 'Yes' : 'No'}\n`;
            content += `Status: ${threat.read ? 'Read' : 'Unread'}\n`;
            content += `Created: ${threat.createdAt.toISOString()}\n`;
            content += '-'.repeat(80) + '\n';
        });
        return content;
    }
    async getExportSummary(hospitalId) {
        const auditLogCount = await this.prisma.auditLog.count({
            where: { hospitalId },
        });
        const threatAlertCount = await this.prisma.threatAlert.count({
            where: { hospitalId },
        });
        return {
            hospitalId,
            exportDate: new Date().toISOString(),
            availableDataSets: {
                auditLogs: {
                    count: auditLogCount,
                    formats: ['CSV', 'JSON', 'PDF'],
                },
                threatAlerts: {
                    count: threatAlertCount,
                    formats: ['CSV', 'JSON', 'PDF'],
                },
            },
            limitations: {
                maxRecordsPerExport: 10000,
                supportedFormats: ['CSV', 'JSON', 'PDF'],
                dateRangeSupport: true,
                estimatedSize: {
                    auditLogsCSV: `${Math.ceil(auditLogCount * 0.2)} KB`,
                    threatAlertsCSV: `${Math.ceil(threatAlertCount * 0.15)} KB`,
                },
            },
        };
    }
};
exports.ExportService = ExportService;
exports.ExportService = ExportService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ExportService);


/***/ }),
/* 26 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ComplianceService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let ComplianceService = class ComplianceService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async generateMonthlyReport(hospitalId, year, month) {
        const startDate = new Date(year, month - 1, 1);
        const endDate = new Date(year, month, 0, 23, 59, 59);
        return this.generateComplianceReport(hospitalId, startDate, endDate, 'MONTHLY');
    }
    async generateQuarterlyReport(hospitalId, year, quarter) {
        const monthStart = (quarter - 1) * 3 + 1;
        const startDate = new Date(year, monthStart - 1, 1);
        const endDate = new Date(year, monthStart + 2, 0, 23, 59, 59);
        return this.generateComplianceReport(hospitalId, startDate, endDate, 'QUARTERLY');
    }
    async generateAnnualReport(hospitalId, year) {
        const startDate = new Date(year, 0, 1);
        const endDate = new Date(year, 11, 31, 23, 59, 59);
        return this.generateComplianceReport(hospitalId, startDate, endDate, 'ANNUAL');
    }
    async generateComplianceReport(hospitalId, startDate, endDate, reportType) {
        const auditLogs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                timestamp: { gte: startDate, lte: endDate },
            },
        });
        const threatAlerts = await this.prisma.threatAlert.findMany({
            where: {
                hospitalId,
                createdAt: { gte: startDate, lte: endDate },
            },
        });
        const metrics = await this.calculateComplianceMetrics(hospitalId, auditLogs, threatAlerts, startDate, endDate);
        const findings = this.generateFindings(metrics, reportType);
        const recommendations = this.generateRecommendations(metrics);
        return {
            hospitalId,
            reportType,
            reportPeriod: { start: startDate, end: endDate },
            generatedDate: new Date(),
            metrics,
            findings,
            recommendations,
            overallComplianceStatus: metrics.complianceScore >= 80
                ? 'COMPLIANT'
                : metrics.complianceScore >= 60
                    ? 'PARTIALLY_COMPLIANT'
                    : 'NON_COMPLIANT',
        };
    }
    async calculateComplianceMetrics(hospitalId, auditLogs, threatAlerts, startDate, endDate) {
        const criticalThreats = threatAlerts.filter((t) => t.severity === 'CRITICAL').length;
        const highThreats = threatAlerts.filter((t) => t.severity === 'HIGH').length;
        const mediumThreats = threatAlerts.filter((t) => t.severity === 'MEDIUM').length;
        const failedLoginAttempts = threatAlerts.filter((t) => t.alertType === 'FAILED_LOGIN_ATTEMPTS').length;
        const bulkOperations = threatAlerts.filter((t) => t.alertType === 'BULK_OPERATIONS').length;
        const permissionEscalations = threatAlerts.filter((t) => t.alertType === 'PERMISSION_ESCALATION').length;
        const suspiciousIPs = threatAlerts.filter((t) => t.alertType === 'SUSPICIOUS_IP').length;
        let authenticationScore = 100;
        if (failedLoginAttempts > 20)
            authenticationScore -= 30;
        else if (failedLoginAttempts > 10)
            authenticationScore -= 15;
        let accessControlScore = 100;
        if (permissionEscalations > 5)
            accessControlScore -= 50;
        else if (permissionEscalations > 0)
            accessControlScore -= 20;
        let dataProtectionScore = 100;
        if (bulkOperations > 10)
            dataProtectionScore -= 20;
        let incidentDetectionScore = 100;
        if (criticalThreats > 0)
            incidentDetectionScore -= 50;
        else if (highThreats > 5)
            incidentDetectionScore -= 30;
        const auditTrailScore = auditLogs.length > 1000 ? 100 : (auditLogs.length / 1000) * 100;
        const complianceScore = (authenticationScore * 0.25 +
            accessControlScore * 0.25 +
            dataProtectionScore * 0.2 +
            incidentDetectionScore * 0.2 +
            auditTrailScore * 0.1) /
            100;
        return {
            complianceScore: Math.round(complianceScore),
            authenticationScore: Math.round(authenticationScore),
            accessControlScore: Math.round(accessControlScore),
            dataProtectionScore: Math.round(dataProtectionScore),
            incidentDetectionScore: Math.round(incidentDetectionScore),
            auditTrailScore: Math.round(auditTrailScore),
            totalThreatsDetected: threatAlerts.length,
            threatsBySeverity: {
                CRITICAL: criticalThreats,
                HIGH: highThreats,
                MEDIUM: mediumThreats,
                LOW: threatAlerts.filter((t) => t.severity === 'LOW').length,
            },
            threatsByType: {
                failedLoginAttempts,
                bulkOperations,
                permissionEscalations,
                suspiciousIPs,
            },
            totalAuditLogsGenerated: auditLogs.length,
            uniqueUsersTracked: new Set(auditLogs.map((a) => a.userId)).size,
            criticalIncidentsResolved: threatAlerts.filter((t) => t.read && t.dismissedAt).length,
            pendingIncidents: threatAlerts.filter((t) => !t.read).length,
        };
    }
    generateFindings(metrics, reportType) {
        const findings = [];
        if (metrics.authenticationScore < 80) {
            findings.push(`Authentication security score is ${metrics.authenticationScore}%. ` +
                `${metrics.threatsByType.failedLoginAttempts} failed login attempts detected.`);
        }
        else {
            findings.push('Authentication mechanisms are adequately controlled.');
        }
        if (metrics.accessControlScore < 100) {
            findings.push(`Access control findings: ${metrics.threatsByType.permissionEscalations} permission escalation attempts detected.`);
        }
        else {
            findings.push('No permission escalation attempts detected.');
        }
        if (metrics.threatsBySeverity.CRITICAL > 0) {
            findings.push(`CRITICAL FINDING: ${metrics.threatsBySeverity.CRITICAL} critical security threats detected.`);
        }
        if (metrics.threatsBySeverity.HIGH > 5) {
            findings.push(`WARNING: ${metrics.threatsBySeverity.HIGH} high-severity threats detected.`);
        }
        if (metrics.auditTrailScore === 100) {
            findings.push('Comprehensive audit trail maintained throughout period.');
        }
        else {
            findings.push(`Audit trail completeness: ${metrics.auditTrailScore}%. ` +
                `${metrics.totalAuditLogsGenerated} audit logs recorded.`);
        }
        if (metrics.dataProtectionScore >= 80) {
            findings.push('Data protection controls are in place.');
        }
        if (metrics.complianceScore >= 80) {
            findings.push(`Overall compliance score: ${metrics.complianceScore}% - Hospital meets security requirements.`);
        }
        else if (metrics.complianceScore >= 60) {
            findings.push(`Overall compliance score: ${metrics.complianceScore}% - Hospital partially meets security requirements. Improvements needed.`);
        }
        else {
            findings.push(`Overall compliance score: ${metrics.complianceScore}% - Hospital does not meet security requirements. Urgent action required.`);
        }
        return findings;
    }
    generateRecommendations(metrics) {
        const recommendations = [];
        if (metrics.authenticationScore < 100) {
            recommendations.push('Implement stricter password policies and multi-factor authentication.');
            recommendations.push('Review and address failed login attempts. Consider implementing account lockout policies.');
        }
        if (metrics.accessControlScore < 100) {
            recommendations.push('Review user roles and permissions. Implement principle of least privilege.');
            recommendations.push('Conduct access control audit. Remove unnecessary admin privileges.');
        }
        if (metrics.threatsBySeverity.CRITICAL > 0) {
            recommendations.push('Immediately investigate and remediate critical security threats.');
            recommendations.push('Implement additional monitoring and alerting for critical threats.');
        }
        if (metrics.threatsBySeverity.HIGH > 0) {
            recommendations.push('Review high-severity threats and implement mitigating controls.');
        }
        if (metrics.dataProtectionScore < 100) {
            recommendations.push('Ensure all sensitive data is properly encrypted and protected.');
            recommendations.push('Review data classification and implement appropriate protection measures.');
        }
        if (metrics.complianceScore < 80) {
            recommendations.push('Develop comprehensive security improvement plan.');
            recommendations.push('Schedule security training for staff to improve awareness.');
            recommendations.push('Conduct full security assessment and audit.');
        }
        recommendations.push('Schedule regular (monthly) compliance reviews.');
        recommendations.push('Keep encryption keys and security credentials current.');
        return recommendations;
    }
    async getComplianceMetrics(hospitalId) {
        const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);
        const auditLogs = await this.prisma.auditLog.findMany({
            where: {
                hospitalId,
                timestamp: { gte: thirtyDaysAgo },
            },
        });
        const threatAlerts = await this.prisma.threatAlert.findMany({
            where: {
                hospitalId,
                createdAt: { gte: thirtyDaysAgo },
            },
        });
        return this.calculateComplianceMetrics(hospitalId, auditLogs, threatAlerts, thirtyDaysAgo, new Date());
    }
};
exports.ComplianceService = ComplianceService;
exports.ComplianceService = ComplianceService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ComplianceService);


/***/ }),
/* 27 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuditModule = void 0;
const common_1 = __webpack_require__(2);
const audit_log_viewer_service_1 = __webpack_require__(21);
const audit_log_controller_1 = __webpack_require__(28);
let AuditModule = class AuditModule {
};
exports.AuditModule = AuditModule;
exports.AuditModule = AuditModule = __decorate([
    (0, common_1.Module)({
        providers: [audit_log_viewer_service_1.AuditLogViewerService],
        controllers: [audit_log_controller_1.AuditLogController],
        exports: [audit_log_viewer_service_1.AuditLogViewerService],
    })
], AuditModule);


/***/ }),
/* 28 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var AuditLogController_1;
var _a, _b, _c, _d, _e, _f, _g, _h, _j;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuditLogController = void 0;
const common_1 = __webpack_require__(2);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const current_user_decorator_1 = __webpack_require__(33);
const audit_log_viewer_service_1 = __webpack_require__(21);
const audit_log_dto_1 = __webpack_require__(34);
let AuditLogController = AuditLogController_1 = class AuditLogController {
    constructor(auditLogViewerService) {
        this.auditLogViewerService = auditLogViewerService;
        this.logger = new common_1.Logger(AuditLogController_1.name);
    }
    async getAuditLogs(filters, pagination, user) {
        try {
            const hospitalId = user.role === 'SUPER_ADMIN'
                ? filters.hospitalId || user.hospitalId
                : user.hospitalId;
            const auditFilters = {
                ...filters,
                hospitalId,
                startDate: filters.startDate ? new Date(filters.startDate) : undefined,
                endDate: filters.endDate ? new Date(filters.endDate) : undefined,
            };
            return await this.auditLogViewerService.getAuditLogs(auditFilters, pagination);
        }
        catch (error) {
            this.logger.error(`Failed to fetch audit logs: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve audit logs', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getAuditLog(id, user) {
        try {
            const log = await this.auditLogViewerService.getAuditLogById(id);
            if (!log) {
                throw new common_1.HttpException('Audit log not found', common_1.HttpStatus.NOT_FOUND);
            }
            if (user.role !== 'SUPER_ADMIN' && log.hospitalId !== user.hospitalId) {
                throw new common_1.HttpException('Unauthorized to view this audit log', common_1.HttpStatus.FORBIDDEN);
            }
            return log;
        }
        catch (error) {
            if (error instanceof common_1.HttpException) {
                throw error;
            }
            this.logger.error(`Failed to fetch audit log ${id}: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve audit log', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async searchAuditLogs(body, user) {
        try {
            const hospitalId = user.role === 'SUPER_ADMIN'
                ? body.filters.hospitalId || user.hospitalId
                : user.hospitalId;
            const auditFilters = {
                ...body.filters,
                hospitalId,
                startDate: body.filters.startDate
                    ? new Date(body.filters.startDate)
                    : undefined,
                endDate: body.filters.endDate
                    ? new Date(body.filters.endDate)
                    : undefined,
            };
            return await this.auditLogViewerService.getAuditLogs(auditFilters, body.pagination || { limit: 50 });
        }
        catch (error) {
            this.logger.error(`Search failed: ${error.message}`);
            throw new common_1.HttpException('Search failed', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getEntityHistory(user, entityType, entityId) {
        try {
            const logs = await this.auditLogViewerService.getEntityHistory(entityType, entityId);
            if (logs.length > 0 &&
                user.role !== 'SUPER_ADMIN' &&
                logs[0].hospitalId !== user.hospitalId) {
                throw new common_1.HttpException('Unauthorized to view this entity history', common_1.HttpStatus.FORBIDDEN);
            }
            const lastLog = logs[0];
            return {
                logs,
                entityType,
                entityId,
                totalChanges: logs.length,
                lastModifiedAt: lastLog?.timestamp || null,
                lastModifiedBy: lastLog?.userId || null,
            };
        }
        catch (error) {
            if (error instanceof common_1.HttpException) {
                throw error;
            }
            this.logger.error(`Failed to fetch entity history: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve entity history', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getUserActivity(user, userId, days) {
        try {
            const daysToFetch = days ? Math.min(parseInt(days), 90) : 30;
            const logs = await this.auditLogViewerService.getUserActivity(userId, user.hospitalId, daysToFetch);
            const actionsByType = {};
            for (const log of logs) {
                actionsByType[log.action] = (actionsByType[log.action] || 0) + 1;
            }
            const startDate = new Date();
            startDate.setDate(startDate.getDate() - daysToFetch);
            return {
                logs,
                userId,
                totalActions: logs.length,
                actionsByType,
                dateRange: {
                    startDate,
                    endDate: new Date(),
                },
            };
        }
        catch (error) {
            this.logger.error(`Failed to fetch user activity: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve user activity', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getSensitiveOperations(user, days) {
        try {
            const daysToFetch = days ? Math.min(parseInt(days), 90) : 30;
            const logs = await this.auditLogViewerService.getSensitiveOperations(user.hospitalId, daysToFetch);
            const operationsByType = {};
            const operationsByEntity = {};
            for (const log of logs) {
                operationsByType[log.action] =
                    (operationsByType[log.action] || 0) + 1;
                operationsByEntity[log.entityType] =
                    (operationsByEntity[log.entityType] || 0) + 1;
            }
            const startDate = new Date();
            startDate.setDate(startDate.getDate() - daysToFetch);
            return {
                logs,
                hospitalId: user.hospitalId,
                totalOperations: logs.length,
                operationsByType,
                operationsByEntity,
                dateRange: {
                    startDate,
                    endDate: new Date(),
                },
            };
        }
        catch (error) {
            this.logger.error(`Failed to fetch sensitive operations: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve sensitive operations', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getSuspiciousActivity(user, days) {
        try {
            const daysToFetch = days ? Math.min(parseInt(days), 90) : 7;
            const logs = await this.auditLogViewerService.getSuspiciousActivity(user.hospitalId, daysToFetch);
            return {
                logs,
                hospitalId: user.hospitalId,
                count: logs.length,
                dateRange: {
                    startDate: new Date(Date.now() - daysToFetch * 24 * 60 * 60 * 1000),
                    endDate: new Date(),
                },
            };
        }
        catch (error) {
            this.logger.error(`Failed to fetch suspicious activities: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve suspicious activities', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
    async getAuditStatistics(user, days) {
        try {
            const daysToFetch = days ? Math.min(parseInt(days), 365) : 30;
            return await this.auditLogViewerService.getAuditStatistics(user.hospitalId, daysToFetch);
        }
        catch (error) {
            this.logger.error(`Failed to fetch audit statistics: ${error.message}`);
            throw new common_1.HttpException('Failed to retrieve audit statistics', common_1.HttpStatus.INTERNAL_SERVER_ERROR);
        }
    }
};
exports.AuditLogController = AuditLogController;
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Query)()),
    __param(2, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof audit_log_dto_1.AuditLogFilterDto !== "undefined" && audit_log_dto_1.AuditLogFilterDto) === "function" ? _b : Object, typeof (_c = typeof audit_log_dto_1.PaginationDto !== "undefined" && audit_log_dto_1.PaginationDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", typeof (_d = typeof Promise !== "undefined" && Promise) === "function" ? _d : Object)
], AuditLogController.prototype, "getAuditLogs", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], AuditLogController.prototype, "getAuditLog", null);
__decorate([
    (0, common_1.Post)('search'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, Object]),
    __metadata("design:returntype", typeof (_e = typeof Promise !== "undefined" && Promise) === "function" ? _e : Object)
], AuditLogController.prototype, "searchAuditLogs", null);
__decorate([
    (0, common_1.Get)('entity/:type/:id/history'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __param(1, (0, common_1.Param)('type')),
    __param(2, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, String, String]),
    __metadata("design:returntype", typeof (_f = typeof Promise !== "undefined" && Promise) === "function" ? _f : Object)
], AuditLogController.prototype, "getEntityHistory", null);
__decorate([
    (0, common_1.Get)('user/:userId/activity'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __param(1, (0, common_1.Param)('userId')),
    __param(2, (0, common_1.Query)('days')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, String, String]),
    __metadata("design:returntype", typeof (_g = typeof Promise !== "undefined" && Promise) === "function" ? _g : Object)
], AuditLogController.prototype, "getUserActivity", null);
__decorate([
    (0, common_1.Get)('sensitive'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __param(1, (0, common_1.Query)('days')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, String]),
    __metadata("design:returntype", typeof (_h = typeof Promise !== "undefined" && Promise) === "function" ? _h : Object)
], AuditLogController.prototype, "getSensitiveOperations", null);
__decorate([
    (0, common_1.Get)('suspicious'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __param(1, (0, common_1.Query)('days')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, String]),
    __metadata("design:returntype", Promise)
], AuditLogController.prototype, "getSuspiciousActivity", null);
__decorate([
    (0, common_1.Get)('statistics'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'ADMIN', 'AUDIT_MANAGER'),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __param(1, (0, common_1.Query)('days')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object, String]),
    __metadata("design:returntype", typeof (_j = typeof Promise !== "undefined" && Promise) === "function" ? _j : Object)
], AuditLogController.prototype, "getAuditStatistics", null);
exports.AuditLogController = AuditLogController = AuditLogController_1 = __decorate([
    (0, common_1.Controller)('api/v1/audit-logs'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof audit_log_viewer_service_1.AuditLogViewerService !== "undefined" && audit_log_viewer_service_1.AuditLogViewerService) === "function" ? _a : Object])
], AuditLogController);


/***/ }),
/* 29 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.JwtAuthGuard = void 0;
const common_1 = __webpack_require__(2);
const passport_1 = __webpack_require__(30);
let JwtAuthGuard = class JwtAuthGuard extends (0, passport_1.AuthGuard)('jwt') {
    canActivate(context) {
        return super.canActivate(context);
    }
};
exports.JwtAuthGuard = JwtAuthGuard;
exports.JwtAuthGuard = JwtAuthGuard = __decorate([
    (0, common_1.Injectable)()
], JwtAuthGuard);


/***/ }),
/* 30 */
/***/ ((module) => {

module.exports = require("@nestjs/passport");

/***/ }),
/* 31 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RolesGuard = exports.ROLES_KEY = void 0;
const common_1 = __webpack_require__(2);
const core_1 = __webpack_require__(1);
exports.ROLES_KEY = 'roles';
let RolesGuard = class RolesGuard {
    constructor(reflector) {
        this.reflector = reflector;
    }
    canActivate(context) {
        const requiredRoles = this.reflector.getAllAndOverride(exports.ROLES_KEY, [
            context.getHandler(),
            context.getClass(),
        ]);
        if (!requiredRoles || requiredRoles.length === 0) {
            return true;
        }
        const { user } = context.switchToHttp().getRequest();
        if (!user) {
            throw new common_1.ForbiddenException('User not authenticated');
        }
        const hasRole = requiredRoles.some((role) => user.role === role);
        if (!hasRole) {
            throw new common_1.ForbiddenException(`Access denied. Required roles: ${requiredRoles.join(', ')}`);
        }
        return true;
    }
};
exports.RolesGuard = RolesGuard;
exports.RolesGuard = RolesGuard = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof core_1.Reflector !== "undefined" && core_1.Reflector) === "function" ? _a : Object])
], RolesGuard);


/***/ }),
/* 32 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.Roles = void 0;
const common_1 = __webpack_require__(2);
const roles_guard_1 = __webpack_require__(31);
const Roles = (...roles) => (0, common_1.SetMetadata)(roles_guard_1.ROLES_KEY, roles);
exports.Roles = Roles;


/***/ }),
/* 33 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CurrentUser = void 0;
const common_1 = __webpack_require__(2);
exports.CurrentUser = (0, common_1.createParamDecorator)((data, ctx) => {
    const request = ctx.switchToHttp().getRequest();
    return request.user;
});


/***/ }),
/* 34 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SensitiveOperationsDto = exports.UserActivityDto = exports.EntityHistoryDto = exports.AuditStatisticsDto = exports.PaginatedAuditLogsDto = exports.AuditLogResponseDto = exports.PaginationDto = exports.AuditLogFilterDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class AuditLogFilterDto {
}
exports.AuditLogFilterDto = AuditLogFilterDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "userId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "entityType", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(['CREATE', 'UPDATE', 'DELETE']),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "action", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsISO8601)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsISO8601)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AuditLogFilterDto.prototype, "searchText", void 0);
class PaginationDto {
    constructor() {
        this.limit = 50;
    }
}
exports.PaginationDto = PaginationDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], PaginationDto.prototype, "cursor", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(500),
    __metadata("design:type", Number)
], PaginationDto.prototype, "limit", void 0);
class AuditLogResponseDto {
}
exports.AuditLogResponseDto = AuditLogResponseDto;
class PaginatedAuditLogsDto {
}
exports.PaginatedAuditLogsDto = PaginatedAuditLogsDto;
class AuditStatisticsDto {
}
exports.AuditStatisticsDto = AuditStatisticsDto;
class EntityHistoryDto {
}
exports.EntityHistoryDto = EntityHistoryDto;
class UserActivityDto {
}
exports.UserActivityDto = UserActivityDto;
class SensitiveOperationsDto {
}
exports.SensitiveOperationsDto = SensitiveOperationsDto;


/***/ }),
/* 35 */
/***/ ((module) => {

module.exports = require("class-validator");

/***/ }),
/* 36 */
/***/ ((module) => {

module.exports = require("class-transformer");

/***/ }),
/* 37 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PerformanceInterceptor = void 0;
const common_1 = __webpack_require__(2);
const operators_1 = __webpack_require__(38);
let PerformanceInterceptor = class PerformanceInterceptor {
    constructor() {
        this.logger = new common_1.Logger('Performance');
    }
    intercept(context, next) {
        const request = context.switchToHttp().getRequest();
        const { method, url } = request;
        const startTime = Date.now();
        return next.handle().pipe((0, operators_1.tap)(() => {
            const endTime = Date.now();
            const duration = endTime - startTime;
            if (duration > 1000) {
                this.logger.warn(`⚠️ Slow request: ${method} ${url} - ${duration}ms`);
            }
            else if (duration > 500) {
                this.logger.log(`⏱️ Medium request: ${method} ${url} - ${duration}ms`);
            }
            else {
                this.logger.debug(`✓ Fast request: ${method} ${url} - ${duration}ms`);
            }
        }));
    }
};
exports.PerformanceInterceptor = PerformanceInterceptor;
exports.PerformanceInterceptor = PerformanceInterceptor = __decorate([
    (0, common_1.Injectable)()
], PerformanceInterceptor);


/***/ }),
/* 38 */
/***/ ((module) => {

module.exports = require("rxjs/operators");

/***/ }),
/* 39 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuditInterceptor = void 0;
const common_1 = __webpack_require__(2);
const operators_1 = __webpack_require__(38);
const audit_service_1 = __webpack_require__(14);
const core_1 = __webpack_require__(1);
let AuditInterceptor = class AuditInterceptor {
    constructor(auditService, request) {
        this.auditService = auditService;
        this.request = request;
    }
    intercept(context, next) {
        const request = context.switchToHttp().getRequest();
        const { method, url, params, body } = request;
        const { user } = request;
        const isAuditableOperation = ['POST', 'PUT', 'DELETE'].includes(method);
        if (!isAuditableOperation || !user?.sub) {
            return next.handle();
        }
        const requestBody = { ...body };
        return next.handle().pipe((0, operators_1.tap)((data) => {
            this.logOperation(request, user, method, url, params, requestBody, data, 'SUCCESS');
        }), (0, operators_1.catchError)((error) => {
            this.logOperation(request, user, method, url, params, requestBody, null, 'FAILED', error.message);
            throw error;
        }));
    }
    async logOperation(request, user, method, url, params, requestBody, responseData, status, errorMessage) {
        try {
            const pathParts = url.split('/');
            const resourcePath = pathParts[pathParts.length - 1] || '';
            const entityType = this.getEntityType(resourcePath, pathParts);
            const entityId = params.id || responseData?.id || 'unknown';
            const actionMap = {
                POST: 'CREATE',
                PUT: 'UPDATE',
                DELETE: 'DELETE',
            };
            const action = status === 'FAILED'
                ? `${actionMap[method]}_FAILED`
                : actionMap[method];
            await this.auditService.log({
                userId: user.sub,
                hospitalId: user.hospitalId || 'unknown',
                action,
                entityType,
                entityId,
                beforeState: method === 'DELETE' ? null : requestBody,
                afterState: status === 'SUCCESS' ? responseData : null,
            });
        }
        catch (error) {
            console.error('[AUDIT_INTERCEPTOR_ERROR]', error);
        }
    }
    getEntityType(resourcePath, pathParts) {
        const resource = resourcePath.split('?')[0];
        const entityMap = {
            'shifts': 'Shift',
            'shift-templates': 'ShiftTemplate',
            'leaves': 'Leave',
            'leave-requests': 'LeaveRequest',
            'leave-types': 'LeaveType',
            'attendance-records': 'AttendanceRecord',
            'biometric-devices': 'BiometricDevice',
            'biometric-enrollments': 'BiometricEnrollment',
            'device-sync': 'DeviceSync',
            'employees': 'Employee',
            'users': 'User',
            'roles': 'Role',
            'permissions': 'Permission',
        };
        return entityMap[resource] || this.convertToEntity(resource);
    }
    convertToEntity(kebab) {
        return kebab
            .split('-')
            .map((word) => word.charAt(0).toUpperCase() + word.slice(1))
            .join('');
    }
};
exports.AuditInterceptor = AuditInterceptor;
exports.AuditInterceptor = AuditInterceptor = __decorate([
    (0, common_1.Injectable)(),
    __param(1, (0, common_1.Inject)(core_1.REQUEST)),
    __metadata("design:paramtypes", [typeof (_a = typeof audit_service_1.AuditService !== "undefined" && audit_service_1.AuditService) === "function" ? _a : Object, Object])
], AuditInterceptor);


/***/ }),
/* 40 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuthModule = void 0;
const common_1 = __webpack_require__(2);
const jwt_1 = __webpack_require__(41);
const passport_1 = __webpack_require__(30);
const config_1 = __webpack_require__(4);
const auth_service_1 = __webpack_require__(42);
const auth_controller_1 = __webpack_require__(44);
const jwt_strategy_1 = __webpack_require__(50);
const database_module_1 = __webpack_require__(52);
const permissions_module_1 = __webpack_require__(53);
const permissions_guard_1 = __webpack_require__(60);
const common_module_1 = __webpack_require__(10);
let AuthModule = class AuthModule {
};
exports.AuthModule = AuthModule;
exports.AuthModule = AuthModule = __decorate([
    (0, common_1.Module)({
        imports: [
            database_module_1.DatabaseModule,
            passport_1.PassportModule,
            common_module_1.CommonModule,
            permissions_module_1.PermissionsModule,
            jwt_1.JwtModule.registerAsync({
                imports: [config_1.ConfigModule],
                inject: [config_1.ConfigService],
                useFactory: (configService) => ({
                    secret: configService.get('JWT_SECRET') || 'development-secret-key',
                    signOptions: {
                        expiresIn: (configService.get('JWT_EXPIRATION') || '24h'),
                    },
                }),
            }),
        ],
        controllers: [auth_controller_1.AuthController],
        providers: [auth_service_1.AuthService, jwt_strategy_1.JwtStrategy, permissions_guard_1.PermissionsGuard],
        exports: [auth_service_1.AuthService, permissions_guard_1.PermissionsGuard],
    })
], AuthModule);


/***/ }),
/* 41 */
/***/ ((module) => {

module.exports = require("@nestjs/jwt");

/***/ }),
/* 42 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuthService = void 0;
const common_1 = __webpack_require__(2);
const jwt_1 = __webpack_require__(41);
const config_1 = __webpack_require__(4);
const argon2 = __webpack_require__(43);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
const crypto_1 = __webpack_require__(19);
let AuthService = class AuthService {
    constructor(prisma, jwtService, configService, cacheService) {
        this.prisma = prisma;
        this.jwtService = jwtService;
        this.configService = configService;
        this.cacheService = cacheService;
    }
    async register(registerDto) {
        const existingUser = await this.prisma.user.findUnique({
            where: { email: registerDto.email },
        });
        if (existingUser) {
            throw new common_1.ConflictException('User with this email already exists');
        }
        if (registerDto.role === 'SUPER_ADMIN') {
            if (registerDto.hospitalId) {
                throw new common_1.BadRequestException('SUPER_ADMIN should not be assigned to a specific hospital');
            }
            if (registerDto.pharmacyId) {
                throw new common_1.BadRequestException('SUPER_ADMIN should not be assigned to a pharmacy');
            }
        }
        else {
            if (!registerDto.hospitalId) {
                throw new common_1.BadRequestException('Hospital ID is required for this role');
            }
            const hospital = await this.prisma.hospital.findUnique({
                where: { id: registerDto.hospitalId },
            });
            if (!hospital) {
                throw new common_1.BadRequestException('Invalid hospital ID');
            }
            if (registerDto.pharmacyId) {
                const pharmacy = await this.prisma.pharmacy.findFirst({
                    where: {
                        id: registerDto.pharmacyId,
                        hospitalId: registerDto.hospitalId,
                    },
                });
                if (!pharmacy) {
                    throw new common_1.BadRequestException('Invalid pharmacy ID for this hospital');
                }
            }
        }
        const hashedPassword = await argon2.hash(registerDto.password);
        const user = await this.prisma.user.create({
            data: {
                email: registerDto.email,
                passwordHash: hashedPassword,
                fullName: registerDto.fullName,
                phone: registerDto.phone,
                role: registerDto.role,
                hospitalId: registerDto.hospitalId,
                pharmacyId: registerDto.pharmacyId,
                status: 'ACTIVE',
            },
            select: {
                id: true,
                email: true,
                fullName: true,
                phone: true,
                role: true,
                hospitalId: true,
                pharmacyId: true,
                status: true,
                createdAt: true,
            },
        });
        const token = await this.generateToken(user);
        const refreshToken = await this.generateRefreshToken(user);
        return {
            user,
            accessToken: token,
            refreshToken,
        };
    }
    async login(loginDto) {
        const user = await this.prisma.user.findUnique({
            where: { email: loginDto.email },
            select: {
                id: true,
                email: true,
                passwordHash: true,
                fullName: true,
                phone: true,
                role: true,
                hospitalId: true,
                pharmacyId: true,
                status: true,
            },
        });
        if (!user) {
            throw new common_1.UnauthorizedException('Invalid email or password');
        }
        if (user.status !== 'ACTIVE') {
            throw new common_1.UnauthorizedException('Account is deactivated');
        }
        const isPasswordValid = await argon2.verify(user.passwordHash, loginDto.password);
        if (!isPasswordValid) {
            throw new common_1.UnauthorizedException('Invalid email or password');
        }
        this.prisma.user.update({
            where: { id: user.id },
            data: { lastLogin: new Date() },
        }).catch(err => {
            console.error('Failed to update lastLogin:', err);
        });
        const token = await this.generateToken(user);
        const refreshToken = await this.generateRefreshToken(user);
        return {
            user: {
                id: user.id,
                email: user.email,
                fullName: user.fullName,
                phone: user.phone,
                role: user.role,
                hospitalId: user.hospitalId,
                pharmacyId: user.pharmacyId,
                status: user.status,
            },
            accessToken: token,
            refreshToken,
        };
    }
    async requestPasswordReset(requestDto) {
        const user = await this.prisma.user.findUnique({
            where: { email: requestDto.email },
        });
        if (!user) {
            return { message: 'If the email exists, a reset link has been sent' };
        }
        const resetToken = (0, crypto_1.randomBytes)(32).toString('hex');
        const resetTokenHash = await argon2.hash(resetToken);
        const resetTokenExpiry = new Date(Date.now() + 3600000);
        await this.prisma.user.update({
            where: { id: user.id },
            data: {
                resetToken: resetTokenHash,
                resetTokenExpiry,
            },
        });
        console.log(`Password reset token for ${user.email}: ${resetToken}`);
        return { message: 'If the email exists, a reset link has been sent' };
    }
    async resetPassword(resetDto) {
        const users = await this.prisma.user.findMany({
            where: {
                resetToken: { not: null },
                resetTokenExpiry: { gte: new Date() },
            },
        });
        let matchedUser = null;
        for (const user of users) {
            if (user.resetToken) {
                const isValid = await argon2.verify(user.resetToken, resetDto.token);
                if (isValid) {
                    matchedUser = user;
                    break;
                }
            }
        }
        if (!matchedUser) {
            throw new common_1.BadRequestException('Invalid or expired reset token');
        }
        const hashedPassword = await argon2.hash(resetDto.newPassword);
        await this.prisma.user.update({
            where: { id: matchedUser.id },
            data: {
                passwordHash: hashedPassword,
                resetToken: null,
                resetTokenExpiry: null,
            },
        });
        return { message: 'Password reset successfully' };
    }
    async changePassword(userId, changeDto) {
        const user = await this.prisma.user.findUnique({
            where: { id: userId },
        });
        if (!user) {
            throw new common_1.BadRequestException('User not found');
        }
        const isPasswordValid = await argon2.verify(user.passwordHash, changeDto.currentPassword);
        if (!isPasswordValid) {
            throw new common_1.UnauthorizedException('Current password is incorrect');
        }
        const hashedPassword = await argon2.hash(changeDto.newPassword);
        await this.prisma.user.update({
            where: { id: userId },
            data: { passwordHash: hashedPassword },
        });
        return { message: 'Password changed successfully' };
    }
    async getProfile(userId) {
        const cacheKey = `user:profile:${userId}`;
        const cached = this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const user = await this.prisma.user.findUnique({
            where: { id: userId },
            select: {
                id: true,
                email: true,
                fullName: true,
                phone: true,
                role: true,
                hospitalId: true,
                pharmacyId: true,
                status: true,
                lastLogin: true,
                createdAt: true,
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                    },
                },
            },
        });
        if (!user) {
            throw new common_1.BadRequestException('User not found');
        }
        this.cacheService.set(cacheKey, user, 300000);
        return user;
    }
    async refreshToken(refreshToken) {
        if (!refreshToken) {
            throw new common_1.UnauthorizedException('Refresh token is required');
        }
        try {
            const payload = this.jwtService.verify(refreshToken, {
                secret: this.configService.get('JWT_REFRESH_SECRET') || this.configService.get('JWT_SECRET'),
            });
            const user = await this.prisma.user.findUnique({
                where: { id: payload.sub },
            });
            if (!user || user.status !== 'ACTIVE') {
                throw new common_1.UnauthorizedException('Invalid refresh token');
            }
            const newAccessToken = await this.generateToken(user);
            return {
                accessToken: newAccessToken,
                refreshToken,
            };
        }
        catch (error) {
            throw new common_1.UnauthorizedException('Invalid or expired refresh token');
        }
    }
    async generateToken(user) {
        const payload = {
            sub: user.id,
            email: user.email,
            role: user.role,
            hospitalId: user.hospitalId,
            pharmacyId: user.pharmacyId,
        };
        return this.jwtService.sign(payload);
    }
    async generateRefreshToken(user) {
        const payload = {
            sub: user.id,
            email: user.email,
            role: user.role,
            hospitalId: user.hospitalId,
            pharmacyId: user.pharmacyId,
        };
        return this.jwtService.sign(payload, {
            secret: this.configService.get('JWT_REFRESH_SECRET') || this.configService.get('JWT_SECRET'),
            expiresIn: '7d',
        });
    }
};
exports.AuthService = AuthService;
exports.AuthService = AuthService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof jwt_1.JwtService !== "undefined" && jwt_1.JwtService) === "function" ? _b : Object, typeof (_c = typeof config_1.ConfigService !== "undefined" && config_1.ConfigService) === "function" ? _c : Object, typeof (_d = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _d : Object])
], AuthService);


/***/ }),
/* 43 */
/***/ ((module) => {

module.exports = require("argon2");

/***/ }),
/* 44 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AuthController = void 0;
const common_1 = __webpack_require__(2);
const throttler_1 = __webpack_require__(8);
const auth_service_1 = __webpack_require__(42);
const dto_1 = __webpack_require__(45);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_user_decorator_1 = __webpack_require__(49);
let AuthController = class AuthController {
    constructor(authService) {
        this.authService = authService;
    }
    async register(registerDto) {
        return this.authService.register(registerDto);
    }
    async login(loginDto) {
        return this.authService.login(loginDto);
    }
    async requestPasswordReset(requestDto) {
        return this.authService.requestPasswordReset(requestDto);
    }
    async resetPassword(resetDto) {
        return this.authService.resetPassword(resetDto);
    }
    async changePassword(userId, changeDto) {
        return this.authService.changePassword(userId, changeDto);
    }
    async getProfile(userId) {
        return this.authService.getProfile(userId);
    }
    async logout() {
        return { message: 'Logged out successfully' };
    }
    async verifyToken(user) {
        return {
            valid: true,
            user: {
                id: user.id,
                email: user.email,
                fullName: user.fullName,
                role: user.role,
                hospitalId: user.hospitalId,
                pharmacyId: user.pharmacyId,
            },
        };
    }
    async refreshToken(refreshToken) {
        return this.authService.refreshToken(refreshToken);
    }
};
exports.AuthController = AuthController;
__decorate([
    (0, common_1.Post)('register'),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.RegisterDto !== "undefined" && dto_1.RegisterDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "register", null);
__decorate([
    (0, common_1.Post)('login'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.LoginDto !== "undefined" && dto_1.LoginDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "login", null);
__decorate([
    (0, common_1.Post)('password-reset/request'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof dto_1.RequestPasswordResetDto !== "undefined" && dto_1.RequestPasswordResetDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "requestPasswordReset", null);
__decorate([
    (0, common_1.Post)('password-reset/confirm'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof dto_1.ResetPasswordDto !== "undefined" && dto_1.ResetPasswordDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "resetPassword", null);
__decorate([
    (0, common_1.Patch)('change-password'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    __param(0, (0, current_user_decorator_1.CurrentUser)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof dto_1.ChangePasswordDto !== "undefined" && dto_1.ChangePasswordDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "changePassword", null);
__decorate([
    (0, common_1.Get)('profile'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    __param(0, (0, current_user_decorator_1.CurrentUser)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "getProfile", null);
__decorate([
    (0, common_1.Post)('logout'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "logout", null);
__decorate([
    (0, common_1.Post)('verify'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, throttler_1.SkipThrottle)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "verifyToken", null);
__decorate([
    (0, common_1.Post)('refresh'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Body)('refreshToken')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], AuthController.prototype, "refreshToken", null);
exports.AuthController = AuthController = __decorate([
    (0, common_1.Controller)('auth'),
    __metadata("design:paramtypes", [typeof (_a = typeof auth_service_1.AuthService !== "undefined" && auth_service_1.AuthService) === "function" ? _a : Object])
], AuthController);


/***/ }),
/* 45 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
__exportStar(__webpack_require__(46), exports);
__exportStar(__webpack_require__(47), exports);
__exportStar(__webpack_require__(48), exports);


/***/ }),
/* 46 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LoginDto = void 0;
const class_validator_1 = __webpack_require__(35);
class LoginDto {
}
exports.LoginDto = LoginDto;
__decorate([
    (0, class_validator_1.IsEmail)({}, { message: 'Please provide a valid email address' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Email is required' }),
    __metadata("design:type", String)
], LoginDto.prototype, "email", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Password is required' }),
    (0, class_validator_1.MinLength)(8, { message: 'Password must be at least 8 characters' }),
    __metadata("design:type", String)
], LoginDto.prototype, "password", void 0);


/***/ }),
/* 47 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RegisterDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class RegisterDto {
}
exports.RegisterDto = RegisterDto;
__decorate([
    (0, class_validator_1.IsEmail)({}, { message: 'Please provide a valid email address' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Email is required' }),
    __metadata("design:type", String)
], RegisterDto.prototype, "email", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Password is required' }),
    (0, class_validator_1.MinLength)(8, { message: 'Password must be at least 8 characters' }),
    (0, class_validator_1.Matches)(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&#])[A-Za-z\d@$!%*?&#]/, {
        message: 'Password must contain uppercase, lowercase, number and special character',
    }),
    __metadata("design:type", String)
], RegisterDto.prototype, "password", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Full name is required' }),
    __metadata("design:type", String)
], RegisterDto.prototype, "fullName", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RegisterDto.prototype, "phone", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.UserRole, { message: 'Invalid user role' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Role is required' }),
    __metadata("design:type", typeof (_a = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _a : Object)
], RegisterDto.prototype, "role", void 0);
__decorate([
    (0, class_validator_1.IsUUID)('4', { message: 'Invalid hospital ID' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RegisterDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)('4', { message: 'Invalid pharmacy ID' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RegisterDto.prototype, "pharmacyId", void 0);


/***/ }),
/* 48 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChangePasswordDto = exports.ResetPasswordDto = exports.RequestPasswordResetDto = void 0;
const class_validator_1 = __webpack_require__(35);
class RequestPasswordResetDto {
}
exports.RequestPasswordResetDto = RequestPasswordResetDto;
__decorate([
    (0, class_validator_1.IsEmail)({}, { message: 'Please provide a valid email address' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Email is required' }),
    __metadata("design:type", String)
], RequestPasswordResetDto.prototype, "email", void 0);
class ResetPasswordDto {
}
exports.ResetPasswordDto = ResetPasswordDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Reset token is required' }),
    __metadata("design:type", String)
], ResetPasswordDto.prototype, "token", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'New password is required' }),
    (0, class_validator_1.MinLength)(8, { message: 'Password must be at least 8 characters' }),
    (0, class_validator_1.Matches)(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&#])[A-Za-z\d@$!%*?&#]/, {
        message: 'Password must contain uppercase, lowercase, number and special character',
    }),
    __metadata("design:type", String)
], ResetPasswordDto.prototype, "newPassword", void 0);
class ChangePasswordDto {
}
exports.ChangePasswordDto = ChangePasswordDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Current password is required' }),
    __metadata("design:type", String)
], ChangePasswordDto.prototype, "currentPassword", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'New password is required' }),
    (0, class_validator_1.MinLength)(8, { message: 'Password must be at least 8 characters' }),
    (0, class_validator_1.Matches)(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&#])[A-Za-z\d@$!%*?&#]/, {
        message: 'Password must contain uppercase, lowercase, number and special character',
    }),
    __metadata("design:type", String)
], ChangePasswordDto.prototype, "newPassword", void 0);


/***/ }),
/* 49 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CurrentUser = void 0;
const common_1 = __webpack_require__(2);
exports.CurrentUser = (0, common_1.createParamDecorator)((data, ctx) => {
    const request = ctx.switchToHttp().getRequest();
    const user = request.user;
    return data ? user?.[data] : user;
});


/***/ }),
/* 50 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.JwtStrategy = void 0;
const common_1 = __webpack_require__(2);
const passport_1 = __webpack_require__(30);
const passport_jwt_1 = __webpack_require__(51);
const config_1 = __webpack_require__(4);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
let JwtStrategy = class JwtStrategy extends (0, passport_1.PassportStrategy)(passport_jwt_1.Strategy) {
    constructor(configService, prisma, cacheService) {
        super({
            jwtFromRequest: passport_jwt_1.ExtractJwt.fromAuthHeaderAsBearerToken(),
            ignoreExpiration: false,
            secretOrKey: configService.get('JWT_SECRET'),
        });
        this.configService = configService;
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    async validate(payload) {
        const cacheKey = `user:auth:${payload.sub}`;
        const cachedUser = await this.cacheService.get(cacheKey);
        if (cachedUser) {
            if (cachedUser.status !== 'ACTIVE') {
                throw new common_1.UnauthorizedException('Invalid or inactive user');
            }
            return cachedUser;
        }
        const user = await this.prisma.user.findUnique({
            where: { id: payload.sub },
            select: {
                id: true,
                email: true,
                fullName: true,
                phone: true,
                role: true,
                hospitalId: true,
                pharmacyId: true,
                status: true,
            },
        });
        if (!user || user.status !== 'ACTIVE') {
            throw new common_1.UnauthorizedException('Invalid or inactive user');
        }
        await this.cacheService.set(cacheKey, user, 120000);
        return user;
    }
};
exports.JwtStrategy = JwtStrategy;
exports.JwtStrategy = JwtStrategy = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof config_1.ConfigService !== "undefined" && config_1.ConfigService) === "function" ? _a : Object, typeof (_b = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _b : Object, typeof (_c = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _c : Object])
], JwtStrategy);


/***/ }),
/* 51 */
/***/ ((module) => {

module.exports = require("passport-jwt");

/***/ }),
/* 52 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DatabaseModule = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let DatabaseModule = class DatabaseModule {
};
exports.DatabaseModule = DatabaseModule;
exports.DatabaseModule = DatabaseModule = __decorate([
    (0, common_1.Global)(),
    (0, common_1.Module)({
        providers: [prisma_service_1.PrismaService],
        exports: [prisma_service_1.PrismaService],
    })
], DatabaseModule);


/***/ }),
/* 53 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PermissionsModule = void 0;
const common_1 = __webpack_require__(2);
const permissions_service_1 = __webpack_require__(54);
const permissions_controller_1 = __webpack_require__(55);
const database_module_1 = __webpack_require__(52);
const common_module_1 = __webpack_require__(10);
let PermissionsModule = class PermissionsModule {
};
exports.PermissionsModule = PermissionsModule;
exports.PermissionsModule = PermissionsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule, common_module_1.CommonModule],
        controllers: [permissions_controller_1.PermissionsController],
        providers: [permissions_service_1.PermissionsService],
        exports: [permissions_service_1.PermissionsService],
    })
], PermissionsModule);


/***/ }),
/* 54 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PermissionsService = void 0;
const common_1 = __webpack_require__(2);
const client_1 = __webpack_require__(16);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
let PermissionsService = class PermissionsService {
    constructor(prisma, cacheService) {
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    async createPermission(createPermissionDto) {
        const { resource, action, scope, description } = createPermissionDto;
        const existing = await this.prisma.permission.findUnique({
            where: {
                resource_action_scope: { resource, action, scope },
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Permission ${resource}:${action}:${scope} already exists`);
        }
        const permission = await this.prisma.permission.create({
            data: { resource, action, scope, description },
        });
        await this.invalidatePermissionCache();
        return permission;
    }
    async findAllPermissions() {
        return this.prisma.permission.findMany({
            include: {
                rolePermissions: {
                    select: {
                        role: true,
                    },
                },
            },
            orderBy: [{ resource: 'asc' }, { action: 'asc' }, { scope: 'asc' }],
        });
    }
    async findPermissionById(id) {
        const permission = await this.prisma.permission.findUnique({
            where: { id },
            include: {
                rolePermissions: {
                    select: {
                        role: true,
                    },
                },
            },
        });
        if (!permission) {
            throw new common_1.NotFoundException(`Permission with ID ${id} not found`);
        }
        return permission;
    }
    async updatePermission(id, updatePermissionDto) {
        await this.findPermissionById(id);
        const permission = await this.prisma.permission.update({
            where: { id },
            data: updatePermissionDto,
        });
        await this.invalidatePermissionCache();
        return permission;
    }
    async deletePermission(id) {
        await this.findPermissionById(id);
        await this.prisma.permission.delete({
            where: { id },
        });
        await this.invalidatePermissionCache();
        return { message: 'Permission deleted successfully' };
    }
    async assignPermissionToRole(assignPermissionDto) {
        const { role, permissionId } = assignPermissionDto;
        await this.findPermissionById(permissionId);
        const existing = await this.prisma.rolePermission.findUnique({
            where: {
                role_permissionId: { role, permissionId },
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Permission already assigned to role ${role}`);
        }
        const rolePermission = await this.prisma.rolePermission.create({
            data: { role, permissionId },
            include: {
                permission: true,
            },
        });
        await this.invalidatePermissionCache(role);
        return rolePermission;
    }
    async removePermissionFromRole(role, permissionId) {
        const existing = await this.prisma.rolePermission.findUnique({
            where: {
                role_permissionId: { role, permissionId },
            },
        });
        if (!existing) {
            throw new common_1.NotFoundException(`Permission not assigned to role ${role}`);
        }
        await this.prisma.rolePermission.delete({
            where: {
                role_permissionId: { role, permissionId },
            },
        });
        await this.invalidatePermissionCache(role);
        return { message: 'Permission removed from role successfully' };
    }
    async getPermissionsByRole(role) {
        const cacheKey = `permissions:role:${role}`;
        const cached = await this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const rolePermissions = await this.prisma.rolePermission.findMany({
            where: { role },
            include: {
                permission: true,
            },
        });
        const permissions = rolePermissions.map((rp) => rp.permission);
        await this.cacheService.set(cacheKey, permissions, 300);
        return permissions;
    }
    async hasPermission(role, resource, action, scope) {
        const permissions = await this.getPermissionsByRole(role);
        return permissions.some((permission) => {
            if (permission.resource !== resource || permission.action !== action) {
                return false;
            }
            if (!scope)
                return true;
            if (permission.scope === 'all')
                return true;
            return permission.scope === scope;
        });
    }
    async getUserPermissions(userId) {
        const user = await this.prisma.user.findUnique({
            where: { id: userId },
            select: {
                role: true,
                pharmacyId: true,
            },
        });
        if (!user) {
            throw new common_1.NotFoundException(`User with ID ${userId} not found`);
        }
        const permissions = await this.getPermissionsByRole(user.role);
        return {
            role: user.role,
            pharmacyId: user.pharmacyId,
            permissions: permissions.map((p) => ({
                id: p.id,
                resource: p.resource,
                action: p.action,
                scope: p.scope,
                description: p.description,
            })),
        };
    }
    async getAllRoles() {
        return Object.values(client_1.UserRole);
    }
    async getRolePermissionMatrix() {
        const roles = await this.getAllRoles();
        const matrix = {};
        for (const role of roles) {
            matrix[role] = await this.getPermissionsByRole(role);
        }
        return matrix;
    }
    async invalidatePermissionCache(role) {
        if (role) {
            await this.cacheService.delete(`permissions:role:${role}`);
        }
        else {
            await this.cacheService.deletePattern('permissions:role:.*');
        }
    }
};
exports.PermissionsService = PermissionsService;
exports.PermissionsService = PermissionsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _b : Object])
], PermissionsService);


/***/ }),
/* 55 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PermissionsController = void 0;
const common_1 = __webpack_require__(2);
const client_1 = __webpack_require__(16);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const permissions_service_1 = __webpack_require__(54);
const create_permission_dto_1 = __webpack_require__(56);
const update_permission_dto_1 = __webpack_require__(57);
const assign_permission_dto_1 = __webpack_require__(59);
let PermissionsController = class PermissionsController {
    constructor(permissionsService) {
        this.permissionsService = permissionsService;
    }
    async createPermission(createPermissionDto) {
        return this.permissionsService.createPermission(createPermissionDto);
    }
    async findAllPermissions() {
        return this.permissionsService.findAllPermissions();
    }
    async findPermissionById(id) {
        return this.permissionsService.findPermissionById(id);
    }
    async updatePermission(id, updatePermissionDto) {
        return this.permissionsService.updatePermission(id, updatePermissionDto);
    }
    async deletePermission(id) {
        return this.permissionsService.deletePermission(id);
    }
    async assignPermissionToRole(assignPermissionDto) {
        return this.permissionsService.assignPermissionToRole(assignPermissionDto);
    }
    async removePermissionFromRole(role, permissionId) {
        return this.permissionsService.removePermissionFromRole(role, permissionId);
    }
    async getPermissionsByRole(role) {
        return this.permissionsService.getPermissionsByRole(role);
    }
    async checkPermission(role, resource, action, scope) {
        const hasPermission = await this.permissionsService.hasPermission(role, resource, action, scope);
        return { hasPermission };
    }
    async getUserPermissions(userId) {
        return this.permissionsService.getUserPermissions(userId);
    }
    async getAllRoles() {
        return this.permissionsService.getAllRoles();
    }
    async getRolePermissionMatrix() {
        return this.permissionsService.getRolePermissionMatrix();
    }
};
exports.PermissionsController = PermissionsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_permission_dto_1.CreatePermissionDto !== "undefined" && create_permission_dto_1.CreatePermissionDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "createPermission", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "findAllPermissions", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "findPermissionById", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof update_permission_dto_1.UpdatePermissionDto !== "undefined" && update_permission_dto_1.UpdatePermissionDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "updatePermission", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "deletePermission", null);
__decorate([
    (0, common_1.Post)('assign'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof assign_permission_dto_1.AssignPermissionDto !== "undefined" && assign_permission_dto_1.AssignPermissionDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "assignPermissionToRole", null);
__decorate([
    (0, common_1.Delete)('role/:role/permission/:permissionId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('role')),
    __param(1, (0, common_1.Param)('permissionId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _e : Object, String]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "removePermissionFromRole", null);
__decorate([
    (0, common_1.Get)('role/:role'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Param)('role')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_f = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "getPermissionsByRole", null);
__decorate([
    (0, common_1.Get)('check/:role'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Param)('role')),
    __param(1, (0, common_1.Query)('resource')),
    __param(2, (0, common_1.Query)('action')),
    __param(3, (0, common_1.Query)('scope')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_g = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _g : Object, String, String, String]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "checkPermission", null);
__decorate([
    (0, common_1.Get)('user/:userId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Param)('userId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "getUserPermissions", null);
__decorate([
    (0, common_1.Get)('roles/all'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.AUDITOR),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "getAllRoles", null);
__decorate([
    (0, common_1.Get)('matrix/all'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], PermissionsController.prototype, "getRolePermissionMatrix", null);
exports.PermissionsController = PermissionsController = __decorate([
    (0, common_1.Controller)('permissions'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof permissions_service_1.PermissionsService !== "undefined" && permissions_service_1.PermissionsService) === "function" ? _a : Object])
], PermissionsController);


/***/ }),
/* 56 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreatePermissionDto = void 0;
const class_validator_1 = __webpack_require__(35);
class CreatePermissionDto {
}
exports.CreatePermissionDto = CreatePermissionDto;
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreatePermissionDto.prototype, "resource", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsIn)(['read', 'write', 'delete', 'approve']),
    __metadata("design:type", String)
], CreatePermissionDto.prototype, "action", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsIn)(['all', 'own', 'own_pharmacy', 'own_department']),
    __metadata("design:type", String)
], CreatePermissionDto.prototype, "scope", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePermissionDto.prototype, "description", void 0);


/***/ }),
/* 57 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdatePermissionDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_permission_dto_1 = __webpack_require__(56);
class UpdatePermissionDto extends (0, mapped_types_1.PartialType)(create_permission_dto_1.CreatePermissionDto) {
}
exports.UpdatePermissionDto = UpdatePermissionDto;


/***/ }),
/* 58 */
/***/ ((module) => {

module.exports = require("@nestjs/mapped-types");

/***/ }),
/* 59 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AssignPermissionDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class AssignPermissionDto {
}
exports.AssignPermissionDto = AssignPermissionDto;
__decorate([
    (0, class_validator_1.IsEnum)(client_1.UserRole),
    __metadata("design:type", typeof (_a = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _a : Object)
], AssignPermissionDto.prototype, "role", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AssignPermissionDto.prototype, "permissionId", void 0);


/***/ }),
/* 60 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PermissionsGuard = void 0;
const common_1 = __webpack_require__(2);
const core_1 = __webpack_require__(1);
const require_permission_decorator_1 = __webpack_require__(61);
const permissions_service_1 = __webpack_require__(54);
let PermissionsGuard = class PermissionsGuard {
    constructor(reflector, permissionsService) {
        this.reflector = reflector;
        this.permissionsService = permissionsService;
    }
    async canActivate(context) {
        const permissionReq = this.reflector.getAllAndOverride(require_permission_decorator_1.REQUIRE_PERMISSION_KEY, [context.getHandler(), context.getClass()]);
        if (!permissionReq) {
            return true;
        }
        const request = context.switchToHttp().getRequest();
        const user = request.user;
        if (!user) {
            throw new common_1.ForbiddenException('User not authenticated');
        }
        const hasPermission = await this.permissionsService.hasPermission(user.role, permissionReq.resource, permissionReq.action, permissionReq.scope);
        if (!hasPermission) {
            throw new common_1.ForbiddenException(`You don't have permission to ${permissionReq.action} ${permissionReq.resource}`);
        }
        return true;
    }
};
exports.PermissionsGuard = PermissionsGuard;
exports.PermissionsGuard = PermissionsGuard = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof core_1.Reflector !== "undefined" && core_1.Reflector) === "function" ? _a : Object, typeof (_b = typeof permissions_service_1.PermissionsService !== "undefined" && permissions_service_1.PermissionsService) === "function" ? _b : Object])
], PermissionsGuard);


/***/ }),
/* 61 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RequirePermission = exports.REQUIRE_PERMISSION_KEY = void 0;
const common_1 = __webpack_require__(2);
exports.REQUIRE_PERMISSION_KEY = 'requirePermission';
const RequirePermission = (resource, action, scope) => (0, common_1.SetMetadata)(exports.REQUIRE_PERMISSION_KEY, {
    resource,
    action,
    scope,
});
exports.RequirePermission = RequirePermission;


/***/ }),
/* 62 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientsModule = void 0;
const common_1 = __webpack_require__(2);
const patients_service_1 = __webpack_require__(63);
const patients_controller_1 = __webpack_require__(66);
const database_module_1 = __webpack_require__(52);
const visits_module_1 = __webpack_require__(74);
const admissions_module_1 = __webpack_require__(76);
let PatientsModule = class PatientsModule {
};
exports.PatientsModule = PatientsModule;
exports.PatientsModule = PatientsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule, visits_module_1.VisitsModule, admissions_module_1.AdmissionsModule],
        providers: [patients_service_1.PatientsService],
        controllers: [patients_controller_1.PatientsController],
        exports: [patients_service_1.PatientsService],
    })
], PatientsModule);


/***/ }),
/* 63 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const visits_service_1 = __webpack_require__(64);
const admissions_service_1 = __webpack_require__(65);
const client_1 = __webpack_require__(16);
let PatientsService = class PatientsService {
    constructor(prisma, visitsService, admissionsService) {
        this.prisma = prisma;
        this.visitsService = visitsService;
        this.admissionsService = admissionsService;
    }
    async createVisitForRegistration(patientId, createPatientDto, userId, hospitalId) {
        return this.visitsService.create({
            hospitalId,
            patientId,
            registrarId: userId,
            visitType: createPatientDto.visitType,
            clinicId: createPatientDto.clinicId,
            departmentId: createPatientDto.department || undefined,
            wardId: createPatientDto.ward || undefined,
            bedId: createPatientDto.bed || undefined,
            attendingDoctorId: createPatientDto.attendingDoctorId || undefined,
            chiefComplaint: createPatientDto.chiefComplaint,
            vitalSigns: createPatientDto.vitalSigns,
            notes: createPatientDto.notes,
        });
    }
    async createAdmissionForRegistration(patientId, visitId, createPatientDto, userId, hospitalId) {
        if (createPatientDto.visitType !== 'WARD_INDOOR') {
            return null;
        }
        if (!createPatientDto.department) {
            throw new common_1.BadRequestException('Department is required for indoor admission');
        }
        if (!createPatientDto.attendingDoctorId) {
            throw new common_1.BadRequestException('Attending doctor is required for indoor admission');
        }
        return this.admissionsService.create({
            hospitalId,
            patientId,
            visitId,
            departmentId: createPatientDto.department,
            roomId: createPatientDto.ward || undefined,
            bedId: createPatientDto.bed || undefined,
            attendingDoctorId: createPatientDto.attendingDoctorId,
            admittingUserId: userId,
            admissionType: client_1.AdmissionType.PLANNED,
            diagnosisOnAdmission: createPatientDto.chiefComplaint,
            notes: createPatientDto.notes,
        });
    }
    async generateNRNumber(hospitalId) {
        const today = new Date();
        const year = today.getFullYear();
        const month = String(today.getMonth() + 1).padStart(2, '0');
        const day = String(today.getDate()).padStart(2, '0');
        const datePrefix = `${year}${month}${day}`;
        const startOfDay = new Date(today.setHours(0, 0, 0, 0));
        const endOfDay = new Date(today.setHours(23, 59, 59, 999));
        const todayCount = await this.prisma.patient.count({
            where: {
                hospitalId,
                registeredAt: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
        });
        const sequence = String(todayCount + 1).padStart(4, '0');
        return `NR-${datePrefix}-${sequence}`;
    }
    async create(createPatientDto, userId, hospitalId) {
        if (createPatientDto.attendingDoctorId) {
            const doctor = await this.prisma.user.findFirst({
                where: {
                    id: createPatientDto.attendingDoctorId,
                    hospitalId,
                    role: 'DOCTOR',
                },
            });
            if (!doctor) {
                throw new common_1.BadRequestException('Invalid attending doctor');
            }
        }
        const identifierFilters = [];
        if (createPatientDto.cnic) {
            identifierFilters.push({ cnic: createPatientDto.cnic });
        }
        if (createPatientDto.nrNumber) {
            identifierFilters.push({ nrNumber: createPatientDto.nrNumber });
        }
        if (identifierFilters.length > 0) {
            const existingPatient = await this.prisma.patient.findFirst({
                where: {
                    hospitalId,
                    OR: identifierFilters,
                },
                include: {
                    attendingDoctor: {
                        select: {
                            id: true,
                            fullName: true,
                            email: true,
                        },
                    },
                    registeredByUser: {
                        select: {
                            id: true,
                            fullName: true,
                            email: true,
                        },
                    },
                },
            });
            if (existingPatient) {
                const visitResult = await this.createVisitForRegistration(existingPatient.id, createPatientDto, userId, hospitalId);
                const visitId = visitResult?.visit?.id;
                if (createPatientDto.visitType === client_1.VisitType.WARD_INDOOR && visitId) {
                    await this.createAdmissionForRegistration(existingPatient.id, visitId, createPatientDto, userId, hospitalId);
                }
                return existingPatient;
            }
        }
        const nrNumber = await this.generateNRNumber(hospitalId);
        const patient = await this.prisma.patient.create({
            data: {
                hospitalId,
                nrNumber,
                fullName: createPatientDto.fullName,
                mobile: createPatientDto.mobile ?? null,
                cnic: createPatientDto.cnic,
                dob: createPatientDto.dob ? new Date(createPatientDto.dob) : null,
                gender: createPatientDto.gender,
                address: createPatientDto.address,
                visitType: createPatientDto.visitType,
                department: createPatientDto.department,
                ward: createPatientDto.ward,
                bed: createPatientDto.bed,
                attendingDoctorId: createPatientDto.attendingDoctorId || null,
                registeredBy: userId,
            },
            include: {
                attendingDoctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                registeredByUser: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
            },
        });
        const visitResult = await this.createVisitForRegistration(patient.id, createPatientDto, userId, hospitalId);
        const visitId = visitResult?.visit?.id;
        if (createPatientDto.visitType === client_1.VisitType.WARD_INDOOR && visitId) {
            await this.createAdmissionForRegistration(patient.id, visitId, createPatientDto, userId, hospitalId);
        }
        return patient;
    }
    async findAll(searchDto, hospitalId) {
        const { search, nrNumber, cnic, mobile, gender, visitType, attendingDoctorId, limit = 50, page = 1, sortBy = 'registeredAt', sortOrder = 'desc' } = searchDto;
        const skip = (page - 1) * limit;
        const where = {
            hospitalId,
        };
        if (search) {
            where.OR = [
                { nrNumber: { contains: search, mode: 'insensitive' } },
                { fullName: { contains: search, mode: 'insensitive' } },
                { mobile: { contains: search } },
                { cnic: { contains: search } },
            ];
        }
        if (nrNumber) {
            where.nrNumber = { contains: nrNumber, mode: 'insensitive' };
        }
        if (cnic) {
            where.cnic = { contains: cnic };
        }
        if (mobile) {
            where.mobile = { contains: mobile };
        }
        if (gender) {
            where.gender = gender;
        }
        if (visitType) {
            where.visitType = visitType;
        }
        if (attendingDoctorId) {
            where.attendingDoctorId = attendingDoctorId;
        }
        const total = await this.prisma.patient.count({ where });
        const patients = await this.prisma.patient.findMany({
            where,
            skip,
            take: limit,
            orderBy: { [sortBy]: sortOrder },
            include: {
                attendingDoctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                registeredByUser: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                _count: {
                    select: {
                        visits: true,
                    },
                },
                visits: {
                    where: {
                        status: { in: ['WAITING', 'IN_PROGRESS', 'CALLED'] },
                    },
                    include: {
                        clinic: {
                            select: {
                                id: true,
                                name: true,
                                opdFee: true,
                                doctor: {
                                    select: {
                                        id: true,
                                        fullName: true,
                                    },
                                },
                                department: {
                                    select: {
                                        id: true,
                                        name: true,
                                    },
                                },
                            },
                        },
                    },
                    orderBy: { createdAt: 'desc' },
                    take: 1,
                },
            },
        });
        const patientsWithDept = await Promise.all(patients.map(async (patient) => {
            let departmentInfo = null;
            if (patient.department) {
                departmentInfo = await this.prisma.department.findUnique({
                    where: { id: patient.department },
                    select: { id: true, name: true, code: true },
                });
            }
            let roomInfo = null;
            let bedInfo = null;
            let admissionInfo = null;
            if (patient.visitType === 'WARD_INDOOR') {
                const activeAdmission = await this.prisma.admission.findFirst({
                    where: {
                        patientId: patient.id,
                        status: 'ADMITTED',
                    },
                    orderBy: { admittedAt: 'desc' },
                    include: {
                        room: { select: { id: true, roomNumber: true, roomType: true } },
                        bed: { select: { id: true, bedNumber: true, bedType: true } },
                        department: { select: { id: true, name: true, code: true } },
                    },
                });
                if (activeAdmission) {
                    admissionInfo = {
                        id: activeAdmission.id,
                        admissionNumber: activeAdmission.admissionNumber,
                        admittedAt: activeAdmission.admittedAt,
                        department: activeAdmission.department,
                    };
                    roomInfo = activeAdmission.room;
                    bedInfo = activeAdmission.bed;
                    if (!departmentInfo && activeAdmission.department) {
                        departmentInfo = activeAdmission.department;
                    }
                }
            }
            return {
                ...patient,
                departmentInfo,
                admissionInfo,
                roomInfo,
                bedInfo,
            };
        }));
        return {
            data: patientsWithDept,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id, hospitalId) {
        const patient = await this.prisma.patient.findFirst({
            where: {
                id,
                hospitalId,
            },
            include: {
                attendingDoctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        phone: true,
                    },
                },
                registeredByUser: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                prescriptions: {
                    orderBy: { createdAt: 'desc' },
                    take: 5,
                },
                issueTransactions: {
                    orderBy: { issuedAt: 'desc' },
                    take: 5,
                },
                visits: {
                    include: {
                        clinic: {
                            select: {
                                id: true,
                                name: true,
                                opdFee: true,
                                doctor: {
                                    select: {
                                        id: true,
                                        fullName: true,
                                    },
                                },
                                department: {
                                    select: {
                                        id: true,
                                        name: true,
                                    },
                                },
                            },
                        },
                    },
                    orderBy: { createdAt: 'desc' },
                },
            },
        });
        if (!patient) {
            throw new common_1.NotFoundException('Patient not found');
        }
        let departmentInfo = null;
        if (patient.department) {
            departmentInfo = await this.prisma.department.findUnique({
                where: { id: patient.department },
                select: { id: true, name: true, code: true },
            });
        }
        let roomInfo = null;
        let bedInfo = null;
        let admissionInfo = null;
        if (patient.visitType === 'WARD_INDOOR') {
            const activeAdmission = await this.prisma.admission.findFirst({
                where: {
                    patientId: patient.id,
                    status: 'ADMITTED',
                },
                orderBy: { admittedAt: 'desc' },
                include: {
                    room: { select: { id: true, roomNumber: true, roomType: true } },
                    bed: { select: { id: true, bedNumber: true, bedType: true } },
                    department: { select: { id: true, name: true, code: true } },
                },
            });
            if (activeAdmission) {
                admissionInfo = {
                    id: activeAdmission.id,
                    admissionNumber: activeAdmission.admissionNumber,
                    admittedAt: activeAdmission.admittedAt,
                    department: activeAdmission.department,
                };
                roomInfo = activeAdmission.room;
                bedInfo = activeAdmission.bed;
                if (!departmentInfo && activeAdmission.department) {
                    departmentInfo = activeAdmission.department;
                }
            }
        }
        return {
            ...patient,
            departmentInfo,
            admissionInfo,
            roomInfo,
            bedInfo,
        };
    }
    async findByNRNumber(nrNumber, hospitalId) {
        const patient = await this.prisma.patient.findFirst({
            where: {
                nrNumber,
                hospitalId,
            },
            include: {
                attendingDoctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
            },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with NR-Number ${nrNumber} not found`);
        }
        return patient;
    }
    async update(id, updatePatientDto, hospitalId) {
        const existingPatient = await this.prisma.patient.findFirst({
            where: {
                id,
                hospitalId,
            },
        });
        if (!existingPatient) {
            throw new common_1.NotFoundException('Patient not found');
        }
        if (updatePatientDto.attendingDoctorId) {
            const doctor = await this.prisma.user.findFirst({
                where: {
                    id: updatePatientDto.attendingDoctorId,
                    hospitalId,
                    role: 'DOCTOR',
                },
            });
            if (!doctor) {
                throw new common_1.BadRequestException('Invalid attending doctor');
            }
        }
        const patient = await this.prisma.patient.update({
            where: { id },
            data: {
                fullName: updatePatientDto.fullName,
                mobile: updatePatientDto.mobile,
                cnic: updatePatientDto.cnic,
                dob: updatePatientDto.dob ? new Date(updatePatientDto.dob) : undefined,
                gender: updatePatientDto.gender,
                address: updatePatientDto.address,
                visitType: updatePatientDto.visitType,
                department: updatePatientDto.department,
                ward: updatePatientDto.ward,
                bed: updatePatientDto.bed,
                attendingDoctorId: updatePatientDto.attendingDoctorId,
            },
            include: {
                attendingDoctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
            },
        });
        return patient;
    }
    async remove(id, hospitalId) {
        const patient = await this.prisma.patient.findFirst({
            where: {
                id,
                hospitalId,
            },
        });
        if (!patient) {
            throw new common_1.NotFoundException('Patient not found');
        }
        const hasActiveRecords = await this.prisma.prescription.count({
            where: { nrNumber: patient.nrNumber },
        });
        if (hasActiveRecords > 0) {
            throw new common_1.BadRequestException('Cannot delete patient with existing prescriptions or transactions');
        }
        await this.prisma.patient.delete({
            where: { id },
        });
        return { message: 'Patient deleted successfully' };
    }
    async getStats(hospitalId) {
        const today = new Date();
        const startOfDay = new Date(today.setHours(0, 0, 0, 0));
        const endOfDay = new Date(today.setHours(23, 59, 59, 999));
        const [total, todayRegistrations, byVisitType, byGender] = await Promise.all([
            this.prisma.patient.count({ where: { hospitalId } }),
            this.prisma.patient.count({
                where: {
                    hospitalId,
                    registeredAt: {
                        gte: startOfDay,
                        lte: endOfDay,
                    },
                },
            }),
            this.prisma.patient.groupBy({
                by: ['visitType'],
                where: { hospitalId },
                _count: true,
            }),
            this.prisma.patient.groupBy({
                by: ['gender'],
                where: { hospitalId },
                _count: true,
            }),
        ]);
        return {
            total,
            todayRegistrations,
            byVisitType: byVisitType.reduce((acc, item) => {
                acc[item.visitType] = item._count;
                return acc;
            }, {}),
            byGender: byGender.reduce((acc, item) => {
                acc[item.gender] = item._count;
                return acc;
            }, {}),
        };
    }
};
exports.PatientsService = PatientsService;
exports.PatientsService = PatientsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof visits_service_1.VisitsService !== "undefined" && visits_service_1.VisitsService) === "function" ? _b : Object, typeof (_c = typeof admissions_service_1.AdmissionsService !== "undefined" && admissions_service_1.AdmissionsService) === "function" ? _c : Object])
], PatientsService);


/***/ }),
/* 64 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let VisitsService = class VisitsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async generateVisitNumber(hospitalId) {
        const today = new Date();
        const year = today.getFullYear();
        const month = String(today.getMonth() + 1).padStart(2, '0');
        const day = String(today.getDate()).padStart(2, '0');
        const datePrefix = `${year}${month}${day}`;
        const startOfDay = new Date(today.setHours(0, 0, 0, 0));
        const endOfDay = new Date(today.setHours(23, 59, 59, 999));
        const todayCount = await this.prisma.visit.count({
            where: {
                hospitalId,
                visitDate: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
        });
        const sequence = String(todayCount + 1).padStart(4, '0');
        return `PV-${datePrefix}-${sequence}`;
    }
    async create(createVisitDto) {
        const visitNumber = await this.generateVisitNumber(createVisitDto.hospitalId);
        const patient = await this.prisma.patient.findUnique({
            where: { id: createVisitDto.patientId },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with ID ${createVisitDto.patientId} not found`);
        }
        if (createVisitDto.visitType === client_1.VisitType.WARD_INDOOR && !createVisitDto.wardId && !createVisitDto.bedId) {
            throw new common_1.BadRequestException('Ward or bed is required for indoor visits');
        }
        if (createVisitDto.departmentId) {
            const department = await this.prisma.department.findFirst({
                where: {
                    id: createVisitDto.departmentId,
                    hospitalId: createVisitDto.hospitalId,
                },
            });
            if (!department) {
                throw new common_1.BadRequestException('Invalid department');
            }
        }
        if (createVisitDto.bedId) {
            const bed = await this.prisma.bed.findFirst({
                where: {
                    id: createVisitDto.bedId,
                    hospitalId: createVisitDto.hospitalId,
                },
            });
            if (!bed) {
                throw new common_1.BadRequestException('Invalid bed');
            }
        }
        if (createVisitDto.attendingDoctorId) {
            const doctor = await this.prisma.user.findFirst({
                where: {
                    id: createVisitDto.attendingDoctorId,
                    hospitalId: createVisitDto.hospitalId,
                    role: 'DOCTOR',
                },
            });
            if (!doctor) {
                throw new common_1.BadRequestException('Invalid attending doctor');
            }
        }
        if (createVisitDto.visitType !== client_1.VisitType.OPD && createVisitDto.clinicId) {
            throw new common_1.BadRequestException('clinicId is only applicable for OPD visits');
        }
        if (createVisitDto.visitType !== client_1.VisitType.OPD) {
            const prisma = this.prisma;
            const visit = await prisma.visit.create({
                data: {
                    hospitalId: createVisitDto.hospitalId,
                    patientId: createVisitDto.patientId,
                    registrarId: createVisitDto.registrarId,
                    visitNumber,
                    visitType: createVisitDto.visitType,
                    departmentId: createVisitDto.departmentId,
                    wardId: createVisitDto.wardId,
                    bedId: createVisitDto.bedId,
                    attendingDoctorId: createVisitDto.attendingDoctorId,
                    tokenNumber: 0,
                    consultationFee: 0,
                    chiefComplaint: createVisitDto.chiefComplaint,
                    vitalSigns: createVisitDto.vitalSigns ? JSON.parse(JSON.stringify(createVisitDto.vitalSigns)) : {},
                    notes: createVisitDto.notes,
                    status: client_1.VisitStatus.WAITING,
                    paymentStatus: client_1.PaymentStatus.UNPAID,
                },
                include: {
                    patient: { select: { id: true, fullName: true, nrNumber: true, mobile: true } },
                    department: { select: { id: true, name: true } },
                    bed: { select: { id: true, bedNumber: true } },
                    registrar: { select: { id: true, fullName: true } },
                    attendingDoctor: { select: { id: true, fullName: true } },
                },
            });
            return { visit };
        }
        if (!createVisitDto.clinicId) {
            throw new common_1.BadRequestException('clinicId is required for OPD visits');
        }
        const clinic = await this.prisma.clinic.findUnique({
            where: { id: createVisitDto.clinicId },
            include: {
                hospital: true,
                department: true,
                doctor: true,
            },
        });
        if (!clinic) {
            throw new common_1.NotFoundException(`Clinic with ID ${createVisitDto.clinicId} not found`);
        }
        if (clinic.status !== 'ACTIVE') {
            throw new common_1.BadRequestException(`Clinic is not currently active`);
        }
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        if (clinic.maxPatientsPerDay) {
            const todayVisits = await this.prisma.visit.count({
                where: {
                    clinicId: clinic.id,
                    visitDate: { gte: today },
                },
            });
            if (todayVisits >= clinic.maxPatientsPerDay) {
                throw new common_1.BadRequestException(`Maximum patients for today (${clinic.maxPatientsPerDay}) has been reached`);
            }
        }
        const lastToken = await this.prisma.token.findFirst({
            where: {
                clinicId: clinic.id,
                tokenDate: today,
            },
            orderBy: { tokenNumber: 'desc' },
        });
        const nextTokenNumber = (lastToken?.tokenNumber || 0) + 1;
        const token = await this.prisma.token.create({
            data: {
                hospitalId: clinic.hospitalId,
                clinicId: clinic.id,
                tokenNumber: nextTokenNumber,
                tokenDate: today,
                status: client_1.TokenStatus.WAITING,
            },
        });
        const prisma = this.prisma;
        const visit = await prisma.visit.create({
            data: {
                hospitalId: clinic.hospitalId,
                patientId: createVisitDto.patientId,
                clinicId: clinic.id,
                tokenId: token.id,
                registrarId: createVisitDto.registrarId,
                visitNumber,
                visitType: client_1.VisitType.OPD,
                departmentId: clinic.departmentId,
                attendingDoctorId: clinic.doctorId,
                tokenNumber: nextTokenNumber,
                consultationFee: clinic.opdFee,
                chiefComplaint: createVisitDto.chiefComplaint,
                vitalSigns: createVisitDto.vitalSigns ? JSON.parse(JSON.stringify(createVisitDto.vitalSigns)) : {},
                notes: createVisitDto.notes,
                status: client_1.VisitStatus.WAITING,
                paymentStatus: client_1.PaymentStatus.UNPAID,
            },
            include: {
                patient: { select: { id: true, fullName: true, nrNumber: true, mobile: true } },
                clinic: {
                    include: {
                        department: { select: { id: true, name: true } },
                        doctor: { select: { id: true, fullName: true } },
                    },
                },
                token: true,
                registrar: { select: { id: true, fullName: true } },
            },
        });
        await this.generateReceipt(visit.id, createVisitDto.registrarId);
        return {
            visit,
            token: {
                tokenNumber: nextTokenNumber,
                date: today,
                clinic: clinic.name,
                doctor: clinic.doctor.fullName,
                department: clinic.department.name,
                fee: Number(clinic.opdFee),
            },
        };
    }
    async findAll(query) {
        const { hospitalId, clinicId, patientId, consultantId, visitType, departmentId, attendingDoctorId, status, paymentStatus, startDate, endDate, page = 1, limit = 10, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (clinicId)
            where.clinicId = clinicId;
        if (patientId)
            where.patientId = patientId;
        if (consultantId)
            where.consultantId = consultantId;
        if (visitType)
            where.visitType = visitType;
        if (departmentId)
            where.departmentId = departmentId;
        if (attendingDoctorId)
            where.attendingDoctorId = attendingDoctorId;
        if (status)
            where.status = status;
        if (paymentStatus)
            where.paymentStatus = paymentStatus;
        if (startDate || endDate) {
            where.visitDate = {};
            if (startDate)
                where.visitDate.gte = new Date(startDate);
            if (endDate)
                where.visitDate.lte = new Date(endDate);
        }
        const prisma = this.prisma;
        const [visits, total] = await Promise.all([
            prisma.visit.findMany({
                where,
                include: {
                    patient: { select: { id: true, fullName: true, nrNumber: true } },
                    clinic: {
                        select: {
                            id: true,
                            name: true,
                            opdFee: true,
                            department: { select: { id: true, name: true } },
                            doctor: { select: { id: true, fullName: true } },
                        },
                    },
                    department: { select: { id: true, name: true } },
                    bed: {
                        select: {
                            id: true,
                            bedNumber: true,
                            bedType: true,
                            dailyRate: true,
                            room: {
                                select: {
                                    id: true,
                                    roomNumber: true,
                                    roomType: true,
                                    dailyRate: true,
                                },
                            },
                        },
                    },
                    registrar: { select: { id: true, fullName: true } },
                    consultant: { select: { id: true, fullName: true } },
                },
                orderBy: { visitDate: 'desc' },
                skip: (page - 1) * limit,
                take: limit,
            }),
            prisma.visit.count({ where }),
        ]);
        return {
            data: visits,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        let visit = await this.prisma.visit.findUnique({
            where: { id },
            include: {
                patient: true,
                clinic: {
                    include: {
                        department: true,
                        doctor: { select: { id: true, fullName: true, email: true, phone: true } },
                    },
                },
                token: true,
                registrar: { select: { id: true, fullName: true } },
                consultant: { select: { id: true, fullName: true } },
                referrals: {
                    include: {
                        toDepartment: { select: { id: true, name: true } },
                    },
                },
                receipts: true,
            },
        });
        if (!visit) {
            visit = await this.prisma.visit.findFirst({
                where: { tokenId: id },
                include: {
                    patient: true,
                    clinic: {
                        include: {
                            department: true,
                            doctor: { select: { id: true, fullName: true, email: true, phone: true } },
                        },
                    },
                    token: true,
                    registrar: { select: { id: true, fullName: true } },
                    consultant: { select: { id: true, fullName: true } },
                    referrals: {
                        include: {
                            toDepartment: { select: { id: true, name: true } },
                        },
                    },
                    receipts: true,
                },
            });
        }
        if (!visit) {
            throw new common_1.NotFoundException(`Visit with ID ${id} not found`);
        }
        return visit;
    }
    async update(id, updateVisitDto, consultantId) {
        const visit = await this.findOne(id);
        const data = { ...updateVisitDto };
        if (consultantId && !data.consultantId) {
            data.consultantId = consultantId;
            data.consultedAt = new Date();
        }
        return this.prisma.visit.update({
            where: { id: visit.id },
            data,
            include: {
                patient: { select: { id: true, fullName: true, nrNumber: true } },
                clinic: {
                    include: {
                        department: { select: { id: true, name: true } },
                        doctor: { select: { id: true, fullName: true } },
                    },
                },
                consultant: { select: { id: true, fullName: true } },
            },
        });
    }
    async cancel(id) {
        const visit = await this.findOne(id);
        if (visit.status === client_1.VisitStatus.COMPLETED) {
            throw new common_1.BadRequestException('Cannot cancel a completed visit');
        }
        await this.prisma.$transaction([
            this.prisma.visit.update({
                where: { id: visit.id },
                data: { status: client_1.VisitStatus.CANCELLED },
            }),
            ...(visit.tokenId
                ? [
                    this.prisma.token.update({
                        where: { id: visit.tokenId },
                        data: { status: client_1.TokenStatus.CANCELLED },
                    }),
                ]
                : []),
        ]);
        return { message: 'Visit cancelled successfully' };
    }
    async complete(id) {
        const visit = await this.findOne(id);
        if (visit.status === client_1.VisitStatus.COMPLETED) {
            throw new common_1.BadRequestException('Visit is already completed');
        }
        if (visit.status === client_1.VisitStatus.CANCELLED) {
            throw new common_1.BadRequestException('Cannot complete a cancelled visit');
        }
        await this.prisma.$transaction([
            this.prisma.visit.update({
                where: { id: visit.id },
                data: {
                    status: client_1.VisitStatus.COMPLETED,
                    completedAt: new Date(),
                },
            }),
            ...(visit.tokenId
                ? [
                    this.prisma.token.update({
                        where: { id: visit.tokenId },
                        data: { status: client_1.TokenStatus.COMPLETED },
                    }),
                ]
                : []),
        ]);
        return { message: 'Visit completed successfully' };
    }
    async callNext(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const nextToken = await this.prisma.token.findFirst({
            where: {
                clinicId,
                tokenDate: today,
                status: client_1.TokenStatus.WAITING,
            },
            orderBy: { tokenNumber: 'asc' },
            include: {
                visits: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
            },
        });
        if (!nextToken) {
            throw new common_1.NotFoundException('No waiting patients in queue');
        }
        await this.prisma.$transaction([
            this.prisma.token.update({
                where: { id: nextToken.id },
                data: {
                    status: client_1.TokenStatus.CALLED,
                    calledAt: new Date(),
                },
            }),
            ...nextToken.visits.map((visit) => this.prisma.visit.update({
                where: { id: visit.id },
                data: { status: client_1.VisitStatus.CALLED },
            })),
        ]);
        return {
            token: nextToken,
            patient: nextToken.visits[0]?.patient,
            message: `Token ${nextToken.tokenNumber} called`,
        };
    }
    async startConsultation(id, consultantId) {
        const visit = await this.findOne(id);
        if (visit.status === client_1.VisitStatus.COMPLETED) {
            throw new common_1.BadRequestException('Visit is already completed');
        }
        await this.prisma.$transaction([
            this.prisma.visit.update({
                where: { id },
                data: {
                    status: client_1.VisitStatus.IN_PROGRESS,
                    consultantId,
                    consultedAt: new Date(),
                },
            }),
            ...(visit.tokenId
                ? [
                    this.prisma.token.update({
                        where: { id: visit.tokenId },
                        data: { status: client_1.TokenStatus.IN_PROGRESS },
                    }),
                ]
                : []),
        ]);
        return this.findOne(id);
    }
    async findTodayByClinic(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const tomorrow = new Date(today);
        tomorrow.setDate(tomorrow.getDate() + 1);
        return this.prisma.visit.findMany({
            where: {
                clinicId,
                visitDate: { gte: today, lt: tomorrow },
            },
            include: {
                patient: { select: { id: true, fullName: true, nrNumber: true, gender: true } },
                token: true,
            },
            orderBy: { tokenNumber: 'asc' },
        });
    }
    async findByPatient(patientId, limit = 10) {
        return this.prisma.visit.findMany({
            where: { patientId },
            include: {
                clinic: {
                    include: {
                        department: { select: { id: true, name: true } },
                        doctor: { select: { id: true, fullName: true } },
                    },
                },
                consultant: { select: { id: true, fullName: true } },
            },
            orderBy: { visitDate: 'desc' },
            take: limit,
        });
    }
    async generateReceipt(id, generatedById) {
        const visit = await this.findOne(id);
        const existingReceipt = await this.prisma.receipt.findFirst({
            where: { visitId: id },
        });
        if (existingReceipt) {
            return existingReceipt;
        }
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await this.prisma.receipt.findFirst({
            where: {
                receiptNumber: { startsWith: `REC-${dateStr}` },
            },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        const receiptNumber = `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
        const prisma = this.prisma;
        const receipt = await prisma.receipt.create({
            data: {
                hospitalId: visit.hospitalId,
                patientId: visit.patientId,
                visitId: visit.id,
                departmentId: visit.clinic.department.id,
                generatedById,
                receiptNumber,
                receiptType: client_1.ReceiptType.OPD_CONSULTATION,
                description: `OPD Consultation - ${visit.clinic.name}`,
                amount: visit.consultationFee,
                totalAmount: visit.consultationFee,
                paidAmount: 0,
                paymentMethod: client_1.PaymentMethod.CASH,
                paymentStatus: client_1.PaymentStatus.UNPAID,
            },
        });
        return receipt;
    }
    async markAsPaid(id, paymentMethod = client_1.PaymentMethod.CASH) {
        const visit = await this.findOne(id);
        await this.prisma.visit.update({
            where: { id },
            data: { paymentStatus: client_1.PaymentStatus.PAID },
        });
        const receipt = await this.prisma.receipt.findFirst({
            where: { visitId: id },
        });
        if (receipt) {
            await this.prisma.receipt.update({
                where: { id: receipt.id },
                data: {
                    paymentStatus: client_1.PaymentStatus.PAID,
                    paymentMethod,
                    paidAt: new Date(),
                },
            });
        }
        return { message: 'Payment recorded successfully' };
    }
};
exports.VisitsService = VisitsService;
exports.VisitsService = VisitsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], VisitsService);


/***/ }),
/* 65 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AdmissionsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let AdmissionsService = class AdmissionsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async generateAdmissionNumber(hospitalId) {
        const today = new Date();
        const year = today.getFullYear();
        const month = String(today.getMonth() + 1).padStart(2, '0');
        const day = String(today.getDate()).padStart(2, '0');
        const prefix = `ADM-${year}${month}${day}`;
        for (let attempt = 0; attempt < 5; attempt += 1) {
            const sequence = Math.floor(1000 + Math.random() * 9000);
            const admissionNumber = `${prefix}-${sequence}`;
            const existing = await this.prisma.admission.findUnique({
                where: { admissionNumber },
                select: { id: true },
            });
            if (!existing) {
                return admissionNumber;
            }
        }
        throw new common_1.ConflictException('Unable to generate unique admission number');
    }
    async create(createAdmissionDto) {
        const { hospitalId, patientId, visitId, departmentId, roomId, bedId, attendingDoctorId, admittingUserId, ...rest } = createAdmissionDto;
        const [hospital, patient, department, attendingDoctor, admittingUser] = await Promise.all([
            this.prisma.hospital.findUnique({ where: { id: hospitalId } }),
            this.prisma.patient.findUnique({ where: { id: patientId } }),
            this.prisma.department.findUnique({ where: { id: departmentId } }),
            this.prisma.user.findUnique({ where: { id: attendingDoctorId } }),
            this.prisma.user.findUnique({ where: { id: admittingUserId } }),
        ]);
        if (!hospital)
            throw new common_1.NotFoundException('Hospital not found');
        if (!patient)
            throw new common_1.NotFoundException('Patient not found');
        if (!department)
            throw new common_1.NotFoundException('Department not found');
        if (!attendingDoctor)
            throw new common_1.NotFoundException('Attending doctor not found');
        if (!admittingUser)
            throw new common_1.NotFoundException('Admitting user not found');
        const activeAdmission = await this.prisma.admission.findFirst({
            where: {
                patientId,
                status: client_1.AdmissionStatus.ADMITTED,
            },
        });
        if (activeAdmission) {
            throw new common_1.ConflictException('Patient already has an active admission. Please discharge the patient first.');
        }
        let room = null;
        let bed = null;
        let roomCharges = new client_1.Prisma.Decimal(0);
        let bedCharges = new client_1.Prisma.Decimal(0);
        if (roomId) {
            room = await this.prisma.room.findUnique({ where: { id: roomId } });
            if (!room)
                throw new common_1.NotFoundException('Room not found');
            if (room.status !== 'AVAILABLE') {
                throw new common_1.BadRequestException('Selected room is not available');
            }
            roomCharges = room.dailyRate;
        }
        if (bedId) {
            bed = await this.prisma.bed.findUnique({ where: { id: bedId } });
            if (!bed)
                throw new common_1.NotFoundException('Bed not found');
            if (bed.status !== client_1.BedStatus.AVAILABLE) {
                throw new common_1.BadRequestException('Selected bed is not available');
            }
            bedCharges = bed.dailyRate;
        }
        const maxAttempts = 3;
        let admission;
        for (let attempt = 0; attempt < maxAttempts; attempt += 1) {
            const admissionNumber = await this.generateAdmissionNumber(hospitalId);
            try {
                admission = await this.prisma.$transaction(async (tx) => {
                    const newAdmission = await tx.admission.create({
                        data: {
                            hospitalId,
                            patientId,
                            visitId,
                            departmentId,
                            roomId,
                            bedId,
                            attendingDoctorId,
                            admittingUserId,
                            admissionNumber,
                            ...rest,
                        },
                        include: {
                            patient: {
                                select: {
                                    id: true,
                                    nrNumber: true,
                                    fullName: true,
                                    gender: true,
                                    mobile: true,
                                },
                            },
                            department: {
                                select: { id: true, name: true, code: true },
                            },
                            room: {
                                select: { id: true, roomNumber: true, roomType: true, dailyRate: true },
                            },
                            bed: {
                                select: { id: true, bedNumber: true, bedType: true, dailyRate: true },
                            },
                            attendingDoctor: {
                                select: { id: true, fullName: true },
                            },
                            admittingUser: {
                                select: { id: true, fullName: true },
                            },
                        },
                    });
                    if (bedId) {
                        await tx.bed.update({
                            where: { id: bedId },
                            data: { status: client_1.BedStatus.OCCUPIED },
                        });
                    }
                    if (roomId) {
                        await tx.room.update({
                            where: { id: roomId },
                            data: { status: 'OCCUPIED' },
                        });
                    }
                    await tx.dailyCharge.create({
                        data: {
                            hospitalId,
                            admissionId: newAdmission.id,
                            chargeDate: new Date(),
                            roomCharges,
                            bedCharges,
                            totalCharges: new client_1.Prisma.Decimal(roomCharges).plus(bedCharges),
                        },
                    });
                    return newAdmission;
                });
                break;
            }
            catch (error) {
                if (error instanceof client_1.Prisma.PrismaClientKnownRequestError &&
                    error.code === 'P2002' &&
                    attempt < maxAttempts - 1) {
                    continue;
                }
                throw error;
            }
        }
        return admission;
    }
    async findAll(query) {
        const { hospitalId, patientId, departmentId, roomId, bedId, admissionType, status, admittedFrom, admittedTo, page = 1, limit = 20, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (patientId)
            where.patientId = patientId;
        if (departmentId)
            where.departmentId = departmentId;
        if (roomId)
            where.roomId = roomId;
        if (bedId)
            where.bedId = bedId;
        if (admissionType)
            where.admissionType = admissionType;
        if (status)
            where.status = status;
        if (admittedFrom || admittedTo) {
            where.admittedAt = {};
            if (admittedFrom)
                where.admittedAt.gte = new Date(admittedFrom);
            if (admittedTo)
                where.admittedAt.lte = new Date(admittedTo);
        }
        const skip = (page - 1) * limit;
        const [admissions, total] = await Promise.all([
            this.prisma.admission.findMany({
                where,
                skip,
                take: limit,
                orderBy: [{ admittedAt: 'desc' }],
                include: {
                    patient: {
                        select: {
                            id: true,
                            nrNumber: true,
                            fullName: true,
                            gender: true,
                            mobile: true,
                        },
                    },
                    department: {
                        select: { id: true, name: true, code: true },
                    },
                    room: {
                        select: { id: true, roomNumber: true, roomType: true },
                    },
                    bed: {
                        select: { id: true, bedNumber: true, bedType: true },
                    },
                    attendingDoctor: {
                        select: { id: true, fullName: true },
                    },
                },
            }),
            this.prisma.admission.count({ where }),
        ]);
        return {
            data: admissions,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const admission = await this.prisma.admission.findUnique({
            where: { id },
            include: {
                patient: true,
                visit: true,
                department: true,
                room: true,
                bed: true,
                attendingDoctor: {
                    select: { id: true, fullName: true, role: true },
                },
                admittingUser: {
                    select: { id: true, fullName: true },
                },
                dischargingUser: {
                    select: { id: true, fullName: true },
                },
                dailyCharges: {
                    orderBy: { chargeDate: 'desc' },
                },
            },
        });
        if (!admission) {
            throw new common_1.NotFoundException(`Admission with ID ${id} not found`);
        }
        const totalCharges = admission.dailyCharges.reduce((sum, charge) => sum.plus(charge.totalCharges), new client_1.Prisma.Decimal(0));
        const admittedDate = new Date(admission.admittedAt);
        const endDate = admission.dischargedAt
            ? new Date(admission.dischargedAt)
            : new Date();
        const durationDays = Math.ceil((endDate.getTime() - admittedDate.getTime()) / (1000 * 60 * 60 * 24));
        return {
            ...admission,
            totalCharges: totalCharges.toString(),
            durationDays,
        };
    }
    async update(id, updateAdmissionDto) {
        const admission = await this.prisma.admission.findUnique({
            where: { id },
            include: { bed: true, room: true },
        });
        if (!admission) {
            throw new common_1.NotFoundException(`Admission with ID ${id} not found`);
        }
        if (admission.status !== client_1.AdmissionStatus.ADMITTED) {
            throw new common_1.BadRequestException('Cannot update admission that is not in ADMITTED status');
        }
        const { bedId, roomId, ...rest } = updateAdmissionDto;
        return this.prisma.$transaction(async (tx) => {
            if (bedId && bedId !== admission.bedId) {
                if (admission.bedId) {
                    await tx.bed.update({
                        where: { id: admission.bedId },
                        data: { status: client_1.BedStatus.AVAILABLE },
                    });
                }
                const newBed = await tx.bed.findUnique({ where: { id: bedId } });
                if (!newBed)
                    throw new common_1.NotFoundException('New bed not found');
                if (newBed.status !== client_1.BedStatus.AVAILABLE) {
                    throw new common_1.BadRequestException('New bed is not available');
                }
                await tx.bed.update({
                    where: { id: bedId },
                    data: { status: client_1.BedStatus.OCCUPIED },
                });
            }
            return tx.admission.update({
                where: { id },
                data: {
                    bedId,
                    roomId,
                    ...rest,
                },
                include: {
                    patient: true,
                    department: true,
                    room: true,
                    bed: true,
                    attendingDoctor: {
                        select: { id: true, fullName: true },
                    },
                },
            });
        });
    }
    async discharge(id, dischargeDto) {
        const admission = await this.prisma.admission.findUnique({
            where: { id },
            include: { bed: true, room: true, dailyCharges: true, patient: true, department: true },
        });
        if (!admission) {
            throw new common_1.NotFoundException(`Admission with ID ${id} not found`);
        }
        if (admission.status !== client_1.AdmissionStatus.ADMITTED) {
            throw new common_1.BadRequestException('Admission is already discharged or cancelled');
        }
        const { dischargingUserId, dischargedAt, dischargeSummary, diagnosisOnDischarge, estimatedTotal } = dischargeDto;
        const dischargingUser = await this.prisma.user.findUnique({
            where: { id: dischargingUserId },
        });
        if (!dischargingUser) {
            throw new common_1.NotFoundException('Discharging user not found');
        }
        const dischargeDate = new Date(dischargedAt);
        const roomCharges = admission.dailyCharges.length
            ? admission.dailyCharges.reduce((sum, charge) => sum.plus(charge.totalCharges), new client_1.Prisma.Decimal(0))
            : this.calculateRoomCharges(admission, dischargeDate);
        return this.prisma.$transaction(async (tx) => {
            const [operationCharges, labCharges, pharmacyCharges, opdCharges] = await Promise.all([
                this.sumOperationCharges(tx, admission.id),
                this.sumLabCharges(tx, admission.patientId, admission.admittedAt, dischargeDate),
                this.sumPharmacyCharges(tx, admission.patient.nrNumber, admission.admittedAt, dischargeDate),
                this.sumOpdCharges(tx, admission.visitId),
            ]);
            const totalCharges = roomCharges
                .plus(operationCharges)
                .plus(labCharges)
                .plus(pharmacyCharges)
                .plus(opdCharges);
            const receiptTotal = typeof estimatedTotal === 'number' && !Number.isNaN(estimatedTotal)
                ? new client_1.Prisma.Decimal(estimatedTotal)
                : totalCharges;
            const dischargedAdmission = await tx.admission.update({
                where: { id },
                data: {
                    status: client_1.AdmissionStatus.DISCHARGED,
                    dischargedAt: dischargeDate,
                    dischargingUserId,
                    dischargeSummary,
                    diagnosisOnDischarge,
                },
                include: {
                    patient: true,
                    department: true,
                    room: true,
                    bed: true,
                    attendingDoctor: {
                        select: { id: true, fullName: true },
                    },
                    dischargingUser: {
                        select: { id: true, fullName: true },
                    },
                    dailyCharges: true,
                },
            });
            if (admission.bedId) {
                await tx.bed.update({
                    where: { id: admission.bedId },
                    data: { status: client_1.BedStatus.AVAILABLE },
                });
            }
            if (admission.roomId) {
                await tx.room.update({
                    where: { id: admission.roomId },
                    data: { status: 'AVAILABLE' },
                });
            }
            const receiptNumber = await this.generateReceiptNumber(tx);
            const receiptNotes = JSON.stringify({
                roomCharges: roomCharges.toString(),
                operationCharges: operationCharges.toString(),
                labCharges: labCharges.toString(),
                pharmacyCharges: pharmacyCharges.toString(),
                opdCharges: opdCharges.toString(),
                calculatedTotal: totalCharges.toString(),
                estimatedTotal: receiptTotal.toString(),
            });
            const receipt = await tx.receipt.create({
                data: {
                    hospitalId: admission.hospitalId,
                    patientId: admission.patientId,
                    visitId: admission.visitId || undefined,
                    departmentId: admission.departmentId,
                    generatedById: dischargingUserId,
                    receiptNumber,
                    receiptType: client_1.ReceiptType.ADMISSION,
                    description: `Final bill for admission ${admission.admissionNumber}`,
                    amount: receiptTotal,
                    discount: new client_1.Prisma.Decimal(0),
                    tax: new client_1.Prisma.Decimal(0),
                    totalAmount: receiptTotal,
                    paidAmount: new client_1.Prisma.Decimal(0),
                    paymentMethod: client_1.PaymentMethod.CASH,
                    paymentStatus: client_1.PaymentStatus.UNPAID,
                    notes: receiptNotes,
                },
            });
            return {
                ...dischargedAdmission,
                totalCharges: totalCharges.toString(),
                receipt,
            };
        });
    }
    async getActiveAdmissions(hospitalId) {
        return this.prisma.admission.findMany({
            where: {
                hospitalId,
                status: client_1.AdmissionStatus.ADMITTED,
            },
            include: {
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                        gender: true,
                    },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
                room: {
                    select: { id: true, roomNumber: true, roomType: true },
                },
                bed: {
                    select: { id: true, bedNumber: true },
                },
                attendingDoctor: {
                    select: { id: true, fullName: true },
                },
            },
            orderBy: { admittedAt: 'asc' },
        });
    }
    calculateRoomCharges(admission, dischargeDate) {
        const rate = admission.bed?.dailyRate || admission.room?.dailyRate || new client_1.Prisma.Decimal(0);
        const admittedAt = new Date(admission.admittedAt);
        const diffMs = Math.max(dischargeDate.getTime() - admittedAt.getTime(), 0);
        const days = Math.max(Math.ceil(diffMs / (1000 * 60 * 60 * 24)), 1);
        return new client_1.Prisma.Decimal(rate).mul(days);
    }
    async sumOperationCharges(tx, admissionId) {
        const operations = await tx.operation.findMany({
            where: { admissionId, status: { not: 'CANCELLED' } },
            select: { operationPrice: true },
        });
        return operations.reduce((sum, operation) => operation.operationPrice ? sum.plus(operation.operationPrice) : sum, new client_1.Prisma.Decimal(0));
    }
    async sumLabCharges(tx, patientId, admittedAt, dischargedAt) {
        const labOrders = await tx.labOrder.findMany({
            where: {
                patientId,
                status: { not: 'CANCELLED' },
                createdAt: { gte: admittedAt, lte: dischargedAt },
            },
            include: { labTest: { select: { price: true } } },
        });
        return labOrders.reduce((sum, order) => (order.labTest?.price ? sum.plus(order.labTest.price) : sum), new client_1.Prisma.Decimal(0));
    }
    async sumPharmacyCharges(tx, nrNumber, admittedAt, dischargedAt) {
        const issueTransactions = await tx.issueTransaction.findMany({
            where: {
                nrNumber,
                issuedAt: { gte: admittedAt, lte: dischargedAt },
            },
            select: { totalAmount: true },
        });
        return issueTransactions.reduce((sum, issue) => sum.plus(issue.totalAmount), new client_1.Prisma.Decimal(0));
    }
    async sumOpdCharges(tx, visitId) {
        if (!visitId) {
            return new client_1.Prisma.Decimal(0);
        }
        const visit = await tx.visit.findUnique({
            where: { id: visitId },
            select: { consultationFee: true },
        });
        return new client_1.Prisma.Decimal(visit?.consultationFee || 0);
    }
    async generateReceiptNumber(tx) {
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await tx.receipt.findFirst({
            where: { receiptNumber: { startsWith: `REC-${dateStr}` } },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        return `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
    }
};
exports.AdmissionsService = AdmissionsService;
exports.AdmissionsService = AdmissionsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], AdmissionsService);


/***/ }),
/* 66 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientsController = void 0;
const common_1 = __webpack_require__(2);
const patients_service_1 = __webpack_require__(63);
const create_patient_dto_1 = __webpack_require__(67);
const update_patient_dto_1 = __webpack_require__(72);
const search_patients_dto_1 = __webpack_require__(73);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const current_user_decorator_1 = __webpack_require__(49);
let PatientsController = class PatientsController {
    constructor(patientsService) {
        this.patientsService = patientsService;
    }
    create(createPatientDto, hospitalId, user) {
        const targetHospitalId = user.role === 'SUPER_ADMIN' && !user.hospitalId
            ? hospitalId
            : user.hospitalId;
        if (!targetHospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.create(createPatientDto, user.id, targetHospitalId);
    }
    findAll(searchDto, user) {
        const hospitalId = searchDto.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.findAll(searchDto, hospitalId);
    }
    getStats(hospitalId, user) {
        const targetHospitalId = hospitalId || user.hospitalId;
        if (!targetHospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.getStats(targetHospitalId);
    }
    findByNRNumber(nrNumber, hospitalId, user) {
        const targetHospitalId = hospitalId || user.hospitalId;
        if (!targetHospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.findByNRNumber(nrNumber, targetHospitalId);
    }
    findOne(id, hospitalId, user) {
        const targetHospitalId = hospitalId || user.hospitalId;
        if (!targetHospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.findOne(id, targetHospitalId);
    }
    update(id, updatePatientDto, user) {
        if (!user.hospitalId) {
            throw new common_1.BadRequestException('Hospital ID is required');
        }
        return this.patientsService.update(id, updatePatientDto, user.hospitalId);
    }
    remove(id, user) {
        return this.patientsService.remove(id, user.hospitalId);
    }
};
exports.PatientsController = PatientsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST', 'DOCTOR', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER'),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Query)('hospitalId')),
    __param(2, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_patient_dto_1.CreatePatientDto !== "undefined" && create_patient_dto_1.CreatePatientDto) === "function" ? _b : Object, String, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST', 'DOCTOR', 'DOCTOR_ASSISTANT', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'PHARMACY_STAFF'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof search_patients_dto_1.SearchPatientsDto !== "undefined" && search_patients_dto_1.SearchPatientsDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('stats'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST'),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)('nr/:nrNumber'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST', 'DOCTOR', 'DOCTOR_ASSISTANT', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'PHARMACY_STAFF'),
    __param(0, (0, common_1.Param)('nrNumber')),
    __param(1, (0, common_1.Query)('hospitalId')),
    __param(2, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "findByNRNumber", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST', 'DOCTOR', 'DOCTOR_ASSISTANT', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'PHARMACY_STAFF'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Query)('hospitalId')),
    __param(2, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'REGISTRATION_STAFF', 'RECEPTIONIST', 'DOCTOR'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_patient_dto_1.UpdatePatientDto !== "undefined" && update_patient_dto_1.UpdatePatientDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)('HOSPITAL_ADMIN'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], PatientsController.prototype, "remove", null);
exports.PatientsController = PatientsController = __decorate([
    (0, common_1.Controller)('patients'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof patients_service_1.PatientsService !== "undefined" && patients_service_1.PatientsService) === "function" ? _a : Object])
], PatientsController);


/***/ }),
/* 67 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreatePatientDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const dto_1 = __webpack_require__(68);
class CreatePatientDto {
}
exports.CreatePatientDto = CreatePatientDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Full name is required' }),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "fullName", void 0);
__decorate([
    (0, class_validator_1.IsDateString)({}, { message: 'Invalid date of birth format' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "dob", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['MALE', 'FEMALE', 'OTHER'], { message: 'Invalid gender' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Gender is required' }),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "gender", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^[0-9]{5}-[0-9]{7}-[0-9]$/, { message: 'Invalid CNIC format (XXXXX-XXXXXXX-X)' }),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "cnic", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^(\+92|0)[0-9]{10}$/, { message: 'Invalid mobile number format' }),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "mobile", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "address", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['OPD', 'EMERGENCY', 'WARD_INDOOR'], { message: 'Invalid visit type' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Visit type is required' }),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "visitType", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "clinicId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "department", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "ward", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "bed", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "chiefComplaint", void 0);
__decorate([
    (0, class_validator_1.IsObject)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.ValidateNested)(),
    (0, class_transformer_1.Type)(() => dto_1.VitalSignsDto),
    __metadata("design:type", typeof (_a = typeof dto_1.VitalSignsDto !== "undefined" && dto_1.VitalSignsDto) === "function" ? _a : Object)
], CreatePatientDto.prototype, "vitalSigns", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePatientDto.prototype, "notes", void 0);


/***/ }),
/* 68 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitQueryDto = exports.UpdateVisitDto = exports.VitalSignsDto = exports.CreateVisitDto = void 0;
var create_visit_dto_1 = __webpack_require__(69);
Object.defineProperty(exports, "CreateVisitDto", ({ enumerable: true, get: function () { return create_visit_dto_1.CreateVisitDto; } }));
Object.defineProperty(exports, "VitalSignsDto", ({ enumerable: true, get: function () { return create_visit_dto_1.VitalSignsDto; } }));
var update_visit_dto_1 = __webpack_require__(70);
Object.defineProperty(exports, "UpdateVisitDto", ({ enumerable: true, get: function () { return update_visit_dto_1.UpdateVisitDto; } }));
var visit_query_dto_1 = __webpack_require__(71);
Object.defineProperty(exports, "VisitQueryDto", ({ enumerable: true, get: function () { return visit_query_dto_1.VisitQueryDto; } }));


/***/ }),
/* 69 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateVisitDto = exports.VitalSignsDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class VitalSignsDto {
}
exports.VitalSignsDto = VitalSignsDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "bloodPressureSystolic", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "bloodPressureDiastolic", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "pulse", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "temperature", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "spo2", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "weight", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "height", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VitalSignsDto.prototype, "respiratoryRate", void 0);
class CreateVisitDto {
}
exports.CreateVisitDto = CreateVisitDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "clinicId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "registrarId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.VisitType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.VisitType !== "undefined" && client_1.VisitType) === "function" ? _a : Object)
], CreateVisitDto.prototype, "visitType", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "wardId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "bedId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "chiefComplaint", void 0);
__decorate([
    (0, class_validator_1.IsObject)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.ValidateNested)(),
    (0, class_transformer_1.Type)(() => VitalSignsDto),
    __metadata("design:type", VitalSignsDto)
], CreateVisitDto.prototype, "vitalSigns", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateVisitDto.prototype, "notes", void 0);


/***/ }),
/* 70 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateVisitDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
const create_visit_dto_1 = __webpack_require__(69);
class UpdateVisitDto {
}
exports.UpdateVisitDto = UpdateVisitDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "chiefComplaint", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "historyOfIllness", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "examination", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "diagnosis", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "treatmentPlan", void 0);
__decorate([
    (0, class_validator_1.IsObject)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.ValidateNested)(),
    (0, class_transformer_1.Type)(() => create_visit_dto_1.VitalSignsDto),
    __metadata("design:type", typeof (_a = typeof create_visit_dto_1.VitalSignsDto !== "undefined" && create_visit_dto_1.VitalSignsDto) === "function" ? _a : Object)
], UpdateVisitDto.prototype, "vitalSigns", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.VisitStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.VisitStatus !== "undefined" && client_1.VisitStatus) === "function" ? _b : Object)
], UpdateVisitDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.PaymentStatus !== "undefined" && client_1.PaymentStatus) === "function" ? _c : Object)
], UpdateVisitDto.prototype, "paymentStatus", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateVisitDto.prototype, "notes", void 0);


/***/ }),
/* 71 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class VisitQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 10;
    }
}
exports.VisitQueryDto = VisitQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "clinicId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "consultantId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.VisitType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.VisitType !== "undefined" && client_1.VisitType) === "function" ? _a : Object)
], VisitQueryDto.prototype, "visitType", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.VisitStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.VisitStatus !== "undefined" && client_1.VisitStatus) === "function" ? _b : Object)
], VisitQueryDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.PaymentStatus !== "undefined" && client_1.PaymentStatus) === "function" ? _c : Object)
], VisitQueryDto.prototype, "paymentStatus", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], VisitQueryDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], VisitQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], VisitQueryDto.prototype, "limit", void 0);


/***/ }),
/* 72 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdatePatientDto = void 0;
const class_validator_1 = __webpack_require__(35);
class UpdatePatientDto {
}
exports.UpdatePatientDto = UpdatePatientDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "fullName", void 0);
__decorate([
    (0, class_validator_1.IsDateString)({}, { message: 'Invalid date of birth format' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "dob", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['MALE', 'FEMALE', 'OTHER'], { message: 'Invalid gender' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "gender", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^[0-9]{5}-[0-9]{7}-[0-9]$/, { message: 'Invalid CNIC format (XXXXX-XXXXXXX-X)' }),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "cnic", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^(\+92|0)[0-9]{10}$/, { message: 'Invalid mobile number format' }),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "mobile", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "address", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['OPD', 'EMERGENCY', 'WARD_INDOOR'], { message: 'Invalid visit type' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "visitType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "department", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "ward", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "bed", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePatientDto.prototype, "attendingDoctorId", void 0);


/***/ }),
/* 73 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchPatientsDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class SearchPatientsDto {
    constructor() {
        this.limit = 50;
        this.page = 1;
        this.sortBy = 'registeredAt';
        this.sortOrder = 'desc';
    }
}
exports.SearchPatientsDto = SearchPatientsDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "search", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "cnic", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "mobile", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['MALE', 'FEMALE', 'OTHER'], { message: 'Invalid gender' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "gender", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['OPD', 'EMERGENCY', 'WARD_INDOOR'], { message: 'Invalid visit type' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "visitType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(200),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchPatientsDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchPatientsDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "sortBy", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['asc', 'desc']),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchPatientsDto.prototype, "sortOrder", void 0);


/***/ }),
/* 74 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitsModule = void 0;
const common_1 = __webpack_require__(2);
const visits_controller_1 = __webpack_require__(75);
const visits_service_1 = __webpack_require__(64);
const database_module_1 = __webpack_require__(52);
let VisitsModule = class VisitsModule {
};
exports.VisitsModule = VisitsModule;
exports.VisitsModule = VisitsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [visits_controller_1.VisitsController],
        providers: [visits_service_1.VisitsService],
        exports: [visits_service_1.VisitsService],
    })
], VisitsModule);


/***/ }),
/* 75 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitsController = void 0;
const common_1 = __webpack_require__(2);
const visits_service_1 = __webpack_require__(64);
const dto_1 = __webpack_require__(68);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let VisitsController = class VisitsController {
    constructor(visitsService) {
        this.visitsService = visitsService;
    }
    async create(createVisitDto) {
        return this.visitsService.create(createVisitDto);
    }
    async findAll(query) {
        return this.visitsService.findAll(query);
    }
    async findToday(clinicId) {
        return this.visitsService.findTodayByClinic(clinicId);
    }
    async findByPatient(patientId, limit) {
        return this.visitsService.findByPatient(patientId, limit);
    }
    async findByClinic(clinicId) {
        return this.visitsService.findTodayByClinic(clinicId);
    }
    async findOne(id) {
        return this.visitsService.findOne(id);
    }
    async update(id, updateVisitDto, req) {
        return this.visitsService.update(id, updateVisitDto, req.user.id);
    }
    async startConsultation(id, req) {
        return this.visitsService.startConsultation(id, req.user.id);
    }
    async complete(id) {
        return this.visitsService.complete(id);
    }
    async callNext(clinicId) {
        return this.visitsService.callNext(clinicId);
    }
    async generateReceipt(id, req) {
        return this.visitsService.generateReceipt(id, req.user.id);
    }
    async markAsPaid(id, paymentMethod) {
        return this.visitsService.markAsPaid(id, paymentMethod);
    }
    async cancel(id) {
        return this.visitsService.cancel(id);
    }
};
exports.VisitsController = VisitsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new visit' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Visit created' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateVisitDto !== "undefined" && dto_1.CreateVisitDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get all visits with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of visits' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.VisitQueryDto !== "undefined" && dto_1.VisitQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('today'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get today visits' }),
    __param(0, (0, common_1.Query)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "findToday", null);
__decorate([
    (0, common_1.Get)('patient/:patientId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get patient visit history' }),
    (0, swagger_1.ApiParam)({ name: 'patientId', description: 'Patient ID' }),
    __param(0, (0, common_1.Param)('patientId')),
    __param(1, (0, common_1.Query)('limit')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "findByPatient", null);
__decorate([
    (0, common_1.Get)('clinic/:clinicId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get clinic queue for today' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "findByClinic", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get visit details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Update visit consultation details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof dto_1.UpdateVisitDto !== "undefined" && dto_1.UpdateVisitDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "update", null);
__decorate([
    (0, common_1.Post)(':id/start'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.DOCTOR),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Start consultation for a visit' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "startConsultation", null);
__decorate([
    (0, common_1.Post)(':id/complete'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Mark visit as complete' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "complete", null);
__decorate([
    (0, common_1.Post)('call-next/:clinicId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Call next patient in queue' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "callNext", null);
__decorate([
    (0, common_1.Post)(':id/receipt'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Generate receipt for visit' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "generateReceipt", null);
__decorate([
    (0, common_1.Post)(':id/pay'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Mark visit as paid' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('paymentMethod')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof client_1.PaymentMethod !== "undefined" && client_1.PaymentMethod) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "markAsPaid", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Cancel a visit' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], VisitsController.prototype, "cancel", null);
exports.VisitsController = VisitsController = __decorate([
    (0, swagger_1.ApiTags)('Visits'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('visits'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof visits_service_1.VisitsService !== "undefined" && visits_service_1.VisitsService) === "function" ? _a : Object])
], VisitsController);


/***/ }),
/* 76 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AdmissionsModule = void 0;
const common_1 = __webpack_require__(2);
const admissions_controller_1 = __webpack_require__(77);
const admissions_service_1 = __webpack_require__(65);
const database_module_1 = __webpack_require__(52);
let AdmissionsModule = class AdmissionsModule {
};
exports.AdmissionsModule = AdmissionsModule;
exports.AdmissionsModule = AdmissionsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [admissions_controller_1.AdmissionsController],
        providers: [admissions_service_1.AdmissionsService],
        exports: [admissions_service_1.AdmissionsService],
    })
], AdmissionsModule);


/***/ }),
/* 77 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AdmissionsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const admissions_service_1 = __webpack_require__(65);
const create_admission_dto_1 = __webpack_require__(78);
const update_admission_dto_1 = __webpack_require__(79);
const discharge_admission_dto_1 = __webpack_require__(80);
const admission_query_dto_1 = __webpack_require__(81);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
let AdmissionsController = class AdmissionsController {
    constructor(admissionsService) {
        this.admissionsService = admissionsService;
    }
    create(createAdmissionDto) {
        return this.admissionsService.create(createAdmissionDto);
    }
    findAll(query) {
        return this.admissionsService.findAll(query);
    }
    getActiveAdmissions(hospitalId) {
        return this.admissionsService.getActiveAdmissions(hospitalId);
    }
    findOne(id) {
        return this.admissionsService.findOne(id);
    }
    update(id, updateAdmissionDto) {
        return this.admissionsService.update(id, updateAdmissionDto);
    }
    discharge(id, dischargeDto) {
        return this.admissionsService.discharge(id, dischargeDto);
    }
};
exports.AdmissionsController = AdmissionsController;
__decorate([
    (0, common_1.Post)(),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new admission' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Admission created successfully' }),
    (0, swagger_1.ApiResponse)({ status: 409, description: 'Patient already has active admission' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_admission_dto_1.CreateAdmissionDto !== "undefined" && create_admission_dto_1.CreateAdmissionDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Get all admissions with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Admissions retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof admission_query_dto_1.AdmissionQueryDto !== "undefined" && admission_query_dto_1.AdmissionQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('active/:hospitalId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get all active admissions for a hospital' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Active admissions retrieved successfully' }),
    __param(0, (0, common_1.Param)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "getActiveAdmissions", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get admission by ID with full details' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Admission retrieved successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Admission not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update admission details' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Admission updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Admission not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_admission_dto_1.UpdateAdmissionDto !== "undefined" && update_admission_dto_1.UpdateAdmissionDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "update", null);
__decorate([
    (0, common_1.Post)(':id/discharge'),
    (0, swagger_1.ApiOperation)({ summary: 'Discharge a patient' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Patient discharged successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Admission not found' }),
    (0, swagger_1.ApiResponse)({ status: 400, description: 'Admission already discharged' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof discharge_admission_dto_1.DischargeAdmissionDto !== "undefined" && discharge_admission_dto_1.DischargeAdmissionDto) === "function" ? _e : Object]),
    __metadata("design:returntype", void 0)
], AdmissionsController.prototype, "discharge", null);
exports.AdmissionsController = AdmissionsController = __decorate([
    (0, swagger_1.ApiTags)('Admissions'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    (0, common_1.Controller)('admissions'),
    __metadata("design:paramtypes", [typeof (_a = typeof admissions_service_1.AdmissionsService !== "undefined" && admissions_service_1.AdmissionsService) === "function" ? _a : Object])
], AdmissionsController);


/***/ }),
/* 78 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateAdmissionDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class CreateAdmissionDto {
}
exports.CreateAdmissionDto = CreateAdmissionDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174001' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "patientId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174002' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "visitId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174003' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174004' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "roomId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174005' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "bedId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174006' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174007' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "admittingUserId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ enum: client_1.AdmissionType, example: client_1.AdmissionType.EMERGENCY }),
    (0, class_validator_1.IsEnum)(client_1.AdmissionType),
    __metadata("design:type", typeof (_a = typeof client_1.AdmissionType !== "undefined" && client_1.AdmissionType) === "function" ? _a : Object)
], CreateAdmissionDto.prototype, "admissionType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '2026-01-25T10:00:00Z' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "expectedDischarge", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Severe chest pain, suspected myocardial infarction' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "diagnosisOnAdmission", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Patient requires 24-hour monitoring' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateAdmissionDto.prototype, "notes", void 0);


/***/ }),
/* 79 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateAdmissionDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
class UpdateAdmissionDto {
}
exports.UpdateAdmissionDto = UpdateAdmissionDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "roomId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "bedId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "attendingDoctorId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "expectedDischarge", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "diagnosisOnAdmission", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "treatmentSummary", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateAdmissionDto.prototype, "notes", void 0);


/***/ }),
/* 80 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DischargeAdmissionDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const swagger_1 = __webpack_require__(3);
class DischargeAdmissionDto {
}
exports.DischargeAdmissionDto = DischargeAdmissionDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], DischargeAdmissionDto.prototype, "dischargingUserId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '2026-01-21T14:30:00Z' }),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], DischargeAdmissionDto.prototype, "dischargedAt", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'Patient fully recovered, discharged on own request' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], DischargeAdmissionDto.prototype, "dischargeSummary", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'Complete bed rest for 2 weeks, follow-up in 1 month' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], DischargeAdmissionDto.prototype, "diagnosisOnDischarge", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 12500, required: false }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], DischargeAdmissionDto.prototype, "estimatedTotal", void 0);


/***/ }),
/* 81 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AdmissionQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class AdmissionQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 20;
    }
}
exports.AdmissionQueryDto = AdmissionQueryDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "patientId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "roomId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "bedId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.AdmissionType }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.AdmissionType),
    __metadata("design:type", typeof (_a = typeof client_1.AdmissionType !== "undefined" && client_1.AdmissionType) === "function" ? _a : Object)
], AdmissionQueryDto.prototype, "admissionType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.AdmissionStatus }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.AdmissionStatus),
    __metadata("design:type", typeof (_b = typeof client_1.AdmissionStatus !== "undefined" && client_1.AdmissionStatus) === "function" ? _b : Object)
], AdmissionQueryDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "admittedFrom", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AdmissionQueryDto.prototype, "admittedTo", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 1 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], AdmissionQueryDto.prototype, "page", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 20 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], AdmissionQueryDto.prototype, "limit", void 0);


/***/ }),
/* 82 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SyncModule = void 0;
const common_1 = __webpack_require__(2);
const sync_service_1 = __webpack_require__(83);
const sync_controller_1 = __webpack_require__(84);
const database_module_1 = __webpack_require__(52);
let SyncModule = class SyncModule {
};
exports.SyncModule = SyncModule;
exports.SyncModule = SyncModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [sync_controller_1.SyncController],
        providers: [sync_service_1.SyncService],
        exports: [sync_service_1.SyncService],
    })
], SyncModule);


/***/ }),
/* 83 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var SyncService_1;
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SyncService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let SyncService = SyncService_1 = class SyncService {
    constructor(prisma) {
        this.prisma = prisma;
        this.logger = new common_1.Logger(SyncService_1.name);
        this.syncInterval = null;
        this.SYNC_BATCH_SIZE = 100;
        this.MAX_RETRY_ATTEMPTS = 5;
    }
    async queueOperation(params) {
        try {
            await this.prisma.syncOperation.create({
                data: {
                    hospitalId: params.hospitalId,
                    pharmacyId: params.pharmacyId,
                    operationType: params.operationType,
                    entityType: params.entityType,
                    entityId: params.entityId,
                    payload: params.payload,
                    version: params.version,
                    status: client_1.SyncStatus.PENDING,
                },
            });
            this.logger.debug(`Queued ${params.operationType} operation for ${params.entityType}:${params.entityId}`);
        }
        catch (error) {
            this.logger.error(`Failed to queue sync operation: ${error.message}`, error.stack);
            throw error;
        }
    }
    startAutoSync(intervalMs = 30000) {
        if (this.syncInterval) {
            this.logger.warn('Auto-sync already running');
            return;
        }
        this.logger.log(`Starting auto-sync with ${intervalMs}ms interval`);
        this.syncInterval = setInterval(async () => {
            await this.syncPendingOperations();
        }, intervalMs);
    }
    stopAutoSync() {
        if (this.syncInterval) {
            clearInterval(this.syncInterval);
            this.syncInterval = null;
            this.logger.log('Auto-sync stopped');
        }
    }
    async syncPendingOperations(hospitalId) {
        const results = { synced: 0, conflicts: 0, failed: 0 };
        try {
            const whereClause = {
                status: client_1.SyncStatus.PENDING,
            };
            if (hospitalId) {
                whereClause.hospitalId = hospitalId;
            }
            const pendingOps = await this.prisma.syncOperation.findMany({
                where: whereClause,
                orderBy: { createdAt: 'asc' },
                take: this.SYNC_BATCH_SIZE,
            });
            if (pendingOps.length === 0) {
                this.logger.debug('No pending sync operations');
                return results;
            }
            this.logger.log(`Processing ${pendingOps.length} pending sync operations`);
            for (const op of pendingOps) {
                try {
                    const result = await this.processSyncOperation(op);
                    if (result.status === 'SYNCED') {
                        results.synced++;
                    }
                    else if (result.status === 'CONFLICT') {
                        results.conflicts++;
                    }
                    else {
                        results.failed++;
                    }
                }
                catch (error) {
                    this.logger.error(`Failed to process sync operation ${op.id}: ${error.message}`, error.stack);
                    results.failed++;
                }
            }
            this.logger.log(`Sync completed - Synced: ${results.synced}, Conflicts: ${results.conflicts}, Failed: ${results.failed}`);
            return results;
        }
        catch (error) {
            this.logger.error(`Sync batch failed: ${error.message}`, error.stack);
            throw error;
        }
    }
    async processSyncOperation(op) {
        try {
            const currentEntity = await this.getEntity(op.entityType, op.entityId);
            if (currentEntity && currentEntity.version > op.version) {
                this.logger.warn(`Conflict detected for ${op.entityType}:${op.entityId} (local: v${op.version}, remote: v${currentEntity.version})`);
                await this.prisma.syncOperation.update({
                    where: { id: op.id },
                    data: {
                        status: client_1.SyncStatus.CONFLICT,
                        conflictResolution: {
                            localVersion: op.version,
                            remoteVersion: currentEntity.version,
                            localData: op.payload,
                            remoteData: currentEntity,
                            timestamp: new Date().toISOString(),
                        },
                    },
                });
                return {
                    status: 'CONFLICT',
                    conflictData: {
                        local: op.payload,
                        remote: currentEntity,
                    },
                };
            }
            await this.applyOperation(op);
            await this.prisma.syncOperation.update({
                where: { id: op.id },
                data: {
                    status: client_1.SyncStatus.SYNCED,
                    syncedAt: new Date(),
                },
            });
            return { status: 'SYNCED' };
        }
        catch (error) {
            this.logger.error(`Failed to process operation ${op.id}: ${error.message}`);
            await this.prisma.syncOperation.update({
                where: { id: op.id },
                data: { status: client_1.SyncStatus.FAILED },
            });
            return { status: 'FAILED' };
        }
    }
    async applyOperation(op) {
        const model = this.getModelDelegate(op.entityType);
        switch (op.operationType) {
            case 'CREATE':
                await model.create({ data: op.payload });
                break;
            case 'UPDATE':
                await model.update({
                    where: { id: op.entityId },
                    data: op.payload,
                });
                break;
            case 'DELETE':
                await model.delete({ where: { id: op.entityId } });
                break;
            default:
                throw new Error(`Unknown operation type: ${op.operationType}`);
        }
    }
    async resolveConflict(syncOperationId, resolution, mergedData) {
        const syncOp = await this.prisma.syncOperation.findUnique({
            where: { id: syncOperationId },
        });
        if (!syncOp || syncOp.status !== client_1.SyncStatus.CONFLICT) {
            throw new Error('Sync operation not found or not in conflict state');
        }
        const conflictData = syncOp.conflictResolution;
        let dataToApply;
        switch (resolution) {
            case 'USE_LOCAL':
                dataToApply = conflictData.localData;
                break;
            case 'USE_REMOTE':
                await this.prisma.syncOperation.update({
                    where: { id: syncOperationId },
                    data: {
                        status: client_1.SyncStatus.SYNCED,
                        syncedAt: new Date(),
                    },
                });
                return;
            case 'MERGE':
                if (!mergedData) {
                    throw new Error('Merged data required for MERGE resolution');
                }
                dataToApply = mergedData;
                break;
        }
        const model = this.getModelDelegate(syncOp.entityType);
        await model.update({
            where: { id: syncOp.entityId },
            data: dataToApply,
        });
        await this.prisma.syncOperation.update({
            where: { id: syncOperationId },
            data: {
                status: client_1.SyncStatus.SYNCED,
                syncedAt: new Date(),
                conflictResolution: {
                    ...conflictData,
                    resolution,
                    resolvedAt: new Date().toISOString(),
                },
            },
        });
        this.logger.log(`Conflict resolved for sync operation ${syncOperationId} using ${resolution}`);
    }
    async getPendingSyncOperations(hospitalId, pharmacyId, lastSyncTimestamp) {
        const whereClause = {
            hospitalId,
            pharmacyId,
            status: client_1.SyncStatus.SYNCED,
        };
        if (lastSyncTimestamp) {
            whereClause.syncedAt = {
                gt: lastSyncTimestamp,
            };
        }
        return this.prisma.syncOperation.findMany({
            where: whereClause,
            orderBy: { createdAt: 'asc' },
        });
    }
    async getSyncStats(hospitalId) {
        const [pending, synced, conflicts, failed, oldestPendingOp] = await Promise.all([
            this.prisma.syncOperation.count({
                where: { hospitalId, status: client_1.SyncStatus.PENDING },
            }),
            this.prisma.syncOperation.count({
                where: { hospitalId, status: client_1.SyncStatus.SYNCED },
            }),
            this.prisma.syncOperation.count({
                where: { hospitalId, status: client_1.SyncStatus.CONFLICT },
            }),
            this.prisma.syncOperation.count({
                where: { hospitalId, status: client_1.SyncStatus.FAILED },
            }),
            this.prisma.syncOperation.findFirst({
                where: { hospitalId, status: client_1.SyncStatus.PENDING },
                orderBy: { createdAt: 'asc' },
                select: { createdAt: true },
            }),
        ]);
        return {
            pending,
            synced,
            conflicts,
            failed,
            oldestPending: oldestPendingOp?.createdAt,
        };
    }
    getModelDelegate(entityType) {
        const modelName = entityType.toLowerCase();
        const delegate = this.prisma[modelName];
        if (!delegate) {
            throw new Error(`Unknown entity type: ${entityType}`);
        }
        return delegate;
    }
    async getEntity(entityType, entityId) {
        const model = this.getModelDelegate(entityType);
        return model.findUnique({ where: { id: entityId } });
    }
};
exports.SyncService = SyncService;
exports.SyncService = SyncService = SyncService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], SyncService);


/***/ }),
/* 84 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SyncController = void 0;
const common_1 = __webpack_require__(2);
const sync_service_1 = __webpack_require__(83);
let SyncController = class SyncController {
    constructor(syncService) {
        this.syncService = syncService;
    }
    async triggerSync(hospitalId) {
        return this.syncService.syncPendingOperations(hospitalId);
    }
    async getPendingOperations(hospitalId, pharmacyId, lastSync) {
        const lastSyncDate = lastSync ? new Date(lastSync) : undefined;
        return this.syncService.getPendingSyncOperations(hospitalId, pharmacyId, lastSyncDate);
    }
    async uploadOperations(operations) {
        const results = {
            queued: 0,
            failed: 0,
            errors: [],
        };
        for (const op of operations) {
            try {
                await this.syncService.queueOperation(op);
                results.queued++;
            }
            catch (error) {
                results.failed++;
                results.errors.push(`${op.entityType}:${op.entityId} - ${error.message}`);
            }
        }
        return results;
    }
    async getSyncStats(hospitalId) {
        return this.syncService.getSyncStats(hospitalId);
    }
    async resolveConflict(syncOperationId, body) {
        await this.syncService.resolveConflict(syncOperationId, body.resolution, body.mergedData);
        return { message: 'Conflict resolved successfully' };
    }
};
exports.SyncController = SyncController;
__decorate([
    (0, common_1.Post)('trigger'),
    __param(0, (0, common_1.Query)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], SyncController.prototype, "triggerSync", null);
__decorate([
    (0, common_1.Get)('pending/:hospitalId/:pharmacyId'),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Param)('pharmacyId')),
    __param(2, (0, common_1.Query)('lastSync')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", Promise)
], SyncController.prototype, "getPendingOperations", null);
__decorate([
    (0, common_1.Post)('upload'),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Array]),
    __metadata("design:returntype", Promise)
], SyncController.prototype, "uploadOperations", null);
__decorate([
    (0, common_1.Get)('stats/:hospitalId'),
    __param(0, (0, common_1.Param)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], SyncController.prototype, "getSyncStats", null);
__decorate([
    (0, common_1.Post)('conflicts/:syncOperationId/resolve'),
    __param(0, (0, common_1.Param)('syncOperationId')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], SyncController.prototype, "resolveConflict", null);
exports.SyncController = SyncController = __decorate([
    (0, common_1.Controller)('sync'),
    __metadata("design:paramtypes", [typeof (_a = typeof sync_service_1.SyncService !== "undefined" && sync_service_1.SyncService) === "function" ? _a : Object])
], SyncController);


/***/ }),
/* 85 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MedicinesModule = void 0;
const common_1 = __webpack_require__(2);
const medicines_service_1 = __webpack_require__(86);
const medicines_controller_1 = __webpack_require__(87);
const database_module_1 = __webpack_require__(52);
const cache_service_1 = __webpack_require__(11);
let MedicinesModule = class MedicinesModule {
};
exports.MedicinesModule = MedicinesModule;
exports.MedicinesModule = MedicinesModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [medicines_controller_1.MedicinesController],
        providers: [medicines_service_1.MedicinesService, cache_service_1.CacheService],
        exports: [medicines_service_1.MedicinesService],
    })
], MedicinesModule);


/***/ }),
/* 86 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MedicinesService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
let MedicinesService = class MedicinesService {
    constructor(prisma, cacheService) {
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    async create(createMedicineDto, hospitalId) {
        if (!hospitalId) {
            throw new common_1.ConflictException('Hospital ID is required to create a medicine');
        }
        const existing = await this.prisma.medicine.findFirst({
            where: {
                hospitalId,
                name: createMedicineDto.name,
                genericName: createMedicineDto.genericName,
                strength: createMedicineDto.strength,
            },
        });
        if (existing) {
            throw new common_1.ConflictException('Medicine with same name, generic name, and strength already exists');
        }
        const medicine = await this.prisma.medicine.create({
            data: {
                ...createMedicineDto,
                hospitalId,
                status: 'ACTIVE',
            },
        });
        await this.cacheService.deletePattern(`medicines:${hospitalId}:.*`);
        return medicine;
    }
    async findAll(searchDto, hospitalId) {
        const { search, name, genericName, form, manufacturer, status, limit = 50, page = 1, sortBy = 'name', sortOrder = 'asc', } = searchDto;
        const cacheKey = `medicines:${hospitalId || 'all'}:${JSON.stringify(searchDto)}`;
        const cached = await this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = {};
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        if (search) {
            where.OR = [
                { name: { contains: search, mode: 'insensitive' } },
                { genericName: { contains: search, mode: 'insensitive' } },
                { manufacturer: { contains: search, mode: 'insensitive' } },
            ];
        }
        if (name) {
            where.name = { contains: name, mode: 'insensitive' };
        }
        if (genericName) {
            where.genericName = { contains: genericName, mode: 'insensitive' };
        }
        if (form) {
            where.form = form;
        }
        if (manufacturer) {
            where.manufacturer = { contains: manufacturer, mode: 'insensitive' };
        }
        if (status) {
            where.status = status;
        }
        const skip = (page - 1) * limit;
        const [medicines, total] = await Promise.all([
            this.prisma.medicine.findMany({
                where,
                skip,
                take: limit,
                orderBy: { [sortBy]: sortOrder },
                include: {
                    _count: {
                        select: {
                            stockBatches: true,
                            prescriptionItems: true,
                        },
                    },
                },
            }),
            this.prisma.medicine.count({ where }),
        ]);
        const result = {
            data: medicines,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
        await this.cacheService.set(cacheKey, result, 5 * 60 * 1000);
        return result;
    }
    async findOne(id, hospitalId) {
        const where = { id };
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        const medicine = await this.prisma.medicine.findFirst({
            where,
            include: {
                stockBatches: {
                    where: {
                        status: 'AVAILABLE',
                        expiryDate: {
                            gte: new Date(),
                        },
                    },
                    select: {
                        id: true,
                        batchNo: true,
                        qtyAvailable: true,
                        expiryDate: true,
                        receivedDate: true,
                    },
                    orderBy: {
                        receivedDate: 'asc',
                    },
                },
                _count: {
                    select: {
                        prescriptionItems: true,
                        issueItems: true,
                    },
                },
            },
        });
        if (!medicine) {
            throw new common_1.NotFoundException('Medicine not found');
        }
        return medicine;
    }
    async update(id, updateMedicineDto, hospitalId) {
        const medicine = await this.findOne(id, hospitalId);
        const updated = await this.prisma.medicine.update({
            where: { id: medicine.id },
            data: updateMedicineDto,
        });
        if (medicine.hospitalId) {
            await this.cacheService.deletePattern(`medicines:${medicine.hospitalId}:.*`);
        }
        await this.cacheService.deletePattern(`medicines:all:.*`);
        return updated;
    }
    async remove(id, hospitalId) {
        const medicine = await this.findOne(id, hospitalId);
        const hasStock = await this.prisma.stockBatch.count({
            where: {
                medicineId: id,
                status: 'AVAILABLE',
                qtyAvailable: { gt: 0 },
            },
        });
        if (hasStock > 0) {
            throw new common_1.ConflictException('Cannot delete medicine with active stock batches');
        }
        const discontinued = await this.prisma.medicine.update({
            where: { id: medicine.id },
            data: { status: 'DISCONTINUED' },
        });
        if (medicine.hospitalId) {
            await this.cacheService.deletePattern(`medicines:${medicine.hospitalId}:.*`);
        }
        await this.cacheService.deletePattern(`medicines:all:.*`);
        return discontinued;
    }
    async getStats(hospitalId) {
        const cacheKey = `medicines:stats:${hospitalId || 'all'}`;
        const cached = await this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = {};
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        const [total, active, byForm] = await Promise.all([
            this.prisma.medicine.count({ where }),
            this.prisma.medicine.count({ where: { ...where, status: 'ACTIVE' } }),
            this.prisma.medicine.groupBy({
                by: ['form'],
                where: { ...where, status: 'ACTIVE' },
                _count: true,
            }),
        ]);
        const result = {
            total,
            active,
            discontinued: total - active,
            byForm: byForm.reduce((acc, item) => {
                acc[item.form] = item._count;
                return acc;
            }, {}),
        };
        await this.cacheService.set(cacheKey, result, 5 * 60 * 1000);
        return result;
    }
};
exports.MedicinesService = MedicinesService;
exports.MedicinesService = MedicinesService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _b : Object])
], MedicinesService);


/***/ }),
/* 87 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MedicinesController = void 0;
const common_1 = __webpack_require__(2);
const medicines_service_1 = __webpack_require__(86);
const create_medicine_dto_1 = __webpack_require__(88);
const update_medicine_dto_1 = __webpack_require__(89);
const search_medicines_dto_1 = __webpack_require__(90);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let MedicinesController = class MedicinesController {
    constructor(medicinesService) {
        this.medicinesService = medicinesService;
    }
    create(createMedicineDto, req) {
        const hospitalId = createMedicineDto.hospitalId || req.user.hospitalId;
        return this.medicinesService.create(createMedicineDto, hospitalId);
    }
    findAll(searchDto, req) {
        const hospitalId = req.user.hospitalId || searchDto.hospitalId;
        return this.medicinesService.findAll(searchDto, hospitalId);
    }
    getStats(hospitalId, req) {
        const targetHospitalId = req.user.hospitalId || hospitalId;
        return this.medicinesService.getStats(targetHospitalId);
    }
    findOne(id, req) {
        return this.medicinesService.findOne(id, req.user.hospitalId);
    }
    update(id, updateMedicineDto, req) {
        return this.medicinesService.update(id, updateMedicineDto, req.user.hospitalId);
    }
    remove(id, req) {
        return this.medicinesService.remove(id, req.user.hospitalId);
    }
};
exports.MedicinesController = MedicinesController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_medicine_dto_1.CreateMedicineDto !== "undefined" && create_medicine_dto_1.CreateMedicineDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.DOCTOR),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof search_medicines_dto_1.SearchMedicinesDto !== "undefined" && search_medicines_dto_1.SearchMedicinesDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.DOCTOR),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_medicine_dto_1.UpdateMedicineDto !== "undefined" && update_medicine_dto_1.UpdateMedicineDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], MedicinesController.prototype, "remove", null);
exports.MedicinesController = MedicinesController = __decorate([
    (0, common_1.Controller)('medicines'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof medicines_service_1.MedicinesService !== "undefined" && medicines_service_1.MedicinesService) === "function" ? _a : Object])
], MedicinesController);


/***/ }),
/* 88 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateMedicineDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateMedicineDto {
}
exports.CreateMedicineDto = CreateMedicineDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateMedicineDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Medicine name is required' }),
    __metadata("design:type", String)
], CreateMedicineDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateMedicineDto.prototype, "genericName", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineForm, { message: 'Invalid medicine form' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Medicine form is required' }),
    __metadata("design:type", typeof (_a = typeof client_1.MedicineForm !== "undefined" && client_1.MedicineForm) === "function" ? _a : Object)
], CreateMedicineDto.prototype, "form", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateMedicineDto.prototype, "strength", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateMedicineDto.prototype, "manufacturer", void 0);


/***/ }),
/* 89 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateMedicineDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdateMedicineDto {
}
exports.UpdateMedicineDto = UpdateMedicineDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "genericName", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineForm, { message: 'Invalid medicine form' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.MedicineForm !== "undefined" && client_1.MedicineForm) === "function" ? _a : Object)
], UpdateMedicineDto.prototype, "form", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "strength", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "manufacturer", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], UpdateMedicineDto.prototype, "reorderLevel", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateMedicineDto.prototype, "storageInstructions", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineStatus, { message: 'Invalid medicine status' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.MedicineStatus !== "undefined" && client_1.MedicineStatus) === "function" ? _b : Object)
], UpdateMedicineDto.prototype, "status", void 0);


/***/ }),
/* 90 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchMedicinesDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class SearchMedicinesDto {
    constructor() {
        this.limit = 50;
        this.page = 1;
        this.sortBy = 'name';
        this.sortOrder = 'asc';
    }
}
exports.SearchMedicinesDto = SearchMedicinesDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "search", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "genericName", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineForm, { message: 'Invalid medicine form' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.MedicineForm !== "undefined" && client_1.MedicineForm) === "function" ? _a : Object)
], SearchMedicinesDto.prototype, "form", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "manufacturer", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineStatus, { message: 'Invalid medicine status' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.MedicineStatus !== "undefined" && client_1.MedicineStatus) === "function" ? _b : Object)
], SearchMedicinesDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(200),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchMedicinesDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchMedicinesDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "sortBy", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['asc', 'desc']),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchMedicinesDto.prototype, "sortOrder", void 0);


/***/ }),
/* 91 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.InventoryModule = void 0;
const common_1 = __webpack_require__(2);
const inventory_service_1 = __webpack_require__(92);
const inventory_controller_1 = __webpack_require__(93);
const database_module_1 = __webpack_require__(52);
const cache_service_1 = __webpack_require__(11);
let InventoryModule = class InventoryModule {
};
exports.InventoryModule = InventoryModule;
exports.InventoryModule = InventoryModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [inventory_controller_1.InventoryController],
        providers: [inventory_service_1.InventoryService, cache_service_1.CacheService],
        exports: [inventory_service_1.InventoryService],
    })
], InventoryModule);


/***/ }),
/* 92 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.InventoryService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
let InventoryService = class InventoryService {
    constructor(prisma, cacheService) {
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    calculateDispensingUnits(qtyReceivedPurchase, unitsPerPurchaseUnit = 1, unitsPerSubUnit = 1) {
        return qtyReceivedPurchase * unitsPerPurchaseUnit * unitsPerSubUnit;
    }
    calculateCostPerDispensingUnit(purchaseUnitPrice, unitsPerPurchaseUnit = 1, unitsPerSubUnit = 1) {
        const totalUnitsPerPurchase = unitsPerPurchaseUnit * unitsPerSubUnit;
        if (totalUnitsPerPurchase === 0)
            return 0;
        return purchaseUnitPrice / totalUnitsPerPurchase;
    }
    async create(createStockBatchDto, hospitalId) {
        let medicineId = createStockBatchDto.medicineId;
        if (!medicineId && createStockBatchDto.medicineName) {
            const medicine = await this.findOrCreateMedicine({
                name: createStockBatchDto.medicineName,
                genericName: createStockBatchDto.genericName,
                form: createStockBatchDto.form,
                strength: createStockBatchDto.strength,
                manufacturer: createStockBatchDto.medicineManufacturer,
            }, hospitalId);
            medicineId = medicine.id;
        }
        if (!medicineId) {
            throw new common_1.BadRequestException('Either medicineId or medicineName with form must be provided');
        }
        if (createStockBatchDto.medicineId) {
            const medicine = await this.prisma.medicine.findFirst({
                where: {
                    id: medicineId,
                    hospitalId,
                },
            });
            if (!medicine) {
                throw new common_1.NotFoundException('Medicine not found');
            }
        }
        const pharmacy = await this.prisma.pharmacy.findFirst({
            where: {
                id: createStockBatchDto.pharmacyId,
                hospitalId,
            },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        const existingBatch = await this.prisma.stockBatch.findFirst({
            where: {
                hospitalId,
                pharmacyId: createStockBatchDto.pharmacyId,
                medicineId: medicineId,
                batchNo: createStockBatchDto.batchNo,
            },
        });
        if (existingBatch) {
            throw new common_1.ConflictException('Stock batch with this batch number already exists for this medicine');
        }
        const stockBatch = await this.prisma.stockBatch.create({
            data: {
                medicineId,
                pharmacyId: createStockBatchDto.pharmacyId,
                batchNo: createStockBatchDto.batchNo,
                qtyReceived: createStockBatchDto.qtyReceived,
                qtyAvailable: createStockBatchDto.qtyReceived,
                expiryDate: new Date(createStockBatchDto.expiryDate),
                manufacturer: createStockBatchDto.manufacturer,
                storageType: createStockBatchDto.storageType,
                purchasePrice: createStockBatchDto.purchasePrice,
                governmentPrice: createStockBatchDto.governmentPrice,
                retailPrice: createStockBatchDto.retailPrice,
                hospitalId,
                receivedDate: createStockBatchDto.receivedDate
                    ? new Date(createStockBatchDto.receivedDate)
                    : new Date(),
                purchaseUnit: createStockBatchDto.purchaseUnit,
                purchaseUnitPrice: createStockBatchDto.purchaseUnitPrice,
                unitsPerPurchaseUnit: createStockBatchDto.unitsPerPurchaseUnit ?? 1,
                subUnit: createStockBatchDto.subUnit,
                unitsPerSubUnit: createStockBatchDto.unitsPerSubUnit ?? 1,
                dispensingUnit: createStockBatchDto.dispensingUnit,
                qtyReceivedPurchase: createStockBatchDto.qtyReceivedPurchase ?? createStockBatchDto.qtyReceived,
                qtyAvailableDispensing: this.calculateDispensingUnits(createStockBatchDto.qtyReceivedPurchase ?? createStockBatchDto.qtyReceived, createStockBatchDto.unitsPerPurchaseUnit ?? 1, createStockBatchDto.unitsPerSubUnit ?? 1),
                costPerDispensingUnit: createStockBatchDto.purchaseUnitPrice
                    ? this.calculateCostPerDispensingUnit(createStockBatchDto.purchaseUnitPrice, createStockBatchDto.unitsPerPurchaseUnit ?? 1, createStockBatchDto.unitsPerSubUnit ?? 1)
                    : null,
                reorderLevel: createStockBatchDto.reorderLevel ?? 10,
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                        strength: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        type: true,
                    },
                },
            },
        });
        await this.invalidateCache(hospitalId);
        return stockBatch;
    }
    async findOrCreateMedicine(medicineData, hospitalId) {
        if (!medicineData.form) {
            throw new common_1.BadRequestException('Medicine form is required when creating from name');
        }
        let medicine = await this.prisma.medicine.findFirst({
            where: {
                hospitalId,
                name: {
                    equals: medicineData.name,
                    mode: 'insensitive',
                },
                form: medicineData.form,
                ...(medicineData.strength && {
                    strength: {
                        equals: medicineData.strength,
                        mode: 'insensitive',
                    },
                }),
            },
        });
        if (!medicine) {
            medicine = await this.prisma.medicine.create({
                data: {
                    name: medicineData.name,
                    genericName: medicineData.genericName,
                    form: medicineData.form,
                    strength: medicineData.strength,
                    manufacturer: medicineData.manufacturer,
                    hospitalId,
                    status: 'ACTIVE',
                },
            });
        }
        return medicine;
    }
    async bulkCreate(batches, hospitalId) {
        const results = {
            successful: [],
            failed: [],
            created: 0,
            errors: 0,
            medicinesCreated: 0,
        };
        for (const batch of batches) {
            try {
                const stockBatch = await this.create(batch, hospitalId);
                results.successful.push({
                    batchNo: batch.batchNo,
                    medicineName: batch.medicineName || 'N/A',
                    stockBatchId: stockBatch.id,
                });
                results.created++;
                if (batch.medicineName && !batch.medicineId) {
                    results.medicinesCreated++;
                }
            }
            catch (error) {
                results.failed.push({
                    batchNo: batch.batchNo,
                    medicineName: batch.medicineName || batch.medicineId || 'Unknown',
                    error: error.message,
                });
                results.errors++;
            }
        }
        return results;
    }
    async findAll(searchDto, hospitalId) {
        const { search, medicineId, pharmacyId, batchNo, status, storageType, dispensingUnit, lowStockOnly, expiringBefore, expiringAfter, limit = 50, page = 1, sortBy = 'receivedDate', sortOrder = 'asc', } = searchDto;
        const cacheKey = `inventory:${hospitalId || 'all'}:${JSON.stringify(searchDto)}`;
        const cached = await this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = {};
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        if (search) {
            where.OR = [
                { batchNo: { contains: search, mode: 'insensitive' } },
                { manufacturer: { contains: search, mode: 'insensitive' } },
                { medicine: { name: { contains: search, mode: 'insensitive' } } },
            ];
        }
        if (medicineId)
            where.medicineId = medicineId;
        if (pharmacyId)
            where.pharmacyId = pharmacyId;
        if (batchNo)
            where.batchNo = { contains: batchNo, mode: 'insensitive' };
        if (status)
            where.status = status;
        if (storageType)
            where.storageType = storageType;
        if (dispensingUnit)
            where.dispensingUnit = { contains: dispensingUnit, mode: 'insensitive' };
        if (lowStockOnly) {
            where.AND = [
                ...(where.AND || []),
                {
                    OR: [
                        {
                            qtyAvailableDispensing: { not: null },
                        },
                        {
                            qtyAvailableDispensing: null,
                            qtyAvailable: { lte: 10 },
                        },
                    ],
                },
            ];
        }
        if (expiringBefore) {
            where.expiryDate = { ...where.expiryDate, lte: new Date(expiringBefore) };
        }
        if (expiringAfter) {
            where.expiryDate = { ...where.expiryDate, gte: new Date(expiringAfter) };
        }
        const skip = (page - 1) * limit;
        const [batches, total] = await Promise.all([
            this.prisma.stockBatch.findMany({
                where,
                skip,
                take: limit,
                orderBy: { [sortBy]: sortOrder },
                include: {
                    medicine: {
                        select: {
                            id: true,
                            name: true,
                            genericName: true,
                            form: true,
                            strength: true,
                        },
                    },
                    pharmacy: {
                        select: {
                            id: true,
                            name: true,
                            type: true,
                        },
                    },
                },
            }),
            this.prisma.stockBatch.count({ where }),
        ]);
        const result = {
            data: batches,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
        await this.cacheService.set(cacheKey, result, 3 * 60 * 1000);
        return result;
    }
    async findOne(id, hospitalId) {
        const batch = await this.prisma.stockBatch.findFirst({
            where: { id, hospitalId },
            include: {
                medicine: true,
                pharmacy: true,
            },
        });
        if (!batch) {
            throw new common_1.NotFoundException('Stock batch not found');
        }
        return batch;
    }
    async update(id, updateStockBatchDto, hospitalId) {
        const batch = await this.findOne(id, hospitalId);
        if (updateStockBatchDto.qtyReceived) {
            const issued = batch.qtyReceived - batch.qtyAvailable;
            if (issued > 0) {
                throw new common_1.BadRequestException('Cannot update received quantity for batch with issued items');
            }
        }
        const updated = await this.prisma.stockBatch.update({
            where: { id: batch.id },
            data: {
                ...updateStockBatchDto,
                expiryDate: updateStockBatchDto.expiryDate
                    ? new Date(updateStockBatchDto.expiryDate)
                    : undefined,
                receivedDate: updateStockBatchDto.receivedDate
                    ? new Date(updateStockBatchDto.receivedDate)
                    : undefined,
            },
            include: {
                medicine: true,
                pharmacy: true,
            },
        });
        await this.invalidateCache(hospitalId);
        return updated;
    }
    async remove(id, hospitalId) {
        const batch = await this.findOne(id, hospitalId);
        if (batch.qtyAvailable < batch.qtyReceived) {
            throw new common_1.ConflictException('Cannot delete batch with issued items');
        }
        const deleted = await this.prisma.stockBatch.update({
            where: { id: batch.id },
            data: { status: 'DEPLETED' },
        });
        await this.invalidateCache(hospitalId);
        return deleted;
    }
    async getAvailableBatchesFIFO(medicineId, pharmacyId, hospitalId) {
        const now = new Date();
        return this.prisma.stockBatch.findMany({
            where: {
                hospitalId,
                pharmacyId,
                medicineId,
                status: 'AVAILABLE',
                qtyAvailable: { gt: 0 },
                expiryDate: { gt: now },
            },
            orderBy: {
                receivedDate: 'asc',
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                        strength: true,
                    },
                },
            },
        });
    }
    async dispenseBatch(batchId, dispensingQty, hospitalId) {
        const batch = await this.findOne(batchId, hospitalId);
        if (batch.status !== 'AVAILABLE') {
            throw new common_1.BadRequestException('Batch is not available for dispensing');
        }
        const currentDispensing = batch.qtyAvailableDispensing ?? batch.qtyAvailable;
        if (currentDispensing < dispensingQty) {
            throw new common_1.BadRequestException(`Insufficient stock. Requested: ${dispensingQty}, Available: ${currentDispensing} ${batch.dispensingUnit || 'units'}`);
        }
        const newDispensingQty = currentDispensing - dispensingQty;
        const newQtyAvailable = batch.qtyAvailable - dispensingQty;
        const updatedBatch = await this.prisma.stockBatch.update({
            where: { id: batch.id },
            data: {
                qtyAvailable: Math.max(0, newQtyAvailable),
                qtyAvailableDispensing: Math.max(0, newDispensingQty),
                status: newDispensingQty <= 0 ? 'DEPLETED' : batch.status,
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                        strength: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        type: true,
                    },
                },
            },
        });
        const lowStockAlert = newDispensingQty <= (batch.reorderLevel ?? 10);
        await this.invalidateCache(hospitalId);
        return { batch: updatedBatch, lowStockAlert };
    }
    async getTotalAvailableStock(medicineId, pharmacyId, hospitalId) {
        const batches = await this.getAvailableBatchesFIFO(medicineId, pharmacyId, hospitalId);
        return batches.reduce((total, batch) => total + batch.qtyAvailable, 0);
    }
    async getLowStockAlerts(hospitalId, pharmacyId, threshold = 10) {
        const cacheKey = `inventory:low-stock:${hospitalId}:${pharmacyId || 'all'}:${threshold}`;
        const cached = this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = { hospitalId, status: 'ACTIVE' };
        const medicines = await this.prisma.medicine.findMany({
            where,
            include: {
                stockBatches: {
                    where: {
                        ...(pharmacyId && { pharmacyId }),
                        status: 'AVAILABLE',
                        qtyAvailable: { gt: 0 },
                        expiryDate: { gt: new Date() },
                    },
                    select: {
                        qtyAvailable: true,
                        qtyAvailableDispensing: true,
                        reorderLevel: true,
                        dispensingUnit: true,
                        pharmacyId: true,
                    },
                },
            },
        });
        const lowStockMedicines = medicines
            .map((medicine) => {
            const totalStock = medicine.stockBatches.reduce((sum, batch) => sum + batch.qtyAvailable, 0);
            const totalDispensingStock = medicine.stockBatches.reduce((sum, batch) => sum + (batch.qtyAvailableDispensing ?? batch.qtyAvailable), 0);
            const effectiveReorderLevel = medicine.stockBatches.length > 0
                ? Math.max(...medicine.stockBatches.map(b => b.reorderLevel ?? threshold))
                : threshold;
            const dispensingUnit = medicine.stockBatches.find(b => b.dispensingUnit)?.dispensingUnit;
            return {
                medicineId: medicine.id,
                medicineName: medicine.name,
                genericName: medicine.genericName,
                form: medicine.form,
                strength: medicine.strength,
                totalStock,
                totalDispensingStock,
                reorderLevel: effectiveReorderLevel,
                dispensingUnit,
                belowReorderLevel: totalDispensingStock <= effectiveReorderLevel,
            };
        })
            .filter((med) => med.totalDispensingStock <= med.reorderLevel && med.totalDispensingStock >= 0);
        this.cacheService.set(cacheKey, lowStockMedicines, 3 * 60 * 1000);
        return lowStockMedicines;
    }
    async getExpiringBatches(hospitalId, daysUntilExpiry = 30) {
        const now = new Date();
        const expiryThreshold = new Date();
        expiryThreshold.setDate(now.getDate() + daysUntilExpiry);
        return this.prisma.stockBatch.findMany({
            where: {
                hospitalId,
                status: 'AVAILABLE',
                qtyAvailable: { gt: 0 },
                expiryDate: {
                    gte: now,
                    lte: expiryThreshold,
                },
            },
            orderBy: {
                expiryDate: 'asc',
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
    }
    async getStats(hospitalId, pharmacyId) {
        if (!hospitalId) {
            return {
                totalBatches: 0,
                availableBatches: 0,
                expiredBatches: 0,
                depletedBatches: 0,
                totalQuantity: 0,
                totalValue: 0,
                lowStock: 0,
                expiringSoon: 0,
            };
        }
        const cacheKey = `inventory:stats:${hospitalId}:${pharmacyId || 'all'}`;
        const cached = this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = { hospitalId };
        if (pharmacyId)
            where.pharmacyId = pharmacyId;
        const [totalBatches, availableBatches, expiredBatches, depletedBatches] = await Promise.all([
            this.prisma.stockBatch.count({ where }),
            this.prisma.stockBatch.count({ where: { ...where, status: 'AVAILABLE' } }),
            this.prisma.stockBatch.count({ where: { ...where, status: 'EXPIRED' } }),
            this.prisma.stockBatch.count({ where: { ...where, status: 'DEPLETED' } }),
        ]);
        const totalQuantityResult = await this.prisma.stockBatch.aggregate({
            where: { ...where, status: 'AVAILABLE' },
            _sum: {
                qtyAvailable: true,
                qtyAvailableDispensing: true,
            },
        });
        const availableBatches_data = await this.prisma.stockBatch.findMany({
            where: { ...where, status: 'AVAILABLE' },
            select: {
                qtyAvailable: true,
                qtyAvailableDispensing: true,
                purchasePrice: true,
                costPerDispensingUnit: true,
            },
        });
        const totalValue = availableBatches_data.reduce((sum, batch) => {
            return sum + (batch.qtyAvailable * Number(batch.purchasePrice));
        }, 0);
        const totalDispensingValue = availableBatches_data.reduce((sum, batch) => {
            if (batch.costPerDispensingUnit && batch.qtyAvailableDispensing) {
                return sum + (batch.qtyAvailableDispensing * Number(batch.costPerDispensingUnit));
            }
            return sum + (batch.qtyAvailable * Number(batch.purchasePrice));
        }, 0);
        const lowStockCount = (await this.getLowStockAlerts(hospitalId, pharmacyId)).length;
        const expiringCount = (await this.getExpiringBatches(hospitalId, 30)).length;
        const result = {
            totalBatches,
            availableBatches,
            expiredBatches,
            depletedBatches,
            totalQuantity: totalQuantityResult._sum.qtyAvailable || 0,
            totalDispensingQuantity: totalQuantityResult._sum.qtyAvailableDispensing || 0,
            totalValue: Math.round(totalValue * 100) / 100,
            totalDispensingValue: Math.round(totalDispensingValue * 100) / 100,
            lowStock: lowStockCount,
            expiringSoon: expiringCount,
        };
        this.cacheService.set(cacheKey, result, 2 * 60 * 1000);
        return result;
    }
    async getPharmacyHospital(pharmacyId) {
        const pharmacy = await this.prisma.pharmacy.findUnique({
            where: { id: pharmacyId },
            select: { id: true, hospitalId: true, name: true },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        return pharmacy;
    }
    async invalidateCache(hospitalId) {
        await this.cacheService.deletePattern(`inventory:${hospitalId}:.*`);
        await this.cacheService.deletePattern(`inventory:all:.*`);
    }
};
exports.InventoryService = InventoryService;
exports.InventoryService = InventoryService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _b : Object])
], InventoryService);


/***/ }),
/* 93 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.InventoryController = void 0;
const common_1 = __webpack_require__(2);
const inventory_service_1 = __webpack_require__(92);
const create_stock_batch_dto_1 = __webpack_require__(94);
const create_stock_batch_bulk_dto_1 = __webpack_require__(95);
const update_stock_batch_dto_1 = __webpack_require__(96);
const search_stock_batch_dto_1 = __webpack_require__(97);
const dispense_batch_dto_1 = __webpack_require__(98);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let InventoryController = class InventoryController {
    constructor(inventoryService) {
        this.inventoryService = inventoryService;
    }
    async create(createStockBatchDto, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId && createStockBatchDto.pharmacyId) {
            const pharmacy = await this.inventoryService.getPharmacyHospital(createStockBatchDto.pharmacyId);
            hospitalId = pharmacy.hospitalId;
        }
        return this.inventoryService.create(createStockBatchDto, hospitalId);
    }
    async bulkCreate(bulkDto, req) {
        const hospitalId = req.user.hospitalId || bulkDto.hospitalId;
        return this.inventoryService.bulkCreate(bulkDto.batches, hospitalId);
    }
    async findAll(searchDto, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId && searchDto.pharmacyId) {
            const pharmacy = await this.inventoryService.getPharmacyHospital(searchDto.pharmacyId);
            hospitalId = pharmacy.hospitalId;
        }
        return this.inventoryService.findAll(searchDto, hospitalId);
    }
    async getAvailableBatchesFIFO(medicineId, pharmacyId, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const pharmacy = await this.inventoryService['prisma'].pharmacy.findUnique({
                where: { id: pharmacyId },
                select: { hospitalId: true },
            });
            hospitalId = pharmacy?.hospitalId;
        }
        return this.inventoryService.getAvailableBatchesFIFO(medicineId, pharmacyId, hospitalId);
    }
    async getAvailableBatches(medicineId, pharmacyId, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const pharmacy = await this.inventoryService['prisma'].pharmacy.findUnique({
                where: { id: pharmacyId },
                select: { hospitalId: true },
            });
            hospitalId = pharmacy?.hospitalId;
        }
        return this.inventoryService.getAvailableBatchesFIFO(medicineId, pharmacyId, hospitalId);
    }
    async getTotalAvailableStock(medicineId, pharmacyId, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const pharmacy = await this.inventoryService['prisma'].pharmacy.findUnique({
                where: { id: pharmacyId },
                select: { hospitalId: true },
            });
            hospitalId = pharmacy?.hospitalId;
        }
        const totalStock = await this.inventoryService.getTotalAvailableStock(medicineId, pharmacyId, hospitalId);
        return { medicineId, pharmacyId, totalQty: totalStock };
    }
    getLowStockAlerts(pharmacyId, threshold, req) {
        return this.inventoryService.getLowStockAlerts(req.user.hospitalId, pharmacyId, threshold || 10);
    }
    getExpiringBatches(days, req) {
        return this.inventoryService.getExpiringBatches(req.user.hospitalId, days || 30);
    }
    async getStats(pharmacyId, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId && pharmacyId) {
            const pharmacy = await this.inventoryService.getPharmacyHospital(pharmacyId);
            hospitalId = pharmacy.hospitalId;
        }
        return this.inventoryService.getStats(hospitalId, pharmacyId);
    }
    async dispenseBatch(dispenseDto, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const batch = await this.inventoryService['prisma'].stockBatch.findUnique({
                where: { id: dispenseDto.batchId },
                select: { hospitalId: true },
            });
            hospitalId = batch?.hospitalId;
        }
        return this.inventoryService.dispenseBatch(dispenseDto.batchId, dispenseDto.dispensingQty, hospitalId);
    }
    findOne(id, req) {
        return this.inventoryService.findOne(id, req.user.hospitalId);
    }
    update(id, updateStockBatchDto, req) {
        return this.inventoryService.update(id, updateStockBatchDto, req.user.hospitalId);
    }
    remove(id, req) {
        return this.inventoryService.remove(id, req.user.hospitalId);
    }
};
exports.InventoryController = InventoryController;
__decorate([
    (0, common_1.Post)('batches'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_stock_batch_dto_1.CreateStockBatchDto !== "undefined" && create_stock_batch_dto_1.CreateStockBatchDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "create", null);
__decorate([
    (0, common_1.Post)('batches/bulk'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof create_stock_batch_bulk_dto_1.BulkCreateStockBatchDto !== "undefined" && create_stock_batch_bulk_dto_1.BulkCreateStockBatchDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "bulkCreate", null);
__decorate([
    (0, common_1.Get)('batches'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof search_stock_batch_dto_1.SearchStockBatchDto !== "undefined" && search_stock_batch_dto_1.SearchStockBatchDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('batches/fifo/:medicineId/:pharmacyId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Param)('medicineId')),
    __param(1, (0, common_1.Param)('pharmacyId')),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "getAvailableBatchesFIFO", null);
__decorate([
    (0, common_1.Get)('batches/available/:medicineId/:pharmacyId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Param)('medicineId')),
    __param(1, (0, common_1.Param)('pharmacyId')),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "getAvailableBatches", null);
__decorate([
    (0, common_1.Get)('batches/stock-level/:medicineId/:pharmacyId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.DOCTOR),
    __param(0, (0, common_1.Param)('medicineId')),
    __param(1, (0, common_1.Param)('pharmacyId')),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "getTotalAvailableStock", null);
__decorate([
    (0, common_1.Get)('alerts/low-stock'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)('pharmacyId')),
    __param(1, (0, common_1.Query)('threshold')),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number, Object]),
    __metadata("design:returntype", void 0)
], InventoryController.prototype, "getLowStockAlerts", null);
__decorate([
    (0, common_1.Get)('alerts/expiring'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)('days')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Number, Object]),
    __metadata("design:returntype", void 0)
], InventoryController.prototype, "getExpiringBatches", null);
__decorate([
    (0, common_1.Get)('stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)('pharmacyId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "getStats", null);
__decorate([
    (0, common_1.Post)('batches/dispense'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof dispense_batch_dto_1.DispenseBatchDto !== "undefined" && dispense_batch_dto_1.DispenseBatchDto) === "function" ? _e : Object, Object]),
    __metadata("design:returntype", Promise)
], InventoryController.prototype, "dispenseBatch", null);
__decorate([
    (0, common_1.Get)('batches/:id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], InventoryController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)('batches/:id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof update_stock_batch_dto_1.UpdateStockBatchDto !== "undefined" && update_stock_batch_dto_1.UpdateStockBatchDto) === "function" ? _f : Object, Object]),
    __metadata("design:returntype", void 0)
], InventoryController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)('batches/:id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], InventoryController.prototype, "remove", null);
exports.InventoryController = InventoryController = __decorate([
    (0, common_1.Controller)('inventory'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof inventory_service_1.InventoryService !== "undefined" && inventory_service_1.InventoryService) === "function" ? _a : Object])
], InventoryController);


/***/ }),
/* 94 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateStockBatchDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class CreateStockBatchDto {
}
exports.CreateStockBatchDto = CreateStockBatchDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "medicineName", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "genericName", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineForm),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.MedicineForm !== "undefined" && client_1.MedicineForm) === "function" ? _a : Object)
], CreateStockBatchDto.prototype, "form", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "strength", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "medicineManufacturer", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Pharmacy ID is required' }),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Batch number is required' }),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "batchNo", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Quantity received must be at least 1' }),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "qtyReceived", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Expiry date is required' }),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "expiryDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "manufacturer", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.StorageType, { message: 'Invalid storage type' }),
    __metadata("design:type", typeof (_b = typeof client_1.StorageType !== "undefined" && client_1.StorageType) === "function" ? _b : Object)
], CreateStockBatchDto.prototype, "storageType", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Purchase price must be non-negative' }),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "purchasePrice", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Government price must be non-negative' }),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "governmentPrice", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Retail price must be non-negative' }),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "retailPrice", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "purchaseUnit", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Purchase unit price must be non-negative' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "purchaseUnitPrice", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Units per purchase unit must be at least 1' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "unitsPerPurchaseUnit", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "subUnit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Units per sub unit must be at least 1' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "unitsPerSubUnit", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "dispensingUnit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Quantity received in purchase units must be at least 1' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "qtyReceivedPurchase", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateStockBatchDto.prototype, "reorderLevel", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateStockBatchDto.prototype, "receivedDate", void 0);


/***/ }),
/* 95 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BulkCreateStockBatchDto = exports.BulkStockBatchItemDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class BulkStockBatchItemDto {
}
exports.BulkStockBatchItemDto = BulkStockBatchItemDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "medicineName", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "genericName", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.MedicineForm),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.MedicineForm !== "undefined" && client_1.MedicineForm) === "function" ? _a : Object)
], BulkStockBatchItemDto.prototype, "form", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "strength", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "medicineManufacturer", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Pharmacy ID is required' }),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Batch number is required' }),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "batchNo", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Quantity received must be at least 1' }),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "qtyReceived", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Expiry date is required' }),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "expiryDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "manufacturer", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.StorageType, { message: 'Invalid storage type' }),
    __metadata("design:type", typeof (_b = typeof client_1.StorageType !== "undefined" && client_1.StorageType) === "function" ? _b : Object)
], BulkStockBatchItemDto.prototype, "storageType", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Purchase price must be non-negative' }),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "purchasePrice", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Government price must be non-negative' }),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "governmentPrice", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0, { message: 'Retail price must be non-negative' }),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "retailPrice", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "purchaseUnit", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "purchaseUnitPrice", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "unitsPerPurchaseUnit", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "subUnit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "unitsPerSubUnit", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "dispensingUnit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "qtyReceivedPurchase", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], BulkStockBatchItemDto.prototype, "reorderLevel", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkStockBatchItemDto.prototype, "receivedDate", void 0);
class BulkCreateStockBatchDto {
}
exports.BulkCreateStockBatchDto = BulkCreateStockBatchDto;
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => BulkStockBatchItemDto),
    __metadata("design:type", Array)
], BulkCreateStockBatchDto.prototype, "batches", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Hospital ID is required' }),
    __metadata("design:type", String)
], BulkCreateStockBatchDto.prototype, "hospitalId", void 0);


/***/ }),
/* 96 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateStockBatchDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_stock_batch_dto_1 = __webpack_require__(94);
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdateStockBatchDto extends (0, mapped_types_1.PartialType)(create_stock_batch_dto_1.CreateStockBatchDto) {
}
exports.UpdateStockBatchDto = UpdateStockBatchDto;
__decorate([
    (0, class_validator_1.IsEnum)(client_1.BatchStatus, { message: 'Invalid batch status' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.BatchStatus !== "undefined" && client_1.BatchStatus) === "function" ? _a : Object)
], UpdateStockBatchDto.prototype, "status", void 0);


/***/ }),
/* 97 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchStockBatchDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class SearchStockBatchDto {
    constructor() {
        this.limit = 50;
        this.page = 1;
        this.sortBy = 'receivedDate';
        this.sortOrder = 'asc';
    }
}
exports.SearchStockBatchDto = SearchStockBatchDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "search", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "batchNo", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.BatchStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.BatchStatus !== "undefined" && client_1.BatchStatus) === "function" ? _a : Object)
], SearchStockBatchDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.StorageType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.StorageType !== "undefined" && client_1.StorageType) === "function" ? _b : Object)
], SearchStockBatchDto.prototype, "storageType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "dispensingUnit", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Transform)(({ value }) => value === 'true' || value === true),
    __metadata("design:type", Boolean)
], SearchStockBatchDto.prototype, "lowStockOnly", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "expiringBefore", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "expiringAfter", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(200),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchStockBatchDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchStockBatchDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "sortBy", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['asc', 'desc']),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchStockBatchDto.prototype, "sortOrder", void 0);


/***/ }),
/* 98 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DispenseBatchDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class DispenseBatchDto {
}
exports.DispenseBatchDto = DispenseBatchDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Batch ID is required' }),
    __metadata("design:type", String)
], DispenseBatchDto.prototype, "batchId", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Dispensing quantity must be at least 1' }),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], DispenseBatchDto.prototype, "dispensingQty", void 0);


/***/ }),
/* 99 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PrescriptionsModule = void 0;
const common_1 = __webpack_require__(2);
const prescriptions_service_1 = __webpack_require__(100);
const prescriptions_controller_1 = __webpack_require__(101);
const database_module_1 = __webpack_require__(52);
let PrescriptionsModule = class PrescriptionsModule {
};
exports.PrescriptionsModule = PrescriptionsModule;
exports.PrescriptionsModule = PrescriptionsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [prescriptions_controller_1.PrescriptionsController],
        providers: [prescriptions_service_1.PrescriptionsService],
        exports: [prescriptions_service_1.PrescriptionsService],
    })
], PrescriptionsModule);


/***/ }),
/* 100 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PrescriptionsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let PrescriptionsService = class PrescriptionsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createPrescriptionDto) {
        const { nrNumber, doctorId, items, ...prescriptionData } = createPrescriptionDto;
        const patient = await this.prisma.patient.findFirst({
            where: { nrNumber },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with NR Number ${nrNumber} not found`);
        }
        let doctor = null;
        if (doctorId) {
            doctor = await this.prisma.user.findFirst({
                where: {
                    id: doctorId,
                    role: 'DOCTOR',
                },
            });
            if (!doctor) {
                throw new common_1.NotFoundException(`Doctor with ID ${doctorId} not found`);
            }
        }
        const medicineIds = items.map((item) => item.medicineId);
        const medicines = await this.prisma.medicine.findMany({
            where: { id: { in: medicineIds } },
        });
        if (medicines.length !== medicineIds.length) {
            throw new common_1.BadRequestException('One or more medicines not found');
        }
        const prescription = await this.prisma.prescription.create({
            data: {
                hospitalId: patient.hospitalId,
                nrNumber: patient.nrNumber,
                doctorId: doctor?.id || null,
                prescriptionType: prescriptionData.prescriptionType,
                scannedImageUrl: prescriptionData.scannedImageUrl,
                notes: prescriptionData.notes,
                status: client_1.PrescriptionStatus.PENDING,
                items: {
                    create: items.map((item) => ({
                        medicineId: item.medicineId,
                        qtyPrescribed: item.qtyPrescribed,
                        dosage: item.dosage,
                        frequency: item.frequency,
                        duration: item.duration,
                        status: 'PENDING',
                    })),
                },
            },
            include: {
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                        gender: true,
                        mobile: true,
                    },
                },
                doctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                strength: true,
                                form: true,
                            },
                        },
                    },
                },
            },
        });
        return prescription;
    }
    async findAll(searchDto) {
        const { nrNumber, doctorId, status, limit = 50, page = 1 } = searchDto;
        const where = {};
        if (nrNumber) {
            where.nrNumber = {
                contains: nrNumber,
                mode: 'insensitive',
            };
        }
        if (doctorId) {
            where.doctorId = doctorId;
        }
        if (status) {
            where.status = status;
        }
        const [prescriptions, total] = await Promise.all([
            this.prisma.prescription.findMany({
                where,
                include: {
                    patient: {
                        select: {
                            id: true,
                            nrNumber: true,
                            fullName: true,
                            gender: true,
                            mobile: true,
                        },
                    },
                    doctor: {
                        select: {
                            id: true,
                            fullName: true,
                            email: true,
                        },
                    },
                    items: {
                        include: {
                            medicine: {
                                select: {
                                    id: true,
                                    name: true,
                                    genericName: true,
                                    strength: true,
                                    form: true,
                                },
                            },
                        },
                    },
                },
                orderBy: { createdAt: 'desc' },
                take: limit,
                skip: (page - 1) * limit,
            }),
            this.prisma.prescription.count({ where }),
        ]);
        return {
            data: prescriptions,
            total,
            page,
            limit,
            totalPages: Math.ceil(total / limit),
        };
    }
    async findOne(id) {
        const prescription = await this.prisma.prescription.findUnique({
            where: { id },
            include: {
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                        gender: true,
                        dob: true,
                        mobile: true,
                        cnic: true,
                        address: true,
                        visitType: true,
                        department: true,
                        ward: true,
                        bed: true,
                        attendingDoctorId: true,
                    },
                },
                doctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                strength: true,
                                form: true,
                            },
                        },
                    },
                },
            },
        });
        if (!prescription) {
            throw new common_1.NotFoundException(`Prescription with ID ${id} not found`);
        }
        return prescription;
    }
    async updateStatus(id, updateStatusDto, user) {
        const prescription = await this.prisma.prescription.findUnique({
            where: { id },
            include: {
                items: true,
            },
        });
        if (!prescription) {
            throw new common_1.NotFoundException(`Prescription with ID ${id} not found`);
        }
        const { status } = updateStatusDto;
        const currentStatus = prescription.status;
        const allowedTransitions = {
            PENDING: [client_1.PrescriptionStatus.ISSUED, client_1.PrescriptionStatus.CANCELLED],
            ISSUED: [],
            PARTIALLY_ISSUED: [client_1.PrescriptionStatus.ISSUED, client_1.PrescriptionStatus.CANCELLED],
            CANCELLED: [],
        };
        const statusEnum = status;
        if (!allowedTransitions[currentStatus]?.includes(statusEnum)) {
            throw new common_1.BadRequestException(`Cannot transition from ${currentStatus} to ${status}`);
        }
        if (statusEnum === client_1.PrescriptionStatus.ISSUED) {
            const now = new Date();
            const hospitalId = prescription.hospitalId || user?.hospitalId;
            let pharmacyId = user?.pharmacyId;
            if (!pharmacyId && hospitalId) {
                const mainPharmacy = await this.prisma.pharmacy.findFirst({
                    where: { hospitalId, type: 'MAIN', status: 'ACTIVE' },
                    select: { id: true },
                });
                pharmacyId = mainPharmacy?.id;
            }
            if (!hospitalId || !pharmacyId) {
                throw new common_1.BadRequestException('Pharmacy context is required to issue a prescription');
            }
            const patient = await this.prisma.patient.findFirst({
                where: { nrNumber: prescription.nrNumber },
            });
            if (!patient) {
                throw new common_1.NotFoundException('Patient not found');
            }
            const updated = await this.prisma.$transaction(async (tx) => {
                let totalAmount = new client_1.Prisma.Decimal(0);
                for (const item of prescription.items) {
                    const batch = await tx.stockBatch.findFirst({
                        where: {
                            hospitalId,
                            pharmacyId,
                            medicineId: item.medicineId,
                            status: 'AVAILABLE',
                            qtyAvailable: { gt: 0 },
                            expiryDate: { gte: now },
                        },
                        orderBy: [{ expiryDate: 'asc' }, { receivedDate: 'asc' }],
                    });
                    if (!batch) {
                        throw new common_1.BadRequestException(`No available stock for medicine ${item.medicineId}`);
                    }
                    if (batch.qtyAvailable < item.qtyPrescribed) {
                        throw new common_1.BadRequestException(`Insufficient stock for medicine ${item.medicineId}. ` +
                            `Requested: ${item.qtyPrescribed}, Available: ${batch.qtyAvailable}`);
                    }
                    const newQtyAvailable = batch.qtyAvailable - item.qtyPrescribed;
                    await tx.stockBatch.update({
                        where: { id: batch.id },
                        data: {
                            qtyAvailable: newQtyAvailable,
                            status: newQtyAvailable === 0 ? 'DEPLETED' : batch.status,
                        },
                    });
                    await tx.prescriptionItem.update({
                        where: { id: item.id },
                        data: { status: 'ISSUED' },
                    });
                    const itemCost = batch.retailPrice.mul(item.qtyPrescribed);
                    totalAmount = totalAmount.add(itemCost);
                }
                const today = new Date();
                const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
                const lastReceipt = await tx.receipt.findFirst({
                    where: { receiptNumber: { startsWith: `REC-${dateStr}` } },
                    orderBy: { receiptNumber: 'desc' },
                });
                const sequence = lastReceipt
                    ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
                    : 1;
                const receiptNumber = `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
                const latestVisit = await tx.visit.findFirst({
                    where: { patientId: patient.id, hospitalId },
                    orderBy: { visitDate: 'desc' },
                    select: { id: true, departmentId: true },
                });
                const doctorDepartmentId = prescription.doctorId
                    ? (await tx.user.findUnique({
                        where: { id: prescription.doctorId },
                        select: { departmentId: true },
                    }))?.departmentId
                    : null;
                const fallbackDepartment = await tx.department.findFirst({
                    where: { hospitalId },
                    select: { id: true },
                });
                const departmentId = latestVisit?.departmentId || doctorDepartmentId || fallbackDepartment?.id;
                if (!departmentId) {
                    throw new common_1.BadRequestException('Department is required to generate pharmacy receipt');
                }
                const txAny = tx;
                await txAny.receipt.create({
                    data: {
                        hospitalId,
                        patientId: patient.id,
                        visitId: latestVisit?.id || undefined,
                        departmentId,
                        generatedById: user?.id || prescription.doctorId || patient.attendingDoctorId,
                        receiptNumber,
                        receiptType: client_1.ReceiptType.PHARMACY,
                        description: `Prescription Issued - ${id.slice(0, 8)}`,
                        amount: totalAmount,
                        totalAmount: totalAmount,
                        paidAmount: new client_1.Prisma.Decimal(0),
                        paymentMethod: client_1.PaymentMethod.CASH,
                        paymentStatus: client_1.PaymentStatus.UNPAID,
                        notes: JSON.stringify({ prescriptionId: id }),
                    },
                });
                return tx.prescription.update({
                    where: { id },
                    data: { status: statusEnum },
                    include: {
                        patient: {
                            select: {
                                id: true,
                                nrNumber: true,
                                fullName: true,
                            },
                        },
                        doctor: {
                            select: {
                                id: true,
                                fullName: true,
                                email: true,
                            },
                        },
                        items: {
                            include: {
                                medicine: {
                                    select: {
                                        id: true,
                                        name: true,
                                        genericName: true,
                                        strength: true,
                                        form: true,
                                    },
                                },
                            },
                        },
                    },
                });
            });
            return updated;
        }
        const updated = await this.prisma.prescription.update({
            where: { id },
            data: { status: statusEnum },
            include: {
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                    },
                },
                doctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                strength: true,
                                form: true,
                            },
                        },
                    },
                },
            },
        });
        return updated;
    }
    async getByPatient(nrNumber) {
        const patient = await this.prisma.patient.findFirst({
            where: { nrNumber },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with NR Number ${nrNumber} not found`);
        }
        const prescriptions = await this.prisma.prescription.findMany({
            where: { nrNumber: patient.nrNumber },
            include: {
                doctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                strength: true,
                                form: true,
                            },
                        },
                    },
                },
            },
            orderBy: { createdAt: 'desc' },
        });
        return prescriptions;
    }
    async getActivePrescriptions(nrNumber) {
        const patient = await this.prisma.patient.findFirst({
            where: { nrNumber },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with NR Number ${nrNumber} not found`);
        }
        const prescriptions = await this.prisma.prescription.findMany({
            where: {
                nrNumber: patient.nrNumber,
                status: {
                    in: [client_1.PrescriptionStatus.PENDING, client_1.PrescriptionStatus.PARTIALLY_ISSUED],
                },
            },
            include: {
                doctor: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                strength: true,
                                form: true,
                            },
                        },
                    },
                },
            },
            orderBy: { createdAt: 'desc' },
        });
        return prescriptions;
    }
};
exports.PrescriptionsService = PrescriptionsService;
exports.PrescriptionsService = PrescriptionsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], PrescriptionsService);


/***/ }),
/* 101 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PrescriptionsController = void 0;
const common_1 = __webpack_require__(2);
const prescriptions_service_1 = __webpack_require__(100);
const create_prescription_dto_1 = __webpack_require__(102);
const search_prescriptions_dto_1 = __webpack_require__(103);
const update_prescription_status_dto_1 = __webpack_require__(104);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let PrescriptionsController = class PrescriptionsController {
    constructor(prescriptionsService) {
        this.prescriptionsService = prescriptionsService;
    }
    create(createPrescriptionDto) {
        return this.prescriptionsService.create(createPrescriptionDto);
    }
    findAll(searchDto) {
        return this.prescriptionsService.findAll(searchDto);
    }
    getByPatient(nrNumber) {
        return this.prescriptionsService.getByPatient(nrNumber);
    }
    getActivePrescriptions(nrNumber) {
        return this.prescriptionsService.getActivePrescriptions(nrNumber);
    }
    findOne(id) {
        return this.prescriptionsService.findOne(id);
    }
    updateStatus(id, updateStatusDto, req) {
        return this.prescriptionsService.updateStatus(id, updateStatusDto, req.user);
    }
};
exports.PrescriptionsController = PrescriptionsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_prescription_dto_1.CreatePrescriptionDto !== "undefined" && create_prescription_dto_1.CreatePrescriptionDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof search_prescriptions_dto_1.SearchPrescriptionsDto !== "undefined" && search_prescriptions_dto_1.SearchPrescriptionsDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('patient/:nrNumber'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('nrNumber')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "getByPatient", null);
__decorate([
    (0, common_1.Get)('patient/:nrNumber/active'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('nrNumber')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "getActivePrescriptions", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id/status'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.DOCTOR_ASSISTANT, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_prescription_status_dto_1.UpdatePrescriptionStatusDto !== "undefined" && update_prescription_status_dto_1.UpdatePrescriptionStatusDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", void 0)
], PrescriptionsController.prototype, "updateStatus", null);
exports.PrescriptionsController = PrescriptionsController = __decorate([
    (0, common_1.Controller)('prescriptions'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof prescriptions_service_1.PrescriptionsService !== "undefined" && prescriptions_service_1.PrescriptionsService) === "function" ? _a : Object])
], PrescriptionsController);


/***/ }),
/* 102 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreatePrescriptionDto = exports.PrescriptionItemDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class PrescriptionItemDto {
}
exports.PrescriptionItemDto = PrescriptionItemDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], PrescriptionItemDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], PrescriptionItemDto.prototype, "qtyPrescribed", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], PrescriptionItemDto.prototype, "dosage", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], PrescriptionItemDto.prototype, "frequency", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], PrescriptionItemDto.prototype, "duration", void 0);
class CreatePrescriptionDto {
}
exports.CreatePrescriptionDto = CreatePrescriptionDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreatePrescriptionDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreatePrescriptionDto.prototype, "doctorId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['E_PRESCRIPTION', 'SCANNED', 'WRITTEN']),
    __metadata("design:type", String)
], CreatePrescriptionDto.prototype, "prescriptionType", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreatePrescriptionDto.prototype, "scannedImageUrl", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreatePrescriptionDto.prototype, "notes", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => PrescriptionItemDto),
    __metadata("design:type", Array)
], CreatePrescriptionDto.prototype, "items", void 0);


/***/ }),
/* 103 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchPrescriptionsDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class SearchPrescriptionsDto {
    constructor() {
        this.limit = 50;
        this.page = 1;
    }
}
exports.SearchPrescriptionsDto = SearchPrescriptionsDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], SearchPrescriptionsDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], SearchPrescriptionsDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], SearchPrescriptionsDto.prototype, "doctorId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(['PENDING', 'ISSUED', 'PARTIALLY_ISSUED', 'CANCELLED']),
    __metadata("design:type", String)
], SearchPrescriptionsDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], SearchPrescriptionsDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], SearchPrescriptionsDto.prototype, "page", void 0);


/***/ }),
/* 104 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdatePrescriptionStatusDto = void 0;
const class_validator_1 = __webpack_require__(35);
class UpdatePrescriptionStatusDto {
}
exports.UpdatePrescriptionStatusDto = UpdatePrescriptionStatusDto;
__decorate([
    (0, class_validator_1.IsEnum)(['PENDING', 'ISSUED', 'PARTIALLY_ISSUED', 'CANCELLED']),
    __metadata("design:type", String)
], UpdatePrescriptionStatusDto.prototype, "status", void 0);


/***/ }),
/* 105 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.IssuanceModule = void 0;
const common_1 = __webpack_require__(2);
const issuance_service_1 = __webpack_require__(106);
const issuance_controller_1 = __webpack_require__(108);
const database_module_1 = __webpack_require__(52);
const inventory_module_1 = __webpack_require__(91);
let IssuanceModule = class IssuanceModule {
};
exports.IssuanceModule = IssuanceModule;
exports.IssuanceModule = IssuanceModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule, inventory_module_1.InventoryModule],
        controllers: [issuance_controller_1.IssuanceController],
        providers: [issuance_service_1.IssuanceService],
        exports: [issuance_service_1.IssuanceService],
    })
], IssuanceModule);


/***/ }),
/* 106 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.IssuanceService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const inventory_service_1 = __webpack_require__(92);
const library_1 = __webpack_require__(107);
const client_1 = __webpack_require__(16);
let IssuanceService = class IssuanceService {
    constructor(prisma, inventoryService) {
        this.prisma = prisma;
        this.inventoryService = inventoryService;
    }
    async create(createIssuanceDto, hospitalId, userId) {
        const { nrNumber, pharmacyId, prescriptionId, priceType, items } = createIssuanceDto;
        const patient = await this.prisma.patient.findFirst({
            where: { nrNumber, hospitalId },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with NR Number ${nrNumber} not found`);
        }
        const pharmacy = await this.prisma.pharmacy.findFirst({
            where: { id: pharmacyId, hospitalId },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        if (prescriptionId) {
            const prescription = await this.prisma.prescription.findFirst({
                where: { id: prescriptionId, nrNumber, hospitalId },
            });
            if (!prescription) {
                throw new common_1.NotFoundException('Prescription not found');
            }
        }
        const itemAllocations = await Promise.all(items.map(async (item) => {
            const availableBatches = await this.inventoryService.getAvailableBatchesFIFO(item.medicineId, pharmacyId, hospitalId);
            if (availableBatches.length === 0) {
                throw new common_1.BadRequestException(`Medicine ${item.medicineId} has no available stock in this pharmacy`);
            }
            const totalAvailable = availableBatches.reduce((sum, batch) => sum + batch.qtyAvailable, 0);
            if (totalAvailable < item.quantity) {
                throw new common_1.BadRequestException(`Insufficient stock for medicine ${availableBatches[0].medicine.name}. ` +
                    `Requested: ${item.quantity}, Available: ${totalAvailable}`);
            }
            let remainingQty = item.quantity;
            const batchAllocations = [];
            for (const batch of availableBatches) {
                if (remainingQty <= 0)
                    break;
                const qtyFromBatch = Math.min(batch.qtyAvailable, remainingQty);
                let unitPrice;
                if (priceType === 'GOVERNMENT') {
                    unitPrice = batch.governmentPrice;
                }
                else if (priceType === 'RETAIL') {
                    unitPrice = batch.retailPrice;
                }
                else {
                    unitPrice = new library_1.Decimal(item.customPrice || 0);
                }
                batchAllocations.push({
                    batchId: batch.id,
                    medicineId: item.medicineId,
                    qtyIssued: qtyFromBatch,
                    unitPrice,
                    totalPrice: unitPrice.mul(qtyFromBatch),
                });
                remainingQty -= qtyFromBatch;
            }
            return batchAllocations;
        }));
        const allAllocations = itemAllocations.flat();
        const totalAmount = allAllocations.reduce((sum, alloc) => sum.add(alloc.totalPrice), new library_1.Decimal(0));
        const issueTransaction = await this.prisma.$transaction(async (tx) => {
            const issue = await tx.issueTransaction.create({
                data: {
                    hospitalId,
                    pharmacyId,
                    nrNumber,
                    prescriptionId,
                    issuedBy: userId,
                    totalAmount,
                    priceType,
                    status: 'COMPLETED',
                },
            });
            for (const allocation of allAllocations) {
                await tx.issueItem.create({
                    data: {
                        issueId: issue.id,
                        batchId: allocation.batchId,
                        medicineId: allocation.medicineId,
                        qtyIssued: allocation.qtyIssued,
                        unitPrice: allocation.unitPrice,
                        totalPrice: allocation.totalPrice,
                    },
                });
                const batch = await tx.stockBatch.findUnique({
                    where: { id: allocation.batchId },
                });
                const newQtyAvailable = batch.qtyAvailable - allocation.qtyIssued;
                await tx.stockBatch.update({
                    where: { id: allocation.batchId },
                    data: {
                        qtyAvailable: newQtyAvailable,
                        status: newQtyAvailable === 0 ? 'DEPLETED' : batch.status,
                    },
                });
            }
            const latestVisit = await tx.visit.findFirst({
                where: { patientId: patient.id, hospitalId },
                orderBy: { visitDate: 'desc' },
                select: { id: true, departmentId: true },
            });
            const doctorDepartmentId = prescriptionId
                ? (await tx.prescription.findUnique({
                    where: { id: prescriptionId },
                    select: { doctor: { select: { departmentId: true } } },
                }))?.doctor?.departmentId
                : null;
            const fallbackDepartment = await tx.department.findFirst({
                where: { hospitalId },
                select: { id: true },
            });
            const departmentId = latestVisit?.departmentId || doctorDepartmentId || fallbackDepartment?.id;
            if (!departmentId) {
                throw new common_1.BadRequestException('Department is required to generate pharmacy receipt');
            }
            const receiptNumber = await this.generateReceiptNumber(tx);
            const txAny = tx;
            await txAny.receipt.create({
                data: {
                    hospitalId,
                    patientId: patient.id,
                    visitId: latestVisit?.id || undefined,
                    departmentId,
                    generatedById: userId,
                    receiptNumber,
                    receiptType: client_1.ReceiptType.PHARMACY,
                    description: `Pharmacy Issue - ${issue.id.slice(0, 8)}`,
                    amount: totalAmount,
                    totalAmount: totalAmount,
                    paidAmount: new client_1.Prisma.Decimal(0),
                    paymentMethod: client_1.PaymentMethod.CASH,
                    paymentStatus: client_1.PaymentStatus.UNPAID,
                    notes: JSON.stringify({ issueTransactionId: issue.id, prescriptionId }),
                },
            });
            return issue;
        });
        return this.findOne(issueTransaction.id, hospitalId);
    }
    async generateReceiptNumber(tx) {
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await tx.receipt.findFirst({
            where: { receiptNumber: { startsWith: `REC-${dateStr}` } },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        return `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
    }
    async findAll(searchDto, hospitalId) {
        const { nrNumber, pharmacyId, prescriptionId, status, issuedAfter, issuedBefore, limit = 50, page = 1, sortBy = 'issuedAt', sortOrder = 'desc', } = searchDto;
        const where = { hospitalId };
        if (nrNumber)
            where.nrNumber = nrNumber;
        if (pharmacyId)
            where.pharmacyId = pharmacyId;
        if (prescriptionId)
            where.prescriptionId = prescriptionId;
        if (status)
            where.status = status;
        if (issuedAfter || issuedBefore) {
            where.issuedAt = {};
            if (issuedAfter)
                where.issuedAt.gte = new Date(issuedAfter);
            if (issuedBefore)
                where.issuedAt.lte = new Date(issuedBefore);
        }
        const skip = (page - 1) * limit;
        const [issues, total] = await Promise.all([
            this.prisma.issueTransaction.findMany({
                where,
                skip,
                take: limit,
                orderBy: { [sortBy]: sortOrder },
                include: {
                    patient: {
                        select: {
                            nrNumber: true,
                            fullName: true,
                            mobile: true,
                        },
                    },
                    pharmacy: {
                        select: {
                            id: true,
                            name: true,
                            type: true,
                        },
                    },
                    issuer: {
                        select: {
                            id: true,
                            fullName: true,
                        },
                    },
                    items: {
                        include: {
                            medicine: {
                                select: {
                                    id: true,
                                    name: true,
                                    genericName: true,
                                    form: true,
                                    strength: true,
                                },
                            },
                            batch: {
                                select: {
                                    batchNo: true,
                                    expiryDate: true,
                                },
                            },
                        },
                    },
                },
            }),
            this.prisma.issueTransaction.count({ where }),
        ]);
        return {
            data: issues,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id, hospitalId) {
        const issue = await this.prisma.issueTransaction.findFirst({
            where: { id, hospitalId },
            include: {
                patient: {
                    select: {
                        nrNumber: true,
                        fullName: true,
                        mobile: true,
                        cnic: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        type: true,
                    },
                },
                prescription: {
                    select: {
                        id: true,
                        prescriptionType: true,
                    },
                },
                issuer: {
                    select: {
                        id: true,
                        fullName: true,
                        role: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                form: true,
                                strength: true,
                            },
                        },
                        batch: {
                            select: {
                                batchNo: true,
                                expiryDate: true,
                                manufacturer: true,
                            },
                        },
                    },
                },
            },
        });
        if (!issue) {
            throw new common_1.NotFoundException('Issue transaction not found');
        }
        return issue;
    }
    async getStats(hospitalId, pharmacyId) {
        const where = { hospitalId };
        if (pharmacyId)
            where.pharmacyId = pharmacyId;
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const [totalIssues, todayIssues, totalAmount, todayAmount] = await Promise.all([
            this.prisma.issueTransaction.count({ where }),
            this.prisma.issueTransaction.count({
                where: { ...where, issuedAt: { gte: today } },
            }),
            this.prisma.issueTransaction.aggregate({
                where,
                _sum: { totalAmount: true },
            }),
            this.prisma.issueTransaction.aggregate({
                where: { ...where, issuedAt: { gte: today } },
                _sum: { totalAmount: true },
            }),
        ]);
        return {
            totalIssues,
            todayIssues,
            totalAmount: totalAmount._sum.totalAmount || 0,
            todayAmount: todayAmount._sum.totalAmount || 0,
        };
    }
};
exports.IssuanceService = IssuanceService;
exports.IssuanceService = IssuanceService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof inventory_service_1.InventoryService !== "undefined" && inventory_service_1.InventoryService) === "function" ? _b : Object])
], IssuanceService);


/***/ }),
/* 107 */
/***/ ((module) => {

module.exports = require("@prisma/client/runtime/library");

/***/ }),
/* 108 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.IssuanceController = void 0;
const common_1 = __webpack_require__(2);
const issuance_service_1 = __webpack_require__(106);
const create_issuance_dto_1 = __webpack_require__(109);
const search_issuance_dto_1 = __webpack_require__(110);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let IssuanceController = class IssuanceController {
    constructor(issuanceService) {
        this.issuanceService = issuanceService;
    }
    create(createIssuanceDto, req) {
        return this.issuanceService.create(createIssuanceDto, req.user.hospitalId, req.user.userId);
    }
    findAll(searchDto, req) {
        return this.issuanceService.findAll(searchDto, req.user.hospitalId);
    }
    getStats(pharmacyId, req) {
        return this.issuanceService.getStats(req.user.hospitalId, pharmacyId);
    }
    findOne(id, req) {
        return this.issuanceService.findOne(id, req.user.hospitalId);
    }
};
exports.IssuanceController = IssuanceController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_issuance_dto_1.CreateIssuanceDto !== "undefined" && create_issuance_dto_1.CreateIssuanceDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], IssuanceController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof search_issuance_dto_1.SearchIssuanceDto !== "undefined" && search_issuance_dto_1.SearchIssuanceDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", void 0)
], IssuanceController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)('pharmacyId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], IssuanceController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], IssuanceController.prototype, "findOne", null);
exports.IssuanceController = IssuanceController = __decorate([
    (0, common_1.Controller)('issuance'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof issuance_service_1.IssuanceService !== "undefined" && issuance_service_1.IssuanceService) === "function" ? _a : Object])
], IssuanceController);


/***/ }),
/* 109 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateIssuanceDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class IssueItemDto {
}
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Medicine ID is required' }),
    __metadata("design:type", String)
], IssueItemDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1, { message: 'Quantity must be at least 1' }),
    __metadata("design:type", Number)
], IssueItemDto.prototype, "quantity", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], IssueItemDto.prototype, "customPrice", void 0);
class CreateIssuanceDto {
}
exports.CreateIssuanceDto = CreateIssuanceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Patient NR Number is required' }),
    __metadata("design:type", String)
], CreateIssuanceDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Pharmacy ID is required' }),
    __metadata("design:type", String)
], CreateIssuanceDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateIssuanceDto.prototype, "prescriptionId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PriceType, { message: 'Invalid price type' }),
    __metadata("design:type", typeof (_a = typeof client_1.PriceType !== "undefined" && client_1.PriceType) === "function" ? _a : Object)
], CreateIssuanceDto.prototype, "priceType", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => IssueItemDto),
    __metadata("design:type", Array)
], CreateIssuanceDto.prototype, "items", void 0);


/***/ }),
/* 110 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchIssuanceDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class SearchIssuanceDto {
    constructor() {
        this.limit = 50;
        this.page = 1;
        this.sortBy = 'issuedAt';
        this.sortOrder = 'desc';
    }
}
exports.SearchIssuanceDto = SearchIssuanceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "nrNumber", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "prescriptionId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.IssueTransactionStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.IssueTransactionStatus !== "undefined" && client_1.IssueTransactionStatus) === "function" ? _a : Object)
], SearchIssuanceDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "issuedAfter", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "issuedBefore", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(200),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchIssuanceDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchIssuanceDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "sortBy", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['asc', 'desc']),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchIssuanceDto.prototype, "sortOrder", void 0);


/***/ }),
/* 111 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PharmaciesModule = void 0;
const common_1 = __webpack_require__(2);
const pharmacies_service_1 = __webpack_require__(112);
const pharmacies_controller_1 = __webpack_require__(113);
const prisma_service_1 = __webpack_require__(15);
let PharmaciesModule = class PharmaciesModule {
};
exports.PharmaciesModule = PharmaciesModule;
exports.PharmaciesModule = PharmaciesModule = __decorate([
    (0, common_1.Module)({
        controllers: [pharmacies_controller_1.PharmaciesController],
        providers: [pharmacies_service_1.PharmaciesService, prisma_service_1.PrismaService],
        exports: [pharmacies_service_1.PharmaciesService],
    })
], PharmaciesModule);


/***/ }),
/* 112 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PharmaciesService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let PharmaciesService = class PharmaciesService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createPharmacyDto, createdByUserId, userHospitalId) {
        const hospital = await this.prisma.hospital.findUnique({
            where: { id: createPharmacyDto.hospitalId },
        });
        if (!hospital) {
            throw new common_1.NotFoundException('Hospital not found');
        }
        const existingPharmacy = await this.prisma.pharmacy.findUnique({
            where: {
                hospitalId_code: {
                    hospitalId: createPharmacyDto.hospitalId,
                    code: createPharmacyDto.code,
                },
            },
        });
        if (existingPharmacy) {
            throw new common_1.ConflictException(`Pharmacy with code '${createPharmacyDto.code}' already exists in this hospital`);
        }
        const pharmacy = await this.prisma.pharmacy.create({
            data: {
                name: createPharmacyDto.name,
                code: createPharmacyDto.code,
                type: createPharmacyDto.type,
                locationWard: createPharmacyDto.locationWard,
                hospitalId: createPharmacyDto.hospitalId,
                status: client_1.PharmacyStatus.ACTIVE,
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        address: true,
                    },
                },
            },
        });
        await this.prisma.auditLog.create({
            data: {
                hospitalId: userHospitalId,
                userId: createdByUserId,
                action: 'CREATE',
                entityType: 'Pharmacy',
                entityId: pharmacy.id,
                afterState: pharmacy,
            },
        });
        return pharmacy;
    }
    async findAll(hospitalId) {
        const where = {};
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        return this.prisma.pharmacy.findMany({
            where,
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        address: true,
                    },
                },
                users: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        role: true,
                    },
                    where: {
                        role: {
                            in: ['MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER'],
                        },
                    },
                    take: 1,
                },
                _count: {
                    select: {
                        stockBatches: true,
                        users: true,
                    },
                },
            },
            orderBy: {
                createdAt: 'desc',
            },
        });
    }
    async findOne(id, userHospitalId) {
        const pharmacy = await this.prisma.pharmacy.findUnique({
            where: { id },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        address: true,
                    },
                },
                users: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        role: true,
                        phone: true,
                    },
                },
                _count: {
                    select: {
                        stockBatches: true,
                        purchaseOrders: true,
                        transfersFrom: true,
                        transfersTo: true,
                    },
                },
            },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        if (userHospitalId && pharmacy.hospitalId !== userHospitalId) {
            throw new common_1.ForbiddenException('Access denied to this pharmacy');
        }
        return pharmacy;
    }
    async update(id, updatePharmacyDto, updatedByUserId, userHospitalId) {
        const pharmacy = await this.findOne(id, userHospitalId);
        if (updatePharmacyDto.code && updatePharmacyDto.code !== pharmacy.code) {
            const existingPharmacy = await this.prisma.pharmacy.findUnique({
                where: {
                    hospitalId_code: {
                        hospitalId: pharmacy.hospitalId,
                        code: updatePharmacyDto.code,
                    },
                },
            });
            if (existingPharmacy) {
                throw new common_1.ConflictException(`Pharmacy with code '${updatePharmacyDto.code}' already exists in this hospital`);
            }
        }
        const updatedPharmacy = await this.prisma.pharmacy.update({
            where: { id },
            data: updatePharmacyDto,
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        address: true,
                    },
                },
            },
        });
        await this.prisma.auditLog.create({
            data: {
                hospitalId: userHospitalId,
                userId: updatedByUserId,
                action: 'UPDATE',
                entityType: 'Pharmacy',
                entityId: id,
                beforeState: pharmacy,
                afterState: updatedPharmacy,
            },
        });
        return updatedPharmacy;
    }
    async remove(id, deletedByUserId, userHospitalId) {
        const pharmacy = await this.findOne(id, userHospitalId);
        const updatedPharmacy = await this.prisma.pharmacy.update({
            where: { id },
            data: { status: client_1.PharmacyStatus.INACTIVE },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
        await this.prisma.auditLog.create({
            data: {
                hospitalId: userHospitalId,
                userId: deletedByUserId,
                action: 'DELETE',
                entityType: 'Pharmacy',
                entityId: id,
                beforeState: pharmacy,
                afterState: updatedPharmacy,
            },
        });
        return updatedPharmacy;
    }
    async getStats(hospitalId) {
        const [total, main, sub, active, inactive] = await Promise.all([
            this.prisma.pharmacy.count({
                where: { hospitalId },
            }),
            this.prisma.pharmacy.count({
                where: { hospitalId, type: 'MAIN' },
            }),
            this.prisma.pharmacy.count({
                where: { hospitalId, type: 'SUB' },
            }),
            this.prisma.pharmacy.count({
                where: { hospitalId, status: client_1.PharmacyStatus.ACTIVE },
            }),
            this.prisma.pharmacy.count({
                where: { hospitalId, status: client_1.PharmacyStatus.INACTIVE },
            }),
        ]);
        return {
            total,
            main,
            sub,
            active,
            inactive,
        };
    }
};
exports.PharmaciesService = PharmaciesService;
exports.PharmaciesService = PharmaciesService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], PharmaciesService);


/***/ }),
/* 113 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PharmaciesController = void 0;
const common_1 = __webpack_require__(2);
const pharmacies_service_1 = __webpack_require__(112);
const dto_1 = __webpack_require__(114);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let PharmaciesController = class PharmaciesController {
    constructor(pharmaciesService) {
        this.pharmaciesService = pharmaciesService;
    }
    create(createPharmacyDto, req) {
        if (req.user.role === client_1.UserRole.HOSPITAL_ADMIN) {
            if (createPharmacyDto.hospitalId !== req.user.hospitalId) {
                throw new common_1.ForbiddenException('You can only create pharmacies in your own hospital');
            }
        }
        const auditHospitalId = req.user.hospitalId || createPharmacyDto.hospitalId;
        return this.pharmaciesService.create(createPharmacyDto, req.user.id, auditHospitalId);
    }
    findAll(hospitalId, req) {
        if (req.user.role !== client_1.UserRole.SUPER_ADMIN) {
            hospitalId = req.user.hospitalId;
        }
        return this.pharmaciesService.findAll(hospitalId);
    }
    getStats(hospitalId, req) {
        if (req.user.role === client_1.UserRole.HOSPITAL_ADMIN) {
            if (hospitalId !== req.user.hospitalId) {
                throw new common_1.ForbiddenException('You can only view stats for your own hospital');
            }
        }
        return this.pharmaciesService.getStats(hospitalId);
    }
    findOne(id, req) {
        const userHospitalId = req.user.role === client_1.UserRole.SUPER_ADMIN ? undefined : req.user.hospitalId;
        return this.pharmaciesService.findOne(id, userHospitalId);
    }
    async update(id, updatePharmacyDto, req) {
        const userHospitalId = req.user.role === client_1.UserRole.SUPER_ADMIN
            ? undefined
            : req.user.hospitalId;
        const pharmacy = await this.pharmaciesService.findOne(id, userHospitalId);
        const auditHospitalId = req.user.hospitalId || pharmacy.hospitalId;
        return this.pharmaciesService.update(id, updatePharmacyDto, req.user.id, auditHospitalId);
    }
    async remove(id, req) {
        const userHospitalId = req.user.role === client_1.UserRole.SUPER_ADMIN
            ? undefined
            : req.user.hospitalId;
        const pharmacy = await this.pharmaciesService.findOne(id, userHospitalId);
        const auditHospitalId = req.user.hospitalId || pharmacy.hospitalId;
        return this.pharmaciesService.remove(id, req.user.id, auditHospitalId);
    }
};
exports.PharmaciesController = PharmaciesController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreatePharmacyDto !== "undefined" && dto_1.CreatePharmacyDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], PharmaciesController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF, client_1.UserRole.AUDITOR),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], PharmaciesController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('stats/:hospitalId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], PharmaciesController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], PharmaciesController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof dto_1.UpdatePharmacyDto !== "undefined" && dto_1.UpdatePharmacyDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], PharmaciesController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], PharmaciesController.prototype, "remove", null);
exports.PharmaciesController = PharmaciesController = __decorate([
    (0, common_1.Controller)('pharmacies'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof pharmacies_service_1.PharmaciesService !== "undefined" && pharmacies_service_1.PharmaciesService) === "function" ? _a : Object])
], PharmaciesController);


/***/ }),
/* 114 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
__exportStar(__webpack_require__(115), exports);
__exportStar(__webpack_require__(116), exports);


/***/ }),
/* 115 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreatePharmacyDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreatePharmacyDto {
}
exports.CreatePharmacyDto = CreatePharmacyDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreatePharmacyDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreatePharmacyDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PharmacyType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.PharmacyType !== "undefined" && client_1.PharmacyType) === "function" ? _a : Object)
], CreatePharmacyDto.prototype, "type", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreatePharmacyDto.prototype, "locationWard", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreatePharmacyDto.prototype, "hospitalId", void 0);


/***/ }),
/* 116 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdatePharmacyDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdatePharmacyDto {
}
exports.UpdatePharmacyDto = UpdatePharmacyDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePharmacyDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePharmacyDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PharmacyType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.PharmacyType !== "undefined" && client_1.PharmacyType) === "function" ? _a : Object)
], UpdatePharmacyDto.prototype, "type", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdatePharmacyDto.prototype, "locationWard", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PharmacyStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.PharmacyStatus !== "undefined" && client_1.PharmacyStatus) === "function" ? _b : Object)
], UpdatePharmacyDto.prototype, "status", void 0);


/***/ }),
/* 117 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UsersModule = void 0;
const common_1 = __webpack_require__(2);
const users_controller_1 = __webpack_require__(118);
const users_service_1 = __webpack_require__(119);
const prisma_service_1 = __webpack_require__(15);
let UsersModule = class UsersModule {
};
exports.UsersModule = UsersModule;
exports.UsersModule = UsersModule = __decorate([
    (0, common_1.Module)({
        controllers: [users_controller_1.UsersController],
        providers: [users_service_1.UsersService, prisma_service_1.PrismaService],
        exports: [users_service_1.UsersService],
    })
], UsersModule);


/***/ }),
/* 118 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UsersController = void 0;
const common_1 = __webpack_require__(2);
const users_service_1 = __webpack_require__(119);
const dto_1 = __webpack_require__(120);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
let UsersController = class UsersController {
    constructor(usersService) {
        this.usersService = usersService;
    }
    async findOne(id, req) {
        return this.usersService.findOne(id, req.user.id);
    }
    async update(id, updateUserDto, req) {
        return this.usersService.update(id, updateUserDto, req.user.id);
    }
    async remove(id, req) {
        return this.usersService.remove(id, req.user.id);
    }
};
exports.UsersController = UsersController;
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], UsersController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof dto_1.UpdateUserDto !== "undefined" && dto_1.UpdateUserDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], UsersController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], UsersController.prototype, "remove", null);
exports.UsersController = UsersController = __decorate([
    (0, common_1.Controller)('users'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof users_service_1.UsersService !== "undefined" && users_service_1.UsersService) === "function" ? _a : Object])
], UsersController);


/***/ }),
/* 119 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UsersService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let UsersService = class UsersService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async findOne(id, requestingUserId) {
        const user = await this.prisma.user.findUnique({
            where: { id },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                        locationWard: true,
                        status: true,
                    },
                },
            },
        });
        if (!user) {
            throw new common_1.NotFoundException(`User with ID ${id} not found`);
        }
        return user;
    }
    async update(id, updateUserDto, requestingUserId) {
        const requestingUser = await this.prisma.user.findUnique({
            where: { id: requestingUserId },
            select: { role: true, hospitalId: true },
        });
        if (!requestingUser) {
            throw new common_1.ForbiddenException('Invalid user');
        }
        const userToUpdate = await this.prisma.user.findUnique({
            where: { id },
            select: { hospitalId: true, role: true },
        });
        if (!userToUpdate) {
            throw new common_1.NotFoundException(`User with ID ${id} not found`);
        }
        if (requestingUser.role === client_1.UserRole.HOSPITAL_ADMIN) {
            if (requestingUser.hospitalId !== userToUpdate.hospitalId) {
                throw new common_1.ForbiddenException('You can only update users in your own hospital');
            }
            if (updateUserDto.role === client_1.UserRole.SUPER_ADMIN) {
                throw new common_1.ForbiddenException('You cannot assign SUPER_ADMIN role');
            }
            if (userToUpdate.role === client_1.UserRole.SUPER_ADMIN) {
                throw new common_1.ForbiddenException('You cannot update SUPER_ADMIN users');
            }
        }
        if (updateUserDto.pharmacyId !== undefined) {
            if (updateUserDto.pharmacyId) {
                const pharmacy = await this.prisma.pharmacy.findUnique({
                    where: { id: updateUserDto.pharmacyId },
                    select: { hospitalId: true, status: true },
                });
                if (!pharmacy) {
                    throw new common_1.BadRequestException('Pharmacy not found');
                }
                if (pharmacy.hospitalId !== userToUpdate.hospitalId) {
                    throw new common_1.BadRequestException('Pharmacy must belong to the same hospital as the user');
                }
                if (pharmacy.status !== 'ACTIVE') {
                    throw new common_1.BadRequestException('Cannot assign user to inactive pharmacy');
                }
            }
        }
        if (updateUserDto.email) {
            const existingUser = await this.prisma.user.findUnique({
                where: { email: updateUserDto.email },
            });
            if (existingUser && existingUser.id !== id) {
                throw new common_1.BadRequestException('Email already in use');
            }
        }
        const updatedUser = await this.prisma.user.update({
            where: { id },
            data: {
                ...updateUserDto,
                version: { increment: 1 },
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                        locationWard: true,
                        status: true,
                    },
                },
            },
        });
        await this.prisma.auditLog.create({
            data: {
                hospitalId: updatedUser.hospitalId,
                userId: requestingUserId,
                action: 'UPDATE',
                entityType: 'User',
                entityId: id,
                afterState: JSON.stringify(updateUserDto),
            },
        });
        return updatedUser;
    }
    async remove(id, requestingUserId) {
        const requestingUser = await this.prisma.user.findUnique({
            where: { id: requestingUserId },
            select: { role: true, hospitalId: true },
        });
        if (!requestingUser) {
            throw new common_1.ForbiddenException('Invalid user');
        }
        const userToDelete = await this.prisma.user.findUnique({
            where: { id },
            select: { hospitalId: true, role: true },
        });
        if (!userToDelete) {
            throw new common_1.NotFoundException(`User with ID ${id} not found`);
        }
        if (requestingUser.role === client_1.UserRole.HOSPITAL_ADMIN) {
            if (requestingUser.hospitalId !== userToDelete.hospitalId) {
                throw new common_1.ForbiddenException('You can only delete users in your own hospital');
            }
            if (userToDelete.role === client_1.UserRole.SUPER_ADMIN) {
                throw new common_1.ForbiddenException('You cannot delete SUPER_ADMIN users');
            }
        }
        if (id === requestingUserId) {
            throw new common_1.BadRequestException('You cannot delete your own account');
        }
        const deletedUser = await this.prisma.user.update({
            where: { id },
            data: {
                status: 'INACTIVE',
                version: { increment: 1 },
            },
            select: {
                hospitalId: true,
            },
        });
        await this.prisma.auditLog.create({
            data: {
                hospitalId: deletedUser.hospitalId,
                userId: requestingUserId,
                action: 'DELETE',
                entityType: 'User',
                entityId: id,
                afterState: JSON.stringify({ status: 'INACTIVE' }),
            },
        });
        return { message: 'User deleted successfully' };
    }
};
exports.UsersService = UsersService;
exports.UsersService = UsersService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], UsersService);


/***/ }),
/* 120 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
__exportStar(__webpack_require__(121), exports);


/***/ }),
/* 121 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateUserDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdateUserDto {
}
exports.UpdateUserDto = UpdateUserDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.MinLength)(2),
    __metadata("design:type", String)
], UpdateUserDto.prototype, "fullName", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEmail)(),
    __metadata("design:type", String)
], UpdateUserDto.prototype, "email", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateUserDto.prototype, "phone", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.UserRole),
    __metadata("design:type", typeof (_a = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _a : Object)
], UpdateUserDto.prototype, "role", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.UserStatus),
    __metadata("design:type", typeof (_b = typeof client_1.UserStatus !== "undefined" && client_1.UserStatus) === "function" ? _b : Object)
], UpdateUserDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateUserDto.prototype, "pharmacyId", void 0);


/***/ }),
/* 122 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TransfersModule = void 0;
const common_1 = __webpack_require__(2);
const transfers_service_1 = __webpack_require__(123);
const transfers_controller_1 = __webpack_require__(124);
const database_module_1 = __webpack_require__(52);
let TransfersModule = class TransfersModule {
};
exports.TransfersModule = TransfersModule;
exports.TransfersModule = TransfersModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [transfers_controller_1.TransfersController],
        providers: [transfers_service_1.TransfersService],
        exports: [transfers_service_1.TransfersService],
    })
], TransfersModule);


/***/ }),
/* 123 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TransfersService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
const client_1 = __webpack_require__(16);
let TransfersService = class TransfersService {
    constructor(prisma, cacheService) {
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    async create(createDto, hospitalId) {
        const [fromPharmacy, toPharmacy] = await Promise.all([
            this.prisma.pharmacy.findUnique({ where: { id: createDto.fromPharmacyId } }),
            this.prisma.pharmacy.findUnique({ where: { id: createDto.toPharmacyId } }),
        ]);
        if (!fromPharmacy || !toPharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        if (fromPharmacy.hospitalId !== hospitalId || toPharmacy.hospitalId !== hospitalId) {
            throw new common_1.BadRequestException('Pharmacies must belong to the same hospital');
        }
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const count = await this.prisma.transferRequest.count({
            where: {
                requestNumber: {
                    startsWith: `TR-${dateStr}`,
                },
            },
        });
        const requestNumber = `TR-${dateStr}-${String(count + 1).padStart(4, '0')}`;
        const transferRequest = await this.prisma.transferRequest.create({
            data: {
                hospitalId,
                fromPharmacyId: createDto.fromPharmacyId,
                toPharmacyId: createDto.toPharmacyId,
                requestNumber,
                requestedBy: createDto.requestedBy,
                notes: createDto.notes,
                status: client_1.TransferStatus.PENDING,
                items: {
                    create: createDto.items.map((item) => ({
                        medicineId: item.medicineId,
                        qtyRequested: item.qtyRequested,
                    })),
                },
            },
            include: {
                fromPharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                    },
                },
                toPharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                    },
                },
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                                form: true,
                                strength: true,
                            },
                        },
                    },
                },
            },
        });
        await this.invalidateCache(hospitalId);
        return transferRequest;
    }
    async findAll(searchDto, hospitalId) {
        const { fromPharmacyId, toPharmacyId, status, limit = 100, page = 1, } = searchDto;
        const cacheKey = `transfers:${hospitalId || 'all'}:${JSON.stringify(searchDto)}`;
        const cached = await this.cacheService.get(cacheKey);
        if (cached) {
            return cached;
        }
        const where = {};
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        if (fromPharmacyId)
            where.fromPharmacyId = fromPharmacyId;
        if (toPharmacyId)
            where.toPharmacyId = toPharmacyId;
        if (status)
            where.status = status;
        const skip = (page - 1) * limit;
        const [transfers, total] = await Promise.all([
            this.prisma.transferRequest.findMany({
                where,
                skip,
                take: limit,
                orderBy: { createdAt: 'desc' },
                include: {
                    fromPharmacy: {
                        select: {
                            id: true,
                            name: true,
                            code: true,
                            type: true,
                        },
                    },
                    toPharmacy: {
                        select: {
                            id: true,
                            name: true,
                            code: true,
                            type: true,
                        },
                    },
                    requester: {
                        select: {
                            id: true,
                            fullName: true,
                        },
                    },
                    approver: {
                        select: {
                            id: true,
                            fullName: true,
                        },
                    },
                    receiver: {
                        select: {
                            id: true,
                            fullName: true,
                        },
                    },
                    items: {
                        include: {
                            medicine: {
                                select: {
                                    id: true,
                                    name: true,
                                    form: true,
                                    strength: true,
                                },
                            },
                        },
                    },
                },
            }),
            this.prisma.transferRequest.count({ where }),
        ]);
        const result = {
            data: transfers,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
        await this.cacheService.set(cacheKey, result, 5 * 60 * 1000);
        return result;
    }
    async findOne(id, hospitalId) {
        const where = { id };
        if (hospitalId) {
            where.hospitalId = hospitalId;
        }
        const transfer = await this.prisma.transferRequest.findFirst({
            where,
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                approver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                receiver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                                destinationBatch: true,
                            },
                        },
                    },
                },
            },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        return transfer;
    }
    async approve(id, approvalData, userId) {
        const transfer = await this.prisma.transferRequest.findUnique({
            where: { id },
            include: { items: true },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        if (transfer.status !== client_1.TransferStatus.PENDING) {
            throw new common_1.BadRequestException('Transfer request is not in pending status');
        }
        const updatedTransfer = await this.prisma.transferRequest.update({
            where: { id },
            data: {
                status: client_1.TransferStatus.APPROVED,
                approvedBy: userId,
                approvedAt: new Date(),
                items: {
                    updateMany: approvalData.items.map((item) => ({
                        where: { id: item.id },
                        data: { qtyApproved: item.qtyApproved },
                    })),
                },
            },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                approver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: true,
                    },
                },
            },
        });
        await this.invalidateCache(transfer.hospitalId);
        return updatedTransfer;
    }
    async reject(id, rejectionData, userId) {
        const transfer = await this.prisma.transferRequest.findUnique({
            where: { id },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        if (transfer.status !== client_1.TransferStatus.PENDING) {
            throw new common_1.BadRequestException('Transfer request is not in pending status');
        }
        const updatedTransfer = await this.prisma.transferRequest.update({
            where: { id },
            data: {
                status: client_1.TransferStatus.REJECTED,
                notes: `REJECTED: ${rejectionData.rejectionReason}`,
                approvedBy: userId,
                approvedAt: new Date(),
            },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                approver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: true,
                    },
                },
            },
        });
        await this.invalidateCache(transfer.hospitalId);
        return updatedTransfer;
    }
    async dispatch(id, dispatchData, userId) {
        const transfer = await this.prisma.transferRequest.findUnique({
            where: { id },
            include: { items: true },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        if (transfer.status !== client_1.TransferStatus.APPROVED) {
            throw new common_1.BadRequestException('Transfer request must be approved before dispatching');
        }
        for (const dispatchItem of dispatchData.items) {
            const transferItem = transfer.items.find((item) => item.id === dispatchItem.itemId);
            if (!transferItem) {
                throw new common_1.BadRequestException(`Transfer item ${dispatchItem.itemId} not found`);
            }
            const totalDispatched = dispatchItem.batches.reduce((sum, b) => sum + b.quantity, 0);
            if (totalDispatched !== transferItem.qtyApproved) {
                throw new common_1.BadRequestException(`Dispatched quantity (${totalDispatched}) must match approved quantity (${transferItem.qtyApproved}) for item ${dispatchItem.itemId}`);
            }
            await this.prisma.transferItem.update({
                where: { id: dispatchItem.itemId },
                data: { qtyDispatched: totalDispatched },
            });
            for (const batchMapping of dispatchItem.batches) {
                const batch = await this.prisma.stockBatch.findUnique({
                    where: { id: batchMapping.batchId },
                });
                if (!batch) {
                    throw new common_1.NotFoundException(`Batch ${batchMapping.batchId} not found`);
                }
                if (batch.pharmacyId !== transfer.toPharmacyId) {
                    throw new common_1.BadRequestException(`Batch ${batch.batchNo} does not belong to the provider pharmacy. Stock must be dispatched from the provider pharmacy.`);
                }
                if (batch.qtyAvailable < batchMapping.quantity) {
                    throw new common_1.BadRequestException(`Insufficient quantity in batch ${batch.batchNo}. Available: ${batch.qtyAvailable}, Requested: ${batchMapping.quantity}`);
                }
                await this.prisma.transferBatchMapping.create({
                    data: {
                        transferItemId: dispatchItem.itemId,
                        sourceBatchId: batchMapping.batchId,
                        qty: batchMapping.quantity,
                    },
                });
                await this.prisma.stockBatch.update({
                    where: { id: batchMapping.batchId },
                    data: {
                        qtyAvailable: { decrement: batchMapping.quantity },
                    },
                });
            }
        }
        const updatedTransfer = await this.prisma.transferRequest.update({
            where: { id },
            data: {
                status: client_1.TransferStatus.DISPATCHED,
                dispatchedAt: new Date(),
            },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                approver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                            },
                        },
                    },
                },
            },
        });
        await this.invalidateCache(transfer.hospitalId);
        return updatedTransfer;
    }
    async receive(id, userId) {
        const transfer = await this.prisma.transferRequest.findUnique({
            where: { id },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                            },
                        },
                    },
                },
            },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        if (transfer.status !== client_1.TransferStatus.DISPATCHED) {
            throw new common_1.BadRequestException('Transfer must be in DISPATCHED status to be received');
        }
        const hasDestinationBatches = transfer.items.some(item => item.batchMappings?.some(mapping => mapping.destinationBatchId));
        if (hasDestinationBatches) {
            throw new common_1.BadRequestException('Transfer has already been received (destination batches exist)');
        }
        for (const item of transfer.items) {
            if (!item.batchMappings || item.batchMappings.length === 0) {
                throw new common_1.BadRequestException(`No batch mappings found for item ${item.medicine.name}. Transfer must be dispatched before receiving.`);
            }
            for (const mapping of item.batchMappings) {
                const sourceBatch = mapping.sourceBatch;
                const destinationBatch = await this.prisma.stockBatch.create({
                    data: {
                        hospitalId: transfer.hospitalId,
                        medicineId: item.medicineId,
                        pharmacyId: transfer.fromPharmacyId,
                        batchNo: sourceBatch.batchNo,
                        expiryDate: sourceBatch.expiryDate,
                        manufacturer: sourceBatch.manufacturer,
                        storageType: sourceBatch.storageType,
                        qtyReceived: mapping.qty,
                        qtyAvailable: mapping.qty,
                        purchasePrice: sourceBatch.purchasePrice,
                        governmentPrice: sourceBatch.governmentPrice,
                        retailPrice: sourceBatch.retailPrice,
                        receivedDate: new Date(),
                        status: client_1.BatchStatus.AVAILABLE,
                    },
                });
                await this.prisma.transferBatchMapping.update({
                    where: { id: mapping.id },
                    data: {
                        destinationBatchId: destinationBatch.id,
                    },
                });
                await this.prisma.transferItem.update({
                    where: { id: item.id },
                    data: {
                        qtyReceived: { increment: mapping.qty },
                    },
                });
            }
        }
        const updatedTransfer = await this.prisma.transferRequest.update({
            where: { id },
            data: {
                status: client_1.TransferStatus.RECEIVED,
                receivedAt: new Date(),
                receivedBy: userId,
            },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                requester: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                approver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                receiver: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                                destinationBatch: true,
                            },
                        },
                    },
                },
            },
        });
        await this.invalidateCache(transfer.hospitalId);
        return updatedTransfer;
    }
    async fixReceivedTransfer(id, userId) {
        const transfer = await this.prisma.transferRequest.findUnique({
            where: { id },
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                                destinationBatch: true,
                            },
                        },
                    },
                },
            },
        });
        if (!transfer) {
            throw new common_1.NotFoundException('Transfer request not found');
        }
        if (transfer.status !== client_1.TransferStatus.RECEIVED) {
            throw new common_1.BadRequestException('Transfer is not in RECEIVED status');
        }
        let batchesCreated = 0;
        let batchesDeleted = 0;
        for (const item of transfer.items) {
            if (!item.batchMappings || item.batchMappings.length === 0) {
                console.warn(`No batch mappings for item ${item.medicine.name} in transfer ${transfer.requestNumber}`);
                continue;
            }
            for (const mapping of item.batchMappings) {
                const sourceBatch = mapping.sourceBatch;
                if (mapping.destinationBatch) {
                    if (mapping.destinationBatch.pharmacyId === transfer.toPharmacyId) {
                        console.log(`Found incorrectly placed batch ${mapping.destinationBatch.batchNo} in ${transfer.toPharmacy.name}, will delete and recreate in ${transfer.fromPharmacy.name}`);
                        await this.prisma.stockBatch.delete({
                            where: { id: mapping.destinationBatch.id },
                        });
                        batchesDeleted++;
                        await this.prisma.transferBatchMapping.update({
                            where: { id: mapping.id },
                            data: { destinationBatchId: null },
                        });
                    }
                    else if (mapping.destinationBatch.pharmacyId === transfer.fromPharmacyId) {
                        console.log(`Batch ${mapping.destinationBatch.batchNo} already in correct pharmacy ${transfer.fromPharmacy.name}`);
                        continue;
                    }
                }
                const destinationBatch = await this.prisma.stockBatch.create({
                    data: {
                        hospitalId: transfer.hospitalId,
                        medicineId: item.medicineId,
                        pharmacyId: transfer.fromPharmacyId,
                        batchNo: sourceBatch.batchNo,
                        expiryDate: sourceBatch.expiryDate,
                        manufacturer: sourceBatch.manufacturer,
                        storageType: sourceBatch.storageType,
                        qtyReceived: mapping.qty,
                        qtyAvailable: mapping.qty,
                        purchasePrice: sourceBatch.purchasePrice,
                        governmentPrice: sourceBatch.governmentPrice,
                        retailPrice: sourceBatch.retailPrice,
                        receivedDate: new Date(),
                        status: client_1.BatchStatus.AVAILABLE,
                    },
                });
                await this.prisma.transferBatchMapping.update({
                    where: { id: mapping.id },
                    data: {
                        destinationBatchId: destinationBatch.id,
                    },
                });
                batchesCreated++;
            }
        }
        return {
            message: `Successfully fixed transfer ${transfer.requestNumber}. Created ${batchesCreated} batches in ${transfer.fromPharmacy.name}${batchesDeleted > 0 ? ` and deleted ${batchesDeleted} incorrectly placed batches from ${transfer.toPharmacy.name}` : ''}.`,
            batchesCreated,
            batchesDeleted,
            transfer: await this.findOne(id),
        };
    }
    async invalidateCache(hospitalId) {
        await this.cacheService.deletePattern(`transfers:${hospitalId}:.*`);
        await this.cacheService.deletePattern(`transfers:all:.*`);
    }
};
exports.TransfersService = TransfersService;
exports.TransfersService = TransfersService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _b : Object])
], TransfersService);


/***/ }),
/* 124 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TransfersController = void 0;
const common_1 = __webpack_require__(2);
const transfers_service_1 = __webpack_require__(123);
const create_transfer_request_dto_1 = __webpack_require__(125);
const search_transfer_request_dto_1 = __webpack_require__(126);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let TransfersController = class TransfersController {
    constructor(transfersService) {
        this.transfersService = transfersService;
    }
    async create(createDto, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const pharmacy = await this.transfersService['prisma'].pharmacy.findUnique({
                where: { id: createDto.fromPharmacyId },
                select: { hospitalId: true },
            });
            hospitalId = pharmacy?.hospitalId;
        }
        if (!hospitalId) {
            throw new Error('Hospital ID not found');
        }
        return this.transfersService.create(createDto, hospitalId);
    }
    async findAll(searchDto, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId && searchDto.hospitalId) {
            hospitalId = searchDto.hospitalId;
        }
        if (!hospitalId && (searchDto.fromPharmacyId || searchDto.toPharmacyId)) {
            const pharmacyId = searchDto.fromPharmacyId || searchDto.toPharmacyId;
            const pharmacy = await this.transfersService['prisma'].pharmacy.findUnique({
                where: { id: pharmacyId },
                select: { hospitalId: true },
            });
            hospitalId = pharmacy?.hospitalId;
        }
        return this.transfersService.findAll(searchDto, hospitalId);
    }
    async findOne(id, req) {
        let hospitalId = req.user.hospitalId;
        if (!hospitalId) {
            const transfer = await this.transfersService['prisma'].transferRequest.findUnique({
                where: { id },
                select: { hospitalId: true },
            });
            hospitalId = transfer?.hospitalId;
        }
        return this.transfersService.findOne(id, hospitalId);
    }
    async approve(id, approvalData, req) {
        return this.transfersService.approve(id, approvalData, req.user.id);
    }
    async reject(id, rejectionData, req) {
        return this.transfersService.reject(id, rejectionData, req.user.id);
    }
    async dispatch(id, dispatchData, req) {
        return this.transfersService.dispatch(id, dispatchData, req.user.id);
    }
    async receive(id, req) {
        return this.transfersService.receive(id, req.user.id);
    }
    async fixReceivedTransfer(id, req) {
        return this.transfersService.fixReceivedTransfer(id, req.user.id);
    }
};
exports.TransfersController = TransfersController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_transfer_request_dto_1.CreateTransferRequestDto !== "undefined" && create_transfer_request_dto_1.CreateTransferRequestDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof search_transfer_request_dto_1.SearchTransferRequestDto !== "undefined" && search_transfer_request_dto_1.SearchTransferRequestDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "findOne", null);
__decorate([
    (0, common_1.Post)(':id/approve'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "approve", null);
__decorate([
    (0, common_1.Post)(':id/reject'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "reject", null);
__decorate([
    (0, common_1.Post)(':id/dispatch'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MAIN_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "dispatch", null);
__decorate([
    (0, common_1.Post)(':id/receive'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.SUB_PHARMACY_MANAGER),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "receive", null);
__decorate([
    (0, common_1.Post)(':id/fix-received'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], TransfersController.prototype, "fixReceivedTransfer", null);
exports.TransfersController = TransfersController = __decorate([
    (0, common_1.Controller)('transfers'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof transfers_service_1.TransfersService !== "undefined" && transfers_service_1.TransfersService) === "function" ? _a : Object])
], TransfersController);


/***/ }),
/* 125 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateTransferRequestDto = exports.TransferItemDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class TransferItemDto {
}
exports.TransferItemDto = TransferItemDto;
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], TransferItemDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], TransferItemDto.prototype, "qtyRequested", void 0);
class CreateTransferRequestDto {
}
exports.CreateTransferRequestDto = CreateTransferRequestDto;
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateTransferRequestDto.prototype, "fromPharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateTransferRequestDto.prototype, "toPharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => TransferItemDto),
    __metadata("design:type", Array)
], CreateTransferRequestDto.prototype, "items", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateTransferRequestDto.prototype, "requestedBy", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateTransferRequestDto.prototype, "notes", void 0);


/***/ }),
/* 126 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SearchTransferRequestDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class SearchTransferRequestDto {
    constructor() {
        this.limit = 100;
        this.page = 1;
    }
}
exports.SearchTransferRequestDto = SearchTransferRequestDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchTransferRequestDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchTransferRequestDto.prototype, "fromPharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SearchTransferRequestDto.prototype, "toPharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.TransferStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.TransferStatus !== "undefined" && client_1.TransferStatus) === "function" ? _a : Object)
], SearchTransferRequestDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(200),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchTransferRequestDto.prototype, "limit", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SearchTransferRequestDto.prototype, "page", void 0);


/***/ }),
/* 127 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReportsModule = void 0;
const common_1 = __webpack_require__(2);
const reports_controller_1 = __webpack_require__(128);
const reports_service_1 = __webpack_require__(129);
const database_module_1 = __webpack_require__(52);
let ReportsModule = class ReportsModule {
};
exports.ReportsModule = ReportsModule;
exports.ReportsModule = ReportsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [reports_controller_1.ReportsController],
        providers: [reports_service_1.ReportsService],
        exports: [reports_service_1.ReportsService],
    })
], ReportsModule);


/***/ }),
/* 128 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReportsController = void 0;
const common_1 = __webpack_require__(2);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const reports_service_1 = __webpack_require__(129);
const daily_transaction_report_dto_1 = __webpack_require__(131);
const date_range_report_dto_1 = __webpack_require__(132);
const detailed_daily_report_dto_1 = __webpack_require__(133);
const financial_summary_dto_1 = __webpack_require__(130);
let ReportsController = class ReportsController {
    constructor(reportsService) {
        this.reportsService = reportsService;
    }
    async getDailyReport(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.reportsService.getDailyTransactionReport({
            ...query,
            hospitalId,
        });
    }
    async getDateRangeReport(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.reportsService.getDateRangeReport({
            ...query,
            hospitalId,
        });
    }
    async getDetailedDailyReport(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.reportsService.getDetailedDailyReport(query.pharmacyId, query.date, hospitalId);
    }
    async getMedicineConsumption(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        const startDate = new Date(query.startDate);
        const endDate = new Date(query.endDate);
        startDate.setHours(0, 0, 0, 0);
        endDate.setHours(23, 59, 59, 999);
        return this.reportsService.getMedicineConsumptionSummary(query.pharmacyId, startDate, endDate);
    }
    async getFinancialSummary(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.reportsService.getFinancialSummary({
            ...query,
            hospitalId,
        });
    }
    async getFinancialDetailed(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.reportsService.getComprehensiveFinancialReport({
            ...query,
            hospitalId,
        });
    }
};
exports.ReportsController = ReportsController;
__decorate([
    (0, common_1.Get)('daily'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof daily_transaction_report_dto_1.DailyTransactionReportDto !== "undefined" && daily_transaction_report_dto_1.DailyTransactionReportDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getDailyReport", null);
__decorate([
    (0, common_1.Get)('range'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof date_range_report_dto_1.DateRangeReportDto !== "undefined" && date_range_report_dto_1.DateRangeReportDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getDateRangeReport", null);
__decorate([
    (0, common_1.Get)('detailed-daily'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof detailed_daily_report_dto_1.DetailedDailyReportDto !== "undefined" && detailed_daily_report_dto_1.DetailedDailyReportDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getDetailedDailyReport", null);
__decorate([
    (0, common_1.Get)('medicine-consumption'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof detailed_daily_report_dto_1.MedicineConsumptionDto !== "undefined" && detailed_daily_report_dto_1.MedicineConsumptionDto) === "function" ? _e : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getMedicineConsumption", null);
__decorate([
    (0, common_1.Get)('financial-summary'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_f = typeof financial_summary_dto_1.FinancialSummaryDto !== "undefined" && financial_summary_dto_1.FinancialSummaryDto) === "function" ? _f : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getFinancialSummary", null);
__decorate([
    (0, common_1.Get)('financial-detailed'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_g = typeof financial_summary_dto_1.FinancialSummaryDto !== "undefined" && financial_summary_dto_1.FinancialSummaryDto) === "function" ? _g : Object, Object]),
    __metadata("design:returntype", Promise)
], ReportsController.prototype, "getFinancialDetailed", null);
exports.ReportsController = ReportsController = __decorate([
    (0, common_1.Controller)('reports'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof reports_service_1.ReportsService !== "undefined" && reports_service_1.ReportsService) === "function" ? _a : Object])
], ReportsController);


/***/ }),
/* 129 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReportsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
const financial_summary_dto_1 = __webpack_require__(130);
let ReportsService = class ReportsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    getPeriodRange(period, date) {
        const start = new Date(date);
        const end = new Date(date);
        if (period === financial_summary_dto_1.FinancialReportPeriod.DAILY) {
            start.setHours(0, 0, 0, 0);
            end.setHours(23, 59, 59, 999);
            return { start, end };
        }
        if (period === financial_summary_dto_1.FinancialReportPeriod.WEEKLY) {
            const day = start.getDay();
            const diffToMonday = (day === 0 ? -6 : 1) - day;
            start.setDate(start.getDate() + diffToMonday);
            start.setHours(0, 0, 0, 0);
            end.setTime(start.getTime());
            end.setDate(start.getDate() + 6);
            end.setHours(23, 59, 59, 999);
            return { start, end };
        }
        if (period === financial_summary_dto_1.FinancialReportPeriod.MONTHLY) {
            start.setDate(1);
            start.setHours(0, 0, 0, 0);
            end.setMonth(start.getMonth() + 1);
            end.setDate(0);
            end.setHours(23, 59, 59, 999);
            return { start, end };
        }
        start.setMonth(0, 1);
        start.setHours(0, 0, 0, 0);
        end.setMonth(11, 31);
        end.setHours(23, 59, 59, 999);
        return { start, end };
    }
    async getFinancialSummary(dto) {
        const { hospitalId, period, date } = dto;
        const targetDate = new Date(date);
        const { start, end } = this.getPeriodRange(period, targetDate);
        const [incomeAgg, expenseAgg, payrollAgg] = await Promise.all([
            this.prisma.receipt.aggregate({
                where: {
                    hospitalId,
                    paymentStatus: client_1.PaymentStatus.PAID,
                    OR: [
                        { paidAt: { gte: start, lte: end } },
                        { paidAt: null, createdAt: { gte: start, lte: end } },
                    ],
                },
                _sum: { paidAmount: true },
            }),
            this.prisma.expenditure.aggregate({
                where: {
                    hospitalId,
                    date: { gte: start, lte: end },
                },
                _sum: { amount: true },
            }),
            this.prisma.payrollRecord.aggregate({
                where: {
                    hospitalId,
                    generatedAt: { gte: start, lte: end },
                },
                _sum: { netPay: true },
            }),
        ]);
        const income = Number(incomeAgg._sum.paidAmount || 0);
        const expenses = Number(expenseAgg._sum.amount || 0);
        const payroll = Number(payrollAgg._sum.netPay || 0);
        const profitLoss = income - (expenses + payroll);
        return {
            period,
            range: {
                start: start.toISOString(),
                end: end.toISOString(),
            },
            income,
            expenses,
            payroll,
            profitLoss,
        };
    }
    async getComprehensiveFinancialReport(dto) {
        const { hospitalId, period, date } = dto;
        const targetDate = new Date(date);
        const { start, end } = this.getPeriodRange(period, targetDate);
        const [incomeByType, expenseAgg, payrollAgg, payrollRecords] = await Promise.all([
            this.prisma.receipt.groupBy({
                by: ['receiptType'],
                where: {
                    hospitalId,
                    paymentStatus: client_1.PaymentStatus.PAID,
                    OR: [
                        { paidAt: { gte: start, lte: end } },
                        { paidAt: null, createdAt: { gte: start, lte: end } },
                    ],
                },
                _sum: { paidAmount: true },
            }),
            this.prisma.expenditure.aggregate({
                where: {
                    hospitalId,
                    date: { gte: start, lte: end },
                },
                _sum: { amount: true },
            }),
            this.prisma.payrollRecord.aggregate({
                where: {
                    hospitalId,
                    generatedAt: { gte: start, lte: end },
                },
                _sum: { netPay: true },
            }),
            this.prisma.payrollRecord.findMany({
                where: {
                    hospitalId,
                    generatedAt: { gte: start, lte: end },
                },
                include: {
                    user: {
                        select: {
                            id: true,
                            department: { select: { name: true } },
                        },
                    },
                },
            }),
        ]);
        const incomeTotals = incomeByType.reduce((acc, item) => {
            acc[item.receiptType] = Number(item._sum.paidAmount || 0);
            return acc;
        }, {});
        const opdIncome = incomeTotals.OPD_CONSULTATION || 0;
        const labIncome = (incomeTotals.LAB_TEST || 0) + (incomeTotals.RADIOLOGY || 0);
        const operationIncome = incomeTotals.PROCEDURE || 0;
        const roomIncome = (incomeTotals.ROOM_CHARGES || 0) + (incomeTotals.ADMISSION || 0);
        const pharmacyIncome = incomeTotals.PHARMACY || 0;
        const otherIncome = incomeTotals.OTHER || 0;
        const totalIncome = opdIncome +
            labIncome +
            operationIncome +
            roomIncome +
            pharmacyIncome +
            otherIncome;
        const expenses = Number(expenseAgg._sum.amount || 0);
        const payroll = Number(payrollAgg._sum.netPay || 0);
        const departmentPayroll = payrollRecords.reduce((acc, record) => {
            const name = record.user?.department?.name || 'Unassigned';
            acc[name] = (acc[name] || 0) + Number(record.netPay || 0);
            return acc;
        }, {});
        const profitLoss = totalIncome - (expenses + payroll);
        return {
            period,
            range: {
                start: start.toISOString(),
                end: end.toISOString(),
            },
            income: {
                opd: opdIncome,
                lab: labIncome,
                operation: operationIncome,
                room: roomIncome,
                pharmacy: pharmacyIncome,
                other: otherIncome,
            },
            expenses,
            payroll,
            payrollByDepartment: departmentPayroll,
            totalIncome,
            profitLoss,
        };
    }
    async getDailyTransactionReport(dto) {
        const { pharmacyId, date, hospitalId } = dto;
        const pharmacy = await this.prisma.pharmacy.findFirst({
            where: {
                id: pharmacyId,
                ...(hospitalId && { hospitalId }),
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        const reportDate = new Date(date);
        const startOfDay = new Date(reportDate);
        startOfDay.setHours(0, 0, 0, 0);
        const endOfDay = new Date(reportDate);
        endOfDay.setHours(23, 59, 59, 999);
        const previousDayEnd = new Date(startOfDay);
        previousDayEnd.setMilliseconds(-1);
        const openingStock = await this.getStockBalanceAtDate(pharmacyId, previousDayEnd);
        const [grnsReceived, transfersReceived] = await Promise.all([
            this.getGRNsForDay(pharmacyId, startOfDay, endOfDay),
            this.getTransfersReceivedForDay(pharmacyId, startOfDay, endOfDay),
        ]);
        const issuesForDay = await this.getIssuesForDay(pharmacyId, startOfDay, endOfDay);
        const transfersSent = await this.getTransfersSentForDay(pharmacyId, startOfDay, endOfDay);
        const closingStock = await this.getStockBalanceAtDate(pharmacyId, endOfDay);
        const totalReceived = grnsReceived.totalValue + transfersReceived.totalValue;
        const totalIssued = issuesForDay.totalValue;
        const totalTransferred = transfersSent.totalValue;
        return {
            pharmacy: {
                id: pharmacy.id,
                name: pharmacy.name,
                type: pharmacy.type,
            },
            hospital: pharmacy.hospital,
            reportDate: date,
            summary: {
                openingBalance: openingStock.totalValue,
                openingQuantity: openingStock.totalQuantity,
                stockReceived: totalReceived,
                stockIssued: totalIssued,
                stockTransferred: totalTransferred,
                closingBalance: closingStock.totalValue,
                closingQuantity: closingStock.totalQuantity,
            },
            receipts: {
                grns: grnsReceived,
                transfersIn: transfersReceived,
            },
            issues: issuesForDay,
            transfersOut: transfersSent,
        };
    }
    async getDateRangeReport(dto) {
        const { pharmacyId, startDate, endDate, hospitalId } = dto;
        const pharmacy = await this.prisma.pharmacy.findFirst({
            where: {
                id: pharmacyId,
                ...(hospitalId && { hospitalId }),
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        const start = new Date(startDate);
        start.setHours(0, 0, 0, 0);
        const end = new Date(endDate);
        end.setHours(23, 59, 59, 999);
        const openingDate = new Date(start);
        openingDate.setMilliseconds(-1);
        const openingStock = await this.getStockBalanceAtDate(pharmacyId, openingDate);
        const [grnsReceived, transfersReceived, issues, transfersSent] = await Promise.all([
            this.getGRNsForPeriod(pharmacyId, start, end),
            this.getTransfersReceivedForPeriod(pharmacyId, start, end),
            this.getIssuesForPeriod(pharmacyId, start, end),
            this.getTransfersSentForPeriod(pharmacyId, start, end),
        ]);
        const closingStock = await this.getStockBalanceAtDate(pharmacyId, end);
        return {
            pharmacy: {
                id: pharmacy.id,
                name: pharmacy.name,
                type: pharmacy.type,
            },
            hospital: pharmacy.hospital,
            period: {
                startDate,
                endDate,
            },
            summary: {
                openingBalance: openingStock.totalValue,
                totalReceived: grnsReceived.totalValue + transfersReceived.totalValue,
                totalIssued: issues.totalValue,
                totalTransferred: transfersSent.totalValue,
                closingBalance: closingStock.totalValue,
            },
            details: {
                grns: grnsReceived,
                transfersIn: transfersReceived,
                issues,
                transfersOut: transfersSent,
            },
        };
    }
    async getStockBalanceAtDate(pharmacyId, date) {
        const batches = await this.prisma.stockBatch.findMany({
            where: {
                pharmacyId,
                createdAt: {
                    lte: date,
                },
                expiryDate: {
                    gte: date,
                },
            },
            include: {
                medicine: {
                    select: {
                        name: true,
                        genericName: true,
                    },
                },
            },
        });
        const totalQuantity = batches.reduce((sum, batch) => sum + batch.qtyAvailable, 0);
        const totalValue = batches.reduce((sum, batch) => {
            const unitPrice = batch.purchasePrice || 0;
            return sum + batch.qtyAvailable * Number(unitPrice);
        }, 0);
        return {
            totalQuantity,
            totalValue: Number(totalValue.toFixed(2)),
            batchCount: batches.length,
        };
    }
    async getGRNsForDay(pharmacyId, startOfDay, endOfDay) {
        const grns = await this.prisma.gRN.findMany({
            where: {
                pharmacyId,
                receivedDate: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
            include: {
                items: {
                    include: {
                        medicine: {
                            select: {
                                name: true,
                                genericName: true,
                            },
                        },
                        batch: {
                            select: {
                                purchasePrice: true,
                            },
                        },
                    },
                },
            },
        });
        const totalValue = grns.reduce((sum, grn) => {
            const grnTotal = grn.items.reduce((itemSum, item) => {
                return itemSum + item.qtyReceived * Number(item.batch.purchasePrice || 0);
            }, 0);
            return sum + grnTotal;
        }, 0);
        const totalQuantity = grns.reduce((sum, grn) => {
            return sum + grn.items.reduce((itemSum, item) => itemSum + item.qtyReceived, 0);
        }, 0);
        return {
            count: grns.length,
            totalQuantity,
            totalValue: Number(totalValue.toFixed(2)),
            transactions: grns.map((grn) => ({
                id: grn.id,
                grnNumber: grn.grnNumber,
                receivedAt: grn.receivedDate,
                itemCount: grn.items.length,
                totalValue: grn.items
                    .reduce((sum, item) => sum + item.qtyReceived * Number(item.batch.purchasePrice || 0), 0)
                    .toFixed(2),
            })),
        };
    }
    async getGRNsForPeriod(pharmacyId, start, end) {
        return this.getGRNsForDay(pharmacyId, start, end);
    }
    async getTransfersReceivedForDay(pharmacyId, startOfDay, endOfDay) {
        const transfers = await this.prisma.transferRequest.findMany({
            where: {
                fromPharmacyId: pharmacyId,
                status: 'RECEIVED',
                receivedAt: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
            include: {
                toPharmacy: {
                    select: {
                        name: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                name: true,
                                genericName: true,
                            },
                        },
                    },
                },
            },
        });
        const totalQuantity = transfers.reduce((sum, transfer) => {
            return sum + transfer.items.reduce((itemSum, item) => itemSum + item.qtyRequested, 0);
        }, 0);
        const totalValue = 0;
        return {
            count: transfers.length,
            totalQuantity,
            totalValue,
            transactions: transfers.map((transfer) => ({
                id: transfer.id,
                requestNumber: transfer.requestNumber,
                fromPharmacy: transfer.toPharmacy.name,
                receivedAt: transfer.receivedAt,
                itemCount: transfer.items.length,
            })),
        };
    }
    async getTransfersReceivedForPeriod(pharmacyId, start, end) {
        return this.getTransfersReceivedForDay(pharmacyId, start, end);
    }
    async getIssuesForDay(pharmacyId, startOfDay, endOfDay) {
        const issues = await this.prisma.issueTransaction.findMany({
            where: {
                pharmacyId,
                issuedAt: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
            include: {
                patient: {
                    select: {
                        nrNumber: true,
                        fullName: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                name: true,
                                genericName: true,
                            },
                        },
                    },
                },
            },
        });
        const totalQuantity = issues.reduce((sum, issue) => {
            return sum + issue.items.reduce((itemSum, item) => itemSum + item.qtyIssued, 0);
        }, 0);
        const totalValue = issues.reduce((sum, issue) => sum + Number(issue.totalAmount || 0), 0);
        return {
            count: issues.length,
            totalQuantity,
            totalValue: Number(totalValue.toFixed(2)),
            transactions: issues.map((issue) => ({
                id: issue.id,
                patient: issue.patient.fullName,
                nrNumber: issue.patient.nrNumber,
                issuedAt: issue.issuedAt,
                itemCount: issue.items.length,
                totalAmount: Number(issue.totalAmount || 0).toFixed(2),
            })),
        };
    }
    async getIssuesForPeriod(pharmacyId, start, end) {
        return this.getIssuesForDay(pharmacyId, start, end);
    }
    async getTransfersSentForDay(pharmacyId, startOfDay, endOfDay) {
        const transfers = await this.prisma.transferRequest.findMany({
            where: {
                toPharmacyId: pharmacyId,
                status: 'RECEIVED',
                dispatchedAt: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
            include: {
                fromPharmacy: {
                    select: {
                        name: true,
                    },
                },
                items: {
                    include: {
                        medicine: {
                            select: {
                                name: true,
                                genericName: true,
                            },
                        },
                    },
                },
            },
        });
        const totalQuantity = transfers.reduce((sum, transfer) => {
            return sum + transfer.items.reduce((itemSum, item) => itemSum + item.qtyRequested, 0);
        }, 0);
        return {
            count: transfers.length,
            totalQuantity,
            totalValue: 0,
            transactions: transfers.map((transfer) => ({
                id: transfer.id,
                requestNumber: transfer.requestNumber,
                toPharmacy: transfer.fromPharmacy.name,
                dispatchedAt: transfer.dispatchedAt,
                itemCount: transfer.items.length,
            })),
        };
    }
    async getTransfersSentForPeriod(pharmacyId, start, end) {
        return this.getTransfersSentForDay(pharmacyId, start, end);
    }
    async getMedicineWiseOpeningStock(pharmacyId, date) {
        const startOfDay = new Date(date);
        startOfDay.setHours(0, 0, 0, 0);
        const previousDayEnd = new Date(startOfDay);
        previousDayEnd.setMilliseconds(-1);
        const batches = await this.prisma.stockBatch.findMany({
            where: {
                pharmacyId,
                receivedDate: {
                    lte: previousDayEnd,
                },
                expiryDate: {
                    gte: previousDayEnd,
                },
                qtyAvailable: {
                    gt: 0,
                },
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                        strength: true,
                    },
                },
            },
            orderBy: {
                medicine: {
                    name: 'asc',
                },
            },
        });
        const medicineMap = new Map();
        batches.forEach((batch) => {
            const medId = batch.medicineId;
            if (!medicineMap.has(medId)) {
                medicineMap.set(medId, {
                    medicineId: medId,
                    medicineName: batch.medicine.name,
                    genericName: batch.medicine.genericName,
                    form: batch.medicine.form,
                    strength: batch.medicine.strength,
                    totalQuantity: 0,
                    totalValue: 0,
                    batchCount: 0,
                    batches: [],
                });
            }
            const medicine = medicineMap.get(medId);
            const batchValue = batch.qtyAvailable * Number(batch.purchasePrice || 0);
            medicine.totalQuantity += batch.qtyAvailable;
            medicine.totalValue += batchValue;
            medicine.batchCount += 1;
            medicine.batches.push({
                batchId: batch.id,
                batchNo: batch.batchNo,
                expiryDate: batch.expiryDate,
                qtyAvailable: batch.qtyAvailable,
                purchasePrice: Number(batch.purchasePrice || 0),
                governmentPrice: Number(batch.governmentPrice || 0),
                retailPrice: Number(batch.retailPrice || 0),
                storageType: batch.storageType,
            });
        });
        return Array.from(medicineMap.values()).map((med) => ({
            ...med,
            totalValue: Number(med.totalValue.toFixed(2)),
        }));
    }
    async getMedicineWiseClosingStock(pharmacyId, date) {
        const endOfDay = new Date(date);
        endOfDay.setHours(23, 59, 59, 999);
        const batches = await this.prisma.stockBatch.findMany({
            where: {
                pharmacyId,
                receivedDate: {
                    lte: endOfDay,
                },
                expiryDate: {
                    gte: endOfDay,
                },
                qtyAvailable: {
                    gt: 0,
                },
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                        strength: true,
                    },
                },
            },
            orderBy: {
                medicine: {
                    name: 'asc',
                },
            },
        });
        const medicineMap = new Map();
        batches.forEach((batch) => {
            const medId = batch.medicineId;
            if (!medicineMap.has(medId)) {
                medicineMap.set(medId, {
                    medicineId: medId,
                    medicineName: batch.medicine.name,
                    genericName: batch.medicine.genericName,
                    form: batch.medicine.form,
                    strength: batch.medicine.strength,
                    totalQuantity: 0,
                    totalValue: 0,
                    batchCount: 0,
                    batches: [],
                });
            }
            const medicine = medicineMap.get(medId);
            const batchValue = batch.qtyAvailable * Number(batch.purchasePrice || 0);
            medicine.totalQuantity += batch.qtyAvailable;
            medicine.totalValue += batchValue;
            medicine.batchCount += 1;
            medicine.batches.push({
                batchId: batch.id,
                batchNo: batch.batchNo,
                expiryDate: batch.expiryDate,
                qtyAvailable: batch.qtyAvailable,
                purchasePrice: Number(batch.purchasePrice || 0),
                governmentPrice: Number(batch.governmentPrice || 0),
                retailPrice: Number(batch.retailPrice || 0),
                storageType: batch.storageType,
            });
        });
        return Array.from(medicineMap.values()).map((med) => ({
            ...med,
            totalValue: Number(med.totalValue.toFixed(2)),
        }));
    }
    async getDetailedGRNs(pharmacyId, startDate, endDate) {
        const grns = await this.prisma.gRN.findMany({
            where: {
                pharmacyId,
                receivedDate: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            include: {
                items: {
                    include: {
                        medicine: true,
                        batch: true,
                    },
                },
                receiver: {
                    select: {
                        fullName: true,
                    },
                },
            },
            orderBy: {
                receivedDate: 'asc',
            },
        });
        return grns.map((grn) => ({
            id: grn.id,
            grnNumber: grn.grnNumber,
            invoiceNumber: null,
            supplierName: null,
            receivedDate: grn.receivedDate,
            receivedBy: grn.receiver?.fullName || null,
            itemCount: grn.items.length,
            totalQuantity: grn.items.reduce((sum, item) => sum + item.qtyReceived, 0),
            totalValue: Number(grn.items
                .reduce((sum, item) => sum + item.qtyReceived * Number(item.batch.purchasePrice || 0), 0)
                .toFixed(2)),
            items: grn.items.map((item) => ({
                grnItemId: item.id,
                medicineId: item.medicineId,
                medicineName: item.medicine.name,
                genericName: item.medicine.genericName,
                form: item.medicine.form,
                strength: item.medicine.strength,
                batchNo: item.batch.batchNo,
                expiryDate: item.batch.expiryDate,
                qtyReceived: item.qtyReceived,
                purchasePrice: Number(item.batch.purchasePrice || 0),
                governmentPrice: Number(item.batch.governmentPrice || 0),
                retailPrice: Number(item.batch.retailPrice || 0),
                storageType: item.batch.storageType,
                totalValue: Number((item.qtyReceived * Number(item.batch.purchasePrice || 0)).toFixed(2)),
            })),
        }));
    }
    async getDetailedIssues(pharmacyId, startDate, endDate) {
        const issues = await this.prisma.issueTransaction.findMany({
            where: {
                pharmacyId,
                issuedAt: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            include: {
                patient: true,
                items: {
                    include: {
                        medicine: true,
                        batch: true,
                    },
                },
                issuer: {
                    select: {
                        fullName: true,
                    },
                },
            },
            orderBy: {
                issuedAt: 'asc',
            },
        });
        return issues.map((issue) => {
            const age = issue.patient.dob
                ? Math.floor((Date.now() - issue.patient.dob.getTime()) / (365.25 * 24 * 60 * 60 * 1000))
                : 0;
            return {
                id: issue.id,
                patientNrNumber: issue.patient.nrNumber,
                patientName: issue.patient.fullName,
                patientAge: age,
                patientGender: issue.patient.gender,
                visitType: issue.patient.visitType,
                issuedAt: issue.issuedAt,
                issuedBy: issue.issuer?.fullName || null,
                prescriptionId: issue.prescriptionId,
                itemCount: issue.items.length,
                totalQuantity: issue.items.reduce((sum, item) => sum + item.qtyIssued, 0),
                totalAmount: Number(issue.totalAmount || 0),
                items: issue.items.map((item) => ({
                    issueItemId: item.id,
                    medicineId: item.medicineId,
                    medicineName: item.medicine.name,
                    genericName: item.medicine.genericName,
                    form: item.medicine.form,
                    strength: item.medicine.strength,
                    batchNo: item.batch.batchNo,
                    expiryDate: item.batch.expiryDate,
                    qtyIssued: item.qtyIssued,
                    unitPrice: Number(item.unitPrice || 0),
                    totalAmount: Number(item.totalPrice || 0),
                })),
            };
        });
    }
    async getDetailedTransfers(pharmacyId, startDate, endDate, direction) {
        const whereClause = direction === 'IN'
            ? {
                fromPharmacyId: pharmacyId,
                status: 'RECEIVED',
                receivedAt: {
                    gte: startDate,
                    lte: endDate,
                },
            }
            : {
                toPharmacyId: pharmacyId,
                status: 'RECEIVED',
                dispatchedAt: {
                    gte: startDate,
                    lte: endDate,
                },
            };
        const transfers = await this.prisma.transferRequest.findMany({
            where: whereClause,
            include: {
                fromPharmacy: true,
                toPharmacy: true,
                items: {
                    include: {
                        medicine: true,
                        batchMappings: {
                            include: {
                                sourceBatch: true,
                                destinationBatch: true,
                            },
                        },
                    },
                },
                requester: {
                    select: {
                        fullName: true,
                    },
                },
                approver: {
                    select: {
                        fullName: true,
                    },
                },
            },
            orderBy: direction === 'IN' ? { receivedAt: 'asc' } : { dispatchedAt: 'asc' },
        });
        return transfers.map((transfer) => ({
            id: transfer.id,
            requestNumber: transfer.requestNumber,
            fromPharmacyId: transfer.fromPharmacyId,
            fromPharmacyName: transfer.fromPharmacy.name,
            toPharmacyId: transfer.toPharmacyId,
            toPharmacyName: transfer.toPharmacy.name,
            requestedAt: transfer.createdAt,
            approvedAt: transfer.approvedAt,
            dispatchedAt: transfer.dispatchedAt,
            receivedAt: transfer.receivedAt,
            status: transfer.status,
            requestedBy: transfer.requester?.fullName || null,
            approvedBy: transfer.approver?.fullName || null,
            itemCount: transfer.items.length,
            totalQuantity: transfer.items.reduce((sum, item) => sum + item.qtyRequested, 0),
            items: transfer.items.map((item) => ({
                transferItemId: item.id,
                medicineId: item.medicineId,
                medicineName: item.medicine.name,
                genericName: item.medicine.genericName,
                form: item.medicine.form,
                strength: item.medicine.strength,
                qtyRequested: item.qtyRequested,
                qtyApproved: item.qtyApproved,
                batchMappings: item.batchMappings.map((mapping) => ({
                    sourceBatchNo: mapping.sourceBatch.batchNo,
                    sourceExpiryDate: mapping.sourceBatch.expiryDate,
                    destinationBatchNo: mapping.destinationBatch ? mapping.destinationBatch.batchNo : '',
                    destinationExpiryDate: mapping.destinationBatch ? mapping.destinationBatch.expiryDate : new Date(),
                    qtyTransferred: mapping.qty,
                })),
            })),
        }));
    }
    async getMedicineConsumptionSummary(pharmacyId, startDate, endDate) {
        const issues = await this.getDetailedIssues(pharmacyId, startDate, endDate);
        const medicineMap = new Map();
        issues.forEach((issue) => {
            issue.items.forEach((item) => {
                const medId = item.medicineId;
                if (!medicineMap.has(medId)) {
                    medicineMap.set(medId, {
                        medicineId: medId,
                        medicineName: item.medicineName,
                        genericName: item.genericName,
                        form: item.form,
                        strength: item.strength,
                        totalQuantityIssued: 0,
                        patients: new Set(),
                        issueCount: 0,
                        totalValue: 0,
                    });
                }
                const medicine = medicineMap.get(medId);
                medicine.totalQuantityIssued += item.qtyIssued;
                medicine.patients.add(issue.patientNrNumber);
                medicine.issueCount += 1;
                medicine.totalValue += item.totalAmount;
            });
        });
        return Array.from(medicineMap.values()).map((med) => ({
            medicineId: med.medicineId,
            medicineName: med.medicineName,
            genericName: med.genericName,
            form: med.form,
            strength: med.strength,
            totalQuantityIssued: med.totalQuantityIssued,
            numberOfPatients: med.patients.size,
            numberOfIssues: med.issueCount,
            totalValue: Number(med.totalValue.toFixed(2)),
            averagePerPatient: Number((med.totalQuantityIssued / med.patients.size).toFixed(2)),
            averagePerIssue: Number((med.totalQuantityIssued / med.issueCount).toFixed(2)),
        }));
    }
    async getDetailedDailyReport(pharmacyId, date, hospitalId) {
        const pharmacy = await this.prisma.pharmacy.findFirst({
            where: {
                id: pharmacyId,
                ...(hospitalId && { hospitalId }),
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
        if (!pharmacy) {
            throw new common_1.NotFoundException('Pharmacy not found');
        }
        const reportDate = new Date(date);
        const startOfDay = new Date(reportDate);
        startOfDay.setHours(0, 0, 0, 0);
        const endOfDay = new Date(reportDate);
        endOfDay.setHours(23, 59, 59, 999);
        const previousDayEnd = new Date(startOfDay);
        previousDayEnd.setMilliseconds(-1);
        const [medicineWiseOpening, medicineWiseClosing, detailedGRNs, detailedTransfersIn, detailedIssues, detailedTransfersOut, medicineConsumption,] = await Promise.all([
            this.getMedicineWiseOpeningStock(pharmacyId, reportDate),
            this.getMedicineWiseClosingStock(pharmacyId, reportDate),
            this.getDetailedGRNs(pharmacyId, startOfDay, endOfDay),
            this.getDetailedTransfers(pharmacyId, startOfDay, endOfDay, 'IN'),
            this.getDetailedIssues(pharmacyId, startOfDay, endOfDay),
            this.getDetailedTransfers(pharmacyId, startOfDay, endOfDay, 'OUT'),
            this.getMedicineConsumptionSummary(pharmacyId, startOfDay, endOfDay),
        ]);
        const openingBalance = medicineWiseOpening.reduce((sum, med) => sum + med.totalValue, 0);
        const openingQuantity = medicineWiseOpening.reduce((sum, med) => sum + med.totalQuantity, 0);
        const closingBalance = medicineWiseClosing.reduce((sum, med) => sum + med.totalValue, 0);
        const closingQuantity = medicineWiseClosing.reduce((sum, med) => sum + med.totalQuantity, 0);
        const stockReceived = detailedGRNs.reduce((sum, grn) => sum + grn.totalValue, 0);
        const stockReceivedQuantity = detailedGRNs.reduce((sum, grn) => sum + grn.totalQuantity, 0);
        const stockIssued = detailedIssues.reduce((sum, issue) => sum + issue.totalAmount, 0);
        const stockIssuedQuantity = detailedIssues.reduce((sum, issue) => sum + issue.totalQuantity, 0);
        const stockTransferred = 0;
        const stockTransferredQuantity = detailedTransfersOut.reduce((sum, transfer) => sum + transfer.totalQuantity, 0);
        return {
            pharmacy: {
                id: pharmacy.id,
                name: pharmacy.name,
                type: pharmacy.type,
                code: pharmacy.code,
            },
            hospital: pharmacy.hospital,
            reportDate: date,
            summary: {
                openingBalance: Number(openingBalance.toFixed(2)),
                openingQuantity,
                stockReceived: Number(stockReceived.toFixed(2)),
                stockReceivedQuantity,
                stockIssued: Number(stockIssued.toFixed(2)),
                stockIssuedQuantity,
                stockTransferred: Number(stockTransferred.toFixed(2)),
                stockTransferredQuantity,
                closingBalance: Number(closingBalance.toFixed(2)),
                closingQuantity,
            },
            medicineWiseOpening,
            medicineWiseClosing,
            detailedGRNs,
            detailedTransfersIn,
            detailedIssues,
            detailedTransfersOut,
            medicineConsumption,
        };
    }
};
exports.ReportsService = ReportsService;
exports.ReportsService = ReportsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ReportsService);


/***/ }),
/* 130 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FinancialSummaryDto = exports.FinancialReportPeriod = void 0;
const class_validator_1 = __webpack_require__(35);
var FinancialReportPeriod;
(function (FinancialReportPeriod) {
    FinancialReportPeriod["DAILY"] = "DAILY";
    FinancialReportPeriod["WEEKLY"] = "WEEKLY";
    FinancialReportPeriod["MONTHLY"] = "MONTHLY";
    FinancialReportPeriod["YEARLY"] = "YEARLY";
})(FinancialReportPeriod || (exports.FinancialReportPeriod = FinancialReportPeriod = {}));
class FinancialSummaryDto {
}
exports.FinancialSummaryDto = FinancialSummaryDto;
__decorate([
    (0, class_validator_1.IsEnum)(FinancialReportPeriod),
    __metadata("design:type", String)
], FinancialSummaryDto.prototype, "period", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], FinancialSummaryDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], FinancialSummaryDto.prototype, "hospitalId", void 0);


/***/ }),
/* 131 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DailyTransactionReportDto = void 0;
const class_validator_1 = __webpack_require__(35);
class DailyTransactionReportDto {
}
exports.DailyTransactionReportDto = DailyTransactionReportDto;
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], DailyTransactionReportDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], DailyTransactionReportDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], DailyTransactionReportDto.prototype, "hospitalId", void 0);


/***/ }),
/* 132 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DateRangeReportDto = void 0;
const class_validator_1 = __webpack_require__(35);
class DateRangeReportDto {
}
exports.DateRangeReportDto = DateRangeReportDto;
__decorate([
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], DateRangeReportDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], DateRangeReportDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], DateRangeReportDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], DateRangeReportDto.prototype, "hospitalId", void 0);


/***/ }),
/* 133 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MedicineConsumptionDto = exports.MedicineStockLedgerDto = exports.DetailedDailyReportDto = void 0;
const class_validator_1 = __webpack_require__(35);
class DetailedDailyReportDto {
}
exports.DetailedDailyReportDto = DetailedDailyReportDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], DetailedDailyReportDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], DetailedDailyReportDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], DetailedDailyReportDto.prototype, "hospitalId", void 0);
class MedicineStockLedgerDto {
}
exports.MedicineStockLedgerDto = MedicineStockLedgerDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MedicineStockLedgerDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], MedicineStockLedgerDto.prototype, "medicineId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MedicineStockLedgerDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], MedicineStockLedgerDto.prototype, "hospitalId", void 0);
class MedicineConsumptionDto {
}
exports.MedicineConsumptionDto = MedicineConsumptionDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MedicineConsumptionDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MedicineConsumptionDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MedicineConsumptionDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], MedicineConsumptionDto.prototype, "hospitalId", void 0);


/***/ }),
/* 134 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalyticsModule = void 0;
const common_1 = __webpack_require__(2);
const analytics_controller_1 = __webpack_require__(135);
const analytics_service_1 = __webpack_require__(136);
const database_module_1 = __webpack_require__(52);
let AnalyticsModule = class AnalyticsModule {
};
exports.AnalyticsModule = AnalyticsModule;
exports.AnalyticsModule = AnalyticsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [analytics_controller_1.AnalyticsController],
        providers: [analytics_service_1.AnalyticsService],
        exports: [analytics_service_1.AnalyticsService],
    })
], AnalyticsModule);


/***/ }),
/* 135 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalyticsController = void 0;
const common_1 = __webpack_require__(2);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const analytics_service_1 = __webpack_require__(136);
const analytics_query_dto_1 = __webpack_require__(137);
const client_1 = __webpack_require__(16);
let AnalyticsController = class AnalyticsController {
    constructor(analyticsService) {
        this.analyticsService = analyticsService;
    }
    getCacheStats() {
        return this.analyticsService.getCacheStats();
    }
    getSystemHealth() {
        return this.analyticsService.getSystemHealth();
    }
    async getOverview(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getDashboardOverview({
            ...query,
            hospitalId,
        });
    }
    async getStockTrends(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getStockTrends({
            ...query,
            hospitalId,
        });
    }
    async getConsumptionTrends(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getConsumptionTrends({
            ...query,
            hospitalId,
        });
    }
    async getTopMedicines(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getTopMedicines({
            ...query,
            hospitalId,
        });
    }
    async getPharmacyPerformance(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getPharmacyPerformance({
            ...query,
            hospitalId,
        });
    }
    async getFinancialOverview(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getFinancialOverview({
            ...query,
            hospitalId,
        });
    }
    async getExpiryAnalysis(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getExpiryAnalysis({
            ...query,
            hospitalId,
        });
    }
    async getTransferMetrics(query, req) {
        const user = req.user;
        const hospitalId = query.hospitalId || user.hospitalId;
        if (!hospitalId) {
            throw new Error('Hospital ID is required');
        }
        if (user.role !== 'SUPER_ADMIN' && hospitalId !== user.hospitalId) {
            throw new Error('Unauthorized to access this hospital data');
        }
        return this.analyticsService.getTransferMetrics({
            ...query,
            hospitalId,
        });
    }
};
exports.AnalyticsController = AnalyticsController;
__decorate([
    (0, common_1.Get)('performance/cache-stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], AnalyticsController.prototype, "getCacheStats", null);
__decorate([
    (0, common_1.Get)('performance/system-health'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], AnalyticsController.prototype, "getSystemHealth", null);
__decorate([
    (0, common_1.Get)('overview'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getOverview", null);
__decorate([
    (0, common_1.Get)('stock-trends'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getStockTrends", null);
__decorate([
    (0, common_1.Get)('consumption-trends'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getConsumptionTrends", null);
__decorate([
    (0, common_1.Get)('top-medicines'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _e : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getTopMedicines", null);
__decorate([
    (0, common_1.Get)('pharmacy-performance'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_f = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _f : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getPharmacyPerformance", null);
__decorate([
    (0, common_1.Get)('financial'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_g = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _g : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getFinancialOverview", null);
__decorate([
    (0, common_1.Get)('expiry-analysis'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_h = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _h : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getExpiryAnalysis", null);
__decorate([
    (0, common_1.Get)('transfer-metrics'),
    (0, roles_decorator_1.Roles)('SUPER_ADMIN', 'HOSPITAL_ADMIN', 'MAIN_PHARMACY_MANAGER', 'SUB_PHARMACY_MANAGER', 'AUDITOR'),
    __param(0, (0, common_1.Query)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_j = typeof analytics_query_dto_1.AnalyticsQueryDto !== "undefined" && analytics_query_dto_1.AnalyticsQueryDto) === "function" ? _j : Object, Object]),
    __metadata("design:returntype", Promise)
], AnalyticsController.prototype, "getTransferMetrics", null);
exports.AnalyticsController = AnalyticsController = __decorate([
    (0, common_1.Controller)('analytics'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof analytics_service_1.AnalyticsService !== "undefined" && analytics_service_1.AnalyticsService) === "function" ? _a : Object])
], AnalyticsController);


/***/ }),
/* 136 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalyticsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const cache_service_1 = __webpack_require__(11);
const analytics_query_dto_1 = __webpack_require__(137);
let AnalyticsService = class AnalyticsService {
    constructor(prisma, cacheService) {
        this.prisma = prisma;
        this.cacheService = cacheService;
    }
    async getDashboardOverview(query) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const [stockValue, totalPatients, totalIssues, totalTransfers, activeAlerts, expiringBatches,] = await Promise.all([
            this.getTotalStockValue(hospitalId, pharmacyId),
            this.getTotalPatients(hospitalId, startDate, endDate),
            this.getTotalIssuances(hospitalId, pharmacyId, startDate, endDate),
            this.getTotalTransfers(hospitalId, pharmacyId, startDate, endDate),
            this.getActiveAlerts(hospitalId, pharmacyId),
            this.getExpiringBatches(hospitalId, pharmacyId, 30),
        ]);
        return {
            period: {
                label: this.getPeriodLabel(period),
                startDate,
                endDate,
            },
            kpis: {
                stockValue,
                totalPatients,
                totalIssues,
                totalTransfers,
                activeAlerts,
                expiringBatches,
            },
        };
    }
    async getStockTrends(query) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const batches = await this.prisma.stockBatch.groupBy({
            by: ['receivedDate'],
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                receivedDate: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            _sum: {
                qtyReceived: true,
                qtyAvailable: true,
            },
            orderBy: {
                receivedDate: 'asc',
            },
        });
        return batches.map((batch) => ({
            date: batch.receivedDate,
            received: batch._sum.qtyReceived || 0,
            available: batch._sum.qtyAvailable || 0,
        }));
    }
    async getConsumptionTrends(query) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const issues = await this.prisma.issueTransaction.findMany({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                issuedAt: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            include: {
                items: {
                    include: {
                        medicine: {
                            select: {
                                id: true,
                                name: true,
                                genericName: true,
                            },
                        },
                    },
                },
            },
        });
        const dailyConsumption = issues.reduce((acc, issue) => {
            const date = issue.issuedAt.toISOString().split('T')[0];
            if (!acc[date]) {
                acc[date] = {
                    date,
                    totalQuantity: 0,
                    totalValue: 0,
                    transactionCount: 0,
                };
            }
            acc[date].transactionCount += 1;
            acc[date].totalValue += Number(issue.totalAmount);
            acc[date].totalQuantity += issue.items.reduce((sum, item) => sum + item.qtyIssued, 0);
            return acc;
        }, {});
        return Object.values(dailyConsumption).sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());
    }
    async getTopMedicines(query, limit = 10) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const issues = await this.prisma.issueTransaction.findMany({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                issuedAt: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            select: {
                id: true,
            },
        });
        const issueIds = issues.map(issue => issue.id);
        if (issueIds.length === 0) {
            return [];
        }
        const issueItems = await this.prisma.issueItem.findMany({
            where: {
                issueId: {
                    in: issueIds,
                },
            },
            include: {
                medicine: {
                    select: {
                        id: true,
                        name: true,
                        genericName: true,
                        form: true,
                    },
                },
            },
        });
        const medicineStats = issueItems.reduce((acc, item) => {
            const medId = item.medicineId;
            if (!acc[medId]) {
                acc[medId] = {
                    medicineId: medId,
                    name: item.medicine.name,
                    genericName: item.medicine.genericName,
                    form: item.medicine.form,
                    totalQuantity: 0,
                    totalValue: 0,
                    transactionCount: 0,
                };
            }
            acc[medId].totalQuantity += item.qtyIssued;
            acc[medId].totalValue += Number(item.totalPrice);
            acc[medId].transactionCount += 1;
            return acc;
        }, {});
        return Object.values(medicineStats)
            .sort((a, b) => b.totalQuantity - a.totalQuantity)
            .slice(0, limit);
    }
    async getPharmacyPerformance(query) {
        const { hospitalId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const pharmacies = await this.prisma.pharmacy.findMany({
            where: {
                ...(hospitalId && { hospitalId }),
                status: 'ACTIVE',
            },
            select: {
                id: true,
                name: true,
                type: true,
            },
        });
        const performanceData = await Promise.all(pharmacies.map(async (pharmacy) => {
            const [stockValue, issues, transfers, alerts] = await Promise.all([
                this.getTotalStockValue(hospitalId, pharmacy.id),
                this.getTotalIssuances(hospitalId, pharmacy.id, startDate, endDate),
                this.getTotalTransfers(hospitalId, pharmacy.id, startDate, endDate),
                this.getActiveAlerts(hospitalId, pharmacy.id),
            ]);
            return {
                pharmacyId: pharmacy.id,
                pharmacyName: pharmacy.name,
                type: pharmacy.type,
                stockValue: stockValue.totalValue,
                stockQuantity: stockValue.totalQuantity,
                issueCount: issues.count,
                issueValue: issues.totalValue,
                transferCount: transfers.sent + transfers.received,
                activeAlerts: alerts.count,
            };
        }));
        return performanceData;
    }
    async getFinancialOverview(query) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const [purchases, issues, stockValue] = await Promise.all([
            this.prisma.gRN.aggregate({
                where: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { pharmacyId }),
                    receivedDate: {
                        gte: startDate,
                        lte: endDate,
                    },
                },
                _count: {
                    id: true,
                },
            }),
            this.prisma.issueTransaction.aggregate({
                where: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { pharmacyId }),
                    issuedAt: {
                        gte: startDate,
                        lte: endDate,
                    },
                },
                _sum: {
                    totalAmount: true,
                },
                _count: {
                    id: true,
                },
            }),
            this.getTotalStockValue(hospitalId, pharmacyId),
        ]);
        const purchaseValue = await this.prisma.gRNItem.aggregate({
            where: {
                grn: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { pharmacyId }),
                    receivedDate: {
                        gte: startDate,
                        lte: endDate,
                    },
                },
            },
            _sum: {
                qtyReceived: true,
            },
        });
        return {
            purchases: {
                count: purchases._count.id,
                totalQuantity: purchaseValue._sum.qtyReceived || 0,
            },
            issues: {
                count: issues._count.id,
                totalValue: Number(issues._sum.totalAmount || 0),
            },
            currentStock: {
                totalValue: stockValue.totalValue,
                totalQuantity: stockValue.totalQuantity,
            },
        };
    }
    async getExpiryAnalysis(query) {
        const { hospitalId, pharmacyId } = query;
        const now = new Date();
        const categories = [
            { label: 'Expired', days: 0 },
            { label: 'Expiring in 30 days', days: 30 },
            { label: 'Expiring in 90 days', days: 90 },
            { label: 'Expiring in 180 days', days: 180 },
            { label: 'Expiring beyond 180 days', days: 365 },
        ];
        const expiryData = await Promise.all(categories.map(async (category, index) => {
            const startDays = index === 0 ? -9999 : categories[index - 1]?.days || 0;
            const endDays = category.days;
            const batches = await this.prisma.stockBatch.aggregate({
                where: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { pharmacyId }),
                    status: 'AVAILABLE',
                    qtyAvailable: {
                        gt: 0,
                    },
                    expiryDate: {
                        gte: new Date(now.getTime() + startDays * 24 * 60 * 60 * 1000),
                        lte: new Date(now.getTime() + endDays * 24 * 60 * 60 * 1000),
                    },
                },
                _sum: {
                    qtyAvailable: true,
                },
                _count: {
                    id: true,
                },
            });
            return {
                category: category.label,
                batchCount: batches._count.id,
                totalQuantity: batches._sum.qtyAvailable || 0,
            };
        }));
        return expiryData;
    }
    async getTransferMetrics(query) {
        const { hospitalId, pharmacyId, period } = query;
        const { startDate, endDate } = this.getDateRange(period, query.startDate, query.endDate);
        const transfers = await this.prisma.transferRequest.findMany({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && {
                    OR: [{ fromPharmacyId: pharmacyId }, { toPharmacyId: pharmacyId }],
                }),
                createdAt: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            select: {
                id: true,
                status: true,
                createdAt: true,
                approvedAt: true,
                dispatchedAt: true,
                receivedAt: true,
            },
        });
        const statusCounts = transfers.reduce((acc, transfer) => {
            acc[transfer.status] = (acc[transfer.status] || 0) + 1;
            return acc;
        }, {});
        const completedTransfers = transfers.filter((t) => t.status === 'RECEIVED');
        const avgTimes = completedTransfers.reduce((acc, transfer) => {
            if (transfer.approvedAt) {
                const approvalTime = transfer.approvedAt.getTime() - transfer.createdAt.getTime();
                acc.approvalTime += approvalTime;
                acc.approvalCount += 1;
            }
            if (transfer.dispatchedAt && transfer.approvedAt) {
                const dispatchTime = transfer.dispatchedAt.getTime() - transfer.approvedAt.getTime();
                acc.dispatchTime += dispatchTime;
                acc.dispatchCount += 1;
            }
            if (transfer.receivedAt && transfer.dispatchedAt) {
                const receiptTime = transfer.receivedAt.getTime() - transfer.dispatchedAt.getTime();
                acc.receiptTime += receiptTime;
                acc.receiptCount += 1;
            }
            if (transfer.receivedAt) {
                const totalTime = transfer.receivedAt.getTime() - transfer.createdAt.getTime();
                acc.totalTime += totalTime;
                acc.totalCount += 1;
            }
            return acc;
        }, {
            approvalTime: 0,
            approvalCount: 0,
            dispatchTime: 0,
            dispatchCount: 0,
            receiptTime: 0,
            receiptCount: 0,
            totalTime: 0,
            totalCount: 0,
        });
        return {
            statusBreakdown: statusCounts,
            totalTransfers: transfers.length,
            completionRate: ((completedTransfers.length / transfers.length) * 100).toFixed(2),
            averageTimes: {
                approval: avgTimes.approvalCount > 0
                    ? Math.round(avgTimes.approvalTime / avgTimes.approvalCount / (1000 * 60 * 60))
                    : 0,
                dispatch: avgTimes.dispatchCount > 0
                    ? Math.round(avgTimes.dispatchTime / avgTimes.dispatchCount / (1000 * 60 * 60))
                    : 0,
                receipt: avgTimes.receiptCount > 0
                    ? Math.round(avgTimes.receiptTime / avgTimes.receiptCount / (1000 * 60 * 60))
                    : 0,
                total: avgTimes.totalCount > 0
                    ? Math.round(avgTimes.totalTime / avgTimes.totalCount / (1000 * 60 * 60 * 24))
                    : 0,
            },
        };
    }
    async getTotalStockValue(hospitalId, pharmacyId) {
        const batches = await this.prisma.stockBatch.aggregate({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                status: 'AVAILABLE',
                qtyAvailable: {
                    gt: 0,
                },
            },
            _sum: {
                qtyAvailable: true,
            },
            _count: {
                id: true,
            },
        });
        const batchesWithPrice = await this.prisma.stockBatch.findMany({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                status: 'AVAILABLE',
                qtyAvailable: {
                    gt: 0,
                },
            },
            select: {
                qtyAvailable: true,
                purchasePrice: true,
            },
        });
        const totalValue = batchesWithPrice.reduce((sum, batch) => {
            return sum + batch.qtyAvailable * Number(batch.purchasePrice || 0);
        }, 0);
        return {
            totalQuantity: batches._sum.qtyAvailable || 0,
            totalValue: Number(totalValue.toFixed(2)),
            batchCount: batches._count.id,
        };
    }
    async getTotalPatients(hospitalId, startDate, endDate) {
        const patients = await this.prisma.patient.aggregate({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(startDate &&
                    endDate && {
                    registeredAt: {
                        gte: startDate,
                        lte: endDate,
                    },
                }),
            },
            _count: {
                id: true,
            },
        });
        return {
            count: patients._count.id,
        };
    }
    async getTotalIssuances(hospitalId, pharmacyId, startDate, endDate) {
        const issues = await this.prisma.issueTransaction.aggregate({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                ...(startDate &&
                    endDate && {
                    issuedAt: {
                        gte: startDate,
                        lte: endDate,
                    },
                }),
            },
            _sum: {
                totalAmount: true,
            },
            _count: {
                id: true,
            },
        });
        return {
            count: issues._count.id,
            totalValue: Number(issues._sum.totalAmount || 0),
        };
    }
    async getTotalTransfers(hospitalId, pharmacyId, startDate, endDate) {
        const [sent, received] = await Promise.all([
            this.prisma.transferRequest.count({
                where: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { fromPharmacyId: pharmacyId }),
                    ...(startDate &&
                        endDate && {
                        createdAt: {
                            gte: startDate,
                            lte: endDate,
                        },
                    }),
                },
            }),
            this.prisma.transferRequest.count({
                where: {
                    ...(hospitalId && { hospitalId }),
                    ...(pharmacyId && { toPharmacyId: pharmacyId }),
                    ...(startDate &&
                        endDate && {
                        createdAt: {
                            gte: startDate,
                            lte: endDate,
                        },
                    }),
                },
            }),
        ]);
        return { sent, received };
    }
    async getActiveAlerts(hospitalId, pharmacyId) {
        const alerts = await this.prisma.alert.aggregate({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                status: 'ACTIVE',
            },
            _count: {
                id: true,
            },
        });
        return {
            count: alerts._count.id,
        };
    }
    async getExpiringBatches(hospitalId, pharmacyId, daysThreshold) {
        const futureDate = new Date();
        futureDate.setDate(futureDate.getDate() + daysThreshold);
        const batches = await this.prisma.stockBatch.aggregate({
            where: {
                ...(hospitalId && { hospitalId }),
                ...(pharmacyId && { pharmacyId }),
                status: 'AVAILABLE',
                qtyAvailable: {
                    gt: 0,
                },
                expiryDate: {
                    lte: futureDate,
                },
            },
            _count: {
                id: true,
            },
            _sum: {
                qtyAvailable: true,
            },
        });
        return {
            count: batches._count.id,
            totalQuantity: batches._sum.qtyAvailable || 0,
        };
    }
    getDateRange(period, customStart, customEnd) {
        const now = new Date();
        let startDate;
        let endDate = now;
        switch (period) {
            case analytics_query_dto_1.TimePeriod.TODAY:
                startDate = new Date(now.setHours(0, 0, 0, 0));
                endDate = new Date(now.setHours(23, 59, 59, 999));
                break;
            case analytics_query_dto_1.TimePeriod.WEEK:
                startDate = new Date(now.setDate(now.getDate() - 7));
                break;
            case analytics_query_dto_1.TimePeriod.MONTH:
                startDate = new Date(now.setMonth(now.getMonth() - 1));
                break;
            case analytics_query_dto_1.TimePeriod.QUARTER:
                startDate = new Date(now.setMonth(now.getMonth() - 3));
                break;
            case analytics_query_dto_1.TimePeriod.YEAR:
                startDate = new Date(now.setFullYear(now.getFullYear() - 1));
                break;
            case analytics_query_dto_1.TimePeriod.CUSTOM:
                if (customStart && customEnd) {
                    startDate = new Date(customStart);
                    endDate = new Date(customEnd);
                }
                else {
                    startDate = new Date(now.setMonth(now.getMonth() - 1));
                }
                break;
            default:
                startDate = new Date(now.setMonth(now.getMonth() - 1));
        }
        return { startDate, endDate };
    }
    getPeriodLabel(period) {
        const labels = {
            [analytics_query_dto_1.TimePeriod.TODAY]: 'Today',
            [analytics_query_dto_1.TimePeriod.WEEK]: 'Last 7 Days',
            [analytics_query_dto_1.TimePeriod.MONTH]: 'Last 30 Days',
            [analytics_query_dto_1.TimePeriod.QUARTER]: 'Last 90 Days',
            [analytics_query_dto_1.TimePeriod.YEAR]: 'Last 365 Days',
            [analytics_query_dto_1.TimePeriod.CUSTOM]: 'Custom Period',
        };
        return labels[period] || 'Last 30 Days';
    }
    getCacheStats() {
        const stats = this.cacheService.getStats();
        return {
            ...stats,
            timestamp: new Date().toISOString(),
            uptime: process.uptime(),
        };
    }
    getSystemHealth() {
        const memoryUsage = process.memoryUsage();
        const cacheStats = this.cacheService.getStats();
        return {
            status: 'healthy',
            timestamp: new Date().toISOString(),
            uptime: process.uptime(),
            memory: {
                rss: `${(memoryUsage.rss / 1024 / 1024).toFixed(2)} MB`,
                heapTotal: `${(memoryUsage.heapTotal / 1024 / 1024).toFixed(2)} MB`,
                heapUsed: `${(memoryUsage.heapUsed / 1024 / 1024).toFixed(2)} MB`,
                external: `${(memoryUsage.external / 1024 / 1024).toFixed(2)} MB`,
            },
            cache: cacheStats,
            node: {
                version: process.version,
                platform: process.platform,
                arch: process.arch,
            },
        };
    }
};
exports.AnalyticsService = AnalyticsService;
exports.AnalyticsService = AnalyticsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof cache_service_1.CacheService !== "undefined" && cache_service_1.CacheService) === "function" ? _b : Object])
], AnalyticsService);


/***/ }),
/* 137 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalyticsQueryDto = exports.TimePeriod = void 0;
const class_validator_1 = __webpack_require__(35);
var TimePeriod;
(function (TimePeriod) {
    TimePeriod["TODAY"] = "today";
    TimePeriod["WEEK"] = "week";
    TimePeriod["MONTH"] = "month";
    TimePeriod["QUARTER"] = "quarter";
    TimePeriod["YEAR"] = "year";
    TimePeriod["CUSTOM"] = "custom";
})(TimePeriod || (exports.TimePeriod = TimePeriod = {}));
class AnalyticsQueryDto {
    constructor() {
        this.period = TimePeriod.MONTH;
    }
}
exports.AnalyticsQueryDto = AnalyticsQueryDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AnalyticsQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AnalyticsQueryDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(TimePeriod),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AnalyticsQueryDto.prototype, "period", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AnalyticsQueryDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AnalyticsQueryDto.prototype, "endDate", void 0);


/***/ }),
/* 138 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FeatureFlagsModule = void 0;
const common_1 = __webpack_require__(2);
const feature_flags_service_1 = __webpack_require__(139);
const feature_flags_controller_1 = __webpack_require__(140);
const database_module_1 = __webpack_require__(52);
const common_module_1 = __webpack_require__(10);
let FeatureFlagsModule = class FeatureFlagsModule {
};
exports.FeatureFlagsModule = FeatureFlagsModule;
exports.FeatureFlagsModule = FeatureFlagsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule, common_module_1.CommonModule],
        controllers: [feature_flags_controller_1.FeatureFlagsController],
        providers: [feature_flags_service_1.FeatureFlagsService],
        exports: [feature_flags_service_1.FeatureFlagsService],
    })
], FeatureFlagsModule);


/***/ }),
/* 139 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var FeatureFlagsService_1;
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FeatureFlagsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const redis_service_1 = __webpack_require__(12);
let FeatureFlagsService = FeatureFlagsService_1 = class FeatureFlagsService {
    constructor(prisma, redis) {
        this.prisma = prisma;
        this.redis = redis;
        this.logger = new common_1.Logger(FeatureFlagsService_1.name);
        this.CACHE_PREFIX = 'feature_flag:';
        this.CACHE_TTL = 300;
    }
    async create(dto) {
        const flag = await this.prisma.featureFlag.create({
            data: {
                hospitalId: dto.hospitalId || null,
                key: dto.key,
                enabled: dto.enabled,
                description: dto.description,
                metadata: dto.metadata || null,
            },
        });
        await this.invalidateCache(dto.key, dto.hospitalId);
        this.logger.log(`Feature flag created: ${dto.key} (enabled: ${dto.enabled})`);
        return flag;
    }
    async findAll(hospitalId) {
        const flags = await this.prisma.featureFlag.findMany({
            where: hospitalId
                ? {
                    OR: [
                        { hospitalId: hospitalId },
                        { hospitalId: null },
                    ],
                }
                : undefined,
            orderBy: { key: 'asc' },
        });
        return flags;
    }
    async findOne(id) {
        const flag = await this.prisma.featureFlag.findUnique({
            where: { id },
        });
        if (!flag) {
            throw new common_1.NotFoundException(`Feature flag with ID ${id} not found`);
        }
        return flag;
    }
    async update(id, dto) {
        const existingFlag = await this.findOne(id);
        const updated = await this.prisma.featureFlag.update({
            where: { id },
            data: {
                enabled: dto.enabled !== undefined ? dto.enabled : existingFlag.enabled,
                description: dto.description !== undefined ? dto.description : existingFlag.description,
                metadata: dto.metadata !== undefined ? dto.metadata : existingFlag.metadata,
            },
        });
        await this.invalidateCache(existingFlag.key, existingFlag.hospitalId);
        this.logger.log(`Feature flag updated: ${existingFlag.key} (enabled: ${updated.enabled})`);
        return updated;
    }
    async remove(id) {
        const flag = await this.findOne(id);
        await this.prisma.featureFlag.delete({
            where: { id },
        });
        await this.invalidateCache(flag.key, flag.hospitalId);
        this.logger.log(`Feature flag deleted: ${flag.key}`);
    }
    async isEnabled(key, hospitalId) {
        const cacheKey = this.getCacheKey(key, hospitalId);
        const cached = await this.redis.get(cacheKey);
        if (cached !== null) {
            return cached;
        }
        let flag = null;
        if (hospitalId) {
            flag = await this.prisma.featureFlag.findUnique({
                where: {
                    hospitalId_key: {
                        hospitalId: hospitalId,
                        key: key,
                    },
                },
            });
        }
        if (!flag) {
            flag = await this.prisma.featureFlag.findUnique({
                where: {
                    hospitalId_key: {
                        hospitalId: null,
                        key: key,
                    },
                },
            });
        }
        const enabled = flag?.enabled ?? false;
        await this.redis.set(cacheKey, enabled, this.CACHE_TTL);
        return enabled;
    }
    getCacheKey(key, hospitalId) {
        return `${this.CACHE_PREFIX}${key}:${hospitalId || 'global'}`;
    }
    async invalidateCache(key, hospitalId) {
        const keys = [
            this.getCacheKey(key, hospitalId || undefined),
            this.getCacheKey(key, undefined),
        ];
        await Promise.all(keys.map((k) => this.redis.del(k)));
        this.logger.debug(`Cache invalidated for feature flag: ${key}`);
    }
};
exports.FeatureFlagsService = FeatureFlagsService;
exports.FeatureFlagsService = FeatureFlagsService = FeatureFlagsService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof redis_service_1.RedisService !== "undefined" && redis_service_1.RedisService) === "function" ? _b : Object])
], FeatureFlagsService);


/***/ }),
/* 140 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FeatureFlagsController = void 0;
const common_1 = __webpack_require__(2);
const feature_flags_service_1 = __webpack_require__(139);
const create_feature_flag_dto_1 = __webpack_require__(141);
const update_feature_flag_dto_1 = __webpack_require__(142);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let FeatureFlagsController = class FeatureFlagsController {
    constructor(featureFlagsService) {
        this.featureFlagsService = featureFlagsService;
    }
    create(createFeatureFlagDto) {
        return this.featureFlagsService.create(createFeatureFlagDto);
    }
    findAll(hospitalId) {
        return this.featureFlagsService.findAll(hospitalId);
    }
    async checkFlag(key, hospitalId) {
        const enabled = await this.featureFlagsService.isEnabled(key, hospitalId);
        return { key, enabled };
    }
    findOne(id) {
        return this.featureFlagsService.findOne(id);
    }
    update(id, updateFeatureFlagDto) {
        return this.featureFlagsService.update(id, updateFeatureFlagDto);
    }
    remove(id) {
        return this.featureFlagsService.remove(id);
    }
};
exports.FeatureFlagsController = FeatureFlagsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_feature_flag_dto_1.CreateFeatureFlagDto !== "undefined" && create_feature_flag_dto_1.CreateFeatureFlagDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], FeatureFlagsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    __param(0, (0, common_1.Query)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], FeatureFlagsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('check/:key'),
    __param(0, (0, common_1.Param)('key')),
    __param(1, (0, common_1.Query)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], FeatureFlagsController.prototype, "checkFlag", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], FeatureFlagsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof update_feature_flag_dto_1.UpdateFeatureFlagDto !== "undefined" && update_feature_flag_dto_1.UpdateFeatureFlagDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], FeatureFlagsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], FeatureFlagsController.prototype, "remove", null);
exports.FeatureFlagsController = FeatureFlagsController = __decorate([
    (0, common_1.Controller)('feature-flags'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof feature_flags_service_1.FeatureFlagsService !== "undefined" && feature_flags_service_1.FeatureFlagsService) === "function" ? _a : Object])
], FeatureFlagsController);


/***/ }),
/* 141 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateFeatureFlagDto = void 0;
const class_validator_1 = __webpack_require__(35);
class CreateFeatureFlagDto {
}
exports.CreateFeatureFlagDto = CreateFeatureFlagDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateFeatureFlagDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateFeatureFlagDto.prototype, "key", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], CreateFeatureFlagDto.prototype, "enabled", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateFeatureFlagDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof Record !== "undefined" && Record) === "function" ? _a : Object)
], CreateFeatureFlagDto.prototype, "metadata", void 0);


/***/ }),
/* 142 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateFeatureFlagDto = void 0;
const class_validator_1 = __webpack_require__(35);
class UpdateFeatureFlagDto {
}
exports.UpdateFeatureFlagDto = UpdateFeatureFlagDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], UpdateFeatureFlagDto.prototype, "enabled", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateFeatureFlagDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof Record !== "undefined" && Record) === "function" ? _a : Object)
], UpdateFeatureFlagDto.prototype, "metadata", void 0);


/***/ }),
/* 143 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DepartmentsModule = void 0;
const common_1 = __webpack_require__(2);
const departments_service_1 = __webpack_require__(144);
const departments_controller_1 = __webpack_require__(145);
const database_module_1 = __webpack_require__(52);
let DepartmentsModule = class DepartmentsModule {
};
exports.DepartmentsModule = DepartmentsModule;
exports.DepartmentsModule = DepartmentsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [departments_controller_1.DepartmentsController],
        providers: [departments_service_1.DepartmentsService],
        exports: [departments_service_1.DepartmentsService],
    })
], DepartmentsModule);


/***/ }),
/* 144 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DepartmentsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let DepartmentsService = class DepartmentsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(hospitalId, createDepartmentDto, user) {
        const existing = await this.prisma.department.findUnique({
            where: {
                hospitalId_code: {
                    hospitalId,
                    code: createDepartmentDto.code,
                },
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Department with code '${createDepartmentDto.code}' already exists in this hospital`);
        }
        if ((user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) && user.hospitalId !== hospitalId) {
            throw new common_1.ForbiddenException('You can only create departments in your own hospital');
        }
        return this.prisma.department.create({
            data: {
                hospitalId,
                ...createDepartmentDto,
                status: createDepartmentDto.status || client_1.DepartmentStatus.ACTIVE,
            },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                subDepartments: true,
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
        });
    }
    async findAll(user) {
        const where = {};
        if (user.role === client_1.UserRole.MASTER_ADMIN) {
        }
        else if (user.role === client_1.UserRole.SUPER_ADMIN) {
        }
        else if (user.role === client_1.UserRole.HOSPITAL_ADMIN || user.role === client_1.UserRole.MAIN_PHARMACY_MANAGER) {
            where.hospitalId = user.hospitalId;
        }
        else if (user.role === client_1.UserRole.DEPARTMENT_ADMIN) {
            where.id = user.managedDepartmentId;
        }
        else {
            where.hospitalId = user.hospitalId;
        }
        return this.prisma.department.findMany({
            where,
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
            orderBy: {
                name: 'asc',
            },
        });
    }
    async findOne(id, user) {
        const department = await this.prisma.department.findUnique({
            where: { id },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                subDepartments: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        status: true,
                    },
                },
                users: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        role: true,
                    },
                },
                departmentAdmins: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${id} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) {
            if (user.hospitalId !== department.hospitalId) {
                throw new common_1.ForbiddenException('You do not have access to this department');
            }
            if (user.role === client_1.UserRole.DEPARTMENT_ADMIN && user.managedDepartmentId !== department.id) {
                throw new common_1.ForbiddenException('You can only access your own department');
            }
        }
        return department;
    }
    async update(id, updateDepartmentDto, user) {
        const department = await this.prisma.department.findUnique({
            where: { id },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${id} not found`);
        }
        const canUpdateAny = user.role === client_1.UserRole.MASTER_ADMIN || user.role === client_1.UserRole.SUPER_ADMIN;
        const canUpdateOwnHospital = user.role === client_1.UserRole.HOSPITAL_ADMIN && user.hospitalId === department.hospitalId;
        if (!canUpdateAny && !canUpdateOwnHospital) {
            throw new common_1.ForbiddenException('You do not have permission to update this department');
        }
        if (updateDepartmentDto.code && updateDepartmentDto.code !== department.code) {
            const existing = await this.prisma.department.findUnique({
                where: {
                    hospitalId_code: {
                        hospitalId: department.hospitalId,
                        code: updateDepartmentDto.code,
                    },
                },
            });
            if (existing) {
                throw new common_1.ConflictException(`Department with code '${updateDepartmentDto.code}' already exists in this hospital`);
            }
        }
        return this.prisma.department.update({
            where: { id },
            data: updateDepartmentDto,
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                subDepartments: true,
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
        });
    }
    async remove(id, user) {
        const department = await this.prisma.department.findUnique({
            where: { id },
            include: {
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${id} not found`);
        }
        const canDeleteAny = user.role === client_1.UserRole.MASTER_ADMIN || user.role === client_1.UserRole.SUPER_ADMIN;
        const canDeleteOwnHospital = user.role === client_1.UserRole.HOSPITAL_ADMIN && user.hospitalId === department.hospitalId;
        if (!canDeleteAny && !canDeleteOwnHospital) {
            throw new common_1.ForbiddenException('You do not have permission to delete this department');
        }
        if (department._count.users > 0) {
            throw new common_1.ConflictException('Cannot delete department with assigned users. Please reassign users first.');
        }
        if (department._count.subDepartments > 0) {
            throw new common_1.ConflictException('Cannot delete department with sub-departments. Please delete sub-departments first.');
        }
        await this.prisma.department.delete({
            where: { id },
        });
        return { message: 'Department deleted successfully' };
    }
    async findByHospital(hospitalId, user) {
        if (user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) {
            if (user.hospitalId !== hospitalId) {
                throw new common_1.ForbiddenException('You do not have access to this hospital');
            }
        }
        return this.prisma.department.findMany({
            where: { hospitalId },
            include: {
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                        subDepartments: true,
                    },
                },
            },
            orderBy: {
                name: 'asc',
            },
        });
    }
};
exports.DepartmentsService = DepartmentsService;
exports.DepartmentsService = DepartmentsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], DepartmentsService);


/***/ }),
/* 145 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DepartmentsController = void 0;
const common_1 = __webpack_require__(2);
const departments_service_1 = __webpack_require__(144);
const create_department_dto_1 = __webpack_require__(146);
const update_department_dto_1 = __webpack_require__(147);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let DepartmentsController = class DepartmentsController {
    constructor(departmentsService) {
        this.departmentsService = departmentsService;
    }
    create(hospitalId, createDepartmentDto, req) {
        return this.departmentsService.create(hospitalId, createDepartmentDto, req.user);
    }
    findAll(hospitalId, req) {
        if (hospitalId) {
            return this.departmentsService.findByHospital(hospitalId, req.user);
        }
        return this.departmentsService.findAll(req.user);
    }
    findByHospital(hospitalId, req) {
        return this.departmentsService.findByHospital(hospitalId, req.user);
    }
    findOne(id, req) {
        return this.departmentsService.findOne(id, req.user);
    }
    update(id, updateDepartmentDto, req) {
        return this.departmentsService.update(id, updateDepartmentDto, req.user);
    }
    remove(id, req) {
        return this.departmentsService.remove(id, req.user);
    }
};
exports.DepartmentsController = DepartmentsController;
__decorate([
    (0, common_1.Post)('hospital/:hospitalId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof create_department_dto_1.CreateDepartmentDto !== "undefined" && create_department_dto_1.CreateDepartmentDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('hospital/:hospitalId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "findByHospital", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof update_department_dto_1.UpdateDepartmentDto !== "undefined" && update_department_dto_1.UpdateDepartmentDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], DepartmentsController.prototype, "remove", null);
exports.DepartmentsController = DepartmentsController = __decorate([
    (0, common_1.Controller)('departments'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof departments_service_1.DepartmentsService !== "undefined" && departments_service_1.DepartmentsService) === "function" ? _a : Object])
], DepartmentsController);


/***/ }),
/* 146 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateDepartmentDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateDepartmentDto {
}
exports.CreateDepartmentDto = CreateDepartmentDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MaxLength)(100),
    __metadata("design:type", String)
], CreateDepartmentDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MaxLength)(20),
    __metadata("design:type", String)
], CreateDepartmentDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.MaxLength)(500),
    __metadata("design:type", String)
], CreateDepartmentDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.DepartmentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.DepartmentStatus !== "undefined" && client_1.DepartmentStatus) === "function" ? _a : Object)
], CreateDepartmentDto.prototype, "status", void 0);


/***/ }),
/* 147 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateDepartmentDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_department_dto_1 = __webpack_require__(146);
class UpdateDepartmentDto extends (0, mapped_types_1.PartialType)(create_department_dto_1.CreateDepartmentDto) {
}
exports.UpdateDepartmentDto = UpdateDepartmentDto;


/***/ }),
/* 148 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SubDepartmentsModule = void 0;
const common_1 = __webpack_require__(2);
const sub_departments_service_1 = __webpack_require__(149);
const sub_departments_controller_1 = __webpack_require__(150);
const database_module_1 = __webpack_require__(52);
let SubDepartmentsModule = class SubDepartmentsModule {
};
exports.SubDepartmentsModule = SubDepartmentsModule;
exports.SubDepartmentsModule = SubDepartmentsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [sub_departments_controller_1.SubDepartmentsController],
        providers: [sub_departments_service_1.SubDepartmentsService],
        exports: [sub_departments_service_1.SubDepartmentsService],
    })
], SubDepartmentsModule);


/***/ }),
/* 149 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SubDepartmentsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let SubDepartmentsService = class SubDepartmentsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(departmentId, createSubDepartmentDto, user) {
        const department = await this.prisma.department.findUnique({
            where: { id: departmentId },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${departmentId} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) {
            if (user.hospitalId !== department.hospitalId) {
                throw new common_1.ForbiddenException('You can only create sub-departments in your own hospital');
            }
            if (user.role === client_1.UserRole.DEPARTMENT_ADMIN && user.managedDepartmentId !== departmentId) {
                throw new common_1.ForbiddenException('You can only create sub-departments in your own department');
            }
        }
        const existing = await this.prisma.subDepartment.findUnique({
            where: {
                departmentId_code: {
                    departmentId,
                    code: createSubDepartmentDto.code,
                },
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Sub-department with code '${createSubDepartmentDto.code}' already exists in this department`);
        }
        return this.prisma.subDepartment.create({
            data: {
                departmentId,
                ...createSubDepartmentDto,
                status: createSubDepartmentDto.status || client_1.SubDepartmentStatus.ACTIVE,
            },
            include: {
                department: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        hospitalId: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
        });
    }
    async findAll(user) {
        const where = {};
        if (user.role === client_1.UserRole.MASTER_ADMIN || user.role === client_1.UserRole.SUPER_ADMIN) {
        }
        else if (user.role === client_1.UserRole.HOSPITAL_ADMIN) {
            where.department = {
                hospitalId: user.hospitalId,
            };
        }
        else if (user.role === client_1.UserRole.DEPARTMENT_ADMIN) {
            where.departmentId = user.managedDepartmentId;
        }
        else {
            where.department = {
                hospitalId: user.hospitalId,
            };
        }
        return this.prisma.subDepartment.findMany({
            where,
            include: {
                department: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        hospitalId: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
            orderBy: {
                name: 'asc',
            },
        });
    }
    async findByDepartment(departmentId, user) {
        const department = await this.prisma.department.findUnique({
            where: { id: departmentId },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${departmentId} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) {
            if (user.hospitalId !== department.hospitalId) {
                throw new common_1.ForbiddenException('You do not have access to this department');
            }
            if (user.role === client_1.UserRole.DEPARTMENT_ADMIN && user.managedDepartmentId !== departmentId) {
                throw new common_1.ForbiddenException('You can only access sub-departments in your own department');
            }
        }
        return this.prisma.subDepartment.findMany({
            where: { departmentId },
            include: {
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
            orderBy: {
                name: 'asc',
            },
        });
    }
    async findOne(id, user) {
        const subDepartment = await this.prisma.subDepartment.findUnique({
            where: { id },
            include: {
                department: {
                    include: {
                        hospital: {
                            select: {
                                id: true,
                                name: true,
                                code: true,
                            },
                        },
                    },
                },
                users: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        role: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
        });
        if (!subDepartment) {
            throw new common_1.NotFoundException(`Sub-department with ID ${id} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN && user.role !== client_1.UserRole.SUPER_ADMIN) {
            if (user.hospitalId !== subDepartment.department.hospitalId) {
                throw new common_1.ForbiddenException('You do not have access to this sub-department');
            }
            if (user.role === client_1.UserRole.DEPARTMENT_ADMIN && user.managedDepartmentId !== subDepartment.departmentId) {
                throw new common_1.ForbiddenException('You can only access sub-departments in your own department');
            }
        }
        return subDepartment;
    }
    async update(id, updateSubDepartmentDto, user) {
        const subDepartment = await this.prisma.subDepartment.findUnique({
            where: { id },
        });
        if (!subDepartment) {
            throw new common_1.NotFoundException(`Sub-department with ID ${id} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN &&
            user.role !== client_1.UserRole.SUPER_ADMIN &&
            user.role !== client_1.UserRole.HOSPITAL_ADMIN) {
            throw new common_1.ForbiddenException('Only MASTER_ADMIN, SUPER_ADMIN, or HOSPITAL_ADMIN can update sub-departments');
        }
        if (updateSubDepartmentDto.code && updateSubDepartmentDto.code !== subDepartment.code) {
            const existing = await this.prisma.subDepartment.findUnique({
                where: {
                    departmentId_code: {
                        departmentId: subDepartment.departmentId,
                        code: updateSubDepartmentDto.code,
                    },
                },
            });
            if (existing) {
                throw new common_1.ConflictException(`Sub-department with code '${updateSubDepartmentDto.code}' already exists in this department`);
            }
        }
        return this.prisma.subDepartment.update({
            where: { id },
            data: updateSubDepartmentDto,
            include: {
                department: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        hospitalId: true,
                    },
                },
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
        });
    }
    async remove(id, user) {
        const subDepartment = await this.prisma.subDepartment.findUnique({
            where: { id },
            include: {
                _count: {
                    select: {
                        users: true,
                    },
                },
            },
        });
        if (!subDepartment) {
            throw new common_1.NotFoundException(`Sub-department with ID ${id} not found`);
        }
        if (user.role !== client_1.UserRole.MASTER_ADMIN &&
            user.role !== client_1.UserRole.SUPER_ADMIN &&
            user.role !== client_1.UserRole.HOSPITAL_ADMIN) {
            throw new common_1.ForbiddenException('Only MASTER_ADMIN, SUPER_ADMIN, or HOSPITAL_ADMIN can delete sub-departments');
        }
        if (subDepartment._count.users > 0) {
            throw new common_1.ConflictException('Cannot delete sub-department with assigned users. Please reassign users first.');
        }
        await this.prisma.subDepartment.delete({
            where: { id },
        });
        return { message: 'Sub-department deleted successfully' };
    }
};
exports.SubDepartmentsService = SubDepartmentsService;
exports.SubDepartmentsService = SubDepartmentsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], SubDepartmentsService);


/***/ }),
/* 150 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SubDepartmentsController = void 0;
const common_1 = __webpack_require__(2);
const sub_departments_service_1 = __webpack_require__(149);
const create_sub_department_dto_1 = __webpack_require__(151);
const update_sub_department_dto_1 = __webpack_require__(152);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const roles_decorator_1 = __webpack_require__(32);
const client_1 = __webpack_require__(16);
let SubDepartmentsController = class SubDepartmentsController {
    constructor(subDepartmentsService) {
        this.subDepartmentsService = subDepartmentsService;
    }
    create(departmentId, createSubDepartmentDto, req) {
        return this.subDepartmentsService.create(departmentId, createSubDepartmentDto, req.user);
    }
    findAll(req) {
        return this.subDepartmentsService.findAll(req.user);
    }
    findByDepartment(departmentId, req) {
        return this.subDepartmentsService.findByDepartment(departmentId, req.user);
    }
    findOne(id, req) {
        return this.subDepartmentsService.findOne(id, req.user);
    }
    update(id, updateSubDepartmentDto, req) {
        return this.subDepartmentsService.update(id, updateSubDepartmentDto, req.user);
    }
    remove(id, req) {
        return this.subDepartmentsService.remove(id, req.user);
    }
};
exports.SubDepartmentsController = SubDepartmentsController;
__decorate([
    (0, common_1.Post)('department/:departmentId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    __param(0, (0, common_1.Param)('departmentId')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof create_sub_department_dto_1.CreateSubDepartmentDto !== "undefined" && create_sub_department_dto_1.CreateSubDepartmentDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE),
    __param(0, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('department/:departmentId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    __param(0, (0, common_1.Param)('departmentId')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "findByDepartment", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof update_sub_department_dto_1.UpdateSubDepartmentDto !== "undefined" && update_sub_department_dto_1.UpdateSubDepartmentDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", void 0)
], SubDepartmentsController.prototype, "remove", null);
exports.SubDepartmentsController = SubDepartmentsController = __decorate([
    (0, common_1.Controller)('sub-departments'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof sub_departments_service_1.SubDepartmentsService !== "undefined" && sub_departments_service_1.SubDepartmentsService) === "function" ? _a : Object])
], SubDepartmentsController);


/***/ }),
/* 151 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateSubDepartmentDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateSubDepartmentDto {
}
exports.CreateSubDepartmentDto = CreateSubDepartmentDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MaxLength)(100),
    __metadata("design:type", String)
], CreateSubDepartmentDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MaxLength)(20),
    __metadata("design:type", String)
], CreateSubDepartmentDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.MaxLength)(500),
    __metadata("design:type", String)
], CreateSubDepartmentDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.SubDepartmentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.SubDepartmentStatus !== "undefined" && client_1.SubDepartmentStatus) === "function" ? _a : Object)
], CreateSubDepartmentDto.prototype, "status", void 0);


/***/ }),
/* 152 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateSubDepartmentDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_sub_department_dto_1 = __webpack_require__(151);
class UpdateSubDepartmentDto extends (0, mapped_types_1.PartialType)(create_sub_department_dto_1.CreateSubDepartmentDto) {
}
exports.UpdateSubDepartmentDto = UpdateSubDepartmentDto;


/***/ }),
/* 153 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ClinicsModule = void 0;
const common_1 = __webpack_require__(2);
const clinics_controller_1 = __webpack_require__(154);
const clinics_service_1 = __webpack_require__(155);
const database_module_1 = __webpack_require__(52);
let ClinicsModule = class ClinicsModule {
};
exports.ClinicsModule = ClinicsModule;
exports.ClinicsModule = ClinicsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [clinics_controller_1.ClinicsController],
        providers: [clinics_service_1.ClinicsService],
        exports: [clinics_service_1.ClinicsService],
    })
], ClinicsModule);


/***/ }),
/* 154 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ClinicsController = void 0;
const common_1 = __webpack_require__(2);
const clinics_service_1 = __webpack_require__(155);
const dto_1 = __webpack_require__(156);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let ClinicsController = class ClinicsController {
    constructor(clinicsService) {
        this.clinicsService = clinicsService;
    }
    async create(createClinicDto) {
        return this.clinicsService.create(createClinicDto);
    }
    async findAll(query) {
        return this.clinicsService.findAll(query);
    }
    async findAvailable(hospitalId, departmentId) {
        return this.clinicsService.findAvailable(hospitalId, departmentId);
    }
    async findByDoctor(doctorId) {
        return this.clinicsService.findByDoctor(doctorId);
    }
    async findByDepartment(departmentId) {
        return this.clinicsService.findByDepartment(departmentId);
    }
    async findOne(id) {
        return this.clinicsService.findOne(id);
    }
    async getStats(id) {
        return this.clinicsService.getClinicStats(id);
    }
    async getDoctors(hospitalId) {
        return this.clinicsService.getDoctors(hospitalId);
    }
    async update(id, updateClinicDto) {
        return this.clinicsService.update(id, updateClinicDto);
    }
    async remove(id) {
        return this.clinicsService.remove(id);
    }
};
exports.ClinicsController = ClinicsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new clinic' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Clinic created successfully' }),
    (0, swagger_1.ApiResponse)({ status: 400, description: 'Invalid input' }),
    (0, swagger_1.ApiResponse)({ status: 409, description: 'Clinic already exists' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateClinicDto !== "undefined" && dto_1.CreateClinicDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get all clinics with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of clinics' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.ClinicQueryDto !== "undefined" && dto_1.ClinicQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('available'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get available clinics for today' }),
    (0, swagger_1.ApiQuery)({ name: 'hospitalId', required: true }),
    (0, swagger_1.ApiQuery)({ name: 'departmentId', required: false }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of available clinics' }),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('departmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "findAvailable", null);
__decorate([
    (0, common_1.Get)('doctor/:doctorId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get clinics by doctor' }),
    (0, swagger_1.ApiParam)({ name: 'doctorId', description: 'Doctor ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of doctor clinics' }),
    __param(0, (0, common_1.Param)('doctorId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "findByDoctor", null);
__decorate([
    (0, common_1.Get)('department/:departmentId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get clinics by department' }),
    (0, swagger_1.ApiParam)({ name: 'departmentId', description: 'Department ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of department clinics' }),
    __param(0, (0, common_1.Param)('departmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "findByDepartment", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get clinic details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Clinic details' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Clinic not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Get)(':id/stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST),
    (0, swagger_1.ApiOperation)({ summary: 'Get clinic statistics for today' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Clinic statistics' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)('doctors/hospital/:hospitalId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    (0, swagger_1.ApiOperation)({ summary: 'Get doctors for hospital' }),
    (0, swagger_1.ApiParam)({ name: 'hospitalId', description: 'Hospital ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of doctors' }),
    __param(0, (0, common_1.Param)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "getDoctors", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, common_1.Patch)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    (0, swagger_1.ApiOperation)({ summary: 'Update clinic' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Clinic updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Clinic not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof dto_1.UpdateClinicDto !== "undefined" && dto_1.UpdateClinicDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Delete clinic' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Clinic deleted successfully' }),
    (0, swagger_1.ApiResponse)({ status: 400, description: 'Cannot delete clinic with visits' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Clinic not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ClinicsController.prototype, "remove", null);
exports.ClinicsController = ClinicsController = __decorate([
    (0, swagger_1.ApiTags)('Clinics'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('clinics'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof clinics_service_1.ClinicsService !== "undefined" && clinics_service_1.ClinicsService) === "function" ? _a : Object])
], ClinicsController);


/***/ }),
/* 155 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ClinicsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let ClinicsService = class ClinicsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createClinicDto) {
        const hospital = await this.prisma.hospital.findUnique({
            where: { id: createClinicDto.hospitalId },
        });
        if (!hospital) {
            throw new common_1.NotFoundException(`Hospital with ID ${createClinicDto.hospitalId} not found`);
        }
        const department = await this.prisma.department.findFirst({
            where: {
                id: createClinicDto.departmentId,
                hospitalId: createClinicDto.hospitalId,
            },
        });
        if (!department) {
            throw new common_1.NotFoundException(`Department with ID ${createClinicDto.departmentId} not found in this hospital`);
        }
        const doctor = await this.prisma.user.findFirst({
            where: {
                id: createClinicDto.doctorId,
                hospitalId: createClinicDto.hospitalId,
                role: client_1.UserRole.DOCTOR,
            },
        });
        if (!doctor) {
            throw new common_1.BadRequestException(`User with ID ${createClinicDto.doctorId} is not a valid doctor in this hospital`);
        }
        const existingClinic = await this.prisma.clinic.findFirst({
            where: {
                hospitalId: createClinicDto.hospitalId,
                departmentId: createClinicDto.departmentId,
                doctorId: createClinicDto.doctorId,
                name: createClinicDto.name,
            },
        });
        if (existingClinic) {
            throw new common_1.ConflictException(`A clinic with the same name already exists for this doctor in this department`);
        }
        return this.prisma.clinic.create({
            data: {
                hospitalId: createClinicDto.hospitalId,
                departmentId: createClinicDto.departmentId,
                doctorId: createClinicDto.doctorId,
                name: createClinicDto.name,
                opdFee: createClinicDto.opdFee,
                availableDays: createClinicDto.availableDays || [],
                availableFrom: createClinicDto.availableFrom,
                availableTo: createClinicDto.availableTo,
                maxPatientsPerDay: createClinicDto.maxPatientsPerDay,
                status: createClinicDto.status || client_1.ClinicStatus.ACTIVE,
            },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                doctor: { select: { id: true, fullName: true, email: true } },
            },
        });
    }
    async findAll(query) {
        const { hospitalId, departmentId, doctorId, status, page = 1, limit = 10 } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (departmentId)
            where.departmentId = departmentId;
        if (doctorId)
            where.doctorId = doctorId;
        if (status)
            where.status = status;
        const [clinics, total] = await Promise.all([
            this.prisma.clinic.findMany({
                where,
                include: {
                    hospital: { select: { id: true, name: true } },
                    department: { select: { id: true, name: true } },
                    doctor: { select: { id: true, fullName: true, email: true } },
                    _count: {
                        select: {
                            visits: true,
                            tokens: true,
                        },
                    },
                },
                orderBy: { createdAt: 'desc' },
                skip: (page - 1) * limit,
                take: limit,
            }),
            this.prisma.clinic.count({ where }),
        ]);
        return {
            data: clinics,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const clinic = await this.prisma.clinic.findUnique({
            where: { id },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                doctor: { select: { id: true, fullName: true, email: true, phone: true } },
                _count: {
                    select: {
                        visits: true,
                        tokens: true,
                    },
                },
            },
        });
        if (!clinic) {
            throw new common_1.NotFoundException(`Clinic with ID ${id} not found`);
        }
        return clinic;
    }
    async update(id, updateClinicDto) {
        await this.findOne(id);
        if (updateClinicDto.doctorId) {
            const doctor = await this.prisma.user.findFirst({
                where: {
                    id: updateClinicDto.doctorId,
                    role: client_1.UserRole.DOCTOR,
                },
            });
            if (!doctor) {
                throw new common_1.BadRequestException(`User with ID ${updateClinicDto.doctorId} is not a valid doctor`);
            }
        }
        return this.prisma.clinic.update({
            where: { id },
            data: updateClinicDto,
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                doctor: { select: { id: true, fullName: true, email: true } },
            },
        });
    }
    async remove(id) {
        await this.findOne(id);
        const visitsCount = await this.prisma.visit.count({
            where: { clinicId: id },
        });
        if (visitsCount > 0) {
            throw new common_1.BadRequestException(`Cannot delete clinic with existing visits. Consider setting status to INACTIVE instead.`);
        }
        return this.prisma.clinic.delete({
            where: { id },
        });
    }
    async findByDoctor(doctorId) {
        return this.prisma.clinic.findMany({
            where: {
                doctorId,
                status: client_1.ClinicStatus.ACTIVE,
            },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                _count: {
                    select: {
                        visits: true,
                    },
                },
            },
            orderBy: { name: 'asc' },
        });
    }
    async findByDepartment(departmentId) {
        return this.prisma.clinic.findMany({
            where: {
                departmentId,
                status: client_1.ClinicStatus.ACTIVE,
            },
            include: {
                doctor: { select: { id: true, fullName: true, email: true } },
                _count: {
                    select: {
                        visits: true,
                    },
                },
            },
            orderBy: { name: 'asc' },
        });
    }
    async findAvailable(hospitalId, departmentId) {
        const today = new Date();
        const days = ['SUNDAY', 'MONDAY', 'TUESDAY', 'WEDNESDAY', 'THURSDAY', 'FRIDAY', 'SATURDAY'];
        const dayOfWeek = days[today.getDay()];
        const where = {
            hospitalId,
            status: client_1.ClinicStatus.ACTIVE,
        };
        if (departmentId) {
            where.departmentId = departmentId;
        }
        where.availableDays = { has: dayOfWeek };
        return this.prisma.clinic.findMany({
            where,
            include: {
                department: { select: { id: true, name: true } },
                doctor: { select: { id: true, fullName: true } },
            },
            orderBy: [
                { department: { name: 'asc' } },
                { name: 'asc' },
            ],
        });
    }
    async getClinicStats(id) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const tomorrow = new Date(today);
        tomorrow.setDate(tomorrow.getDate() + 1);
        const clinic = await this.findOne(id);
        const [todayVisits, waitingCount, completedCount] = await Promise.all([
            this.prisma.visit.count({
                where: {
                    clinicId: id,
                    visitDate: { gte: today, lt: tomorrow },
                },
            }),
            this.prisma.visit.count({
                where: {
                    clinicId: id,
                    visitDate: { gte: today, lt: tomorrow },
                    status: 'WAITING',
                },
            }),
            this.prisma.visit.count({
                where: {
                    clinicId: id,
                    visitDate: { gte: today, lt: tomorrow },
                    status: 'COMPLETED',
                },
            }),
        ]);
        return {
            clinic,
            stats: {
                todayVisits,
                waitingCount,
                completedCount,
                availableSlots: clinic.maxPatientsPerDay
                    ? Math.max(0, clinic.maxPatientsPerDay - todayVisits)
                    : null,
            },
        };
    }
    async getDoctors(hospitalId) {
        return this.prisma.user.findMany({
            where: {
                hospitalId,
                role: client_1.UserRole.DOCTOR,
                status: 'ACTIVE',
            },
            select: {
                id: true,
                fullName: true,
                email: true,
            },
            orderBy: { fullName: 'asc' },
        });
    }
};
exports.ClinicsService = ClinicsService;
exports.ClinicsService = ClinicsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ClinicsService);


/***/ }),
/* 156 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ClinicQueryDto = exports.UpdateClinicDto = exports.CreateClinicDto = void 0;
var create_clinic_dto_1 = __webpack_require__(157);
Object.defineProperty(exports, "CreateClinicDto", ({ enumerable: true, get: function () { return create_clinic_dto_1.CreateClinicDto; } }));
var update_clinic_dto_1 = __webpack_require__(158);
Object.defineProperty(exports, "UpdateClinicDto", ({ enumerable: true, get: function () { return update_clinic_dto_1.UpdateClinicDto; } }));
var clinic_query_dto_1 = __webpack_require__(159);
Object.defineProperty(exports, "ClinicQueryDto", ({ enumerable: true, get: function () { return clinic_query_dto_1.ClinicQueryDto; } }));


/***/ }),
/* 157 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateClinicDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class CreateClinicDto {
}
exports.CreateClinicDto = CreateClinicDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "doctorId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateClinicDto.prototype, "opdFee", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Array)
], CreateClinicDto.prototype, "availableDays", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^([01]?[0-9]|2[0-3]):[0-5][0-9]$/, {
        message: 'availableFrom must be in HH:mm format',
    }),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "availableFrom", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^([01]?[0-9]|2[0-3]):[0-5][0-9]$/, {
        message: 'availableTo must be in HH:mm format',
    }),
    __metadata("design:type", String)
], CreateClinicDto.prototype, "availableTo", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateClinicDto.prototype, "maxPatientsPerDay", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ClinicStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.ClinicStatus !== "undefined" && client_1.ClinicStatus) === "function" ? _a : Object)
], CreateClinicDto.prototype, "status", void 0);


/***/ }),
/* 158 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateClinicDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_clinic_dto_1 = __webpack_require__(157);
class UpdateClinicDto extends (0, mapped_types_1.PartialType)(create_clinic_dto_1.CreateClinicDto) {
}
exports.UpdateClinicDto = UpdateClinicDto;


/***/ }),
/* 159 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ClinicQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class ClinicQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 10;
    }
}
exports.ClinicQueryDto = ClinicQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ClinicQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ClinicQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ClinicQueryDto.prototype, "doctorId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ClinicStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.ClinicStatus !== "undefined" && client_1.ClinicStatus) === "function" ? _a : Object)
], ClinicQueryDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ClinicQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ClinicQueryDto.prototype, "limit", void 0);


/***/ }),
/* 160 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TokensModule = void 0;
const common_1 = __webpack_require__(2);
const tokens_controller_1 = __webpack_require__(161);
const tokens_service_1 = __webpack_require__(162);
const database_module_1 = __webpack_require__(52);
let TokensModule = class TokensModule {
};
exports.TokensModule = TokensModule;
exports.TokensModule = TokensModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [tokens_controller_1.TokensController],
        providers: [tokens_service_1.TokensService],
        exports: [tokens_service_1.TokensService],
    })
], TokensModule);


/***/ }),
/* 161 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TokensController = void 0;
const common_1 = __webpack_require__(2);
const tokens_service_1 = __webpack_require__(162);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let TokensController = class TokensController {
    constructor(tokensService) {
        this.tokensService = tokensService;
    }
    async findTodayByClinic(clinicId) {
        return this.tokensService.findTodayByClinic(clinicId);
    }
    async getCurrentToken(clinicId) {
        return this.tokensService.getCurrentToken(clinicId);
    }
    async getWaitingTokens(clinicId) {
        return this.tokensService.getWaitingTokens(clinicId);
    }
    async getDisplayData(clinicId) {
        return this.tokensService.getDisplayData(clinicId);
    }
    async callToken(id) {
        return this.tokensService.callToken(id);
    }
    async getNextTokenNumber(clinicId) {
        const nextNumber = await this.tokensService.getNextTokenNumber(clinicId);
        return { nextTokenNumber: nextNumber };
    }
};
exports.TokensController = TokensController;
__decorate([
    (0, common_1.Get)('clinic/:clinicId/today'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get today tokens for a clinic' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Today tokens with stats' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "findTodayByClinic", null);
__decorate([
    (0, common_1.Get)('clinic/:clinicId/current'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get current token for a clinic' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Current token or null' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "getCurrentToken", null);
__decorate([
    (0, common_1.Get)('clinic/:clinicId/waiting'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get waiting tokens for a clinic' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of waiting tokens' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "getWaitingTokens", null);
__decorate([
    (0, common_1.Get)('clinic/:clinicId/display'),
    (0, swagger_1.ApiOperation)({ summary: 'Get display data for token screens' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Display data with current and upcoming tokens' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "getDisplayData", null);
__decorate([
    (0, common_1.Post)(':id/call'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Call a specific token' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Token ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Token called successfully' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "callToken", null);
__decorate([
    (0, common_1.Get)('clinic/:clinicId/next-number'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get next token number for a clinic' }),
    (0, swagger_1.ApiParam)({ name: 'clinicId', description: 'Clinic ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Next token number' }),
    __param(0, (0, common_1.Param)('clinicId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], TokensController.prototype, "getNextTokenNumber", null);
exports.TokensController = TokensController = __decorate([
    (0, swagger_1.ApiTags)('Tokens'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('tokens'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof tokens_service_1.TokensService !== "undefined" && tokens_service_1.TokensService) === "function" ? _a : Object])
], TokensController);


/***/ }),
/* 162 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TokensService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let TokensService = class TokensService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async findTodayByClinic(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const tokens = await this.prisma.token.findMany({
            where: {
                clinicId,
                tokenDate: today,
            },
            include: {
                visits: {
                    include: {
                        patient: {
                            select: {
                                id: true,
                                fullName: true,
                                nrNumber: true,
                                gender: true,
                            },
                        },
                    },
                },
                clinic: {
                    select: {
                        id: true,
                        name: true,
                        doctor: { select: { id: true, fullName: true } },
                    },
                },
            },
            orderBy: { tokenNumber: 'asc' },
        });
        const currentToken = tokens.find((t) => t.status === client_1.TokenStatus.CALLED || t.status === client_1.TokenStatus.IN_PROGRESS);
        const waitingTokens = tokens.filter((t) => t.status === client_1.TokenStatus.WAITING);
        const completedTokens = tokens.filter((t) => t.status === client_1.TokenStatus.COMPLETED);
        return {
            tokens,
            current: currentToken || null,
            waiting: waitingTokens,
            completed: completedTokens,
            stats: {
                total: tokens.length,
                waiting: waitingTokens.length,
                completed: completedTokens.length,
                inProgress: currentToken ? 1 : 0,
            },
        };
    }
    async getCurrentToken(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const currentToken = await this.prisma.token.findFirst({
            where: {
                clinicId,
                tokenDate: today,
                status: { in: [client_1.TokenStatus.CALLED, client_1.TokenStatus.IN_PROGRESS] },
            },
            include: {
                visits: {
                    include: {
                        patient: {
                            select: {
                                id: true,
                                fullName: true,
                                nrNumber: true,
                            },
                        },
                    },
                },
                clinic: {
                    select: {
                        id: true,
                        name: true,
                        doctor: { select: { id: true, fullName: true } },
                        department: { select: { id: true, name: true } },
                    },
                },
            },
        });
        return currentToken;
    }
    async getWaitingTokens(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        return this.prisma.token.findMany({
            where: {
                clinicId,
                tokenDate: today,
                status: client_1.TokenStatus.WAITING,
            },
            include: {
                visits: {
                    include: {
                        patient: {
                            select: {
                                id: true,
                                fullName: true,
                                nrNumber: true,
                            },
                        },
                    },
                },
            },
            orderBy: { tokenNumber: 'asc' },
        });
    }
    async callToken(id) {
        const token = await this.prisma.token.findUnique({
            where: { id },
            include: {
                visits: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
            },
        });
        if (!token) {
            throw new common_1.NotFoundException(`Token with ID ${id} not found`);
        }
        const updatedToken = await this.prisma.token.update({
            where: { id },
            data: {
                status: client_1.TokenStatus.CALLED,
                calledAt: new Date(),
            },
            include: {
                visits: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
                clinic: {
                    include: {
                        department: { select: { id: true, name: true } },
                        doctor: { select: { id: true, fullName: true } },
                    },
                },
            },
        });
        await this.prisma.visit.updateMany({
            where: { tokenId: id },
            data: { status: 'CALLED' },
        });
        return updatedToken;
    }
    async getNextTokenNumber(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const lastToken = await this.prisma.token.findFirst({
            where: {
                clinicId,
                tokenDate: today,
            },
            orderBy: { tokenNumber: 'desc' },
        });
        return (lastToken?.tokenNumber || 0) + 1;
    }
    async getDisplayData(clinicId) {
        const today = new Date();
        today.setHours(0, 0, 0, 0);
        const clinic = await this.prisma.clinic.findUnique({
            where: { id: clinicId },
            include: {
                department: { select: { id: true, name: true } },
                doctor: { select: { id: true, fullName: true } },
            },
        });
        if (!clinic) {
            throw new common_1.NotFoundException(`Clinic with ID ${clinicId} not found`);
        }
        const currentToken = await this.prisma.token.findFirst({
            where: {
                clinicId,
                tokenDate: today,
                status: { in: [client_1.TokenStatus.CALLED, client_1.TokenStatus.IN_PROGRESS] },
            },
            orderBy: { calledAt: 'desc' },
        });
        const upcomingTokens = await this.prisma.token.findMany({
            where: {
                clinicId,
                tokenDate: today,
                status: client_1.TokenStatus.WAITING,
            },
            orderBy: { tokenNumber: 'asc' },
            take: 5,
        });
        return {
            clinic: {
                id: clinic.id,
                name: clinic.name,
                department: clinic.department.name,
                doctor: clinic.doctor.fullName,
            },
            currentToken: currentToken?.tokenNumber || null,
            upcomingTokens: upcomingTokens.map((t) => t.tokenNumber),
            lastUpdated: new Date(),
        };
    }
};
exports.TokensService = TokensService;
exports.TokensService = TokensService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], TokensService);


/***/ }),
/* 163 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReferralsModule = void 0;
const common_1 = __webpack_require__(2);
const referrals_controller_1 = __webpack_require__(164);
const referrals_service_1 = __webpack_require__(165);
const database_module_1 = __webpack_require__(52);
let ReferralsModule = class ReferralsModule {
};
exports.ReferralsModule = ReferralsModule;
exports.ReferralsModule = ReferralsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [referrals_controller_1.ReferralsController],
        providers: [referrals_service_1.ReferralsService],
        exports: [referrals_service_1.ReferralsService],
    })
], ReferralsModule);


/***/ }),
/* 164 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReferralsController = void 0;
const common_1 = __webpack_require__(2);
const referrals_service_1 = __webpack_require__(165);
const dto_1 = __webpack_require__(166);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let ReferralsController = class ReferralsController {
    constructor(referralsService) {
        this.referralsService = referralsService;
    }
    async create(createReferralDto) {
        return this.referralsService.create(createReferralDto);
    }
    async findAll(query) {
        return this.referralsService.findAll(query);
    }
    async findPendingByDepartment(departmentId) {
        return this.referralsService.findPendingByDepartment(departmentId);
    }
    async findByReferrer(referrerId) {
        return this.referralsService.findByReferrer(referrerId);
    }
    async getStats(hospitalId, departmentId) {
        return this.referralsService.getStats(hospitalId, departmentId);
    }
    async findOne(id) {
        return this.referralsService.findOne(id);
    }
    async update(id, updateReferralDto) {
        return this.referralsService.update(id, updateReferralDto);
    }
    async accept(id) {
        return this.referralsService.accept(id);
    }
    async startWork(id) {
        return this.referralsService.startWork(id);
    }
    async complete(id, notes) {
        return this.referralsService.complete(id, notes);
    }
    async cancel(id, reason) {
        return this.referralsService.cancel(id, reason);
    }
};
exports.ReferralsController = ReferralsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new referral' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Referral created successfully' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateReferralDto !== "undefined" && dto_1.CreateReferralDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get all referrals with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of referrals' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.ReferralQueryDto !== "undefined" && dto_1.ReferralQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('department/:departmentId/pending'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST),
    (0, swagger_1.ApiOperation)({ summary: 'Get pending referrals for a department' }),
    (0, swagger_1.ApiParam)({ name: 'departmentId', description: 'Department ID' }),
    __param(0, (0, common_1.Param)('departmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "findPendingByDepartment", null);
__decorate([
    (0, common_1.Get)('referrer/:referrerId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get referrals made by a doctor' }),
    (0, swagger_1.ApiParam)({ name: 'referrerId', description: 'Referrer (Doctor) ID' }),
    __param(0, (0, common_1.Param)('referrerId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "findByReferrer", null);
__decorate([
    (0, common_1.Get)('stats'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN),
    (0, swagger_1.ApiOperation)({ summary: 'Get referral statistics' }),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('departmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "getStats", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get referral details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST),
    (0, swagger_1.ApiOperation)({ summary: 'Update a referral' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof dto_1.UpdateReferralDto !== "undefined" && dto_1.UpdateReferralDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "update", null);
__decorate([
    (0, common_1.Post)(':id/accept'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Accept a referral' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "accept", null);
__decorate([
    (0, common_1.Post)(':id/start'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Start working on a referral' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "startWork", null);
__decorate([
    (0, common_1.Post)(':id/complete'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.LAB_TECHNICIAN, client_1.UserRole.RADIOLOGIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Complete a referral' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('notes')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "complete", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.DOCTOR),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Cancel a referral' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Referral ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('reason')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ReferralsController.prototype, "cancel", null);
exports.ReferralsController = ReferralsController = __decorate([
    (0, swagger_1.ApiTags)('Referrals'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('referrals'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof referrals_service_1.ReferralsService !== "undefined" && referrals_service_1.ReferralsService) === "function" ? _a : Object])
], ReferralsController);


/***/ }),
/* 165 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReferralsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let ReferralsService = class ReferralsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createReferralDto) {
        const visit = await this.prisma.visit.findUnique({
            where: { id: createReferralDto.visitId },
        });
        if (!visit) {
            throw new common_1.NotFoundException(`Visit with ID ${createReferralDto.visitId} not found`);
        }
        const [fromDept, toDept] = await Promise.all([
            this.prisma.department.findUnique({
                where: { id: createReferralDto.fromDepartmentId },
            }),
            this.prisma.department.findUnique({
                where: { id: createReferralDto.toDepartmentId },
            }),
        ]);
        if (!fromDept) {
            throw new common_1.NotFoundException(`Source department with ID ${createReferralDto.fromDepartmentId} not found`);
        }
        if (!toDept) {
            throw new common_1.NotFoundException(`Target department with ID ${createReferralDto.toDepartmentId} not found`);
        }
        if (createReferralDto.fromDepartmentId === createReferralDto.toDepartmentId) {
            throw new common_1.BadRequestException('Cannot create referral to the same department');
        }
        return this.prisma.referral.create({
            data: {
                hospitalId: createReferralDto.hospitalId,
                visitId: createReferralDto.visitId,
                fromDepartmentId: createReferralDto.fromDepartmentId,
                toDepartmentId: createReferralDto.toDepartmentId,
                referrerId: createReferralDto.referrerId,
                referralType: createReferralDto.referralType,
                priority: createReferralDto.priority || client_1.ReferralPriority.NORMAL,
                reason: createReferralDto.reason,
                notes: createReferralDto.notes,
                status: client_1.ReferralStatus.PENDING,
            },
            include: {
                visit: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
                fromDepartment: { select: { id: true, name: true } },
                toDepartment: { select: { id: true, name: true } },
                referrer: { select: { id: true, fullName: true } },
            },
        });
    }
    async findAll(query) {
        const { hospitalId, visitId, fromDepartmentId, toDepartmentId, referralType, status, priority, page = 1, limit = 10, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (visitId)
            where.visitId = visitId;
        if (fromDepartmentId)
            where.fromDepartmentId = fromDepartmentId;
        if (toDepartmentId)
            where.toDepartmentId = toDepartmentId;
        if (referralType)
            where.referralType = referralType;
        if (status)
            where.status = status;
        if (priority)
            where.priority = priority;
        const [referrals, total] = await Promise.all([
            this.prisma.referral.findMany({
                where,
                include: {
                    visit: {
                        include: {
                            patient: { select: { id: true, fullName: true, nrNumber: true } },
                            clinic: { select: { id: true, name: true } },
                        },
                    },
                    fromDepartment: { select: { id: true, name: true } },
                    toDepartment: { select: { id: true, name: true } },
                    referrer: { select: { id: true, fullName: true } },
                },
                orderBy: [{ priority: 'desc' }, { createdAt: 'desc' }],
                skip: (page - 1) * limit,
                take: limit,
            }),
            this.prisma.referral.count({ where }),
        ]);
        return {
            data: referrals,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const referral = await this.prisma.referral.findUnique({
            where: { id },
            include: {
                visit: {
                    include: {
                        patient: true,
                        clinic: {
                            include: {
                                doctor: { select: { id: true, fullName: true } },
                            },
                        },
                    },
                },
                fromDepartment: true,
                toDepartment: true,
                referrer: { select: { id: true, fullName: true, email: true } },
            },
        });
        if (!referral) {
            throw new common_1.NotFoundException(`Referral with ID ${id} not found`);
        }
        return referral;
    }
    async update(id, updateReferralDto) {
        await this.findOne(id);
        return this.prisma.referral.update({
            where: { id },
            data: updateReferralDto,
            include: {
                visit: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
                fromDepartment: { select: { id: true, name: true } },
                toDepartment: { select: { id: true, name: true } },
                referrer: { select: { id: true, fullName: true } },
            },
        });
    }
    async accept(id) {
        const referral = await this.findOne(id);
        if (referral.status !== client_1.ReferralStatus.PENDING) {
            throw new common_1.BadRequestException(`Referral is not in PENDING status. Current status: ${referral.status}`);
        }
        return this.prisma.referral.update({
            where: { id },
            data: {
                status: client_1.ReferralStatus.ACCEPTED,
                acceptedAt: new Date(),
            },
            include: {
                visit: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
                fromDepartment: { select: { id: true, name: true } },
                toDepartment: { select: { id: true, name: true } },
            },
        });
    }
    async startWork(id) {
        const referral = await this.findOne(id);
        if (referral.status !== client_1.ReferralStatus.ACCEPTED) {
            throw new common_1.BadRequestException(`Referral must be ACCEPTED before starting work. Current status: ${referral.status}`);
        }
        return this.prisma.referral.update({
            where: { id },
            data: {
                status: client_1.ReferralStatus.IN_PROGRESS,
            },
        });
    }
    async complete(id, notes) {
        const referral = await this.findOne(id);
        if (referral.status !== client_1.ReferralStatus.ACCEPTED &&
            referral.status !== client_1.ReferralStatus.IN_PROGRESS) {
            throw new common_1.BadRequestException(`Referral must be ACCEPTED or IN_PROGRESS to complete. Current status: ${referral.status}`);
        }
        return this.prisma.referral.update({
            where: { id },
            data: {
                status: client_1.ReferralStatus.COMPLETED,
                completedAt: new Date(),
                notes: notes || referral.notes,
            },
        });
    }
    async cancel(id, reason) {
        const referral = await this.findOne(id);
        if (referral.status === client_1.ReferralStatus.COMPLETED) {
            throw new common_1.BadRequestException('Cannot cancel a completed referral');
        }
        return this.prisma.referral.update({
            where: { id },
            data: {
                status: client_1.ReferralStatus.CANCELLED,
                notes: reason ? `Cancelled: ${reason}` : referral.notes,
            },
        });
    }
    async findPendingByDepartment(departmentId) {
        return this.prisma.referral.findMany({
            where: {
                toDepartmentId: departmentId,
                status: client_1.ReferralStatus.PENDING,
            },
            include: {
                visit: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                        clinic: { select: { id: true, name: true } },
                    },
                },
                fromDepartment: { select: { id: true, name: true } },
                referrer: { select: { id: true, fullName: true } },
            },
            orderBy: [{ priority: 'desc' }, { createdAt: 'asc' }],
        });
    }
    async findByReferrer(referrerId) {
        return this.prisma.referral.findMany({
            where: { referrerId },
            include: {
                visit: {
                    include: {
                        patient: { select: { id: true, fullName: true, nrNumber: true } },
                    },
                },
                toDepartment: { select: { id: true, name: true } },
            },
            orderBy: { createdAt: 'desc' },
        });
    }
    async getStats(hospitalId, departmentId) {
        const where = { hospitalId };
        if (departmentId) {
            where.OR = [
                { fromDepartmentId: departmentId },
                { toDepartmentId: departmentId },
            ];
        }
        const [pending, accepted, inProgress, completed, cancelled] = await Promise.all([
            this.prisma.referral.count({
                where: { ...where, status: client_1.ReferralStatus.PENDING },
            }),
            this.prisma.referral.count({
                where: { ...where, status: client_1.ReferralStatus.ACCEPTED },
            }),
            this.prisma.referral.count({
                where: { ...where, status: client_1.ReferralStatus.IN_PROGRESS },
            }),
            this.prisma.referral.count({
                where: { ...where, status: client_1.ReferralStatus.COMPLETED },
            }),
            this.prisma.referral.count({
                where: { ...where, status: client_1.ReferralStatus.CANCELLED },
            }),
        ]);
        return {
            pending,
            accepted,
            inProgress,
            completed,
            cancelled,
            total: pending + accepted + inProgress + completed + cancelled,
        };
    }
};
exports.ReferralsService = ReferralsService;
exports.ReferralsService = ReferralsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ReferralsService);


/***/ }),
/* 166 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReferralQueryDto = exports.UpdateReferralDto = exports.CreateReferralDto = void 0;
var create_referral_dto_1 = __webpack_require__(167);
Object.defineProperty(exports, "CreateReferralDto", ({ enumerable: true, get: function () { return create_referral_dto_1.CreateReferralDto; } }));
var update_referral_dto_1 = __webpack_require__(168);
Object.defineProperty(exports, "UpdateReferralDto", ({ enumerable: true, get: function () { return update_referral_dto_1.UpdateReferralDto; } }));
var referral_query_dto_1 = __webpack_require__(169);
Object.defineProperty(exports, "ReferralQueryDto", ({ enumerable: true, get: function () { return referral_query_dto_1.ReferralQueryDto; } }));


/***/ }),
/* 167 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateReferralDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateReferralDto {
}
exports.CreateReferralDto = CreateReferralDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "visitId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "fromDepartmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "toDepartmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "referrerId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.ReferralType !== "undefined" && client_1.ReferralType) === "function" ? _a : Object)
], CreateReferralDto.prototype, "referralType", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralPriority),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.ReferralPriority !== "undefined" && client_1.ReferralPriority) === "function" ? _b : Object)
], CreateReferralDto.prototype, "priority", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateReferralDto.prototype, "notes", void 0);


/***/ }),
/* 168 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateReferralDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdateReferralDto {
}
exports.UpdateReferralDto = UpdateReferralDto;
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.ReferralStatus !== "undefined" && client_1.ReferralStatus) === "function" ? _a : Object)
], UpdateReferralDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateReferralDto.prototype, "notes", void 0);


/***/ }),
/* 169 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReferralQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class ReferralQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 10;
    }
}
exports.ReferralQueryDto = ReferralQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReferralQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReferralQueryDto.prototype, "visitId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReferralQueryDto.prototype, "fromDepartmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReferralQueryDto.prototype, "toDepartmentId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.ReferralType !== "undefined" && client_1.ReferralType) === "function" ? _a : Object)
], ReferralQueryDto.prototype, "referralType", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.ReferralStatus !== "undefined" && client_1.ReferralStatus) === "function" ? _b : Object)
], ReferralQueryDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReferralPriority),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.ReferralPriority !== "undefined" && client_1.ReferralPriority) === "function" ? _c : Object)
], ReferralQueryDto.prototype, "priority", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ReferralQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ReferralQueryDto.prototype, "limit", void 0);


/***/ }),
/* 170 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReceiptsModule = void 0;
const common_1 = __webpack_require__(2);
const receipts_controller_1 = __webpack_require__(171);
const receipts_service_1 = __webpack_require__(172);
const database_module_1 = __webpack_require__(52);
let ReceiptsModule = class ReceiptsModule {
};
exports.ReceiptsModule = ReceiptsModule;
exports.ReceiptsModule = ReceiptsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [receipts_controller_1.ReceiptsController],
        providers: [receipts_service_1.ReceiptsService],
        exports: [receipts_service_1.ReceiptsService],
    })
], ReceiptsModule);


/***/ }),
/* 171 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReceiptsController = void 0;
const common_1 = __webpack_require__(2);
const receipts_service_1 = __webpack_require__(172);
const dto_1 = __webpack_require__(173);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let ReceiptsController = class ReceiptsController {
    constructor(receiptsService) {
        this.receiptsService = receiptsService;
    }
    async create(createReceiptDto) {
        return this.receiptsService.create(createReceiptDto);
    }
    async findAll(query) {
        return this.receiptsService.findAll(query);
    }
    async getDailyRevenue(hospitalId, date) {
        return this.receiptsService.getDailyRevenue(hospitalId, date ? new Date(date) : undefined);
    }
    async findByPatient(patientId, limit) {
        return this.receiptsService.findByPatient(patientId, limit);
    }
    async findByReceiptNumber(receiptNumber) {
        return this.receiptsService.findByReceiptNumber(receiptNumber);
    }
    async findOne(id) {
        return this.receiptsService.findOne(id);
    }
    async getPrintData(id) {
        return this.receiptsService.getPrintData(id);
    }
    async markAsPaid(id, paymentMethod) {
        return this.receiptsService.markAsPaid(id, paymentMethod);
    }
    async updatePayment(id, body) {
        return this.receiptsService.updatePayment(id, body.paidAmount, body.paymentMethod);
    }
    async refund(id, reason) {
        return this.receiptsService.refund(id, reason);
    }
};
exports.ReceiptsController = ReceiptsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.PHARMACY_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Generate a new receipt' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Receipt created successfully' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateReceiptDto !== "undefined" && dto_1.CreateReceiptDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.BILLING_STAFF, client_1.UserRole.RECEPTIONIST, client_1.UserRole.AUDITOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get all receipts with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of receipts' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.ReceiptQueryDto !== "undefined" && dto_1.ReceiptQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('daily-revenue'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.BILLING_STAFF, client_1.UserRole.AUDITOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get daily revenue summary' }),
    (0, swagger_1.ApiQuery)({ name: 'hospitalId', required: true }),
    (0, swagger_1.ApiQuery)({ name: 'date', required: false }),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "getDailyRevenue", null);
__decorate([
    (0, common_1.Get)('patient/:patientId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.DOCTOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get receipts by patient' }),
    (0, swagger_1.ApiParam)({ name: 'patientId', description: 'Patient ID' }),
    __param(0, (0, common_1.Param)('patientId')),
    __param(1, (0, common_1.Query)('limit')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "findByPatient", null);
__decorate([
    (0, common_1.Get)('number/:receiptNumber'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.AUDITOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get receipt by number' }),
    (0, swagger_1.ApiParam)({ name: 'receiptNumber', description: 'Receipt Number' }),
    __param(0, (0, common_1.Param)('receiptNumber')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "findByReceiptNumber", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.AUDITOR),
    (0, swagger_1.ApiOperation)({ summary: 'Get receipt details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Receipt ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Get)(':id/print'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Get receipt data for printing' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Receipt ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "getPrintData", null);
__decorate([
    (0, common_1.Post)(':id/pay'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Mark receipt as paid' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Receipt ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('paymentMethod')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof client_1.PaymentMethod !== "undefined" && client_1.PaymentMethod) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "markAsPaid", null);
__decorate([
    (0, common_1.Post)(':id/payment'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE, client_1.UserRole.MAIN_PHARMACY_MANAGER, client_1.UserRole.SUB_PHARMACY_MANAGER, client_1.UserRole.PHARMACY_STAFF),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Update receipt payment (partial/full)' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Receipt ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof dto_1.UpdateReceiptPaymentDto !== "undefined" && dto_1.UpdateReceiptPaymentDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "updatePayment", null);
__decorate([
    (0, common_1.Post)(':id/refund'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Refund a receipt' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Receipt ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('reason')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ReceiptsController.prototype, "refund", null);
exports.ReceiptsController = ReceiptsController = __decorate([
    (0, swagger_1.ApiTags)('Receipts'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('receipts'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof receipts_service_1.ReceiptsService !== "undefined" && receipts_service_1.ReceiptsService) === "function" ? _a : Object])
], ReceiptsController);


/***/ }),
/* 172 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReceiptsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let ReceiptsService = class ReceiptsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async generateReceiptNumber() {
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await this.prisma.receipt.findFirst({
            where: {
                receiptNumber: { startsWith: `REC-${dateStr}` },
            },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        return `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
    }
    async create(createReceiptDto) {
        const patient = await this.prisma.patient.findUnique({
            where: { id: createReceiptDto.patientId },
        });
        if (!patient) {
            throw new common_1.NotFoundException(`Patient with ID ${createReceiptDto.patientId} not found`);
        }
        const receiptNumber = await this.generateReceiptNumber();
        const amount = createReceiptDto.amount;
        const discount = createReceiptDto.discount || 0;
        const tax = createReceiptDto.tax || 0;
        const totalAmount = amount - discount + tax;
        const normalizedPaidAmount = Math.max(createReceiptDto.paidAmount ?? 0, 0);
        const resolvedPaymentStatus = this.resolvePaymentStatus(normalizedPaidAmount, totalAmount);
        return this.prisma.receipt.create({
            data: {
                hospitalId: createReceiptDto.hospitalId,
                patientId: createReceiptDto.patientId,
                visitId: createReceiptDto.visitId,
                departmentId: createReceiptDto.departmentId,
                generatedById: createReceiptDto.generatedById,
                receiptNumber,
                receiptType: createReceiptDto.receiptType,
                description: createReceiptDto.description,
                amount,
                discount,
                tax,
                totalAmount,
                paidAmount: normalizedPaidAmount,
                paymentMethod: createReceiptDto.paymentMethod || client_1.PaymentMethod.CASH,
                paymentStatus: createReceiptDto.paymentStatus || resolvedPaymentStatus,
                paidAt: normalizedPaidAmount > 0 ? new Date() : undefined,
                notes: createReceiptDto.notes,
            },
            include: {
                hospital: { select: { id: true, name: true } },
                patient: { select: { id: true, fullName: true, nrNumber: true } },
                department: { select: { id: true, name: true } },
                generatedBy: { select: { id: true, fullName: true } },
            },
        });
    }
    async findAll(query) {
        const { hospitalId, patientId, departmentId, receiptType, paymentStatus, paymentMethod, startDate, endDate, page = 1, limit = 10, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (patientId)
            where.patientId = patientId;
        if (departmentId)
            where.departmentId = departmentId;
        if (receiptType)
            where.receiptType = receiptType;
        if (paymentStatus)
            where.paymentStatus = paymentStatus;
        if (paymentMethod)
            where.paymentMethod = paymentMethod;
        if (startDate || endDate) {
            where.createdAt = {};
            if (startDate)
                where.createdAt.gte = new Date(startDate);
            if (endDate)
                where.createdAt.lte = new Date(endDate);
        }
        const [receipts, total] = await Promise.all([
            this.prisma.receipt.findMany({
                where,
                include: {
                    hospital: { select: { id: true, name: true } },
                    patient: { select: { id: true, fullName: true, nrNumber: true } },
                    department: { select: { id: true, name: true } },
                    generatedBy: { select: { id: true, fullName: true } },
                },
                orderBy: { createdAt: 'desc' },
                skip: (page - 1) * limit,
                take: limit,
            }),
            this.prisma.receipt.count({ where }),
        ]);
        return {
            data: receipts,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const receipt = await this.prisma.receipt.findUnique({
            where: { id },
            include: {
                hospital: true,
                patient: true,
                visit: {
                    include: {
                        clinic: {
                            include: {
                                doctor: { select: { id: true, fullName: true } },
                            },
                        },
                    },
                },
                department: true,
                generatedBy: { select: { id: true, fullName: true, email: true } },
            },
        });
        if (!receipt) {
            throw new common_1.NotFoundException(`Receipt with ID ${id} not found`);
        }
        return receipt;
    }
    async findByReceiptNumber(receiptNumber) {
        const receipt = await this.prisma.receipt.findUnique({
            where: { receiptNumber },
            include: {
                hospital: true,
                patient: true,
                department: true,
                generatedBy: { select: { id: true, fullName: true } },
            },
        });
        if (!receipt) {
            throw new common_1.NotFoundException(`Receipt with number ${receiptNumber} not found`);
        }
        return receipt;
    }
    async findByPatient(patientId, limit = 10) {
        return this.prisma.receipt.findMany({
            where: { patientId },
            include: {
                department: { select: { id: true, name: true } },
                generatedBy: { select: { id: true, fullName: true } },
            },
            orderBy: { createdAt: 'desc' },
            take: limit,
        });
    }
    async markAsPaid(id, paymentMethod = client_1.PaymentMethod.CASH) {
        const receipt = await this.findOne(id);
        return this.prisma.receipt.update({
            where: { id },
            data: {
                paymentStatus: client_1.PaymentStatus.PAID,
                paymentMethod,
                paidAmount: receipt.totalAmount,
                paidAt: new Date(),
            },
            include: {
                patient: { select: { id: true, fullName: true, nrNumber: true } },
                department: { select: { id: true, name: true } },
            },
        });
    }
    async updatePayment(id, paidAmount, paymentMethod) {
        const receipt = await this.findOne(id);
        const normalizedPaidAmount = Math.max(paidAmount, 0);
        const paymentStatus = this.resolvePaymentStatus(normalizedPaidAmount, Number(receipt.totalAmount));
        return this.prisma.receipt.update({
            where: { id },
            data: {
                paidAmount: normalizedPaidAmount,
                paymentStatus,
                paymentMethod: paymentMethod || receipt.paymentMethod,
                paidAt: normalizedPaidAmount > 0 ? new Date() : null,
            },
            include: {
                patient: { select: { id: true, fullName: true, nrNumber: true } },
                department: { select: { id: true, name: true } },
            },
        });
    }
    async refund(id, reason) {
        const receipt = await this.findOne(id);
        return this.prisma.receipt.update({
            where: { id },
            data: {
                paymentStatus: client_1.PaymentStatus.REFUNDED,
                notes: reason ? `Refunded: ${reason}` : receipt.notes,
            },
        });
    }
    async getPrintData(id) {
        const receipt = await this.findOne(id);
        return {
            receipt,
            printDate: new Date(),
            format: {
                receiptNumber: receipt.receiptNumber,
                date: receipt.createdAt,
                patient: {
                    name: receipt.patient.fullName,
                    nrNumber: receipt.patient.nrNumber,
                    mobile: receipt.patient.mobile,
                },
                hospital: {
                    name: receipt.hospital.name,
                    address: receipt.hospital.address,
                    phone: receipt.hospital.phone,
                    email: receipt.hospital.email,
                },
                department: receipt.department.name,
                type: receipt.receiptType,
                description: receipt.description,
                amount: Number(receipt.amount),
                discount: Number(receipt.discount),
                tax: Number(receipt.tax),
                totalAmount: Number(receipt.totalAmount),
                paymentMethod: receipt.paymentMethod,
                paymentStatus: receipt.paymentStatus,
                paidAt: receipt.paidAt,
                generatedBy: receipt.generatedBy.fullName,
            },
        };
    }
    async getDailyRevenue(hospitalId, date) {
        const targetDate = date || new Date();
        targetDate.setHours(0, 0, 0, 0);
        const nextDate = new Date(targetDate);
        nextDate.setDate(nextDate.getDate() + 1);
        const receipts = await this.prisma.receipt.findMany({
            where: {
                hospitalId,
                createdAt: { gte: targetDate, lt: nextDate },
                paymentStatus: client_1.PaymentStatus.PAID,
            },
            include: {
                department: { select: { id: true, name: true } },
            },
        });
        const byDepartment = {};
        receipts.forEach((r) => {
            const deptId = r.department.id;
            if (!byDepartment[deptId]) {
                byDepartment[deptId] = { name: r.department.name, total: 0, count: 0 };
            }
            byDepartment[deptId].total += Number(r.totalAmount);
            byDepartment[deptId].count += 1;
        });
        const byType = {};
        receipts.forEach((r) => {
            byType[r.receiptType] = (byType[r.receiptType] || 0) + Number(r.totalAmount);
        });
        const total = receipts.reduce((sum, r) => sum + Number(r.totalAmount), 0);
        return {
            date: targetDate,
            total,
            receiptCount: receipts.length,
            byDepartment: Object.entries(byDepartment).map(([id, data]) => ({
                departmentId: id,
                ...data,
            })),
            byType,
        };
    }
    resolvePaymentStatus(paidAmount, totalAmount) {
        if (paidAmount <= 0) {
            return client_1.PaymentStatus.UNPAID;
        }
        if (paidAmount >= totalAmount) {
            return client_1.PaymentStatus.PAID;
        }
        return client_1.PaymentStatus.PARTIALLY_PAID;
    }
};
exports.ReceiptsService = ReceiptsService;
exports.ReceiptsService = ReceiptsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ReceiptsService);


/***/ }),
/* 173 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateReceiptPaymentDto = exports.ReceiptQueryDto = exports.CreateReceiptDto = void 0;
var create_receipt_dto_1 = __webpack_require__(174);
Object.defineProperty(exports, "CreateReceiptDto", ({ enumerable: true, get: function () { return create_receipt_dto_1.CreateReceiptDto; } }));
var receipt_query_dto_1 = __webpack_require__(175);
Object.defineProperty(exports, "ReceiptQueryDto", ({ enumerable: true, get: function () { return receipt_query_dto_1.ReceiptQueryDto; } }));
var update_receipt_payment_dto_1 = __webpack_require__(176);
Object.defineProperty(exports, "UpdateReceiptPaymentDto", ({ enumerable: true, get: function () { return update_receipt_payment_dto_1.UpdateReceiptPaymentDto; } }));


/***/ }),
/* 174 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateReceiptDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class CreateReceiptDto {
}
exports.CreateReceiptDto = CreateReceiptDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "visitId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "generatedById", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReceiptType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.ReceiptType !== "undefined" && client_1.ReceiptType) === "function" ? _a : Object)
], CreateReceiptDto.prototype, "receiptType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateReceiptDto.prototype, "amount", void 0);
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateReceiptDto.prototype, "discount", void 0);
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateReceiptDto.prototype, "tax", void 0);
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateReceiptDto.prototype, "paidAmount", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentMethod),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.PaymentMethod !== "undefined" && client_1.PaymentMethod) === "function" ? _b : Object)
], CreateReceiptDto.prototype, "paymentMethod", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.PaymentStatus !== "undefined" && client_1.PaymentStatus) === "function" ? _c : Object)
], CreateReceiptDto.prototype, "paymentStatus", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateReceiptDto.prototype, "notes", void 0);


/***/ }),
/* 175 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReceiptQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class ReceiptQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 10;
    }
}
exports.ReceiptQueryDto = ReceiptQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReceiptQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReceiptQueryDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReceiptQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ReceiptType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.ReceiptType !== "undefined" && client_1.ReceiptType) === "function" ? _a : Object)
], ReceiptQueryDto.prototype, "receiptType", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.PaymentStatus !== "undefined" && client_1.PaymentStatus) === "function" ? _b : Object)
], ReceiptQueryDto.prototype, "paymentStatus", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.PaymentMethod),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.PaymentMethod !== "undefined" && client_1.PaymentMethod) === "function" ? _c : Object)
], ReceiptQueryDto.prototype, "paymentMethod", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReceiptQueryDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ReceiptQueryDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ReceiptQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], ReceiptQueryDto.prototype, "limit", void 0);


/***/ }),
/* 176 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateReceiptPaymentDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class UpdateReceiptPaymentDto {
}
exports.UpdateReceiptPaymentDto = UpdateReceiptPaymentDto;
__decorate([
    (0, class_validator_1.IsNumber)({ maxDecimalPlaces: 2 }),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], UpdateReceiptPaymentDto.prototype, "paidAmount", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.PaymentMethod),
    __metadata("design:type", typeof (_a = typeof client_1.PaymentMethod !== "undefined" && client_1.PaymentMethod) === "function" ? _a : Object)
], UpdateReceiptPaymentDto.prototype, "paymentMethod", void 0);


/***/ }),
/* 177 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PaymentsModule = void 0;
const common_1 = __webpack_require__(2);
const payments_service_1 = __webpack_require__(178);
const payments_controller_1 = __webpack_require__(179);
const database_module_1 = __webpack_require__(52);
let PaymentsModule = class PaymentsModule {
};
exports.PaymentsModule = PaymentsModule;
exports.PaymentsModule = PaymentsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [payments_controller_1.PaymentsController],
        providers: [payments_service_1.PaymentsService],
    })
], PaymentsModule);


/***/ }),
/* 178 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PaymentsService = void 0;
const common_1 = __webpack_require__(2);
const client_1 = __webpack_require__(16);
const prisma_service_1 = __webpack_require__(15);
let PaymentsService = class PaymentsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async getPatientSummary(patientId) {
        const patient = await this.prisma.patient.findUnique({
            where: { id: patientId },
            select: { id: true, nrNumber: true, fullName: true, hospitalId: true },
        });
        if (!patient) {
            throw new common_1.NotFoundException('Patient not found');
        }
        const [admissions, visits, operations, labOrders, issueTransactions, receipts] = await Promise.all([
            this.prisma.admission.findMany({
                where: { patientId },
                include: {
                    bed: { select: { dailyRate: true } },
                    room: { select: { dailyRate: true } },
                    dailyCharges: true,
                },
            }),
            this.prisma.visit.findMany({
                where: { patientId },
                select: { id: true, consultationFee: true, visitDate: true },
            }),
            this.prisma.operation.findMany({
                where: { patientId, status: { not: 'CANCELLED' } },
                select: { operationPrice: true },
            }),
            this.prisma.labOrder.findMany({
                where: { patientId, status: { not: 'CANCELLED' } },
                include: { labTest: { select: { price: true } } },
            }),
            this.prisma.issueTransaction.findMany({
                where: { nrNumber: patient.nrNumber },
                select: { totalAmount: true },
            }),
            this.prisma.receipt.findMany({
                where: { patientId },
                orderBy: { createdAt: 'desc' },
            }),
        ]);
        const roomCharges = this.sumRoomCharges(admissions);
        const consultationCharges = this.sumConsultationCharges(visits);
        const operationCharges = this.sumDecimals(operations.map((op) => op.operationPrice));
        const labCharges = this.sumDecimals(labOrders.map((order) => order.labTest?.price));
        const pharmacyCharges = this.sumDecimals(issueTransactions.map((issue) => issue.totalAmount));
        const totalCharges = roomCharges
            .plus(consultationCharges)
            .plus(operationCharges)
            .plus(labCharges)
            .plus(pharmacyCharges);
        return {
            patient,
            breakdown: {
                roomCharges: roomCharges.toNumber(),
                consultationCharges: consultationCharges.toNumber(),
                operationCharges: operationCharges.toNumber(),
                labCharges: labCharges.toNumber(),
                pharmacyCharges: pharmacyCharges.toNumber(),
                total: totalCharges.toNumber(),
            },
            receipts,
        };
    }
    async getVisitSummary(visitId) {
        const visit = await this.prisma.visit.findUnique({
            where: { id: visitId },
            include: {
                patient: { select: { id: true, nrNumber: true, fullName: true } },
            },
        });
        if (!visit) {
            throw new common_1.NotFoundException('Visit not found');
        }
        const visitDate = new Date(visit.visitDate);
        const dayStart = new Date(visitDate);
        dayStart.setHours(0, 0, 0, 0);
        const dayEnd = new Date(visitDate);
        dayEnd.setHours(23, 59, 59, 999);
        const [operations, labOrders, issueTransactions, receipts] = await Promise.all([
            this.prisma.operation.findMany({
                where: { visitId, status: { not: 'CANCELLED' } },
                select: { operationPrice: true },
            }),
            this.prisma.labOrder.findMany({
                where: { visitId, status: { not: 'CANCELLED' } },
                include: { labTest: { select: { price: true } } },
            }),
            this.prisma.issueTransaction.findMany({
                where: {
                    nrNumber: visit.patient.nrNumber,
                    issuedAt: { gte: dayStart, lte: dayEnd },
                },
                select: { totalAmount: true },
            }),
            this.prisma.receipt.findMany({
                where: { visitId },
                orderBy: { createdAt: 'desc' },
            }),
        ]);
        const consultationCharges = new client_1.Prisma.Decimal(visit.consultationFee || 0);
        const operationCharges = this.sumDecimals(operations.map((op) => op.operationPrice));
        const labCharges = this.sumDecimals(labOrders.map((order) => order.labTest?.price));
        const pharmacyCharges = this.sumDecimals(issueTransactions.map((issue) => issue.totalAmount));
        const totalCharges = consultationCharges
            .plus(operationCharges)
            .plus(labCharges)
            .plus(pharmacyCharges);
        return {
            visit,
            breakdown: {
                consultationCharges: consultationCharges.toNumber(),
                operationCharges: operationCharges.toNumber(),
                labCharges: labCharges.toNumber(),
                pharmacyCharges: pharmacyCharges.toNumber(),
                total: totalCharges.toNumber(),
            },
            receipts,
        };
    }
    sumRoomCharges(admissions) {
        return admissions.reduce((sum, admission) => {
            if (admission.dailyCharges?.length) {
                const dailyTotal = this.sumDecimals(admission.dailyCharges.map((charge) => charge.totalCharges));
                return sum.plus(dailyTotal);
            }
            const rate = admission.bed?.dailyRate || admission.room?.dailyRate || new client_1.Prisma.Decimal(0);
            const admittedAt = new Date(admission.admittedAt);
            const dischargedAt = admission.dischargedAt
                ? new Date(admission.dischargedAt)
                : new Date();
            const diffMs = Math.max(dischargedAt.getTime() - admittedAt.getTime(), 0);
            const days = Math.max(Math.ceil(diffMs / (1000 * 60 * 60 * 24)), 1);
            return sum.plus(new client_1.Prisma.Decimal(rate).mul(days));
        }, new client_1.Prisma.Decimal(0));
    }
    sumConsultationCharges(visits) {
        return visits.reduce((sum, visit) => {
            return sum.plus(new client_1.Prisma.Decimal(visit.consultationFee || 0));
        }, new client_1.Prisma.Decimal(0));
    }
    sumDecimals(values) {
        return values.reduce((sum, value) => {
            if (value === null || value === undefined) {
                return sum;
            }
            return sum.plus(new client_1.Prisma.Decimal(value));
        }, new client_1.Prisma.Decimal(0));
    }
};
exports.PaymentsService = PaymentsService;
exports.PaymentsService = PaymentsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], PaymentsService);


/***/ }),
/* 179 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PaymentsController = void 0;
const common_1 = __webpack_require__(2);
const payments_service_1 = __webpack_require__(178);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let PaymentsController = class PaymentsController {
    constructor(paymentsService) {
        this.paymentsService = paymentsService;
    }
    async getPatientSummary(patientId) {
        return this.paymentsService.getPatientSummary(patientId);
    }
    async getVisitSummary(visitId) {
        return this.paymentsService.getVisitSummary(visitId);
    }
};
exports.PaymentsController = PaymentsController;
__decorate([
    (0, common_1.Get)('patient/:patientId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get consolidated payment summary for a patient' }),
    (0, swagger_1.ApiParam)({ name: 'patientId', description: 'Patient ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Patient payment summary' }),
    __param(0, (0, common_1.Param)('patientId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PaymentsController.prototype, "getPatientSummary", null);
__decorate([
    (0, common_1.Get)('visit/:visitId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.BILLING_STAFF, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get consolidated payment summary for a visit' }),
    (0, swagger_1.ApiParam)({ name: 'visitId', description: 'Visit ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Visit payment summary' }),
    __param(0, (0, common_1.Param)('visitId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PaymentsController.prototype, "getVisitSummary", null);
exports.PaymentsController = PaymentsController = __decorate([
    (0, swagger_1.ApiTags)('Payments'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('payments'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof payments_service_1.PaymentsService !== "undefined" && payments_service_1.PaymentsService) === "function" ? _a : Object])
], PaymentsController);


/***/ }),
/* 180 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientVisitsModule = void 0;
const common_1 = __webpack_require__(2);
const patient_visits_controller_1 = __webpack_require__(181);
const patient_visits_service_1 = __webpack_require__(182);
const visits_module_1 = __webpack_require__(74);
let PatientVisitsModule = class PatientVisitsModule {
};
exports.PatientVisitsModule = PatientVisitsModule;
exports.PatientVisitsModule = PatientVisitsModule = __decorate([
    (0, common_1.Module)({
        imports: [visits_module_1.VisitsModule],
        controllers: [patient_visits_controller_1.PatientVisitsController],
        providers: [patient_visits_service_1.PatientVisitsService],
    })
], PatientVisitsModule);


/***/ }),
/* 181 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientVisitsController = void 0;
const common_1 = __webpack_require__(2);
const patient_visits_service_1 = __webpack_require__(182);
const dto_1 = __webpack_require__(183);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
const swagger_1 = __webpack_require__(3);
let PatientVisitsController = class PatientVisitsController {
    constructor(patientVisitsService) {
        this.patientVisitsService = patientVisitsService;
    }
    async create(createVisitDto) {
        return this.patientVisitsService.create(createVisitDto);
    }
    async findAll(query) {
        return this.patientVisitsService.findAll(query);
    }
    async findByPatient(patientId, limit) {
        return this.patientVisitsService.findByPatient(patientId, limit);
    }
    async findOne(id) {
        return this.patientVisitsService.findOne(id);
    }
    async update(id, updateVisitDto, req) {
        return this.patientVisitsService.update(id, updateVisitDto, req.user.id);
    }
    async cancel(id) {
        return this.patientVisitsService.cancel(id);
    }
};
exports.PatientVisitsController = PatientVisitsController;
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new patient visit' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Patient visit created' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateVisitDto !== "undefined" && dto_1.CreateVisitDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'List patient visits with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of visits' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof dto_1.VisitQueryDto !== "undefined" && dto_1.VisitQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('patient/:patientId'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get patient visit history' }),
    (0, swagger_1.ApiParam)({ name: 'patientId', description: 'Patient ID' }),
    __param(0, (0, common_1.Param)('patientId')),
    __param(1, (0, common_1.Query)('limit')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "findByPatient", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.DEPARTMENT_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Get patient visit details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.SUPER_ADMIN, client_1.UserRole.DOCTOR, client_1.UserRole.NURSE),
    (0, swagger_1.ApiOperation)({ summary: 'Update visit consultation details' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof dto_1.UpdateVisitDto !== "undefined" && dto_1.UpdateVisitDto) === "function" ? _d : Object, Object]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.MASTER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Cancel a patient visit' }),
    (0, swagger_1.ApiParam)({ name: 'id', description: 'Visit ID' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PatientVisitsController.prototype, "cancel", null);
exports.PatientVisitsController = PatientVisitsController = __decorate([
    (0, swagger_1.ApiTags)('Patient Visits'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.Controller)('patient-visits'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof patient_visits_service_1.PatientVisitsService !== "undefined" && patient_visits_service_1.PatientVisitsService) === "function" ? _a : Object])
], PatientVisitsController);


/***/ }),
/* 182 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PatientVisitsService = void 0;
const common_1 = __webpack_require__(2);
const visits_service_1 = __webpack_require__(64);
let PatientVisitsService = class PatientVisitsService {
    constructor(visitsService) {
        this.visitsService = visitsService;
    }
    create(dto) {
        return this.visitsService.create(dto);
    }
    findAll(query) {
        return this.visitsService.findAll(query);
    }
    findOne(id) {
        return this.visitsService.findOne(id);
    }
    update(id, dto, consultantId) {
        return this.visitsService.update(id, dto, consultantId);
    }
    cancel(id) {
        return this.visitsService.cancel(id);
    }
    findByPatient(patientId, limit) {
        return this.visitsService.findByPatient(patientId, limit);
    }
};
exports.PatientVisitsService = PatientVisitsService;
exports.PatientVisitsService = PatientVisitsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof visits_service_1.VisitsService !== "undefined" && visits_service_1.VisitsService) === "function" ? _a : Object])
], PatientVisitsService);


/***/ }),
/* 183 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisitQueryDto = exports.UpdateVisitDto = exports.VitalSignsDto = exports.CreateVisitDto = void 0;
var dto_1 = __webpack_require__(68);
Object.defineProperty(exports, "CreateVisitDto", ({ enumerable: true, get: function () { return dto_1.CreateVisitDto; } }));
Object.defineProperty(exports, "VitalSignsDto", ({ enumerable: true, get: function () { return dto_1.VitalSignsDto; } }));
var dto_2 = __webpack_require__(68);
Object.defineProperty(exports, "UpdateVisitDto", ({ enumerable: true, get: function () { return dto_2.UpdateVisitDto; } }));
var dto_3 = __webpack_require__(68);
Object.defineProperty(exports, "VisitQueryDto", ({ enumerable: true, get: function () { return dto_3.VisitQueryDto; } }));


/***/ }),
/* 184 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RoomsModule = void 0;
const common_1 = __webpack_require__(2);
const rooms_controller_1 = __webpack_require__(185);
const rooms_service_1 = __webpack_require__(186);
const database_module_1 = __webpack_require__(52);
let RoomsModule = class RoomsModule {
};
exports.RoomsModule = RoomsModule;
exports.RoomsModule = RoomsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [rooms_controller_1.RoomsController],
        providers: [rooms_service_1.RoomsService],
        exports: [rooms_service_1.RoomsService],
    })
], RoomsModule);


/***/ }),
/* 185 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RoomsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const rooms_service_1 = __webpack_require__(186);
const create_room_dto_1 = __webpack_require__(187);
const update_room_dto_1 = __webpack_require__(188);
const room_query_dto_1 = __webpack_require__(189);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
let RoomsController = class RoomsController {
    constructor(roomsService) {
        this.roomsService = roomsService;
    }
    create(createRoomDto) {
        return this.roomsService.create(createRoomDto);
    }
    findAll(query) {
        return this.roomsService.findAll(query);
    }
    findAvailable(hospitalId, roomType) {
        return this.roomsService.findAvailable(hospitalId, roomType);
    }
    getOccupancyStats(hospitalId) {
        return this.roomsService.getOccupancyStats(hospitalId);
    }
    findOne(id) {
        return this.roomsService.findOne(id);
    }
    update(id, updateRoomDto) {
        return this.roomsService.update(id, updateRoomDto);
    }
    remove(id) {
        return this.roomsService.remove(id);
    }
};
exports.RoomsController = RoomsController;
__decorate([
    (0, common_1.Post)(),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new room' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Room created successfully' }),
    (0, swagger_1.ApiResponse)({ status: 409, description: 'Room number already exists' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_room_dto_1.CreateRoomDto !== "undefined" && create_room_dto_1.CreateRoomDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Get all rooms with filters and pagination' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of rooms retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof room_query_dto_1.RoomQueryDto !== "undefined" && room_query_dto_1.RoomQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('available/:hospitalId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get available rooms for a hospital' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Available rooms retrieved successfully' }),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Query)('roomType')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "findAvailable", null);
__decorate([
    (0, common_1.Get)('occupancy/:hospitalId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get room and bed occupancy statistics' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Occupancy stats retrieved successfully' }),
    __param(0, (0, common_1.Param)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "getOccupancyStats", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get a room by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Room retrieved successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Room not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update a room' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Room updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Room not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_room_dto_1.UpdateRoomDto !== "undefined" && update_room_dto_1.UpdateRoomDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Delete a room' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Room deleted successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Room not found' }),
    (0, swagger_1.ApiResponse)({
        status: 400,
        description: 'Cannot delete room with active admissions',
    }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], RoomsController.prototype, "remove", null);
exports.RoomsController = RoomsController = __decorate([
    (0, swagger_1.ApiTags)('Rooms'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    (0, common_1.Controller)('rooms'),
    __metadata("design:paramtypes", [typeof (_a = typeof rooms_service_1.RoomsService !== "undefined" && rooms_service_1.RoomsService) === "function" ? _a : Object])
], RoomsController);


/***/ }),
/* 186 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RoomsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let RoomsService = class RoomsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createRoomDto) {
        const { hospitalId, roomNumber, dailyRate, ...rest } = createRoomDto;
        const existingRoom = await this.prisma.room.findUnique({
            where: {
                hospitalId_roomNumber: {
                    hospitalId,
                    roomNumber,
                },
            },
        });
        if (existingRoom) {
            throw new common_1.ConflictException(`Room with number ${roomNumber} already exists in this hospital`);
        }
        const hospital = await this.prisma.hospital.findUnique({
            where: { id: hospitalId },
        });
        if (!hospital) {
            throw new common_1.NotFoundException(`Hospital with ID ${hospitalId} not found`);
        }
        if (rest.departmentId) {
            const department = await this.prisma.department.findUnique({
                where: { id: rest.departmentId },
            });
            if (!department) {
                throw new common_1.NotFoundException(`Department with ID ${rest.departmentId} not found`);
            }
        }
        return this.prisma.room.create({
            data: {
                hospitalId,
                roomNumber,
                dailyRate: new client_1.Prisma.Decimal(dailyRate),
                ...rest,
            },
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                beds: {
                    select: { id: true, bedNumber: true, status: true },
                },
            },
        });
    }
    async findAll(query) {
        const { hospitalId, departmentId, roomType, status, floor, building, page = 1, limit = 20, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (departmentId)
            where.departmentId = departmentId;
        if (roomType)
            where.roomType = roomType;
        if (status)
            where.status = status;
        if (floor)
            where.floor = floor;
        if (building)
            where.building = { contains: building, mode: 'insensitive' };
        const skip = (page - 1) * limit;
        const [rooms, total] = await Promise.all([
            this.prisma.room.findMany({
                where,
                skip,
                take: limit,
                orderBy: [{ roomNumber: 'asc' }],
                include: {
                    hospital: { select: { id: true, name: true, code: true } },
                    department: { select: { id: true, name: true, code: true } },
                    beds: {
                        select: {
                            id: true,
                            bedNumber: true,
                            status: true,
                        },
                    },
                    _count: {
                        select: {
                            beds: true,
                            admissions: { where: { status: 'ADMITTED' } },
                        },
                    },
                },
            }),
            this.prisma.room.count({ where }),
        ]);
        return {
            data: rooms,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const room = await this.prisma.room.findUnique({
            where: { id },
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                beds: {
                    select: {
                        id: true,
                        bedNumber: true,
                        bedType: true,
                        status: true,
                        dailyRate: true,
                    },
                    orderBy: { bedNumber: 'asc' },
                },
                admissions: {
                    where: { status: 'ADMITTED' },
                    select: {
                        id: true,
                        admissionNumber: true,
                        patient: {
                            select: {
                                id: true,
                                nrNumber: true,
                                fullName: true,
                            },
                        },
                        attendingDoctor: {
                            select: {
                                id: true,
                                fullName: true,
                            },
                        },
                        admittedAt: true,
                    },
                },
                _count: {
                    select: {
                        beds: true,
                        admissions: { where: { status: 'ADMITTED' } },
                    },
                },
            },
        });
        if (!room) {
            throw new common_1.NotFoundException(`Room with ID ${id} not found`);
        }
        return room;
    }
    async update(id, updateRoomDto) {
        const room = await this.prisma.room.findUnique({ where: { id } });
        if (!room) {
            throw new common_1.NotFoundException(`Room with ID ${id} not found`);
        }
        if (updateRoomDto.roomNumber && updateRoomDto.roomNumber !== room.roomNumber) {
            const existingRoom = await this.prisma.room.findUnique({
                where: {
                    hospitalId_roomNumber: {
                        hospitalId: room.hospitalId,
                        roomNumber: updateRoomDto.roomNumber,
                    },
                },
            });
            if (existingRoom) {
                throw new common_1.ConflictException(`Room with number ${updateRoomDto.roomNumber} already exists in this hospital`);
            }
        }
        const { dailyRate, ...rest } = updateRoomDto;
        const data = { ...rest };
        if (dailyRate !== undefined) {
            data.dailyRate = new client_1.Prisma.Decimal(dailyRate);
        }
        return this.prisma.room.update({
            where: { id },
            data,
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                beds: {
                    select: { id: true, bedNumber: true, status: true },
                },
            },
        });
    }
    async remove(id) {
        const room = await this.prisma.room.findUnique({
            where: { id },
            include: {
                admissions: { where: { status: 'ADMITTED' } },
            },
        });
        if (!room) {
            throw new common_1.NotFoundException(`Room with ID ${id} not found`);
        }
        if (room.admissions.length > 0) {
            throw new common_1.BadRequestException(`Cannot delete room with active admissions. Please discharge all patients first.`);
        }
        return this.prisma.room.delete({ where: { id } });
    }
    async findAvailable(hospitalId, roomType) {
        const where = {
            hospitalId,
            status: client_1.RoomStatus.AVAILABLE,
        };
        if (roomType) {
            where.roomType = roomType;
        }
        return this.prisma.room.findMany({
            where,
            include: {
                department: { select: { id: true, name: true, code: true } },
                beds: {
                    where: { status: 'AVAILABLE' },
                    select: {
                        id: true,
                        bedNumber: true,
                        bedType: true,
                        dailyRate: true,
                    },
                },
            },
            orderBy: { roomNumber: 'asc' },
        });
    }
    async getOccupancyStats(hospitalId) {
        const rooms = await this.prisma.room.findMany({
            where: { hospitalId },
            include: {
                beds: true,
                admissions: {
                    where: { status: 'ADMITTED' },
                },
            },
        });
        const totalRooms = rooms.length;
        const occupiedRooms = rooms.filter((r) => r.admissions.length > 0).length;
        const totalBeds = rooms.reduce((sum, r) => sum + r.beds.length, 0);
        const occupiedBeds = rooms.reduce((sum, r) => sum + r.admissions.length, 0);
        const roomsByType = rooms.reduce((acc, room) => {
            const type = room.roomType;
            if (!acc[type]) {
                acc[type] = { total: 0, occupied: 0 };
            }
            acc[type].total += 1;
            if (room.admissions.length > 0) {
                acc[type].occupied += 1;
            }
            return acc;
        }, {});
        return {
            totalRooms,
            occupiedRooms,
            availableRooms: totalRooms - occupiedRooms,
            roomOccupancyRate: totalRooms > 0 ? ((occupiedRooms / totalRooms) * 100).toFixed(2) : '0.00',
            totalBeds,
            occupiedBeds,
            availableBeds: totalBeds - occupiedBeds,
            bedOccupancyRate: totalBeds > 0 ? ((occupiedBeds / totalBeds) * 100).toFixed(2) : '0.00',
            roomsByType,
        };
    }
};
exports.RoomsService = RoomsService;
exports.RoomsService = RoomsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], RoomsService);


/***/ }),
/* 187 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateRoomDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class CreateRoomDto {
}
exports.CreateRoomDto = CreateRoomDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateRoomDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174001' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateRoomDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '101' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateRoomDto.prototype, "roomNumber", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ enum: client_1.RoomType, example: client_1.RoomType.PRIVATE }),
    (0, class_validator_1.IsEnum)(client_1.RoomType),
    __metadata("design:type", typeof (_a = typeof client_1.RoomType !== "undefined" && client_1.RoomType) === "function" ? _a : Object)
], CreateRoomDto.prototype, "roomType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 1 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateRoomDto.prototype, "floor", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Building A' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateRoomDto.prototype, "building", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 2, description: 'Number of beds in the room' }),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], CreateRoomDto.prototype, "capacity", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 5000.0, description: 'Daily rate for the room' }),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], CreateRoomDto.prototype, "dailyRate", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        example: ['AC', 'TV', 'ATTACHED_BATH', 'WIFI'],
        description: 'Room amenities',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.IsString)({ each: true }),
    __metadata("design:type", Array)
], CreateRoomDto.prototype, "amenities", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.RoomStatus, example: client_1.RoomStatus.AVAILABLE }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.RoomStatus),
    __metadata("design:type", typeof (_b = typeof client_1.RoomStatus !== "undefined" && client_1.RoomStatus) === "function" ? _b : Object)
], CreateRoomDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Newly renovated room' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateRoomDto.prototype, "notes", void 0);


/***/ }),
/* 188 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateRoomDto = void 0;
const swagger_1 = __webpack_require__(3);
const create_room_dto_1 = __webpack_require__(187);
class UpdateRoomDto extends (0, swagger_1.PartialType)(create_room_dto_1.CreateRoomDto) {
}
exports.UpdateRoomDto = UpdateRoomDto;


/***/ }),
/* 189 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RoomQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class RoomQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 20;
    }
}
exports.RoomQueryDto = RoomQueryDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], RoomQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], RoomQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.RoomType }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.RoomType),
    __metadata("design:type", typeof (_a = typeof client_1.RoomType !== "undefined" && client_1.RoomType) === "function" ? _a : Object)
], RoomQueryDto.prototype, "roomType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.RoomStatus }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.RoomStatus),
    __metadata("design:type", typeof (_b = typeof client_1.RoomStatus !== "undefined" && client_1.RoomStatus) === "function" ? _b : Object)
], RoomQueryDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], RoomQueryDto.prototype, "floor", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], RoomQueryDto.prototype, "building", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 1 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], RoomQueryDto.prototype, "page", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 20 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], RoomQueryDto.prototype, "limit", void 0);


/***/ }),
/* 190 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BedsModule = void 0;
const common_1 = __webpack_require__(2);
const beds_controller_1 = __webpack_require__(191);
const beds_service_1 = __webpack_require__(192);
const database_module_1 = __webpack_require__(52);
let BedsModule = class BedsModule {
};
exports.BedsModule = BedsModule;
exports.BedsModule = BedsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [beds_controller_1.BedsController],
        providers: [beds_service_1.BedsService],
        exports: [beds_service_1.BedsService],
    })
], BedsModule);


/***/ }),
/* 191 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BedsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const beds_service_1 = __webpack_require__(192);
const create_bed_dto_1 = __webpack_require__(193);
const update_bed_dto_1 = __webpack_require__(194);
const bed_query_dto_1 = __webpack_require__(195);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
let BedsController = class BedsController {
    constructor(bedsService) {
        this.bedsService = bedsService;
    }
    create(createBedDto) {
        return this.bedsService.create(createBedDto);
    }
    findAll(query) {
        return this.bedsService.findAll(query);
    }
    findAvailable(hospitalId, roomId, bedType) {
        return this.bedsService.findAvailable(hospitalId, roomId, bedType);
    }
    findOne(id) {
        return this.bedsService.findOne(id);
    }
    update(id, updateBedDto) {
        return this.bedsService.update(id, updateBedDto);
    }
    updateStatus(id, status) {
        return this.bedsService.updateStatus(id, status);
    }
    remove(id) {
        return this.bedsService.remove(id);
    }
};
exports.BedsController = BedsController;
__decorate([
    (0, common_1.Post)(),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new bed' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Bed created successfully' }),
    (0, swagger_1.ApiResponse)({ status: 409, description: 'Bed number already exists' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_bed_dto_1.CreateBedDto !== "undefined" && create_bed_dto_1.CreateBedDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Get all beds with filters and pagination' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'List of beds retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof bed_query_dto_1.BedQueryDto !== "undefined" && bed_query_dto_1.BedQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('available/:hospitalId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get available beds for a hospital' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Available beds retrieved successfully' }),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Query)('roomId')),
    __param(2, (0, common_1.Query)('bedType')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "findAvailable", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get a bed by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Bed retrieved successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Bed not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update a bed' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Bed updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Bed not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_bed_dto_1.UpdateBedDto !== "undefined" && update_bed_dto_1.UpdateBedDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "update", null);
__decorate([
    (0, common_1.Patch)(':id/status'),
    (0, swagger_1.ApiOperation)({ summary: 'Update bed status' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Bed status updated successfully' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('status')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof client_1.BedStatus !== "undefined" && client_1.BedStatus) === "function" ? _e : Object]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "updateStatus", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Delete a bed' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Bed deleted successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Bed not found' }),
    (0, swagger_1.ApiResponse)({
        status: 400,
        description: 'Cannot delete bed with active admissions',
    }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], BedsController.prototype, "remove", null);
exports.BedsController = BedsController = __decorate([
    (0, swagger_1.ApiTags)('Beds'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    (0, common_1.Controller)('beds'),
    __metadata("design:paramtypes", [typeof (_a = typeof beds_service_1.BedsService !== "undefined" && beds_service_1.BedsService) === "function" ? _a : Object])
], BedsController);


/***/ }),
/* 192 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BedsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let BedsService = class BedsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createBedDto) {
        const { hospitalId, bedNumber, dailyRate, ...rest } = createBedDto;
        const existingBed = await this.prisma.bed.findUnique({
            where: {
                hospitalId_bedNumber: {
                    hospitalId,
                    bedNumber,
                },
            },
        });
        if (existingBed) {
            throw new common_1.ConflictException(`Bed with number ${bedNumber} already exists in this hospital`);
        }
        const hospital = await this.prisma.hospital.findUnique({
            where: { id: hospitalId },
        });
        if (!hospital) {
            throw new common_1.NotFoundException(`Hospital with ID ${hospitalId} not found`);
        }
        if (rest.roomId) {
            const room = await this.prisma.room.findUnique({
                where: { id: rest.roomId },
                select: { id: true, hospitalId: true, capacity: true },
            });
            if (!room) {
                throw new common_1.NotFoundException(`Room with ID ${rest.roomId} not found`);
            }
            if (room.hospitalId !== hospitalId) {
                throw new common_1.BadRequestException('Room does not belong to this hospital');
            }
            const bedsInRoom = await this.prisma.bed.count({
                where: { roomId: rest.roomId },
            });
            if (bedsInRoom >= room.capacity) {
                throw new common_1.BadRequestException(`Room capacity reached. Maximum beds allowed: ${room.capacity}`);
            }
        }
        return this.prisma.bed.create({
            data: {
                hospitalId,
                bedNumber,
                dailyRate: new client_1.Prisma.Decimal(dailyRate),
                ...rest,
            },
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                room: { select: { id: true, roomNumber: true, roomType: true } },
            },
        });
    }
    async findAll(query) {
        const { hospitalId, departmentId, roomId, bedType, status, page = 1, limit = 20 } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (departmentId)
            where.departmentId = departmentId;
        if (roomId)
            where.roomId = roomId;
        if (bedType)
            where.bedType = bedType;
        if (status)
            where.status = status;
        const skip = (page - 1) * limit;
        const [beds, total] = await Promise.all([
            this.prisma.bed.findMany({
                where,
                skip,
                take: limit,
                orderBy: [{ bedNumber: 'asc' }],
                include: {
                    hospital: { select: { id: true, name: true, code: true } },
                    department: { select: { id: true, name: true, code: true } },
                    room: { select: { id: true, roomNumber: true, roomType: true } },
                    admissions: {
                        where: { status: 'ADMITTED' },
                        select: {
                            id: true,
                            admissionNumber: true,
                            patient: {
                                select: {
                                    id: true,
                                    nrNumber: true,
                                    fullName: true,
                                },
                            },
                        },
                    },
                },
            }),
            this.prisma.bed.count({ where }),
        ]);
        return {
            data: beds,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const bed = await this.prisma.bed.findUnique({
            where: { id },
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                room: { select: { id: true, roomNumber: true, roomType: true } },
                admissions: {
                    where: { status: 'ADMITTED' },
                    select: {
                        id: true,
                        admissionNumber: true,
                        patient: {
                            select: {
                                id: true,
                                nrNumber: true,
                                fullName: true,
                                gender: true,
                            },
                        },
                        attendingDoctor: {
                            select: {
                                id: true,
                                fullName: true,
                            },
                        },
                        admittedAt: true,
                    },
                },
            },
        });
        if (!bed) {
            throw new common_1.NotFoundException(`Bed with ID ${id} not found`);
        }
        return bed;
    }
    async update(id, updateBedDto) {
        const bed = await this.prisma.bed.findUnique({ where: { id } });
        if (!bed) {
            throw new common_1.NotFoundException(`Bed with ID ${id} not found`);
        }
        if (updateBedDto.bedNumber && updateBedDto.bedNumber !== bed.bedNumber) {
            const existingBed = await this.prisma.bed.findUnique({
                where: {
                    hospitalId_bedNumber: {
                        hospitalId: bed.hospitalId,
                        bedNumber: updateBedDto.bedNumber,
                    },
                },
            });
            if (existingBed) {
                throw new common_1.ConflictException(`Bed with number ${updateBedDto.bedNumber} already exists in this hospital`);
            }
        }
        if (updateBedDto.roomId && updateBedDto.roomId !== bed.roomId) {
            const room = await this.prisma.room.findUnique({
                where: { id: updateBedDto.roomId },
                select: { id: true, capacity: true },
            });
            if (!room) {
                throw new common_1.NotFoundException(`Room with ID ${updateBedDto.roomId} not found`);
            }
            const bedsInRoom = await this.prisma.bed.count({
                where: { roomId: updateBedDto.roomId },
            });
            if (bedsInRoom >= room.capacity) {
                throw new common_1.BadRequestException(`Room capacity reached. Maximum beds allowed: ${room.capacity}`);
            }
        }
        const { dailyRate, ...rest } = updateBedDto;
        const data = { ...rest };
        if (dailyRate !== undefined) {
            data.dailyRate = new client_1.Prisma.Decimal(dailyRate);
        }
        return this.prisma.bed.update({
            where: { id },
            data,
            include: {
                hospital: { select: { id: true, name: true, code: true } },
                department: { select: { id: true, name: true, code: true } },
                room: { select: { id: true, roomNumber: true, roomType: true } },
            },
        });
    }
    async remove(id) {
        const bed = await this.prisma.bed.findUnique({
            where: { id },
            include: {
                admissions: { where: { status: 'ADMITTED' } },
            },
        });
        if (!bed) {
            throw new common_1.NotFoundException(`Bed with ID ${id} not found`);
        }
        if (bed.admissions.length > 0) {
            throw new common_1.BadRequestException(`Cannot delete bed with active admissions. Please discharge the patient first.`);
        }
        return this.prisma.bed.delete({ where: { id } });
    }
    async findAvailable(hospitalId, roomId, bedType) {
        const where = {
            hospitalId,
            status: client_1.BedStatus.AVAILABLE,
        };
        if (roomId)
            where.roomId = roomId;
        if (bedType)
            where.bedType = bedType;
        return this.prisma.bed.findMany({
            where,
            include: {
                room: { select: { id: true, roomNumber: true, roomType: true } },
                department: { select: { id: true, name: true, code: true } },
            },
            orderBy: { bedNumber: 'asc' },
        });
    }
    async updateStatus(id, status) {
        const bed = await this.prisma.bed.findUnique({ where: { id } });
        if (!bed) {
            throw new common_1.NotFoundException(`Bed with ID ${id} not found`);
        }
        return this.prisma.bed.update({
            where: { id },
            data: { status },
            include: {
                room: { select: { id: true, roomNumber: true } },
            },
        });
    }
};
exports.BedsService = BedsService;
exports.BedsService = BedsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], BedsService);


/***/ }),
/* 193 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateBedDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class CreateBedDto {
}
exports.CreateBedDto = CreateBedDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateBedDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174001' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateBedDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174002' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateBedDto.prototype, "roomId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'B-101-1' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateBedDto.prototype, "bedNumber", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ enum: client_1.BedType, example: client_1.BedType.STANDARD }),
    (0, class_validator_1.IsEnum)(client_1.BedType),
    __metadata("design:type", typeof (_a = typeof client_1.BedType !== "undefined" && client_1.BedType) === "function" ? _a : Object)
], CreateBedDto.prototype, "bedType", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 1500.0, description: 'Daily rate for the bed' }),
    (0, class_transformer_1.Type)(() => Number),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], CreateBedDto.prototype, "dailyRate", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        example: ['ELECTRIC', 'SIDE_RAILS', 'IV_POLE'],
        description: 'Bed features',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.IsString)({ each: true }),
    __metadata("design:type", Array)
], CreateBedDto.prototype, "features", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.BedStatus, example: client_1.BedStatus.AVAILABLE }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.BedStatus),
    __metadata("design:type", typeof (_b = typeof client_1.BedStatus !== "undefined" && client_1.BedStatus) === "function" ? _b : Object)
], CreateBedDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Recently serviced bed' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateBedDto.prototype, "notes", void 0);


/***/ }),
/* 194 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateBedDto = void 0;
const swagger_1 = __webpack_require__(3);
const create_bed_dto_1 = __webpack_require__(193);
class UpdateBedDto extends (0, swagger_1.PartialType)(create_bed_dto_1.CreateBedDto) {
}
exports.UpdateBedDto = UpdateBedDto;


/***/ }),
/* 195 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BedQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class BedQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 20;
    }
}
exports.BedQueryDto = BedQueryDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], BedQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], BedQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], BedQueryDto.prototype, "roomId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.BedType }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.BedType),
    __metadata("design:type", typeof (_a = typeof client_1.BedType !== "undefined" && client_1.BedType) === "function" ? _a : Object)
], BedQueryDto.prototype, "bedType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.BedStatus }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.BedStatus),
    __metadata("design:type", typeof (_b = typeof client_1.BedStatus !== "undefined" && client_1.BedStatus) === "function" ? _b : Object)
], BedQueryDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 1 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], BedQueryDto.prototype, "page", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ default: 20 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], BedQueryDto.prototype, "limit", void 0);


/***/ }),
/* 196 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationsModule = void 0;
const common_1 = __webpack_require__(2);
const operations_controller_1 = __webpack_require__(197);
const operations_service_1 = __webpack_require__(198);
const database_module_1 = __webpack_require__(52);
let OperationsModule = class OperationsModule {
};
exports.OperationsModule = OperationsModule;
exports.OperationsModule = OperationsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [operations_controller_1.OperationsController],
        providers: [operations_service_1.OperationsService],
        exports: [operations_service_1.OperationsService],
    })
], OperationsModule);


/***/ }),
/* 197 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_guard_1 = __webpack_require__(31);
const operations_service_1 = __webpack_require__(198);
const create_operation_dto_1 = __webpack_require__(199);
const update_operation_dto_1 = __webpack_require__(200);
const operation_query_dto_1 = __webpack_require__(201);
const update_operation_status_dto_1 = __webpack_require__(202);
const reschedule_operation_dto_1 = __webpack_require__(203);
const create_theatre_dto_1 = __webpack_require__(204);
const update_theatre_dto_1 = __webpack_require__(205);
const theatre_query_dto_1 = __webpack_require__(206);
const theatre_availability_query_dto_1 = __webpack_require__(207);
let OperationsController = class OperationsController {
    constructor(operationsService) {
        this.operationsService = operationsService;
    }
    create(createOperationDto, req) {
        return this.operationsService.create(createOperationDto, req.user.id);
    }
    findAll(query) {
        return this.operationsService.findAll(query);
    }
    getTheatreAvailability(query) {
        return this.operationsService.getTheatreAvailability(query);
    }
    createTheatre(createDto) {
        return this.operationsService.createTheatre(createDto);
    }
    findTheatres(query) {
        return this.operationsService.findTheatres(query);
    }
    findTheatre(id) {
        return this.operationsService.findTheatre(id);
    }
    updateTheatre(id, updateDto) {
        return this.operationsService.updateTheatre(id, updateDto);
    }
    findOne(id) {
        return this.operationsService.findOne(id);
    }
    update(id, updateDto) {
        return this.operationsService.update(id, updateDto);
    }
    updateStatus(id, statusDto) {
        return this.operationsService.updateStatus(id, statusDto);
    }
    reschedule(id, rescheduleDto) {
        return this.operationsService.reschedule(id, rescheduleDto);
    }
};
exports.OperationsController = OperationsController;
__decorate([
    (0, common_1.Post)(),
    (0, swagger_1.ApiOperation)({ summary: 'Create a new operation' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Operation created successfully' }),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_operation_dto_1.CreateOperationDto !== "undefined" && create_operation_dto_1.CreateOperationDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Get operations with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operations retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof operation_query_dto_1.OperationQueryDto !== "undefined" && operation_query_dto_1.OperationQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('theatres/availability'),
    (0, swagger_1.ApiOperation)({ summary: 'Get theatre availability for a date' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Theatre availability retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_d = typeof theatre_availability_query_dto_1.OperationTheatreAvailabilityQueryDto !== "undefined" && theatre_availability_query_dto_1.OperationTheatreAvailabilityQueryDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "getTheatreAvailability", null);
__decorate([
    (0, common_1.Post)('theatres'),
    (0, swagger_1.ApiOperation)({ summary: 'Create an operation theatre' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Operation theatre created successfully' }),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_e = typeof create_theatre_dto_1.CreateOperationTheatreDto !== "undefined" && create_theatre_dto_1.CreateOperationTheatreDto) === "function" ? _e : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "createTheatre", null);
__decorate([
    (0, common_1.Get)('theatres'),
    (0, swagger_1.ApiOperation)({ summary: 'Get operation theatres with filters' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation theatres retrieved successfully' }),
    __param(0, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_f = typeof theatre_query_dto_1.OperationTheatreQueryDto !== "undefined" && theatre_query_dto_1.OperationTheatreQueryDto) === "function" ? _f : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "findTheatres", null);
__decorate([
    (0, common_1.Get)('theatres/:id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get operation theatre by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation theatre retrieved successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation theatre not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "findTheatre", null);
__decorate([
    (0, common_1.Patch)('theatres/:id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update operation theatre' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation theatre updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation theatre not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_g = typeof update_theatre_dto_1.UpdateOperationTheatreDto !== "undefined" && update_theatre_dto_1.UpdateOperationTheatreDto) === "function" ? _g : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "updateTheatre", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get operation by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation retrieved successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation not found' }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update operation details' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_h = typeof update_operation_dto_1.UpdateOperationDto !== "undefined" && update_operation_dto_1.UpdateOperationDto) === "function" ? _h : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "update", null);
__decorate([
    (0, common_1.Post)(':id/status'),
    (0, swagger_1.ApiOperation)({ summary: 'Update operation status' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation status updated successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_j = typeof update_operation_status_dto_1.UpdateOperationStatusDto !== "undefined" && update_operation_status_dto_1.UpdateOperationStatusDto) === "function" ? _j : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "updateStatus", null);
__decorate([
    (0, common_1.Post)(':id/reschedule'),
    (0, swagger_1.ApiOperation)({ summary: 'Reschedule an operation' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Operation rescheduled successfully' }),
    (0, swagger_1.ApiResponse)({ status: 404, description: 'Operation not found' }),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof reschedule_operation_dto_1.RescheduleOperationDto !== "undefined" && reschedule_operation_dto_1.RescheduleOperationDto) === "function" ? _k : Object]),
    __metadata("design:returntype", void 0)
], OperationsController.prototype, "reschedule", null);
exports.OperationsController = OperationsController = __decorate([
    (0, swagger_1.ApiTags)('Operations'),
    (0, swagger_1.ApiBearerAuth)(),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    (0, common_1.Controller)('operations'),
    __metadata("design:paramtypes", [typeof (_a = typeof operations_service_1.OperationsService !== "undefined" && operations_service_1.OperationsService) === "function" ? _a : Object])
], OperationsController);


/***/ }),
/* 198 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
var OperationPatientType;
(function (OperationPatientType) {
    OperationPatientType["OPD"] = "OPD";
    OperationPatientType["IN_HOUSE"] = "IN_HOUSE";
})(OperationPatientType || (OperationPatientType = {}));
var OperationStatus;
(function (OperationStatus) {
    OperationStatus["SCHEDULED"] = "SCHEDULED";
    OperationStatus["PRE_OP"] = "PRE_OP";
    OperationStatus["IN_PROGRESS"] = "IN_PROGRESS";
    OperationStatus["COMPLETED"] = "COMPLETED";
    OperationStatus["CANCELLED"] = "CANCELLED";
    OperationStatus["POSTPONED"] = "POSTPONED";
})(OperationStatus || (OperationStatus = {}));
var OperationTheatreStatus;
(function (OperationTheatreStatus) {
    OperationTheatreStatus["ACTIVE"] = "ACTIVE";
    OperationTheatreStatus["MAINTENANCE"] = "MAINTENANCE";
    OperationTheatreStatus["INACTIVE"] = "INACTIVE";
})(OperationTheatreStatus || (OperationTheatreStatus = {}));
let OperationsService = class OperationsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    ensureScheduledInFuture(scheduledAt) {
        if (scheduledAt.getTime() < Date.now()) {
            throw new common_1.BadRequestException('Scheduled date/time cannot be in the past');
        }
    }
    getOperationWindow(scheduledAt, durationMinutes) {
        const start = new Date(scheduledAt);
        const end = new Date(scheduledAt.getTime() + durationMinutes * 60 * 1000);
        return { start, end };
    }
    async assertTheatreAvailability(theatreId, scheduledAt, durationMinutes, excludeOperationId) {
        const dayStart = new Date(scheduledAt);
        dayStart.setHours(0, 0, 0, 0);
        const dayEnd = new Date(scheduledAt);
        dayEnd.setHours(23, 59, 59, 999);
        const prisma = this.prisma;
        const operations = await prisma.operation.findMany({
            where: {
                theatreId,
                status: {
                    notIn: [OperationStatus.CANCELLED, OperationStatus.COMPLETED],
                },
                scheduledAt: {
                    gte: dayStart,
                    lte: dayEnd,
                },
                ...(excludeOperationId ? { NOT: { id: excludeOperationId } } : {}),
            },
            select: {
                id: true,
                scheduledAt: true,
                estimatedDurationMinutes: true,
            },
        });
        const { start, end } = this.getOperationWindow(scheduledAt, durationMinutes);
        const hasConflict = operations.some((operation) => {
            const opDuration = operation.estimatedDurationMinutes || 60;
            const opStart = new Date(operation.scheduledAt);
            const opEnd = new Date(opStart.getTime() + opDuration * 60 * 1000);
            return start < opEnd && end > opStart;
        });
        if (hasConflict) {
            throw new common_1.ConflictException('Selected theatre is not available at this time');
        }
    }
    async generateReceiptNumber() {
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await this.prisma.receipt.findFirst({
            where: {
                receiptNumber: { startsWith: `REC-${dateStr}` },
            },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        return `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
    }
    async create(createDto, createdById) {
        const { hospitalId, patientId, departmentId, surgeonId, theatreId, scheduledAt, patientType, visitId, admissionId, estimatedDurationMinutes, } = createDto;
        if (patientType === OperationPatientType.OPD && !visitId) {
            throw new common_1.BadRequestException('visitId is required for OPD operations');
        }
        if (patientType === OperationPatientType.IN_HOUSE && !admissionId) {
            throw new common_1.BadRequestException('admissionId is required for in-house operations');
        }
        if (patientType === OperationPatientType.OPD && admissionId) {
            throw new common_1.BadRequestException('admissionId should not be provided for OPD operations');
        }
        if (patientType === OperationPatientType.IN_HOUSE && visitId) {
            throw new common_1.BadRequestException('visitId should not be provided for in-house operations');
        }
        const scheduledDate = new Date(scheduledAt);
        this.ensureScheduledInFuture(scheduledDate);
        const [hospital, patient, department, surgeon, theatre] = await Promise.all([
            this.prisma.hospital.findUnique({ where: { id: hospitalId } }),
            this.prisma.patient.findUnique({ where: { id: patientId } }),
            this.prisma.department.findUnique({ where: { id: departmentId } }),
            this.prisma.user.findUnique({ where: { id: surgeonId } }),
            this.prisma.operationTheatre.findUnique({ where: { id: theatreId } }),
        ]);
        if (!hospital)
            throw new common_1.NotFoundException('Hospital not found');
        if (!patient)
            throw new common_1.NotFoundException('Patient not found');
        if (!department)
            throw new common_1.NotFoundException('Department not found');
        if (!surgeon)
            throw new common_1.NotFoundException('Surgeon not found');
        if (!theatre)
            throw new common_1.NotFoundException('Operation theatre not found');
        if (surgeon.departmentId && surgeon.departmentId !== departmentId) {
            throw new common_1.BadRequestException('Surgeon does not belong to the selected department');
        }
        if (theatre.status !== OperationTheatreStatus.ACTIVE) {
            throw new common_1.BadRequestException('Operation theatre is not active');
        }
        if (theatre.hospitalId !== hospitalId) {
            throw new common_1.BadRequestException('Operation theatre does not belong to the hospital');
        }
        if (visitId) {
            const visit = await this.prisma.visit.findUnique({ where: { id: visitId } });
            if (!visit)
                throw new common_1.NotFoundException('Visit not found');
            if (visit.patientId !== patientId || visit.hospitalId !== hospitalId) {
                throw new common_1.BadRequestException('Visit does not belong to the patient/hospital');
            }
        }
        let resolvedAdmission = null;
        if (admissionId) {
            const admission = await this.prisma.admission.findUnique({
                where: { id: admissionId },
            });
            if (!admission)
                throw new common_1.NotFoundException('Admission not found');
            if (admission.patientId !== patientId || admission.hospitalId !== hospitalId) {
                throw new common_1.BadRequestException('Admission does not belong to the patient/hospital');
            }
            if (admission.status !== client_1.AdmissionStatus.ADMITTED) {
                throw new common_1.BadRequestException('Admission is not active');
            }
            resolvedAdmission = admission;
        }
        const duration = estimatedDurationMinutes ?? 60;
        await this.assertTheatreAvailability(theatreId, scheduledDate, duration);
        const operation = await this.prisma.operation.create({
            data: {
                hospitalId,
                patientId,
                patientType,
                visitId,
                admissionId,
                departmentId,
                operationType: createDto.operationType,
                surgeonId,
                theatreId,
                scheduledAt: scheduledDate,
                estimatedDurationMinutes: duration,
                emergencyFlag: createDto.emergencyFlag ?? false,
                operationPrice: createDto.operationPrice ?? undefined,
                notes: createDto.notes,
                preOpNotes: createDto.preOpNotes,
            },
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
                theatre: {
                    select: { id: true, name: true, code: true, status: true },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
                visit: true,
                admission: true,
            },
        });
        const operationAmount = createDto.operationPrice ?? 0;
        const receiptNumber = await this.generateReceiptNumber();
        await this.prisma.receipt.create({
            data: {
                hospitalId,
                patientId,
                visitId: visitId ?? resolvedAdmission?.visitId ?? undefined,
                departmentId,
                generatedById: createdById,
                receiptNumber,
                receiptType: client_1.ReceiptType.PROCEDURE,
                description: `Procedure - ${createDto.operationType}`,
                amount: operationAmount,
                discount: 0,
                tax: 0,
                totalAmount: operationAmount,
                paidAmount: 0,
                paymentMethod: client_1.PaymentMethod.CASH,
                paymentStatus: client_1.PaymentStatus.UNPAID,
                notes: createDto.notes,
            },
        });
        return operation;
    }
    async findAll(query) {
        const { hospitalId, patientId, surgeonId, departmentId, theatreId, status, startDate, endDate, page = 1, limit = 20, } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (patientId)
            where.patientId = patientId;
        if (surgeonId)
            where.surgeonId = surgeonId;
        if (departmentId)
            where.departmentId = departmentId;
        if (theatreId)
            where.theatreId = theatreId;
        if (status)
            where.status = status;
        if (startDate || endDate) {
            where.scheduledAt = {};
            if (startDate)
                where.scheduledAt.gte = new Date(startDate);
            if (endDate)
                where.scheduledAt.lte = new Date(endDate);
        }
        const skip = (page - 1) * limit;
        const [operations, total] = await Promise.all([
            this.prisma.operation.findMany({
                where,
                skip,
                take: limit,
                orderBy: [{ scheduledAt: 'desc' }],
                include: {
                    patient: {
                        select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                    },
                    surgeon: {
                        select: { id: true, fullName: true },
                    },
                    theatre: {
                        select: { id: true, name: true, code: true, status: true },
                    },
                    department: {
                        select: { id: true, name: true, code: true },
                    },
                },
            }),
            this.prisma.operation.count({ where }),
        ]);
        return {
            data: operations,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findOne(id) {
        const operation = await this.prisma.operation.findUnique({
            where: { id },
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
                theatre: {
                    select: { id: true, name: true, code: true, status: true },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
                visit: true,
                admission: true,
            },
        });
        if (!operation)
            throw new common_1.NotFoundException('Operation not found');
        return operation;
    }
    async update(id, updateDto) {
        const operation = await this.prisma.operation.findUnique({ where: { id } });
        if (!operation)
            throw new common_1.NotFoundException('Operation not found');
        const scheduledAt = updateDto.scheduledAt
            ? new Date(updateDto.scheduledAt)
            : operation.scheduledAt;
        const theatreId = updateDto.theatreId ?? operation.theatreId;
        const duration = updateDto.estimatedDurationMinutes ?? operation.estimatedDurationMinutes;
        if (updateDto.scheduledAt) {
            this.ensureScheduledInFuture(scheduledAt);
        }
        if (updateDto.surgeonId) {
            const surgeon = await this.prisma.user.findUnique({ where: { id: updateDto.surgeonId } });
            if (!surgeon)
                throw new common_1.NotFoundException('Surgeon not found');
            if (surgeon.departmentId && surgeon.departmentId !== operation.departmentId) {
                throw new common_1.BadRequestException('Surgeon does not belong to the selected department');
            }
        }
        if (updateDto.theatreId) {
            const theatre = await this.prisma.operationTheatre.findUnique({
                where: { id: updateDto.theatreId },
            });
            if (!theatre)
                throw new common_1.NotFoundException('Operation theatre not found');
            if (theatre.status !== OperationTheatreStatus.ACTIVE) {
                throw new common_1.BadRequestException('Operation theatre is not active');
            }
        }
        if (updateDto.scheduledAt || updateDto.theatreId || updateDto.estimatedDurationMinutes) {
            await this.assertTheatreAvailability(theatreId, scheduledAt, duration, id);
        }
        return this.prisma.operation.update({
            where: { id },
            data: {
                operationType: updateDto.operationType,
                surgeonId: updateDto.surgeonId,
                theatreId: updateDto.theatreId,
                scheduledAt: updateDto.scheduledAt ? scheduledAt : undefined,
                estimatedDurationMinutes: updateDto.estimatedDurationMinutes,
                emergencyFlag: updateDto.emergencyFlag,
                operationPrice: updateDto.operationPrice,
                notes: updateDto.notes,
                preOpNotes: updateDto.preOpNotes,
                postOpNotes: updateDto.postOpNotes,
                recoveryNotes: updateDto.recoveryNotes,
                followUpAt: updateDto.followUpAt ? new Date(updateDto.followUpAt) : undefined,
            },
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
                theatre: {
                    select: { id: true, name: true, code: true, status: true },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
            },
        });
    }
    async updateStatus(id, statusDto) {
        const operation = await this.prisma.operation.findUnique({ where: { id } });
        if (!operation)
            throw new common_1.NotFoundException('Operation not found');
        const finalizedStatuses = [
            OperationStatus.CANCELLED,
            OperationStatus.COMPLETED,
        ];
        if (finalizedStatuses.includes(operation.status)) {
            throw new common_1.BadRequestException('Operation is already finalized');
        }
        const now = new Date();
        const data = {
            status: statusDto.status,
            postOpNotes: statusDto.postOpNotes,
            recoveryNotes: statusDto.recoveryNotes,
            followUpAt: statusDto.followUpAt ? new Date(statusDto.followUpAt) : undefined,
        };
        if (statusDto.status === OperationStatus.IN_PROGRESS && !operation.startedAt) {
            data.startedAt = now;
        }
        if (statusDto.status === OperationStatus.COMPLETED) {
            data.completedAt = now;
        }
        if (statusDto.status === OperationStatus.CANCELLED) {
            data.cancelledAt = now;
        }
        return this.prisma.operation.update({
            where: { id },
            data,
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
                theatre: {
                    select: { id: true, name: true, code: true, status: true },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
            },
        });
    }
    async reschedule(id, rescheduleDto) {
        const operation = await this.prisma.operation.findUnique({ where: { id } });
        if (!operation)
            throw new common_1.NotFoundException('Operation not found');
        const scheduledAt = new Date(rescheduleDto.scheduledAt);
        this.ensureScheduledInFuture(scheduledAt);
        const theatreId = rescheduleDto.theatreId ?? operation.theatreId;
        const duration = rescheduleDto.estimatedDurationMinutes ?? operation.estimatedDurationMinutes;
        await this.assertTheatreAvailability(theatreId, scheduledAt, duration, id);
        return this.prisma.operation.update({
            where: { id },
            data: {
                scheduledAt,
                theatreId: rescheduleDto.theatreId,
                estimatedDurationMinutes: rescheduleDto.estimatedDurationMinutes,
                status: OperationStatus.SCHEDULED,
            },
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true, gender: true, mobile: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
                theatre: {
                    select: { id: true, name: true, code: true, status: true },
                },
                department: {
                    select: { id: true, name: true, code: true },
                },
            },
        });
    }
    async createTheatre(createDto) {
        const [hospital, department] = await Promise.all([
            this.prisma.hospital.findUnique({ where: { id: createDto.hospitalId } }),
            createDto.departmentId
                ? this.prisma.department.findUnique({ where: { id: createDto.departmentId } })
                : Promise.resolve(null),
        ]);
        if (!hospital)
            throw new common_1.NotFoundException('Hospital not found');
        if (createDto.departmentId && !department) {
            throw new common_1.NotFoundException('Department not found');
        }
        const existing = await this.prisma.operationTheatre.findFirst({
            where: {
                hospitalId: createDto.hospitalId,
                code: createDto.code,
            },
        });
        if (existing) {
            throw new common_1.ConflictException('Operation theatre code already exists');
        }
        return this.prisma.operationTheatre.create({
            data: {
                hospitalId: createDto.hospitalId,
                departmentId: createDto.departmentId,
                name: createDto.name,
                code: createDto.code,
                location: createDto.location,
                status: createDto.status ?? OperationTheatreStatus.ACTIVE,
                notes: createDto.notes,
            },
        });
    }
    async findTheatres(query) {
        const { hospitalId, departmentId, status, page = 1, limit = 20 } = query;
        const where = {};
        if (hospitalId)
            where.hospitalId = hospitalId;
        if (departmentId)
            where.departmentId = departmentId;
        if (status)
            where.status = status;
        const skip = (page - 1) * limit;
        const [theatres, total] = await Promise.all([
            this.prisma.operationTheatre.findMany({
                where,
                skip,
                take: limit,
                orderBy: [{ name: 'asc' }],
            }),
            this.prisma.operationTheatre.count({ where }),
        ]);
        return {
            data: theatres,
            meta: {
                total,
                page,
                limit,
                totalPages: Math.ceil(total / limit),
            },
        };
    }
    async findTheatre(id) {
        const theatre = await this.prisma.operationTheatre.findUnique({ where: { id } });
        if (!theatre)
            throw new common_1.NotFoundException('Operation theatre not found');
        return theatre;
    }
    async updateTheatre(id, updateDto) {
        const theatre = await this.prisma.operationTheatre.findUnique({ where: { id } });
        if (!theatre)
            throw new common_1.NotFoundException('Operation theatre not found');
        if (updateDto.departmentId) {
            const department = await this.prisma.department.findUnique({
                where: { id: updateDto.departmentId },
            });
            if (!department)
                throw new common_1.NotFoundException('Department not found');
        }
        return this.prisma.operationTheatre.update({
            where: { id },
            data: {
                departmentId: updateDto.departmentId,
                name: updateDto.name,
                code: updateDto.code,
                location: updateDto.location,
                status: updateDto.status,
                notes: updateDto.notes,
            },
        });
    }
    async getTheatreAvailability(query) {
        const theatre = await this.prisma.operationTheatre.findUnique({
            where: { id: query.theatreId },
        });
        if (!theatre)
            throw new common_1.NotFoundException('Operation theatre not found');
        const date = new Date(query.date);
        const dayStart = new Date(date);
        dayStart.setHours(0, 0, 0, 0);
        const dayEnd = new Date(date);
        dayEnd.setHours(23, 59, 59, 999);
        const operations = await this.prisma.operation.findMany({
            where: {
                theatreId: query.theatreId,
                scheduledAt: {
                    gte: dayStart,
                    lte: dayEnd,
                },
                status: {
                    notIn: [OperationStatus.CANCELLED],
                },
            },
            orderBy: [{ scheduledAt: 'asc' }],
            include: {
                patient: {
                    select: { id: true, fullName: true, nrNumber: true },
                },
                surgeon: {
                    select: { id: true, fullName: true },
                },
            },
        });
        return {
            theatre: {
                id: theatre.id,
                name: theatre.name,
                code: theatre.code,
                status: theatre.status,
            },
            date: query.date,
            operations,
        };
    }
};
exports.OperationsService = OperationsService;
exports.OperationsService = OperationsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], OperationsService);


/***/ }),
/* 199 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateOperationDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class CreateOperationDto {
}
exports.CreateOperationDto = CreateOperationDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174001' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "patientId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ enum: client_1.OperationPatientType, example: client_1.OperationPatientType.OPD }),
    (0, class_validator_1.IsEnum)(client_1.OperationPatientType),
    __metadata("design:type", typeof (_a = typeof client_1.OperationPatientType !== "undefined" && client_1.OperationPatientType) === "function" ? _a : Object)
], CreateOperationDto.prototype, "patientType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174002' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "visitId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174003' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "admissionId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174004' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'Appendectomy' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "operationType", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174005' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "surgeonId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174006' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "theatreId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '2026-02-05T10:30:00Z' }),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "scheduledAt", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 90 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(15),
    __metadata("design:type", Number)
], CreateOperationDto.prototype, "estimatedDurationMinutes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: false }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], CreateOperationDto.prototype, "emergencyFlag", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 15000 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], CreateOperationDto.prototype, "operationPrice", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Patient has mild hypertension.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "notes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'NPO after midnight.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationDto.prototype, "preOpNotes", void 0);


/***/ }),
/* 200 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateOperationDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
class UpdateOperationDto {
}
exports.UpdateOperationDto = UpdateOperationDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Appendectomy' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "operationType", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174005' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "surgeonId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174006' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "theatreId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '2026-02-05T10:30:00Z' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "scheduledAt", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 90 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(15),
    __metadata("design:type", Number)
], UpdateOperationDto.prototype, "estimatedDurationMinutes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: true }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], UpdateOperationDto.prototype, "emergencyFlag", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 15000 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], UpdateOperationDto.prototype, "operationPrice", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Updated notes' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "notes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Pre-op checklist completed.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "preOpNotes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Post-op summary' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "postOpNotes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Recovery stable.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "recoveryNotes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '2026-02-10T10:30:00Z' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], UpdateOperationDto.prototype, "followUpAt", void 0);


/***/ }),
/* 201 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class OperationQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 20;
    }
}
exports.OperationQueryDto = OperationQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "surgeonId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "theatreId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.OperationStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.OperationStatus !== "undefined" && client_1.OperationStatus) === "function" ? _a : Object)
], OperationQueryDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationQueryDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], OperationQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], OperationQueryDto.prototype, "limit", void 0);


/***/ }),
/* 202 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateOperationStatusDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class UpdateOperationStatusDto {
}
exports.UpdateOperationStatusDto = UpdateOperationStatusDto;
__decorate([
    (0, swagger_1.ApiProperty)({ enum: client_1.OperationStatus, example: client_1.OperationStatus.PRE_OP }),
    (0, class_validator_1.IsEnum)(client_1.OperationStatus),
    __metadata("design:type", typeof (_a = typeof client_1.OperationStatus !== "undefined" && client_1.OperationStatus) === "function" ? _a : Object)
], UpdateOperationStatusDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Patient shifted to recovery' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationStatusDto.prototype, "postOpNotes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Vitals stable' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationStatusDto.prototype, "recoveryNotes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '2026-02-10T10:30:00Z' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], UpdateOperationStatusDto.prototype, "followUpAt", void 0);


/***/ }),
/* 203 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RescheduleOperationDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const swagger_2 = __webpack_require__(3);
class RescheduleOperationDto {
}
exports.RescheduleOperationDto = RescheduleOperationDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '2026-02-05T12:30:00Z' }),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], RescheduleOperationDto.prototype, "scheduledAt", void 0);
__decorate([
    (0, swagger_2.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174006' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], RescheduleOperationDto.prototype, "theatreId", void 0);
__decorate([
    (0, swagger_2.ApiPropertyOptional)({ example: 120 }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.Min)(15),
    __metadata("design:type", Number)
], RescheduleOperationDto.prototype, "estimatedDurationMinutes", void 0);


/***/ }),
/* 204 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateOperationTheatreDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class CreateOperationTheatreDto {
}
exports.CreateOperationTheatreDto = CreateOperationTheatreDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174000' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "hospitalId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174010' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'Main OT-1' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "name", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: 'OT-1' }),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "code", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Building A - Floor 2' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "location", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.OperationTheatreStatus, example: client_1.OperationTheatreStatus.ACTIVE }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.OperationTheatreStatus),
    __metadata("design:type", typeof (_a = typeof client_1.OperationTheatreStatus !== "undefined" && client_1.OperationTheatreStatus) === "function" ? _a : Object)
], CreateOperationTheatreDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Equipped for general surgery.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CreateOperationTheatreDto.prototype, "notes", void 0);


/***/ }),
/* 205 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateOperationTheatreDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class UpdateOperationTheatreDto {
}
exports.UpdateOperationTheatreDto = UpdateOperationTheatreDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: '123e4567-e89b-12d3-a456-426614174010' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], UpdateOperationTheatreDto.prototype, "departmentId", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Main OT-1' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationTheatreDto.prototype, "name", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'OT-1' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationTheatreDto.prototype, "code", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Building A - Floor 2' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationTheatreDto.prototype, "location", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ enum: client_1.OperationTheatreStatus, example: client_1.OperationTheatreStatus.ACTIVE }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.OperationTheatreStatus),
    __metadata("design:type", typeof (_a = typeof client_1.OperationTheatreStatus !== "undefined" && client_1.OperationTheatreStatus) === "function" ? _a : Object)
], UpdateOperationTheatreDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({ example: 'Renovated in 2026.' }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateOperationTheatreDto.prototype, "notes", void 0);


/***/ }),
/* 206 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationTheatreQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
const class_transformer_1 = __webpack_require__(36);
class OperationTheatreQueryDto {
    constructor() {
        this.page = 1;
        this.limit = 20;
    }
}
exports.OperationTheatreQueryDto = OperationTheatreQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationTheatreQueryDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], OperationTheatreQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.OperationTheatreStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.OperationTheatreStatus !== "undefined" && client_1.OperationTheatreStatus) === "function" ? _a : Object)
], OperationTheatreQueryDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], OperationTheatreQueryDto.prototype, "page", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], OperationTheatreQueryDto.prototype, "limit", void 0);


/***/ }),
/* 207 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.OperationTheatreAvailabilityQueryDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
class OperationTheatreAvailabilityQueryDto {
}
exports.OperationTheatreAvailabilityQueryDto = OperationTheatreAvailabilityQueryDto;
__decorate([
    (0, swagger_1.ApiProperty)({ example: '123e4567-e89b-12d3-a456-426614174006' }),
    (0, class_validator_1.IsUUID)(),
    __metadata("design:type", String)
], OperationTheatreAvailabilityQueryDto.prototype, "theatreId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({ example: '2026-02-05' }),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], OperationTheatreAvailabilityQueryDto.prototype, "date", void 0);


/***/ }),
/* 208 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabTestsModule = void 0;
const common_1 = __webpack_require__(2);
const lab_tests_controller_1 = __webpack_require__(209);
const lab_tests_service_1 = __webpack_require__(210);
const database_module_1 = __webpack_require__(52);
let LabTestsModule = class LabTestsModule {
};
exports.LabTestsModule = LabTestsModule;
exports.LabTestsModule = LabTestsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [lab_tests_controller_1.LabTestsController],
        providers: [lab_tests_service_1.LabTestsService],
        exports: [lab_tests_service_1.LabTestsService],
    })
], LabTestsModule);


/***/ }),
/* 209 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabTestsController = void 0;
const common_1 = __webpack_require__(2);
const lab_tests_service_1 = __webpack_require__(210);
const create_lab_test_dto_1 = __webpack_require__(211);
const update_lab_test_dto_1 = __webpack_require__(212);
const jwt_auth_guard_1 = __webpack_require__(29);
const client_1 = __webpack_require__(16);
let LabTestsController = class LabTestsController {
    constructor(labTestsService) {
        this.labTestsService = labTestsService;
    }
    create(createLabTestDto) {
        return this.labTestsService.create(createLabTestDto);
    }
    findAll(hospitalId, departmentId, subDepartmentId, testCategory, status) {
        return this.labTestsService.findAll(hospitalId, {
            departmentId,
            subDepartmentId,
            testCategory,
            status,
        });
    }
    getCategories(hospitalId) {
        return this.labTestsService.getCategories(hospitalId);
    }
    findByDepartment(departmentId) {
        return this.labTestsService.findByDepartment(departmentId);
    }
    findByCategory(hospitalId, category) {
        return this.labTestsService.findByCategory(hospitalId, category);
    }
    findOne(id) {
        return this.labTestsService.findOne(id);
    }
    update(id, updateLabTestDto) {
        return this.labTestsService.update(id, updateLabTestDto);
    }
    updateStatus(id, status) {
        return this.labTestsService.updateStatus(id, status);
    }
    remove(id) {
        return this.labTestsService.remove(id);
    }
};
exports.LabTestsController = LabTestsController;
__decorate([
    (0, common_1.Post)(),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof create_lab_test_dto_1.CreateLabTestDto !== "undefined" && create_lab_test_dto_1.CreateLabTestDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('departmentId')),
    __param(2, (0, common_1.Query)('subDepartmentId')),
    __param(3, (0, common_1.Query)('testCategory')),
    __param(4, (0, common_1.Query)('status')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String, String, typeof (_c = typeof client_1.LabTestStatus !== "undefined" && client_1.LabTestStatus) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('categories'),
    __param(0, (0, common_1.Query)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "getCategories", null);
__decorate([
    (0, common_1.Get)('department/:departmentId'),
    __param(0, (0, common_1.Param)('departmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "findByDepartment", null);
__decorate([
    (0, common_1.Get)('category/:hospitalId/:category'),
    __param(0, (0, common_1.Param)('hospitalId')),
    __param(1, (0, common_1.Param)('category')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "findByCategory", null);
__decorate([
    (0, common_1.Get)(':id'),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Patch)(':id'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof update_lab_test_dto_1.UpdateLabTestDto !== "undefined" && update_lab_test_dto_1.UpdateLabTestDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "update", null);
__decorate([
    (0, common_1.Patch)(':id/status'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('status')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof client_1.LabTestStatus !== "undefined" && client_1.LabTestStatus) === "function" ? _e : Object]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "updateStatus", null);
__decorate([
    (0, common_1.Delete)(':id'),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabTestsController.prototype, "remove", null);
exports.LabTestsController = LabTestsController = __decorate([
    (0, common_1.Controller)('lab-tests'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof lab_tests_service_1.LabTestsService !== "undefined" && lab_tests_service_1.LabTestsService) === "function" ? _a : Object])
], LabTestsController);


/***/ }),
/* 210 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabTestsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let LabTestsService = class LabTestsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createLabTestDto) {
        const existing = await this.prisma.labTest.findUnique({
            where: {
                hospitalId_testCode: {
                    hospitalId: createLabTestDto.hospitalId,
                    testCode: createLabTestDto.testCode,
                },
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Test code ${createLabTestDto.testCode} already exists in this hospital`);
        }
        return this.prisma.labTest.create({
            data: createLabTestDto,
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
            },
        });
    }
    async findAll(hospitalId, filters) {
        return this.prisma.labTest.findMany({
            where: {
                hospitalId,
                ...(filters?.departmentId && { departmentId: filters.departmentId }),
                ...(filters?.subDepartmentId && { subDepartmentId: filters.subDepartmentId }),
                ...(filters?.testCategory && { testCategory: filters.testCategory }),
                ...(filters?.status && { status: filters.status }),
            },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
                _count: { select: { labOrders: true } },
            },
            orderBy: [
                { testCategory: 'asc' },
                { testName: 'asc' },
            ],
        });
    }
    async findOne(id) {
        const labTest = await this.prisma.labTest.findUnique({
            where: { id },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
                _count: { select: { labOrders: true } },
            },
        });
        if (!labTest) {
            throw new common_1.NotFoundException(`Lab test with ID ${id} not found`);
        }
        return labTest;
    }
    async findByDepartment(departmentId) {
        return this.prisma.labTest.findMany({
            where: {
                departmentId,
                status: client_1.LabTestStatus.ACTIVE,
            },
            include: {
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
            },
            orderBy: { testName: 'asc' },
        });
    }
    async findByCategory(hospitalId, category) {
        return this.prisma.labTest.findMany({
            where: {
                hospitalId,
                testCategory: category,
                status: client_1.LabTestStatus.ACTIVE,
            },
            include: {
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
            },
            orderBy: { testName: 'asc' },
        });
    }
    async getCategories(hospitalId) {
        const categories = await this.prisma.labTest.groupBy({
            by: ['testCategory'],
            where: {
                hospitalId,
                status: client_1.LabTestStatus.ACTIVE,
            },
            _count: { testCategory: true },
        });
        return categories.map(cat => ({
            category: cat.testCategory,
            count: cat._count.testCategory,
        }));
    }
    async update(id, updateLabTestDto) {
        await this.findOne(id);
        if (updateLabTestDto.testCode && updateLabTestDto.hospitalId) {
            const existing = await this.prisma.labTest.findFirst({
                where: {
                    hospitalId: updateLabTestDto.hospitalId,
                    testCode: updateLabTestDto.testCode,
                    NOT: { id },
                },
            });
            if (existing) {
                throw new common_1.ConflictException(`Test code ${updateLabTestDto.testCode} already exists in this hospital`);
            }
        }
        return this.prisma.labTest.update({
            where: { id },
            data: {
                ...updateLabTestDto,
                version: { increment: 1 },
            },
            include: {
                hospital: { select: { id: true, name: true } },
                department: { select: { id: true, name: true } },
                subDepartment: { select: { id: true, name: true } },
            },
        });
    }
    async remove(id) {
        await this.findOne(id);
        const ordersCount = await this.prisma.labOrder.count({
            where: { labTestId: id },
        });
        if (ordersCount > 0) {
            throw new common_1.ConflictException(`Cannot delete test with ${ordersCount} existing orders. Set status to DISCONTINUED instead.`);
        }
        return this.prisma.labTest.delete({
            where: { id },
        });
    }
    async updateStatus(id, status) {
        await this.findOne(id);
        return this.prisma.labTest.update({
            where: { id },
            data: {
                status,
                version: { increment: 1 },
            },
        });
    }
};
exports.LabTestsService = LabTestsService;
exports.LabTestsService = LabTestsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], LabTestsService);


/***/ }),
/* 211 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateLabTestDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateLabTestDto {
}
exports.CreateLabTestDto = CreateLabTestDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "subDepartmentId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "testCode", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "testName", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "testCategory", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], CreateLabTestDto.prototype, "price", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "turnaroundTime", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabTestDto.prototype, "requirements", void 0);
__decorate([
    (0, class_validator_1.IsObject)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Object)
], CreateLabTestDto.prototype, "normalRange", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.LabTestStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.LabTestStatus !== "undefined" && client_1.LabTestStatus) === "function" ? _a : Object)
], CreateLabTestDto.prototype, "status", void 0);


/***/ }),
/* 212 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateLabTestDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_lab_test_dto_1 = __webpack_require__(211);
class UpdateLabTestDto extends (0, mapped_types_1.PartialType)(create_lab_test_dto_1.CreateLabTestDto) {
}
exports.UpdateLabTestDto = UpdateLabTestDto;


/***/ }),
/* 213 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabOrdersModule = void 0;
const common_1 = __webpack_require__(2);
const lab_orders_controller_1 = __webpack_require__(214);
const lab_orders_service_1 = __webpack_require__(215);
const lab_result_pdf_service_1 = __webpack_require__(216);
const database_module_1 = __webpack_require__(52);
let LabOrdersModule = class LabOrdersModule {
};
exports.LabOrdersModule = LabOrdersModule;
exports.LabOrdersModule = LabOrdersModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [lab_orders_controller_1.LabOrdersController],
        providers: [lab_orders_service_1.LabOrdersService, lab_result_pdf_service_1.LabResultPdfService],
        exports: [lab_orders_service_1.LabOrdersService],
    })
], LabOrdersModule);


/***/ }),
/* 214 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabOrdersController = void 0;
const common_1 = __webpack_require__(2);
const express_1 = __webpack_require__(17);
const lab_orders_service_1 = __webpack_require__(215);
const lab_result_pdf_service_1 = __webpack_require__(216);
const create_lab_order_dto_1 = __webpack_require__(218);
const update_lab_order_dto_1 = __webpack_require__(219);
const collect_sample_dto_1 = __webpack_require__(220);
const enter_result_dto_1 = __webpack_require__(221);
const approve_result_dto_1 = __webpack_require__(222);
const jwt_auth_guard_1 = __webpack_require__(29);
const client_1 = __webpack_require__(16);
let LabOrdersController = class LabOrdersController {
    constructor(labOrdersService, labResultPdfService) {
        this.labOrdersService = labOrdersService;
        this.labResultPdfService = labResultPdfService;
    }
    create(createLabOrderDto) {
        return this.labOrdersService.create(createLabOrderDto);
    }
    findAll(hospitalId, patientId, visitId, status, priority, startDate, endDate) {
        return this.labOrdersService.findAll(hospitalId, {
            patientId,
            visitId,
            status,
            priority,
            startDate: startDate ? new Date(startDate) : undefined,
            endDate: endDate ? new Date(endDate) : undefined,
        });
    }
    getPendingOrders(hospitalId) {
        return this.labOrdersService.getPendingOrders(hospitalId);
    }
    getStatistics(hospitalId, startDate, endDate) {
        return this.labOrdersService.getStatistics(hospitalId, startDate ? new Date(startDate) : undefined, endDate ? new Date(endDate) : undefined);
    }
    findByPatient(patientId) {
        return this.labOrdersService.findByPatient(patientId);
    }
    findOne(id) {
        return this.labOrdersService.findOne(id);
    }
    collectSample(id, collectSampleDto) {
        return this.labOrdersService.collectSample(id, collectSampleDto);
    }
    enterResult(id, enterResultDto) {
        return this.labOrdersService.enterResult(id, enterResultDto);
    }
    approveResult(id, approveResultDto) {
        return this.labOrdersService.approveResult(id, approveResultDto);
    }
    cancelOrder(id) {
        return this.labOrdersService.cancelOrder(id);
    }
    updatePaymentStatus(id, paymentStatus, amountPaid) {
        return this.labOrdersService.updatePaymentStatus(id, paymentStatus, amountPaid);
    }
    async generatePdf(id, res) {
        const pdfBuffer = await this.labResultPdfService.generateResultPdf(id);
        res.set({
            'Content-Type': 'application/pdf',
            'Content-Disposition': `attachment; filename=lab-result-${id}.pdf`,
            'Content-Length': pdfBuffer.length,
        });
        res.end(pdfBuffer);
    }
    update(id, updateLabOrderDto) {
        return this.labOrdersService.update(id, updateLabOrderDto);
    }
};
exports.LabOrdersController = LabOrdersController;
__decorate([
    (0, common_1.Post)(),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_c = typeof create_lab_order_dto_1.CreateLabOrderDto !== "undefined" && create_lab_order_dto_1.CreateLabOrderDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "create", null);
__decorate([
    (0, common_1.Get)(),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('patientId')),
    __param(2, (0, common_1.Query)('visitId')),
    __param(3, (0, common_1.Query)('status')),
    __param(4, (0, common_1.Query)('priority')),
    __param(5, (0, common_1.Query)('startDate')),
    __param(6, (0, common_1.Query)('endDate')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String, typeof (_d = typeof client_1.LabOrderStatus !== "undefined" && client_1.LabOrderStatus) === "function" ? _d : Object, typeof (_e = typeof client_1.TestPriority !== "undefined" && client_1.TestPriority) === "function" ? _e : Object, String, String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)('pending'),
    __param(0, (0, common_1.Query)('hospitalId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "getPendingOrders", null);
__decorate([
    (0, common_1.Get)('statistics'),
    __param(0, (0, common_1.Query)('hospitalId')),
    __param(1, (0, common_1.Query)('startDate')),
    __param(2, (0, common_1.Query)('endDate')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "getStatistics", null);
__decorate([
    (0, common_1.Get)('patient/:patientId'),
    __param(0, (0, common_1.Param)('patientId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "findByPatient", null);
__decorate([
    (0, common_1.Get)(':id'),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "findOne", null);
__decorate([
    (0, common_1.Post)(':id/collect-sample'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof collect_sample_dto_1.CollectSampleDto !== "undefined" && collect_sample_dto_1.CollectSampleDto) === "function" ? _f : Object]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "collectSample", null);
__decorate([
    (0, common_1.Post)(':id/enter-result'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_g = typeof enter_result_dto_1.EnterResultDto !== "undefined" && enter_result_dto_1.EnterResultDto) === "function" ? _g : Object]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "enterResult", null);
__decorate([
    (0, common_1.Post)(':id/approve-result'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_h = typeof approve_result_dto_1.ApproveResultDto !== "undefined" && approve_result_dto_1.ApproveResultDto) === "function" ? _h : Object]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "approveResult", null);
__decorate([
    (0, common_1.Post)(':id/cancel'),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "cancelOrder", null);
__decorate([
    (0, common_1.Patch)(':id/payment'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('paymentStatus')),
    __param(2, (0, common_1.Body)('amountPaid')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Number]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "updatePaymentStatus", null);
__decorate([
    (0, common_1.Get)(':id/pdf'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Res)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_j = typeof express_1.Response !== "undefined" && express_1.Response) === "function" ? _j : Object]),
    __metadata("design:returntype", Promise)
], LabOrdersController.prototype, "generatePdf", null);
__decorate([
    (0, common_1.Patch)(':id'),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof update_lab_order_dto_1.UpdateLabOrderDto !== "undefined" && update_lab_order_dto_1.UpdateLabOrderDto) === "function" ? _k : Object]),
    __metadata("design:returntype", void 0)
], LabOrdersController.prototype, "update", null);
exports.LabOrdersController = LabOrdersController = __decorate([
    (0, common_1.Controller)('lab-orders'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof lab_orders_service_1.LabOrdersService !== "undefined" && lab_orders_service_1.LabOrdersService) === "function" ? _a : Object, typeof (_b = typeof lab_result_pdf_service_1.LabResultPdfService !== "undefined" && lab_result_pdf_service_1.LabResultPdfService) === "function" ? _b : Object])
], LabOrdersController);


/***/ }),
/* 215 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabOrdersService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let LabOrdersService = class LabOrdersService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(createLabOrderDto) {
        let patientId = createLabOrderDto.patientId;
        if (patientId.startsWith('NR-')) {
            const patient = await this.prisma.patient.findUnique({
                where: { nrNumber: patientId },
            });
            if (!patient) {
                throw new common_1.NotFoundException(`Patient with NR number ${patientId} not found`);
            }
            patientId = patient.id;
        }
        const today = new Date();
        const dateStr = today.toISOString().split('T')[0].replace(/-/g, '');
        const lastOrder = await this.prisma.labOrder.findFirst({
            where: {
                hospitalId: createLabOrderDto.hospitalId,
                orderNumber: {
                    startsWith: `LAB-${dateStr}`,
                },
            },
            orderBy: { orderNumber: 'desc' },
        });
        let sequence = 1;
        if (lastOrder) {
            const lastSeq = parseInt(lastOrder.orderNumber.split('-')[2]);
            sequence = lastSeq + 1;
        }
        const orderNumber = `LAB-${dateStr}-${sequence.toString().padStart(4, '0')}`;
        const labTest = await this.prisma.labTest.findUnique({
            where: { id: createLabOrderDto.labTestId },
        });
        if (!labTest) {
            throw new common_1.NotFoundException('Lab test not found');
        }
        const visit = createLabOrderDto.visitId
            ? await this.prisma.visit.findUnique({
                where: { id: createLabOrderDto.visitId },
                select: { id: true, departmentId: true },
            })
            : null;
        const orderedBy = await this.prisma.user.findUnique({
            where: { id: createLabOrderDto.orderedById },
            select: { departmentId: true },
        });
        const departmentId = labTest.departmentId || visit?.departmentId || orderedBy?.departmentId || null;
        if (!departmentId) {
            throw new common_1.BadRequestException('Department is required to generate lab receipt');
        }
        const labOrder = await this.prisma.$transaction(async (tx) => {
            const order = await tx.labOrder.create({
                data: {
                    ...createLabOrderDto,
                    patientId,
                    orderNumber,
                    priority: createLabOrderDto.priority || client_1.TestPriority.ROUTINE,
                    status: client_1.LabOrderStatus.PENDING,
                },
                include: {
                    hospital: { select: { id: true, name: true } },
                    patient: {
                        select: {
                            id: true,
                            nrNumber: true,
                            fullName: true,
                            mobile: true,
                        },
                    },
                    visit: { select: { id: true, tokenNumber: true } },
                    labTest: {
                        select: {
                            id: true,
                            testCode: true,
                            testName: true,
                            testCategory: true,
                            price: true,
                            turnaroundTime: true,
                        },
                    },
                    orderedBy: {
                        select: {
                            id: true,
                            fullName: true,
                            role: true,
                        },
                    },
                },
            });
            const receiptNumber = await this.generateReceiptNumber(tx);
            const txAny = tx;
            await txAny.receipt.create({
                data: {
                    hospitalId: createLabOrderDto.hospitalId,
                    patientId,
                    visitId: createLabOrderDto.visitId || undefined,
                    departmentId,
                    generatedById: createLabOrderDto.orderedById,
                    receiptNumber,
                    receiptType: client_1.ReceiptType.LAB_TEST,
                    description: `Lab Test - ${labTest.testName}`,
                    amount: new client_1.Prisma.Decimal(labTest.price || 0),
                    totalAmount: new client_1.Prisma.Decimal(labTest.price || 0),
                    paidAmount: new client_1.Prisma.Decimal(0),
                    paymentMethod: client_1.PaymentMethod.CASH,
                    paymentStatus: client_1.PaymentStatus.UNPAID,
                    notes: JSON.stringify({ labOrderId: order.id, labTestId: labTest.id }),
                },
            });
            return order;
        });
        return labOrder;
    }
    async generateReceiptNumber(tx) {
        const today = new Date();
        const dateStr = today.toISOString().slice(0, 10).replace(/-/g, '');
        const lastReceipt = await tx.receipt.findFirst({
            where: { receiptNumber: { startsWith: `REC-${dateStr}` } },
            orderBy: { receiptNumber: 'desc' },
        });
        const sequence = lastReceipt
            ? parseInt(lastReceipt.receiptNumber.split('-')[2]) + 1
            : 1;
        return `REC-${dateStr}-${sequence.toString().padStart(4, '0')}`;
    }
    async findAll(hospitalId, filters) {
        return this.prisma.labOrder.findMany({
            where: {
                hospitalId,
                ...(filters?.patientId && { patientId: filters.patientId }),
                ...(filters?.visitId && { visitId: filters.visitId }),
                ...(filters?.status && { status: filters.status }),
                ...(filters?.priority && { priority: filters.priority }),
                ...(filters?.startDate && filters?.endDate && {
                    createdAt: {
                        gte: filters.startDate,
                        lte: filters.endDate,
                    },
                }),
            },
            include: {
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                    }
                },
                labTest: {
                    select: {
                        testCode: true,
                        testName: true,
                        testCategory: true,
                    }
                },
                orderedBy: {
                    select: {
                        fullName: true,
                        role: true,
                    }
                },
            },
            orderBy: [
                { priority: 'desc' },
                { createdAt: 'desc' },
            ],
        });
    }
    async findOne(id) {
        const labOrder = await this.prisma.labOrder.findUnique({
            where: { id },
            include: {
                hospital: { select: { id: true, name: true } },
                patient: {
                    select: {
                        id: true,
                        nrNumber: true,
                        fullName: true,
                        mobile: true,
                        gender: true,
                        dob: true,
                    }
                },
                visit: {
                    select: {
                        id: true,
                        tokenNumber: true,
                        visitDate: true,
                    }
                },
                labTest: {
                    select: {
                        id: true,
                        testCode: true,
                        testName: true,
                        testCategory: true,
                        price: true,
                        turnaroundTime: true,
                        requirements: true,
                        normalRange: true,
                    }
                },
                orderedBy: {
                    select: {
                        id: true,
                        fullName: true,
                        role: true,
                    }
                },
                sampleCollectedBy: {
                    select: {
                        fullName: true,
                    }
                },
                resultsEnteredBy: {
                    select: {
                        fullName: true,
                    }
                },
                resultsApprovedBy: {
                    select: {
                        fullName: true,
                    }
                },
            },
        });
        if (!labOrder) {
            throw new common_1.NotFoundException(`Lab order with ID ${id} not found`);
        }
        return labOrder;
    }
    async findByPatient(patientId) {
        return this.prisma.labOrder.findMany({
            where: { patientId },
            include: {
                labTest: {
                    select: {
                        testCode: true,
                        testName: true,
                        testCategory: true,
                    }
                },
                orderedBy: {
                    select: {
                        fullName: true,
                    }
                },
            },
            orderBy: { createdAt: 'desc' },
        });
    }
    async getPendingOrders(hospitalId) {
        return this.prisma.labOrder.findMany({
            where: {
                hospitalId,
                status: client_1.LabOrderStatus.PENDING,
            },
            include: {
                patient: {
                    select: {
                        nrNumber: true,
                        fullName: true,
                    }
                },
                labTest: {
                    select: {
                        testName: true,
                        testCategory: true,
                    }
                },
            },
            orderBy: [
                { priority: 'desc' },
                { createdAt: 'asc' },
            ],
        });
    }
    async collectSample(id, collectSampleDto) {
        const order = await this.findOne(id);
        if (order.status !== client_1.LabOrderStatus.PENDING) {
            throw new common_1.BadRequestException(`Cannot collect sample for order in ${order.status} status`);
        }
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                status: client_1.LabOrderStatus.SAMPLE_COLLECTED,
                sampleCollectedAt: new Date(),
                sampleCollectedById: collectSampleDto.sampleCollectedById,
                sampleType: collectSampleDto.sampleType,
                sampleNotes: collectSampleDto.sampleNotes,
                version: { increment: 1 },
            },
            include: {
                patient: { select: { fullName: true } },
                labTest: { select: { testName: true } },
            },
        });
    }
    async enterResult(id, enterResultDto) {
        const order = await this.findOne(id);
        if (order.status !== client_1.LabOrderStatus.SAMPLE_COLLECTED &&
            order.status !== client_1.LabOrderStatus.IN_PROGRESS) {
            throw new common_1.BadRequestException(`Cannot enter result for order in ${order.status} status`);
        }
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                status: client_1.LabOrderStatus.COMPLETED,
                resultsEnteredAt: new Date(),
                resultsEnteredById: enterResultDto.resultsEnteredById,
                results: enterResultDto.results,
                resultNotes: enterResultDto.resultNotes,
                resultFiles: enterResultDto.resultFiles,
                version: { increment: 1 },
            },
            include: {
                patient: { select: { fullName: true } },
                labTest: { select: { testName: true } },
            },
        });
    }
    async approveResult(id, approveResultDto) {
        const order = await this.findOne(id);
        if (order.status !== client_1.LabOrderStatus.COMPLETED) {
            throw new common_1.BadRequestException(`Cannot approve result for order in ${order.status} status`);
        }
        if (!order.results) {
            throw new common_1.BadRequestException('No results entered for this order');
        }
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                status: client_1.LabOrderStatus.APPROVED,
                resultsApprovedAt: new Date(),
                resultsApprovedById: approveResultDto.resultsApprovedById,
                approvalNotes: approveResultDto.approvalNotes,
                version: { increment: 1 },
            },
            include: {
                patient: { select: { fullName: true } },
                labTest: { select: { testName: true } },
                resultsApprovedBy: { select: { fullName: true } },
            },
        });
    }
    async updatePaymentStatus(id, paymentStatus, amountPaid) {
        await this.findOne(id);
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                paymentStatus,
                amountPaid,
                version: { increment: 1 },
            },
        });
    }
    async update(id, updateLabOrderDto) {
        await this.findOne(id);
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                ...updateLabOrderDto,
                version: { increment: 1 },
            },
            include: {
                patient: { select: { fullName: true } },
                labTest: { select: { testName: true } },
            },
        });
    }
    async cancelOrder(id) {
        const order = await this.findOne(id);
        if (order.status === client_1.LabOrderStatus.APPROVED) {
            throw new common_1.BadRequestException('Cannot cancel approved order');
        }
        return this.prisma.labOrder.update({
            where: { id },
            data: {
                status: client_1.LabOrderStatus.CANCELLED,
                version: { increment: 1 },
            },
        });
    }
    async getStatistics(hospitalId, startDate, endDate) {
        const where = {
            hospitalId,
            ...(startDate && endDate && {
                createdAt: {
                    gte: startDate,
                    lte: endDate,
                },
            }),
        };
        const [total, pending, sampleCollected, inProgress, completed, approved, cancelled, byPriority, byCategory,] = await Promise.all([
            this.prisma.labOrder.count({ where }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.PENDING } }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.SAMPLE_COLLECTED } }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.IN_PROGRESS } }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.COMPLETED } }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.APPROVED } }),
            this.prisma.labOrder.count({ where: { ...where, status: client_1.LabOrderStatus.CANCELLED } }),
            this.prisma.labOrder.groupBy({
                by: ['priority'],
                where,
                _count: { priority: true },
            }),
            this.prisma.labOrder.groupBy({
                by: ['labTestId'],
                where,
                _count: { labTestId: true },
                orderBy: { _count: { labTestId: 'desc' } },
                take: 10,
            }),
        ]);
        return {
            total,
            byStatus: {
                pending,
                sampleCollected,
                inProgress,
                completed,
                approved,
                cancelled,
            },
            byPriority: byPriority.map(p => ({
                priority: p.priority,
                count: p._count.priority,
            })),
            topTests: byCategory,
        };
    }
};
exports.LabOrdersService = LabOrdersService;
exports.LabOrdersService = LabOrdersService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], LabOrdersService);


/***/ }),
/* 216 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LabResultPdfService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const PDFDocument = __webpack_require__(217);
let LabResultPdfService = class LabResultPdfService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async generateResultPdf(labOrderId) {
        const labOrder = await this.prisma.labOrder.findUnique({
            where: { id: labOrderId },
            include: {
                hospital: true,
                patient: true,
                labTest: true,
                orderedBy: { select: { fullName: true, role: true } },
                resultsApprovedBy: { select: { fullName: true, role: true } },
            },
        });
        if (!labOrder) {
            throw new Error('Lab order not found');
        }
        return new Promise((resolve, reject) => {
            try {
                const doc = new PDFDocument({ margin: 50, size: 'A4' });
                const chunks = [];
                doc.on('data', (chunk) => chunks.push(chunk));
                doc.on('end', () => resolve(Buffer.concat(chunks)));
                doc.on('error', reject);
                doc
                    .fontSize(20)
                    .font('Helvetica-Bold')
                    .text(labOrder.hospital.name, { align: 'center' })
                    .fontSize(10)
                    .font('Helvetica')
                    .text(labOrder.hospital.address || '', { align: 'center' })
                    .text(`Phone: ${labOrder.hospital.phone || 'N/A'}`, { align: 'center' })
                    .moveDown(1);
                doc
                    .fontSize(16)
                    .font('Helvetica-Bold')
                    .text('LABORATORY TEST REPORT', { align: 'center', underline: true })
                    .moveDown(1);
                const leftColumn = 70;
                const rightColumn = 320;
                let yPosition = doc.y;
                doc.fontSize(10).font('Helvetica');
                doc
                    .font('Helvetica-Bold')
                    .text('Patient Information:', leftColumn, yPosition)
                    .font('Helvetica')
                    .moveDown(0.3)
                    .text(`NR Number: ${labOrder.patient.nrNumber}`, leftColumn)
                    .text(`Name: ${labOrder.patient.fullName}`, leftColumn)
                    .text(`Gender: ${labOrder.patient.gender}`, leftColumn)
                    .text(`Age: ${labOrder.patient.dob ? this.calculateAge(labOrder.patient.dob) : 'N/A'}`, leftColumn)
                    .text(`Mobile: ${labOrder.patient.mobile}`, leftColumn);
                yPosition = doc.y - 90;
                doc
                    .font('Helvetica-Bold')
                    .text('Order Information:', rightColumn, yPosition)
                    .font('Helvetica')
                    .moveDown(0.3)
                    .text(`Order No: ${labOrder.orderNumber}`, rightColumn)
                    .text(`Order Date: ${this.formatDate(labOrder.createdAt)}`, rightColumn)
                    .text(`Sample Collected: ${labOrder.sampleCollectedAt ? this.formatDate(labOrder.sampleCollectedAt) : 'N/A'}`, rightColumn)
                    .text(`Report Date: ${labOrder.resultsApprovedAt ? this.formatDate(labOrder.resultsApprovedAt) : this.formatDate(new Date())}`, rightColumn)
                    .text(`Priority: ${labOrder.priority}`, rightColumn);
                doc.moveDown(2);
                doc
                    .fontSize(12)
                    .font('Helvetica-Bold')
                    .text('Test Details', { underline: true })
                    .moveDown(0.5);
                doc
                    .fontSize(10)
                    .font('Helvetica')
                    .text(`Test Name: ${labOrder.labTest.testName}`)
                    .text(`Test Code: ${labOrder.labTest.testCode}`)
                    .text(`Category: ${labOrder.labTest.testCategory}`)
                    .moveDown(1);
                if (labOrder.clinicalNotes) {
                    doc
                        .font('Helvetica-Bold')
                        .text('Clinical Notes:')
                        .font('Helvetica')
                        .text(labOrder.clinicalNotes)
                        .moveDown(1);
                }
                doc
                    .fontSize(12)
                    .font('Helvetica-Bold')
                    .text('Test Results', { underline: true })
                    .moveDown(0.5);
                if (labOrder.results && typeof labOrder.results === 'object') {
                    const tableTop = doc.y;
                    const col1 = leftColumn;
                    const col2 = 250;
                    const col3 = 370;
                    const col4 = 470;
                    doc
                        .fontSize(10)
                        .font('Helvetica-Bold')
                        .text('Parameter', col1, tableTop)
                        .text('Result', col2, tableTop)
                        .text('Unit', col3, tableTop)
                        .text('Reference', col4, tableTop);
                    doc
                        .moveTo(leftColumn, tableTop + 15)
                        .lineTo(550, tableTop + 15)
                        .stroke();
                    let y = tableTop + 25;
                    doc.font('Helvetica');
                    const results = Array.isArray(labOrder.results)
                        ? labOrder.results
                        : [labOrder.results];
                    for (const result of results) {
                        if (y > 700) {
                            doc.addPage();
                            y = 50;
                        }
                        const resultObj = result;
                        doc
                            .text(resultObj.parameter || 'N/A', col1, y, { width: 170 })
                            .text(resultObj.value || resultObj.result || 'N/A', col2, y, {
                            width: 110,
                        })
                            .text(resultObj.unit || '', col3, y, { width: 90 })
                            .text(resultObj.normalRange || resultObj.reference || 'N/A', col4, y, {
                            width: 80,
                        });
                        y += 25;
                    }
                    doc.moveDown(2);
                }
                else if (labOrder.results) {
                    doc.font('Helvetica').text(JSON.stringify(labOrder.results, null, 2));
                }
                else {
                    doc.font('Helvetica').text('No results available');
                }
                if (labOrder.resultNotes) {
                    doc
                        .moveDown(1)
                        .font('Helvetica-Bold')
                        .text('Notes:')
                        .font('Helvetica')
                        .text(labOrder.resultNotes);
                }
                const footerY = 680;
                doc.moveTo(leftColumn, footerY).lineTo(550, footerY).stroke();
                doc.moveDown(2);
                doc
                    .fontSize(9)
                    .font('Helvetica-Bold')
                    .text(`Ordered By: ${labOrder.orderedBy.fullName}`, leftColumn, footerY + 20)
                    .font('Helvetica')
                    .text(`Role: ${labOrder.orderedBy.role}`, leftColumn);
                if (labOrder.resultsApprovedBy) {
                    doc
                        .font('Helvetica-Bold')
                        .text(`Approved By: ${labOrder.resultsApprovedBy.fullName}`, rightColumn, footerY + 20)
                        .font('Helvetica')
                        .text(`Role: ${labOrder.resultsApprovedBy.role}`, rightColumn)
                        .text(`Date: ${this.formatDate(labOrder.resultsApprovedAt)}`, rightColumn);
                }
                doc
                    .fontSize(8)
                    .font('Helvetica')
                    .text('This is a computer-generated report and does not require a signature.', 50, 750, { align: 'center' });
                doc.end();
            }
            catch (error) {
                reject(error);
            }
        });
    }
    calculateAge(dob) {
        const today = new Date();
        const birthDate = new Date(dob);
        let age = today.getFullYear() - birthDate.getFullYear();
        const monthDiff = today.getMonth() - birthDate.getMonth();
        if (monthDiff < 0 ||
            (monthDiff === 0 && today.getDate() < birthDate.getDate())) {
            age--;
        }
        return `${age} years`;
    }
    formatDate(date) {
        return new Date(date).toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'short',
            day: '2-digit',
            hour: '2-digit',
            minute: '2-digit',
        });
    }
};
exports.LabResultPdfService = LabResultPdfService;
exports.LabResultPdfService = LabResultPdfService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], LabResultPdfService);


/***/ }),
/* 217 */
/***/ ((module) => {

module.exports = require("pdfkit");

/***/ }),
/* 218 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateLabOrderDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateLabOrderDto {
}
exports.CreateLabOrderDto = CreateLabOrderDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "hospitalId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "patientId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "visitId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "labTestId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "orderedById", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.TestPriority),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.TestPriority !== "undefined" && client_1.TestPriority) === "function" ? _a : Object)
], CreateLabOrderDto.prototype, "priority", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLabOrderDto.prototype, "clinicalNotes", void 0);


/***/ }),
/* 219 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateLabOrderDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_lab_order_dto_1 = __webpack_require__(218);
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class UpdateLabOrderDto extends (0, mapped_types_1.PartialType)(create_lab_order_dto_1.CreateLabOrderDto) {
}
exports.UpdateLabOrderDto = UpdateLabOrderDto;
__decorate([
    (0, class_validator_1.IsEnum)(client_1.LabOrderStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.LabOrderStatus !== "undefined" && client_1.LabOrderStatus) === "function" ? _a : Object)
], UpdateLabOrderDto.prototype, "status", void 0);


/***/ }),
/* 220 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CollectSampleDto = void 0;
const class_validator_1 = __webpack_require__(35);
class CollectSampleDto {
}
exports.CollectSampleDto = CollectSampleDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CollectSampleDto.prototype, "sampleCollectedById", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CollectSampleDto.prototype, "sampleType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CollectSampleDto.prototype, "sampleNotes", void 0);


/***/ }),
/* 221 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.EnterResultDto = void 0;
const class_validator_1 = __webpack_require__(35);
class EnterResultDto {
}
exports.EnterResultDto = EnterResultDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], EnterResultDto.prototype, "resultsEnteredById", void 0);
__decorate([
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Object)
], EnterResultDto.prototype, "results", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], EnterResultDto.prototype, "resultNotes", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Array)
], EnterResultDto.prototype, "resultFiles", void 0);


/***/ }),
/* 222 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ApproveResultDto = void 0;
const class_validator_1 = __webpack_require__(35);
class ApproveResultDto {
}
exports.ApproveResultDto = ApproveResultDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ApproveResultDto.prototype, "resultsApprovedById", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ApproveResultDto.prototype, "approvalNotes", void 0);


/***/ }),
/* 223 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PayrollModule = void 0;
const common_1 = __webpack_require__(2);
const payroll_controller_1 = __webpack_require__(224);
const payroll_service_1 = __webpack_require__(226);
const database_module_1 = __webpack_require__(52);
let PayrollModule = class PayrollModule {
};
exports.PayrollModule = PayrollModule;
exports.PayrollModule = PayrollModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [payroll_controller_1.PayrollController],
        providers: [payroll_service_1.PayrollService],
        exports: [payroll_service_1.PayrollService],
    })
], PayrollModule);


/***/ }),
/* 224 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PayrollController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
const payroll_service_1 = __webpack_require__(226);
const payroll_dto_1 = __webpack_require__(227);
let PayrollController = class PayrollController {
    constructor(payrollService) {
        this.payrollService = payrollService;
    }
    async listSettings(hospitalId) {
        return this.payrollService.listSettings(hospitalId);
    }
    async getSetting(hospitalId, employeeId) {
        return this.payrollService.getSetting(hospitalId, employeeId);
    }
    async upsertSetting(hospitalId, employeeId, dto) {
        return this.payrollService.upsertSetting(hospitalId, employeeId, dto);
    }
    async runPayroll(hospitalId, query) {
        return this.payrollService.runPayroll(hospitalId, query.year, query.month, query.employeeId);
    }
    async runPayrollBatch(hospitalId, query) {
        return this.payrollService.runPayrollBatch(hospitalId, query.year, query.month);
    }
    async isPayrollGenerated(hospitalId, query) {
        return this.payrollService.isPayrollGenerated(hospitalId, query.year, query.month);
    }
    async generatePayrollBatch(hospitalId, dto) {
        return this.payrollService.generatePayrollBatch(hospitalId, dto.year, dto.month);
    }
};
exports.PayrollController = PayrollController;
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'List payroll settings' }),
    (0, common_1.Get)('settings'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "listSettings", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get payroll setting for employee' }),
    (0, common_1.Get)('settings/:employeeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "getSetting", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create/update payroll setting for employee' }),
    (0, common_1.Put)('settings/:employeeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_b = typeof payroll_dto_1.UpsertPayrollSettingDto !== "undefined" && payroll_dto_1.UpsertPayrollSettingDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "upsertSetting", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Run payroll for employee' }),
    (0, common_1.Get)('run'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof payroll_dto_1.PayrollRunQueryDto !== "undefined" && payroll_dto_1.PayrollRunQueryDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "runPayroll", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Run payroll for all employees' }),
    (0, common_1.Get)('run/batch'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof payroll_dto_1.PayrollRunQueryDto !== "undefined" && payroll_dto_1.PayrollRunQueryDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "runPayrollBatch", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Check if payroll already generated for month' }),
    (0, common_1.Get)('generated'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof payroll_dto_1.PayrollRunQueryDto !== "undefined" && payroll_dto_1.PayrollRunQueryDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "isPayrollGenerated", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Generate payroll for all employees' }),
    (0, common_1.Post)('generate'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof payroll_dto_1.PayrollGenerateDto !== "undefined" && payroll_dto_1.PayrollGenerateDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], PayrollController.prototype, "generatePayrollBatch", null);
exports.PayrollController = PayrollController = __decorate([
    (0, swagger_1.ApiTags)('Payroll'),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.Controller)('payroll'),
    __metadata("design:paramtypes", [typeof (_a = typeof payroll_service_1.PayrollService !== "undefined" && payroll_service_1.PayrollService) === "function" ? _a : Object])
], PayrollController);


/***/ }),
/* 225 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CurrentHospital = void 0;
const common_1 = __webpack_require__(2);
exports.CurrentHospital = (0, common_1.createParamDecorator)((data, ctx) => {
    const request = ctx.switchToHttp().getRequest();
    return request.user?.hospitalId || request.headers['x-hospital-id'];
});


/***/ }),
/* 226 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PayrollService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
const library_1 = __webpack_require__(107);
let PayrollService = class PayrollService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    calculatePayroll(setting, counts, year, month) {
        const daysInMonth = new Date(year, month, 0).getDate();
        const monthlySalary = Number(setting.monthlySalary);
        const allowanceAmount = Number(setting.allowanceAmount || 0);
        const otherDeductionAmount = Number(setting.otherDeductionAmount || 0);
        const dailyRate = daysInMonth > 0 ? monthlySalary / daysInMonth : 0;
        const absentDeduction = counts.absent * dailyRate;
        const halfDayDeduction = counts.halfDay * dailyRate * 0.5;
        let lateDeduction = 0;
        if (setting.latePenaltyType === client_1.LatePenaltyType.HALF_DAY) {
            lateDeduction = counts.late * dailyRate * 0.5;
        }
        else if (setting.latePenaltyType === client_1.LatePenaltyType.ABSENT) {
            lateDeduction = counts.late * dailyRate;
        }
        const leaveDeduction = setting.leavePayType === client_1.LeavePayType.UNPAID ? counts.onLeave * dailyRate : 0;
        const totalDeductions = absentDeduction + halfDayDeduction + lateDeduction + leaveDeduction + otherDeductionAmount;
        const grossPay = monthlySalary + allowanceAmount;
        const netPay = Math.max(0, grossPay - totalDeductions);
        return {
            monthlySalary,
            allowanceAmount: Number(allowanceAmount.toFixed(2)),
            otherDeductionAmount: Number(otherDeductionAmount.toFixed(2)),
            dailyRate: Number(dailyRate.toFixed(2)),
            deductions: {
                absent: Number(absentDeduction.toFixed(2)),
                halfDay: Number(halfDayDeduction.toFixed(2)),
                late: Number(lateDeduction.toFixed(2)),
                leave: Number(leaveDeduction.toFixed(2)),
                total: Number(totalDeductions.toFixed(2)),
            },
            netPay: Number(netPay.toFixed(2)),
            policy: {
                latePenaltyType: setting.latePenaltyType,
                leavePayType: setting.leavePayType,
            },
        };
    }
    async upsertSetting(hospitalId, employeeId, dto) {
        return this.prisma.payrollSetting.upsert({
            where: {
                userId: employeeId,
            },
            update: {
                monthlySalary: new library_1.Decimal(dto.monthlySalary),
                allowanceAmount: new library_1.Decimal(dto.allowanceAmount ?? 0),
                otherDeductionAmount: new library_1.Decimal(dto.otherDeductionAmount ?? 0),
                latePenaltyType: dto.latePenaltyType,
                leavePayType: dto.leavePayType,
            },
            create: {
                hospitalId,
                userId: employeeId,
                monthlySalary: new library_1.Decimal(dto.monthlySalary),
                allowanceAmount: new library_1.Decimal(dto.allowanceAmount ?? 0),
                otherDeductionAmount: new library_1.Decimal(dto.otherDeductionAmount ?? 0),
                latePenaltyType: dto.latePenaltyType,
                leavePayType: dto.leavePayType,
            },
            include: {
                user: true,
            },
        });
    }
    async getSetting(hospitalId, employeeId) {
        const setting = await this.prisma.payrollSetting.findFirst({
            where: { hospitalId, userId: employeeId },
            include: { user: true },
        });
        if (!setting) {
            throw new common_1.NotFoundException('Payroll setting not found');
        }
        return setting;
    }
    async listSettings(hospitalId) {
        return this.prisma.payrollSetting.findMany({
            where: { hospitalId },
            include: { user: true },
            orderBy: { createdAt: 'desc' },
        });
    }
    async runPayroll(hospitalId, year, month, employeeId) {
        const setting = await this.prisma.payrollSetting.findFirst({
            where: { hospitalId, userId: employeeId },
        });
        if (!setting) {
            throw new common_1.NotFoundException('Payroll setting not found');
        }
        const startDate = new Date(Date.UTC(year, month - 1, 1, 0, 0, 0));
        const endDate = new Date(Date.UTC(year, month, 0, 23, 59, 59));
        const records = await this.prisma.attendanceRecord.findMany({
            where: {
                hospitalId,
                userId: employeeId,
                attendanceDate: {
                    gte: startDate,
                    lte: endDate,
                },
            },
        });
        const approvedLeaves = await this.prisma.leave.findMany({
            where: {
                hospitalId,
                userId: employeeId,
                status: 'APPROVED',
                startDate: { lte: endDate },
                endDate: { gte: startDate },
            },
            select: { startDate: true, endDate: true },
        });
        const counts = {
            present: 0,
            absent: 0,
            late: 0,
            halfDay: 0,
            onLeave: 0,
        };
        records.forEach((record) => {
            switch (record.status) {
                case client_1.AttendanceStatus.PRESENT:
                    counts.present += 1;
                    if (record.lateByMinutes && record.lateByMinutes > 0) {
                        counts.late += 1;
                    }
                    break;
                case client_1.AttendanceStatus.ABSENT:
                    counts.absent += 1;
                    break;
                case client_1.AttendanceStatus.LATE:
                    counts.late += 1;
                    break;
                case client_1.AttendanceStatus.HALF_DAY:
                    counts.halfDay += 1;
                    break;
                case client_1.AttendanceStatus.ON_LEAVE:
                case client_1.AttendanceStatus.SICK_LEAVE:
                    counts.onLeave += 1;
                    break;
                default:
                    break;
            }
        });
        const approvedLeaveDays = approvedLeaves.reduce((total, leave) => {
            const overlapStart = leave.startDate > startDate ? leave.startDate : startDate;
            const overlapEnd = leave.endDate < endDate ? leave.endDate : endDate;
            const days = Math.floor((Date.UTC(overlapEnd.getUTCFullYear(), overlapEnd.getUTCMonth(), overlapEnd.getUTCDate()) -
                Date.UTC(overlapStart.getUTCFullYear(), overlapStart.getUTCMonth(), overlapStart.getUTCDate())) /
                (1000 * 60 * 60 * 24)) + 1;
            return total + Math.max(0, days);
        }, 0);
        counts.onLeave = Math.max(counts.onLeave, approvedLeaveDays);
        const payroll = this.calculatePayroll(setting, counts, year, month);
        return {
            employeeId,
            year,
            month,
            monthlySalary: payroll.monthlySalary,
            allowanceAmount: payroll.allowanceAmount,
            otherDeductionAmount: payroll.otherDeductionAmount,
            dailyRate: payroll.dailyRate,
            counts,
            deductions: payroll.deductions,
            netPay: payroll.netPay,
            policy: payroll.policy,
        };
    }
    async runPayrollBatch(hospitalId, year, month) {
        const startDate = new Date(Date.UTC(year, month - 1, 1, 0, 0, 0));
        const endDate = new Date(Date.UTC(year, month, 0, 23, 59, 59));
        const [users, settings, records, approvedLeaves] = await Promise.all([
            this.prisma.user.findMany({
                where: { hospitalId },
                select: {
                    id: true,
                    fullName: true,
                    email: true,
                    role: true,
                    department: { select: { name: true } },
                },
                orderBy: { fullName: 'asc' },
            }),
            this.prisma.payrollSetting.findMany({
                where: { hospitalId },
            }),
            this.prisma.attendanceRecord.findMany({
                where: {
                    hospitalId,
                    attendanceDate: { gte: startDate, lte: endDate },
                },
                select: {
                    userId: true,
                    status: true,
                    lateByMinutes: true,
                },
            }),
            this.prisma.leave.findMany({
                where: {
                    hospitalId,
                    status: 'APPROVED',
                    startDate: { lte: endDate },
                    endDate: { gte: startDate },
                },
                select: { userId: true, startDate: true, endDate: true },
            }),
        ]);
        const settingsByUser = new Map(settings.map((setting) => [setting.userId, setting]));
        const countsByUser = new Map();
        const leaveDaysByUser = new Map();
        records.forEach((record) => {
            const counts = countsByUser.get(record.userId) || {
                present: 0,
                absent: 0,
                late: 0,
                halfDay: 0,
                onLeave: 0,
            };
            switch (record.status) {
                case client_1.AttendanceStatus.PRESENT:
                    counts.present += 1;
                    if (record.lateByMinutes && record.lateByMinutes > 0) {
                        counts.late += 1;
                    }
                    break;
                case client_1.AttendanceStatus.ABSENT:
                    counts.absent += 1;
                    break;
                case client_1.AttendanceStatus.LATE:
                    counts.late += 1;
                    break;
                case client_1.AttendanceStatus.HALF_DAY:
                    counts.halfDay += 1;
                    break;
                case client_1.AttendanceStatus.ON_LEAVE:
                case client_1.AttendanceStatus.SICK_LEAVE:
                    counts.onLeave += 1;
                    break;
                default:
                    break;
            }
            countsByUser.set(record.userId, counts);
        });
        approvedLeaves.forEach((leave) => {
            const overlapStart = leave.startDate > startDate ? leave.startDate : startDate;
            const overlapEnd = leave.endDate < endDate ? leave.endDate : endDate;
            const days = Math.floor((Date.UTC(overlapEnd.getUTCFullYear(), overlapEnd.getUTCMonth(), overlapEnd.getUTCDate()) -
                Date.UTC(overlapStart.getUTCFullYear(), overlapStart.getUTCMonth(), overlapStart.getUTCDate())) /
                (1000 * 60 * 60 * 24)) + 1;
            const totalDays = Math.max(0, days);
            if (totalDays <= 0)
                return;
            leaveDaysByUser.set(leave.userId, (leaveDaysByUser.get(leave.userId) || 0) + totalDays);
        });
        return users.map((user) => {
            const counts = countsByUser.get(user.id) || {
                present: 0,
                absent: 0,
                late: 0,
                halfDay: 0,
                onLeave: 0,
            };
            const approvedLeaveDays = leaveDaysByUser.get(user.id) || 0;
            counts.onLeave = Math.max(counts.onLeave, approvedLeaveDays);
            const setting = settingsByUser.get(user.id);
            if (!setting) {
                return {
                    employeeId: user.id,
                    employeeName: user.fullName || user.email,
                    departmentName: user.department?.name || 'Unassigned',
                    role: user.role,
                    counts,
                    monthlySalary: 0,
                    allowanceAmount: 0,
                    otherDeductionAmount: 0,
                    netPay: 0,
                    configured: false,
                };
            }
            const payroll = this.calculatePayroll(setting, counts, year, month);
            return {
                employeeId: user.id,
                employeeName: user.fullName || user.email,
                departmentName: user.department?.name || 'Unassigned',
                role: user.role,
                counts,
                monthlySalary: payroll.monthlySalary,
                allowanceAmount: payroll.allowanceAmount,
                otherDeductionAmount: payroll.otherDeductionAmount,
                netPay: payroll.netPay,
                configured: true,
            };
        });
    }
    async isPayrollGenerated(hospitalId, year, month) {
        const existing = await this.prisma.payrollRecord.findFirst({
            where: { hospitalId, year, month },
            select: { id: true },
        });
        return { generated: !!existing };
    }
    async generatePayrollBatch(hospitalId, year, month) {
        if (month < 1 || month > 12) {
            throw new common_1.BadRequestException('Invalid month');
        }
        const existing = await this.prisma.payrollRecord.findFirst({
            where: { hospitalId, year, month },
            select: { id: true },
        });
        if (existing) {
            throw new common_1.ConflictException('Payroll for this month has already been generated.');
        }
        const batchData = await this.runPayrollBatch(hospitalId, year, month);
        await this.prisma.$transaction(batchData.map((row) => this.prisma.payrollRecord.create({
            data: {
                hospitalId,
                userId: row.employeeId,
                year,
                month,
                monthlySalary: new library_1.Decimal(row.monthlySalary || 0),
                allowanceAmount: new library_1.Decimal(row.allowanceAmount || 0),
                otherDeductionAmount: new library_1.Decimal(row.otherDeductionAmount || 0),
                totalPresent: row.counts.present,
                totalAbsent: row.counts.absent,
                totalLate: row.counts.late,
                totalHalfDay: row.counts.halfDay,
                totalLeave: row.counts.onLeave,
                netPay: new library_1.Decimal(row.netPay || 0),
            },
        })));
        return batchData;
    }
};
exports.PayrollService = PayrollService;
exports.PayrollService = PayrollService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], PayrollService);


/***/ }),
/* 227 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PayrollGenerateDto = exports.PayrollRunQueryDto = exports.UpsertPayrollSettingDto = exports.LeavePayType = exports.LatePenaltyType = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
var LatePenaltyType;
(function (LatePenaltyType) {
    LatePenaltyType["NONE"] = "NONE";
    LatePenaltyType["HALF_DAY"] = "HALF_DAY";
    LatePenaltyType["ABSENT"] = "ABSENT";
})(LatePenaltyType || (exports.LatePenaltyType = LatePenaltyType = {}));
var LeavePayType;
(function (LeavePayType) {
    LeavePayType["PAID"] = "PAID";
    LeavePayType["UNPAID"] = "UNPAID";
})(LeavePayType || (exports.LeavePayType = LeavePayType = {}));
class UpsertPayrollSettingDto {
}
exports.UpsertPayrollSettingDto = UpsertPayrollSettingDto;
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], UpsertPayrollSettingDto.prototype, "monthlySalary", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], UpsertPayrollSettingDto.prototype, "allowanceAmount", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], UpsertPayrollSettingDto.prototype, "otherDeductionAmount", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(LatePenaltyType),
    __metadata("design:type", String)
], UpsertPayrollSettingDto.prototype, "latePenaltyType", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(LeavePayType),
    __metadata("design:type", String)
], UpsertPayrollSettingDto.prototype, "leavePayType", void 0);
class PayrollRunQueryDto {
}
exports.PayrollRunQueryDto = PayrollRunQueryDto;
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(2000),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], PayrollRunQueryDto.prototype, "year", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], PayrollRunQueryDto.prototype, "month", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], PayrollRunQueryDto.prototype, "employeeId", void 0);
class PayrollGenerateDto {
}
exports.PayrollGenerateDto = PayrollGenerateDto;
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(2000),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], PayrollGenerateDto.prototype, "year", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], PayrollGenerateDto.prototype, "month", void 0);


/***/ }),
/* 228 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExpenditureModule = void 0;
const common_1 = __webpack_require__(2);
const expenditure_controller_1 = __webpack_require__(229);
const expenditure_service_1 = __webpack_require__(230);
const database_module_1 = __webpack_require__(52);
let ExpenditureModule = class ExpenditureModule {
};
exports.ExpenditureModule = ExpenditureModule;
exports.ExpenditureModule = ExpenditureModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [expenditure_controller_1.ExpenditureController],
        providers: [expenditure_service_1.ExpenditureService],
        exports: [expenditure_service_1.ExpenditureService],
    })
], ExpenditureModule);


/***/ }),
/* 229 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExpenditureController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
const expenditure_service_1 = __webpack_require__(230);
const expenditure_dto_1 = __webpack_require__(231);
let ExpenditureController = class ExpenditureController {
    constructor(expenditureService) {
        this.expenditureService = expenditureService;
    }
    create(hospitalId, req, dto) {
        return this.expenditureService.create(hospitalId, req.user.id, dto);
    }
    findAll(hospitalId, filter) {
        return this.expenditureService.findAll(hospitalId, filter);
    }
    getTotals(hospitalId, date) {
        return this.expenditureService.getTotalsByPeriod(hospitalId, date);
    }
    findOne(hospitalId, id) {
        return this.expenditureService.findOne(id, hospitalId);
    }
    update(hospitalId, id, dto) {
        return this.expenditureService.update(id, hospitalId, dto);
    }
    remove(hospitalId, id) {
        return this.expenditureService.remove(id, hospitalId);
    }
};
exports.ExpenditureController = ExpenditureController;
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create a new expenditure record' }),
    (0, common_1.Post)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Req)()),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, typeof (_b = typeof expenditure_dto_1.CreateExpenditureDto !== "undefined" && expenditure_dto_1.CreateExpenditureDto) === "function" ? _b : Object]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "create", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'List all expenditure records with optional filters' }),
    (0, common_1.Get)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof expenditure_dto_1.ExpenditureFilterDto !== "undefined" && expenditure_dto_1.ExpenditureFilterDto) === "function" ? _c : Object]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "findAll", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get day/month/year totals for a specific date' }),
    (0, common_1.Get)('totals'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "getTotals", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get a single expenditure record' }),
    (0, common_1.Get)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "findOne", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update an expenditure record' }),
    (0, common_1.Put)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_d = typeof expenditure_dto_1.UpdateExpenditureDto !== "undefined" && expenditure_dto_1.UpdateExpenditureDto) === "function" ? _d : Object]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "update", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Delete an expenditure record' }),
    (0, common_1.Delete)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", void 0)
], ExpenditureController.prototype, "remove", null);
exports.ExpenditureController = ExpenditureController = __decorate([
    (0, swagger_1.ApiTags)('Expenditure'),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.Controller)('expenditure'),
    __metadata("design:paramtypes", [typeof (_a = typeof expenditure_service_1.ExpenditureService !== "undefined" && expenditure_service_1.ExpenditureService) === "function" ? _a : Object])
], ExpenditureController);


/***/ }),
/* 230 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExpenditureService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const library_1 = __webpack_require__(107);
let ExpenditureService = class ExpenditureService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async create(hospitalId, userId, dto) {
        return this.prisma.expenditure.create({
            data: {
                hospital: {
                    connect: { id: hospitalId },
                },
                creator: {
                    connect: { id: userId },
                },
                date: new Date(dto.date),
                type: dto.type,
                amount: new library_1.Decimal(dto.amount),
                description: dto.description,
            },
            include: {
                creator: {
                    select: {
                        id: true,
                        fullName: true,
                    },
                },
            },
        });
    }
    async findAll(hospitalId, filter) {
        const where = { hospitalId };
        if (filter?.startDate || filter?.endDate) {
            where.date = {};
            if (filter.startDate)
                where.date.gte = new Date(filter.startDate);
            if (filter.endDate)
                where.date.lte = new Date(filter.endDate);
        }
        if (filter?.type) {
            where.type = filter.type;
        }
        return this.prisma.expenditure.findMany({
            where,
            include: {
                creator: {
                    select: {
                        id: true,
                        fullName: true,
                    },
                },
            },
            orderBy: { date: 'desc' },
        });
    }
    async findOne(id, hospitalId) {
        return this.prisma.expenditure.findFirst({
            where: { id, hospitalId },
            include: {
                creator: {
                    select: {
                        id: true,
                        fullName: true,
                    },
                },
            },
        });
    }
    async update(id, hospitalId, dto) {
        const data = {};
        if (dto.date)
            data.date = new Date(dto.date);
        if (dto.type)
            data.type = dto.type;
        if (dto.amount !== undefined)
            data.amount = new library_1.Decimal(dto.amount);
        if (dto.description !== undefined)
            data.description = dto.description;
        return this.prisma.expenditure.update({
            where: { id, hospitalId },
            data,
            include: {
                creator: {
                    select: {
                        id: true,
                        fullName: true,
                    },
                },
            },
        });
    }
    async remove(id, hospitalId) {
        return this.prisma.expenditure.delete({
            where: { id, hospitalId },
        });
    }
    async getDailyTotal(hospitalId, date) {
        const startOfDay = new Date(date);
        startOfDay.setHours(0, 0, 0, 0);
        const endOfDay = new Date(date);
        endOfDay.setHours(23, 59, 59, 999);
        const result = await this.prisma.expenditure.aggregate({
            where: {
                hospitalId,
                date: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            },
            _sum: {
                amount: true,
            },
        });
        return {
            date,
            total: result._sum.amount || new library_1.Decimal(0),
        };
    }
    async getMonthlyTotal(hospitalId, year, month) {
        const startOfMonth = new Date(year, month - 1, 1);
        const endOfMonth = new Date(year, month, 0, 23, 59, 59, 999);
        const result = await this.prisma.expenditure.aggregate({
            where: {
                hospitalId,
                date: {
                    gte: startOfMonth,
                    lte: endOfMonth,
                },
            },
            _sum: {
                amount: true,
            },
        });
        return {
            year,
            month,
            total: result._sum.amount || new library_1.Decimal(0),
        };
    }
    async getYearlyTotal(hospitalId, year) {
        const startOfYear = new Date(year, 0, 1);
        const endOfYear = new Date(year, 11, 31, 23, 59, 59, 999);
        const result = await this.prisma.expenditure.aggregate({
            where: {
                hospitalId,
                date: {
                    gte: startOfYear,
                    lte: endOfYear,
                },
            },
            _sum: {
                amount: true,
            },
        });
        return {
            year,
            total: result._sum.amount || new library_1.Decimal(0),
        };
    }
    async getTotalsByPeriod(hospitalId, date) {
        const targetDate = date ? new Date(date) : new Date();
        const year = targetDate.getFullYear();
        const month = targetDate.getMonth() + 1;
        const dateStr = targetDate.toISOString().split('T')[0];
        const [daily, monthly, yearly] = await Promise.all([
            this.getDailyTotal(hospitalId, dateStr),
            this.getMonthlyTotal(hospitalId, year, month),
            this.getYearlyTotal(hospitalId, year),
        ]);
        return { daily, monthly, yearly };
    }
};
exports.ExpenditureService = ExpenditureService;
exports.ExpenditureService = ExpenditureService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ExpenditureService);


/***/ }),
/* 231 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExpenditureFilterDto = exports.UpdateExpenditureDto = exports.CreateExpenditureDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateExpenditureDto {
}
exports.CreateExpenditureDto = CreateExpenditureDto;
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateExpenditureDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ExpenditureType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.ExpenditureType !== "undefined" && client_1.ExpenditureType) === "function" ? _a : Object)
], CreateExpenditureDto.prototype, "type", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Number)
], CreateExpenditureDto.prototype, "amount", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateExpenditureDto.prototype, "description", void 0);
class UpdateExpenditureDto {
}
exports.UpdateExpenditureDto = UpdateExpenditureDto;
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateExpenditureDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ExpenditureType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_b = typeof client_1.ExpenditureType !== "undefined" && client_1.ExpenditureType) === "function" ? _b : Object)
], UpdateExpenditureDto.prototype, "type", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], UpdateExpenditureDto.prototype, "amount", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateExpenditureDto.prototype, "description", void 0);
class ExpenditureFilterDto {
}
exports.ExpenditureFilterDto = ExpenditureFilterDto;
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ExpenditureFilterDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ExpenditureFilterDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.ExpenditureType),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.ExpenditureType !== "undefined" && client_1.ExpenditureType) === "function" ? _c : Object)
], ExpenditureFilterDto.prototype, "type", void 0);


/***/ }),
/* 232 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceModule = void 0;
const common_1 = __webpack_require__(2);
const biometric_devices_module_1 = __webpack_require__(233);
const biometric_enrollments_module_1 = __webpack_require__(237);
const attendance_records_module_1 = __webpack_require__(241);
const shifts_module_1 = __webpack_require__(246);
const leaves_module_1 = __webpack_require__(250);
const device_sync_module_1 = __webpack_require__(254);
let AttendanceModule = class AttendanceModule {
};
exports.AttendanceModule = AttendanceModule;
exports.AttendanceModule = AttendanceModule = __decorate([
    (0, common_1.Module)({
        imports: [
            biometric_devices_module_1.BiometricDevicesModule,
            biometric_enrollments_module_1.BiometricEnrollmentsModule,
            attendance_records_module_1.AttendanceRecordsModule,
            shifts_module_1.ShiftsModule,
            leaves_module_1.LeavesModule,
            device_sync_module_1.DeviceSyncModule,
        ],
        exports: [biometric_devices_module_1.BiometricDevicesModule, biometric_enrollments_module_1.BiometricEnrollmentsModule, attendance_records_module_1.AttendanceRecordsModule, shifts_module_1.ShiftsModule, leaves_module_1.LeavesModule, device_sync_module_1.DeviceSyncModule],
    })
], AttendanceModule);


/***/ }),
/* 233 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricDevicesModule = void 0;
const common_1 = __webpack_require__(2);
const biometric_devices_service_1 = __webpack_require__(234);
const biometric_devices_controller_1 = __webpack_require__(235);
const database_module_1 = __webpack_require__(52);
let BiometricDevicesModule = class BiometricDevicesModule {
};
exports.BiometricDevicesModule = BiometricDevicesModule;
exports.BiometricDevicesModule = BiometricDevicesModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [biometric_devices_controller_1.BiometricDevicesController],
        providers: [biometric_devices_service_1.BiometricDevicesService],
        exports: [biometric_devices_service_1.BiometricDevicesService],
    })
], BiometricDevicesModule);


/***/ }),
/* 234 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricDevicesService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let BiometricDevicesService = class BiometricDevicesService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async registerDevice(hospitalId, dto) {
        const existingDevice = await this.prisma.biometricDevice.findUnique({
            where: {
                serialNumber: dto.serialNumber,
            },
        });
        if (existingDevice) {
            throw new common_1.BadRequestException(`Device with serial number ${dto.serialNumber} already registered`);
        }
        return this.prisma.biometricDevice.create({
            data: {
                ...dto,
                hospitalId,
                isOnline: false,
                lastSyncStatus: null,
            },
        });
    }
    async getDeviceById(hospitalId, deviceId) {
        const device = await this.prisma.biometricDevice.findFirst({
            where: {
                id: deviceId,
                hospitalId,
            },
        });
        if (!device) {
            throw new common_1.NotFoundException(`Device ${deviceId} not found`);
        }
        return device;
    }
    async getDevices(hospitalId, query) {
        const { deviceType, status, skip = 0, take = 10 } = query || {};
        return this.prisma.biometricDevice.findMany({
            where: {
                hospitalId,
                ...(deviceType && { deviceType }),
                ...(status && { status }),
            },
            skip,
            take,
            orderBy: { createdAt: 'desc' },
        });
    }
    async getDeviceCount(hospitalId, status) {
        return this.prisma.biometricDevice.count({
            where: {
                hospitalId,
                ...(status && { status }),
            },
        });
    }
    async updateDevice(hospitalId, deviceId, dto) {
        await this.getDeviceById(hospitalId, deviceId);
        return this.prisma.biometricDevice.update({
            where: { id: deviceId },
            data: dto,
        });
    }
    async updateDeviceStatus(hospitalId, deviceId, status) {
        return this.updateDevice(hospitalId, deviceId, { status });
    }
    async setDeviceOnlineStatus(hospitalId, deviceId, isOnline) {
        await this.getDeviceById(hospitalId, deviceId);
        return this.prisma.biometricDevice.update({
            where: { id: deviceId },
            data: {
                isOnline,
                lastSyncTime: isOnline ? new Date() : undefined,
            },
        });
    }
    async updateDeviceSyncStatus(hospitalId, deviceId, lastSyncStatus, isOnline = true) {
        await this.getDeviceById(hospitalId, deviceId);
        return this.prisma.biometricDevice.update({
            where: { id: deviceId },
            data: {
                lastSyncTime: new Date(),
                lastSyncStatus,
                isOnline,
            },
        });
    }
    async getDeviceEnrollmentCount(deviceId) {
        return this.prisma.biometricEnrollment.count({
            where: {
                deviceId,
                isActive: true,
            },
        });
    }
    async getActiveDevices(hospitalId) {
        return this.prisma.biometricDevice.findMany({
            where: {
                hospitalId,
                status: client_1.DeviceStatus.ACTIVE,
            },
            orderBy: { createdAt: 'desc' },
        });
    }
    async deleteDevice(hospitalId, deviceId) {
        await this.getDeviceById(hospitalId, deviceId);
        return this.prisma.biometricDevice.update({
            where: { id: deviceId },
            data: {
                status: client_1.DeviceStatus.DISABLED,
            },
        });
    }
};
exports.BiometricDevicesService = BiometricDevicesService;
exports.BiometricDevicesService = BiometricDevicesService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], BiometricDevicesService);


/***/ }),
/* 235 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricDevicesController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const biometric_devices_service_1 = __webpack_require__(234);
const create_biometric_device_dto_1 = __webpack_require__(236);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
let BiometricDevicesController = class BiometricDevicesController {
    constructor(deviceService) {
        this.deviceService = deviceService;
    }
    async registerDevice(hospitalId, dto) {
        return this.deviceService.registerDevice(hospitalId, dto);
    }
    async getDevices(hospitalId, query) {
        return this.deviceService.getDevices(hospitalId, query);
    }
    async getActiveDevices(hospitalId) {
        return this.deviceService.getActiveDevices(hospitalId);
    }
    async getDeviceCount(hospitalId) {
        const total = await this.deviceService.getDeviceCount(hospitalId);
        const active = await this.deviceService.getDeviceCount(hospitalId, 'ACTIVE');
        const inactive = await this.deviceService.getDeviceCount(hospitalId, 'INACTIVE');
        return {
            total,
            active,
            inactive,
        };
    }
    async getDeviceById(hospitalId, deviceId) {
        return this.deviceService.getDeviceById(hospitalId, deviceId);
    }
    async getDeviceStatus(hospitalId, deviceId) {
        const device = await this.deviceService.getDeviceById(hospitalId, deviceId);
        return {
            id: device.id,
            status: device.status,
            isOnline: device.isOnline,
            lastSyncTime: device.lastSyncTime,
            lastSyncStatus: device.lastSyncStatus,
        };
    }
    async getEnrollmentCount(deviceId) {
        const count = await this.deviceService.getDeviceEnrollmentCount(deviceId);
        return { deviceId, enrollmentCount: count };
    }
    async updateDevice(hospitalId, deviceId, dto) {
        return this.deviceService.updateDevice(hospitalId, deviceId, dto);
    }
    async setOnlineStatus(hospitalId, deviceId, body) {
        return this.deviceService.setDeviceOnlineStatus(hospitalId, deviceId, body.isOnline);
    }
    async deleteDevice(hospitalId, deviceId) {
        await this.deviceService.deleteDevice(hospitalId, deviceId);
    }
};
exports.BiometricDevicesController = BiometricDevicesController;
__decorate([
    (0, common_1.Post)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    (0, swagger_1.ApiOperation)({
        summary: 'Register a new biometric device',
        description: 'Register a new biometric device at a hospital location. Supports fingerprint, face, and iris recognition devices.',
        operationId: 'registerBiometricDevice',
    }),
    (0, swagger_1.ApiResponse)({
        status: 201,
        description: 'Device registered successfully',
        schema: {
            example: {
                id: 'dev-12345678',
                name: 'Device 1 - Reception',
                deviceType: 'FINGERPRINT',
                status: 'ACTIVE',
                isOnline: true,
                createdAt: '2026-02-17T10:30:00Z',
            },
        },
    }),
    (0, swagger_1.ApiResponse)({
        status: 400,
        description: 'Invalid device data',
    }),
    (0, swagger_1.ApiResponse)({
        status: 401,
        description: 'Unauthorized - JWT token missing or invalid',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof create_biometric_device_dto_1.CreateBiometricDeviceDto !== "undefined" && create_biometric_device_dto_1.CreateBiometricDeviceDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "registerDevice", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({
        summary: 'Get all biometric devices',
        description: 'Retrieve all biometric devices registered at the hospital with optional filtering',
    }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Devices retrieved successfully',
        isArray: true,
        schema: {
            example: [
                {
                    id: 'dev-12345678',
                    name: 'Device 1 - Reception',
                    deviceType: 'FINGERPRINT',
                    status: 'ACTIVE',
                    isOnline: true,
                },
            ],
        },
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof create_biometric_device_dto_1.QueryBiometricDevicesDto !== "undefined" && create_biometric_device_dto_1.QueryBiometricDevicesDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getDevices", null);
__decorate([
    (0, common_1.Get)('active'),
    (0, swagger_1.ApiOperation)({ summary: 'Get active biometric devices' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Active devices retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getActiveDevices", null);
__decorate([
    (0, common_1.Get)('count'),
    (0, swagger_1.ApiOperation)({ summary: 'Get device count' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Device count retrieved successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getDeviceCount", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get device by ID' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Device retrieved successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getDeviceById", null);
__decorate([
    (0, common_1.Get)(':id/status'),
    (0, swagger_1.ApiOperation)({ summary: 'Get device status' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Device status retrieved successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getDeviceStatus", null);
__decorate([
    (0, common_1.Get)(':id/enrollments/count'),
    (0, swagger_1.ApiOperation)({ summary: 'Get enrollment count for device' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Enrollment count retrieved successfully',
    }),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "getEnrollmentCount", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Update device configuration' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Device updated successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_d = typeof create_biometric_device_dto_1.UpdateBiometricDeviceDto !== "undefined" && create_biometric_device_dto_1.UpdateBiometricDeviceDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "updateDevice", null);
__decorate([
    (0, common_1.Put)(':id/online-status'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Update device online status' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Device online status updated successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "setOnlineStatus", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.NO_CONTENT),
    (0, swagger_1.ApiOperation)({ summary: 'Delete/disable device' }),
    (0, swagger_1.ApiResponse)({
        status: 204,
        description: 'Device deleted successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricDevicesController.prototype, "deleteDevice", null);
exports.BiometricDevicesController = BiometricDevicesController = __decorate([
    (0, swagger_1.ApiTags)('Biometric Devices'),
    (0, common_1.Controller)('biometric-devices'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    __metadata("design:paramtypes", [typeof (_a = typeof biometric_devices_service_1.BiometricDevicesService !== "undefined" && biometric_devices_service_1.BiometricDevicesService) === "function" ? _a : Object])
], BiometricDevicesController);


/***/ }),
/* 236 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c, _d, _e, _f;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SyncDeviceDto = exports.QueryBiometricDevicesDto = exports.UpdateBiometricDeviceDto = exports.CreateBiometricDeviceDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class CreateBiometricDeviceDto {
}
exports.CreateBiometricDeviceDto = CreateBiometricDeviceDto;
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Device name/identifier',
        example: 'Device 1 - Reception',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateBiometricDeviceDto.prototype, "name", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        enum: client_1.BiometricType,
        description: 'Type of biometric device',
        example: 'FINGERPRINT',
    }),
    (0, class_validator_1.IsEnum)(client_1.BiometricType),
    __metadata("design:type", typeof (_a = typeof client_1.BiometricType !== "undefined" && client_1.BiometricType) === "function" ? _a : Object)
], CreateBiometricDeviceDto.prototype, "deviceType", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Device serial number from manufacturer',
        example: 'ZKT-001-2026',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateBiometricDeviceDto.prototype, "serialNumber", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'IP address of the device',
        example: '192.168.1.100',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateBiometricDeviceDto.prototype, "ipAddress", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Port number for device communication',
        example: 4370,
    }),
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.IsPositive)(),
    __metadata("design:type", Number)
], CreateBiometricDeviceDto.prototype, "port", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Physical location of the device',
        example: 'Reception Area',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateBiometricDeviceDto.prototype, "location", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Device-specific configuration as JSON',
        example: { timezone: 'Asia/Kolkata', language: 'en' },
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsJSON)(),
    __metadata("design:type", typeof (_b = typeof Record !== "undefined" && Record) === "function" ? _b : Object)
], CreateBiometricDeviceDto.prototype, "configuration", void 0);
class UpdateBiometricDeviceDto {
}
exports.UpdateBiometricDeviceDto = UpdateBiometricDeviceDto;
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Updated device name',
        example: 'Device 1 - Main Reception',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateBiometricDeviceDto.prototype, "name", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Updated IP address',
        example: '192.168.1.101',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateBiometricDeviceDto.prototype, "ipAddress", void 0);
__decorate([
    (0, class_validator_1.IsInt)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], UpdateBiometricDeviceDto.prototype, "port", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateBiometricDeviceDto.prototype, "location", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.DeviceStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_c = typeof client_1.DeviceStatus !== "undefined" && client_1.DeviceStatus) === "function" ? _c : Object)
], UpdateBiometricDeviceDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsJSON)(),
    __metadata("design:type", typeof (_d = typeof Record !== "undefined" && Record) === "function" ? _d : Object)
], UpdateBiometricDeviceDto.prototype, "configuration", void 0);
class QueryBiometricDevicesDto {
}
exports.QueryBiometricDevicesDto = QueryBiometricDevicesDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.BiometricType),
    __metadata("design:type", typeof (_e = typeof client_1.BiometricType !== "undefined" && client_1.BiometricType) === "function" ? _e : Object)
], QueryBiometricDevicesDto.prototype, "deviceType", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.DeviceStatus),
    __metadata("design:type", typeof (_f = typeof client_1.DeviceStatus !== "undefined" && client_1.DeviceStatus) === "function" ? _f : Object)
], QueryBiometricDevicesDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], QueryBiometricDevicesDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], QueryBiometricDevicesDto.prototype, "take", void 0);
class SyncDeviceDto {
}
exports.SyncDeviceDto = SyncDeviceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], SyncDeviceDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsInt)(),
    __metadata("design:type", Number)
], SyncDeviceDto.prototype, "lastSyncTimestamp", void 0);


/***/ }),
/* 237 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricEnrollmentsModule = void 0;
const common_1 = __webpack_require__(2);
const biometric_enrollments_service_1 = __webpack_require__(238);
const biometric_enrollments_controller_1 = __webpack_require__(239);
const database_module_1 = __webpack_require__(52);
let BiometricEnrollmentsModule = class BiometricEnrollmentsModule {
};
exports.BiometricEnrollmentsModule = BiometricEnrollmentsModule;
exports.BiometricEnrollmentsModule = BiometricEnrollmentsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [biometric_enrollments_controller_1.BiometricEnrollmentsController],
        providers: [biometric_enrollments_service_1.BiometricEnrollmentsService],
        exports: [biometric_enrollments_service_1.BiometricEnrollmentsService],
    })
], BiometricEnrollmentsModule);


/***/ }),
/* 238 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricEnrollmentsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
const encryption_service_1 = __webpack_require__(18);
const crypto = __webpack_require__(19);
let BiometricEnrollmentsService = class BiometricEnrollmentsService {
    constructor(prisma, encryptionService) {
        this.prisma = prisma;
        this.encryptionService = encryptionService;
    }
    async startEnrollment(hospitalId, dto) {
        const employee = await this.prisma.user.findUnique({
            where: { id: dto.employeeId },
        });
        if (!employee || employee.hospitalId !== hospitalId) {
            throw new common_1.NotFoundException(`Employee ${dto.employeeId} not found`);
        }
        const device = await this.prisma.biometricDevice.findFirst({
            where: {
                id: dto.deviceId,
                hospitalId,
            },
        });
        if (!device) {
            throw new common_1.NotFoundException(`Device ${dto.deviceId} not found`);
        }
        const existing = await this.prisma.biometricEnrollment.findFirst({
            where: {
                userId: dto.employeeId,
                deviceId: dto.deviceId,
                enrollmentType: dto.enrollmentType,
                isActive: true,
            },
        });
        if (existing) {
            throw new common_1.ConflictException(`Employee already enrolled for ${dto.enrollmentType} on this device`);
        }
        return this.prisma.biometricEnrollment.create({
            data: {
                userId: dto.employeeId,
                deviceId: dto.deviceId,
                enrollmentType: dto.enrollmentType,
                hospitalId,
                status: client_1.EnrollmentStatus.PENDING,
                templateData: '',
                qualityScore: 0,
                isActive: true,
            },
        });
    }
    async getEnrollment(hospitalId, enrollmentId) {
        const enrollment = await this.prisma.biometricEnrollment.findFirst({
            where: {
                id: enrollmentId,
                hospitalId,
            },
        });
        if (!enrollment) {
            throw new common_1.NotFoundException(`Enrollment ${enrollmentId} not found`);
        }
        return enrollment;
    }
    async getEmployeeEnrollments(hospitalId, employeeId) {
        return this.prisma.biometricEnrollment.findMany({
            where: {
                userId: employeeId,
                hospitalId,
            },
            orderBy: { createdAt: 'desc' },
        });
    }
    async queryEnrollments(hospitalId, query) {
        const { employeeId, deviceId, enrollmentType, status, skip = 0, take = 10 } = query || {};
        return this.prisma.biometricEnrollment.findMany({
            where: {
                hospitalId,
                ...(employeeId && { userId: employeeId }),
                ...(deviceId && { deviceId }),
                ...(enrollmentType && { enrollmentType }),
                ...(status && { status }),
            },
            skip,
            take,
            orderBy: { createdAt: 'desc' },
        });
    }
    async enrollBiometric(hospitalId, enrollmentId, dto) {
        const enrollment = await this.getEnrollment(hospitalId, enrollmentId);
        if (enrollment.status === client_1.EnrollmentStatus.COMPLETED) {
            throw new common_1.BadRequestException('Enrollment already completed');
        }
        if (enrollment.status === client_1.EnrollmentStatus.REVOKED) {
            throw new common_1.BadRequestException('Enrollment has been revoked');
        }
        if (dto.quality < 70) {
            throw new common_1.BadRequestException(`Biometric quality ${dto.quality} below minimum threshold of 70`);
        }
        const encryptedData = this.encryptBiometric(dto.biometricData);
        const fingerprint = crypto
            .createHash('sha256')
            .update(dto.biometricData)
            .digest('hex');
        const duplicate = await this.prisma.biometricEnrollment.findFirst({
            where: {
                hospitalId,
                enrollmentType: enrollment.enrollmentType,
                status: client_1.EnrollmentStatus.COMPLETED,
            },
        });
        if (duplicate && duplicate.id !== enrollmentId) {
            throw new common_1.ConflictException('This biometric enrollment type is already used by another employee');
        }
        const newAttemptCount = (enrollment.attemptCount || 0) + 1;
        const newStatus = newAttemptCount >= 3 ? client_1.EnrollmentStatus.COMPLETED : client_1.EnrollmentStatus.PENDING;
        return this.prisma.biometricEnrollment.update({
            where: { id: enrollmentId },
            data: {
                templateData: encryptedData,
                qualityScore: dto.quality,
                attemptCount: newAttemptCount,
                status: newStatus,
                lastVerifiedAt: new Date(),
                version: (enrollment.version || 1) + 1,
            },
        });
    }
    async verifyEnrollment(hospitalId, enrollmentId, dto) {
        const enrollment = await this.getEnrollment(hospitalId, enrollmentId);
        if (enrollment.status !== client_1.EnrollmentStatus.COMPLETED) {
            throw new common_1.BadRequestException('Enrollment is not yet complete');
        }
        if (!enrollment.templateData) {
            throw new common_1.BadRequestException('No biometric template found for verification');
        }
        const storedTemplate = this.decryptBiometric(enrollment.templateData);
        const verificationFingerprint = crypto
            .createHash('sha256')
            .update(dto.verificationData)
            .digest('hex');
        const templateFingerprint = crypto
            .createHash('sha256')
            .update(storedTemplate)
            .digest('hex');
        const matchScore = verificationFingerprint === templateFingerprint ? 100 : 0;
        await this.prisma.biometricEnrollment.update({
            where: { id: enrollmentId },
            data: { lastVerifiedAt: new Date() },
        });
        return {
            success: matchScore >= 80,
            matchScore,
            message: matchScore >= 80 ? 'Verification successful' : 'Verification failed',
        };
    }
    async revokeEnrollment(hospitalId, enrollmentId, dto) {
        const enrollment = await this.getEnrollment(hospitalId, enrollmentId);
        return this.prisma.biometricEnrollment.update({
            where: { id: enrollmentId },
            data: {
                status: client_1.EnrollmentStatus.REVOKED,
                isActive: false,
                rejectionReason: dto.reason,
            },
        });
    }
    async getEmployeeEnrollmentCount(hospitalId, employeeId) {
        return this.prisma.biometricEnrollment.count({
            where: {
                userId: employeeId,
                hospitalId,
                status: client_1.EnrollmentStatus.COMPLETED,
                isActive: true,
            },
        });
    }
    async getEnrollmentCountByType(hospitalId, enrollmentType) {
        return this.prisma.biometricEnrollment.count({
            where: {
                hospitalId,
                enrollmentType,
                status: client_1.EnrollmentStatus.COMPLETED,
                isActive: true,
            },
        });
    }
    async updateEnrollmentMetadata(hospitalId, enrollmentId, dto) {
        await this.getEnrollment(hospitalId, enrollmentId);
        return this.prisma.biometricEnrollment.update({
            where: { id: enrollmentId },
            data: {
                ...(dto.notes && { rejectionReason: dto.notes }),
            },
        });
    }
    async getDeviceEnrollments(deviceId) {
        return this.prisma.biometricEnrollment.findMany({
            where: {
                deviceId,
                status: client_1.EnrollmentStatus.COMPLETED,
                isActive: true,
            },
        });
    }
    encryptBiometric(data) {
        return this.encryptionService.encrypt(data);
    }
    decryptBiometric(encryptedData) {
        return this.encryptionService.decrypt(encryptedData);
    }
};
exports.BiometricEnrollmentsService = BiometricEnrollmentsService;
exports.BiometricEnrollmentsService = BiometricEnrollmentsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object, typeof (_b = typeof encryption_service_1.EncryptionService !== "undefined" && encryption_service_1.EncryptionService) === "function" ? _b : Object])
], BiometricEnrollmentsService);


/***/ }),
/* 239 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BiometricEnrollmentsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const biometric_enrollments_service_1 = __webpack_require__(238);
const biometric_enrollment_dto_1 = __webpack_require__(240);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
const client_1 = __webpack_require__(16);
let BiometricEnrollmentsController = class BiometricEnrollmentsController {
    constructor(enrollmentService) {
        this.enrollmentService = enrollmentService;
    }
    async startEnrollment(hospitalId, dto) {
        return this.enrollmentService.startEnrollment(hospitalId, dto);
    }
    async queryEnrollments(hospitalId, query) {
        return this.enrollmentService.queryEnrollments(hospitalId, query);
    }
    async getEnrollment(hospitalId, enrollmentId) {
        return this.enrollmentService.getEnrollment(hospitalId, enrollmentId);
    }
    async getEmployeeEnrollments(hospitalId, employeeId) {
        return this.enrollmentService.getEmployeeEnrollments(hospitalId, employeeId);
    }
    async getEmployeeEnrollmentCount(hospitalId, employeeId) {
        const count = await this.enrollmentService.getEmployeeEnrollmentCount(hospitalId, employeeId);
        return { employeeId, enrollmentCount: count };
    }
    async getEnrollmentCountByType(hospitalId, enrollmentType) {
        const count = await this.enrollmentService.getEnrollmentCountByType(hospitalId, enrollmentType);
        return { enrollmentType, count };
    }
    async enrollBiometric(hospitalId, enrollmentId, dto) {
        return this.enrollmentService.enrollBiometric(hospitalId, enrollmentId, dto);
    }
    async verifyEnrollment(hospitalId, enrollmentId, dto) {
        return this.enrollmentService.verifyEnrollment(hospitalId, enrollmentId, dto);
    }
    async updateMetadata(hospitalId, enrollmentId, dto) {
        return this.enrollmentService.updateEnrollmentMetadata(hospitalId, enrollmentId, dto);
    }
    async revokeEnrollment(hospitalId, enrollmentId, dto) {
        await this.enrollmentService.revokeEnrollment(hospitalId, enrollmentId, dto);
    }
};
exports.BiometricEnrollmentsController = BiometricEnrollmentsController;
__decorate([
    (0, common_1.Post)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    (0, swagger_1.ApiOperation)({ summary: 'Start a new biometric enrollment' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Enrollment started successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof biometric_enrollment_dto_1.StartEnrollmentDto !== "undefined" && biometric_enrollment_dto_1.StartEnrollmentDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "startEnrollment", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Query biometric enrollments' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Enrollments retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof biometric_enrollment_dto_1.QueryEnrollmentsDto !== "undefined" && biometric_enrollment_dto_1.QueryEnrollmentsDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "queryEnrollments", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get enrollment by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Enrollment retrieved successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "getEnrollment", null);
__decorate([
    (0, common_1.Get)('employee/:employeeId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get all enrollments for an employee' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Employee enrollments retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "getEmployeeEnrollments", null);
__decorate([
    (0, common_1.Get)('employee/:employeeId/count'),
    (0, swagger_1.ApiOperation)({ summary: 'Get enrollment count for employee' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Enrollment count retrieved successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "getEmployeeEnrollmentCount", null);
__decorate([
    (0, common_1.Get)('type/:enrollmentType/count'),
    (0, swagger_1.ApiOperation)({ summary: 'Get enrollment count by type' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Enrollment count retrieved successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('enrollmentType')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof client_1.EnrollmentType !== "undefined" && client_1.EnrollmentType) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "getEnrollmentCountByType", null);
__decorate([
    (0, common_1.Post)(':id/enroll'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Enroll a biometric sample' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Biometric enrolled successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_e = typeof biometric_enrollment_dto_1.EnrollBiometricDto !== "undefined" && biometric_enrollment_dto_1.EnrollBiometricDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "enrollBiometric", null);
__decorate([
    (0, common_1.Post)(':id/verify'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Verify enrollment' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Verification result returned',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_f = typeof biometric_enrollment_dto_1.VerifyEnrollmentDto !== "undefined" && biometric_enrollment_dto_1.VerifyEnrollmentDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "verifyEnrollment", null);
__decorate([
    (0, common_1.Put)(':id/metadata'),
    (0, swagger_1.ApiOperation)({ summary: 'Update enrollment metadata' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Metadata updated successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_g = typeof biometric_enrollment_dto_1.UpdateEnrollmentMetadataDto !== "undefined" && biometric_enrollment_dto_1.UpdateEnrollmentMetadataDto) === "function" ? _g : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "updateMetadata", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.NO_CONTENT),
    (0, swagger_1.ApiOperation)({ summary: 'Revoke enrollment' }),
    (0, swagger_1.ApiResponse)({
        status: 204,
        description: 'Enrollment revoked successfully',
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_h = typeof biometric_enrollment_dto_1.RevokeEnrollmentDto !== "undefined" && biometric_enrollment_dto_1.RevokeEnrollmentDto) === "function" ? _h : Object]),
    __metadata("design:returntype", Promise)
], BiometricEnrollmentsController.prototype, "revokeEnrollment", null);
exports.BiometricEnrollmentsController = BiometricEnrollmentsController = __decorate([
    (0, swagger_1.ApiTags)('Biometric Enrollments'),
    (0, common_1.Controller)('biometric-enrollments'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    __metadata("design:paramtypes", [typeof (_a = typeof biometric_enrollments_service_1.BiometricEnrollmentsService !== "undefined" && biometric_enrollments_service_1.BiometricEnrollmentsService) === "function" ? _a : Object])
], BiometricEnrollmentsController);


/***/ }),
/* 240 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c, _d, _e, _f;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateEnrollmentMetadataDto = exports.QueryEnrollmentsDto = exports.RevokeEnrollmentDto = exports.VerifyEnrollmentDto = exports.EnrollBiometricDto = exports.StartEnrollmentDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class StartEnrollmentDto {
}
exports.StartEnrollmentDto = StartEnrollmentDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], StartEnrollmentDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], StartEnrollmentDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.EnrollmentType),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.EnrollmentType !== "undefined" && client_1.EnrollmentType) === "function" ? _a : Object)
], StartEnrollmentDto.prototype, "enrollmentType", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], StartEnrollmentDto.prototype, "notes", void 0);
class EnrollBiometricDto {
}
exports.EnrollBiometricDto = EnrollBiometricDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], EnrollBiometricDto.prototype, "enrollmentId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], EnrollBiometricDto.prototype, "biometricData", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], EnrollBiometricDto.prototype, "quality", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], EnrollBiometricDto.prototype, "deviceSerial", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsObject)(),
    __metadata("design:type", typeof (_b = typeof Record !== "undefined" && Record) === "function" ? _b : Object)
], EnrollBiometricDto.prototype, "metadata", void 0);
class VerifyEnrollmentDto {
}
exports.VerifyEnrollmentDto = VerifyEnrollmentDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], VerifyEnrollmentDto.prototype, "enrollmentId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], VerifyEnrollmentDto.prototype, "verificationData", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsObject)(),
    __metadata("design:type", typeof (_c = typeof Record !== "undefined" && Record) === "function" ? _c : Object)
], VerifyEnrollmentDto.prototype, "metadata", void 0);
class RevokeEnrollmentDto {
}
exports.RevokeEnrollmentDto = RevokeEnrollmentDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], RevokeEnrollmentDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], RevokeEnrollmentDto.prototype, "revokedBy", void 0);
class QueryEnrollmentsDto {
}
exports.QueryEnrollmentsDto = QueryEnrollmentsDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], QueryEnrollmentsDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], QueryEnrollmentsDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.EnrollmentType),
    __metadata("design:type", typeof (_d = typeof client_1.EnrollmentType !== "undefined" && client_1.EnrollmentType) === "function" ? _d : Object)
], QueryEnrollmentsDto.prototype, "enrollmentType", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.EnrollmentStatus),
    __metadata("design:type", typeof (_e = typeof client_1.EnrollmentStatus !== "undefined" && client_1.EnrollmentStatus) === "function" ? _e : Object)
], QueryEnrollmentsDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], QueryEnrollmentsDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], QueryEnrollmentsDto.prototype, "take", void 0);
class UpdateEnrollmentMetadataDto {
}
exports.UpdateEnrollmentMetadataDto = UpdateEnrollmentMetadataDto;
__decorate([
    (0, class_validator_1.IsObject)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_f = typeof Record !== "undefined" && Record) === "function" ? _f : Object)
], UpdateEnrollmentMetadataDto.prototype, "metadata", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateEnrollmentMetadataDto.prototype, "notes", void 0);


/***/ }),
/* 241 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceRecordsModule = void 0;
const common_1 = __webpack_require__(2);
const attendance_records_service_1 = __webpack_require__(242);
const attendance_auto_absent_service_1 = __webpack_require__(243);
const attendance_records_controller_1 = __webpack_require__(244);
const database_module_1 = __webpack_require__(52);
let AttendanceRecordsModule = class AttendanceRecordsModule {
};
exports.AttendanceRecordsModule = AttendanceRecordsModule;
exports.AttendanceRecordsModule = AttendanceRecordsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [attendance_records_controller_1.AttendanceRecordsController],
        providers: [attendance_records_service_1.AttendanceRecordsService, attendance_auto_absent_service_1.AttendanceAutoAbsentService],
        exports: [attendance_records_service_1.AttendanceRecordsService],
    })
], AttendanceRecordsModule);


/***/ }),
/* 242 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceRecordsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let AttendanceRecordsService = class AttendanceRecordsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async markAttendance(hospitalId, dto) {
        const employee = await this.prisma.user.findUnique({
            where: { id: dto.employeeId },
        });
        if (!employee || employee.hospitalId !== hospitalId) {
            throw new common_1.NotFoundException(`Employee ${dto.employeeId} not found`);
        }
        const existingRecord = await this.prisma.attendanceRecord.findFirst({
            where: {
                userId: dto.employeeId,
                hospitalId,
                attendanceDate: new Date(dto.attendanceDate),
            },
        });
        if (existingRecord) {
            throw new common_1.BadRequestException('Attendance already marked for this date');
        }
        const activeLeave = await this.prisma.leave.findFirst({
            where: {
                userId: dto.employeeId,
                hospitalId,
                startDate: { lte: new Date(dto.attendanceDate) },
                endDate: { gte: new Date(dto.attendanceDate) },
                status: 'APPROVED',
            },
        });
        const status = this.calculateAttendanceStatus(new Date(dto.checkInTime), dto.checkOutTime ? new Date(dto.checkOutTime) : undefined, activeLeave);
        return this.prisma.attendanceRecord.create({
            data: {
                userId: dto.employeeId,
                hospitalId,
                attendanceDate: new Date(dto.attendanceDate),
                checkInTime: new Date(dto.checkInTime),
                checkOutTime: dto.checkOutTime ? new Date(dto.checkOutTime) : null,
                status,
                workingHours: new client_1.Prisma.Decimal(this.calculateWorkingHours(new Date(dto.checkInTime), dto.checkOutTime ? new Date(dto.checkOutTime) : undefined)),
                isManualEntry: dto.isManualMark || false,
                remarks: dto.notes,
                lateByMinutes: this.calculateLateMinutes(new Date(dto.checkInTime)),
                earlyDepartureMinutes: dto.checkOutTime ? this.calculateEarlyMinutes(new Date(dto.checkOutTime)) : 0,
            },
        });
    }
    async getAttendanceRecord(hospitalId, recordId) {
        const record = await this.prisma.attendanceRecord.findFirst({
            where: {
                id: recordId,
                hospitalId,
            },
        });
        if (!record) {
            throw new common_1.NotFoundException(`Attendance record ${recordId} not found`);
        }
        return record;
    }
    async queryAttendanceRecords(hospitalId, query) {
        const { employeeId, departmentId, startDate, endDate, status, skip = 0, take = 10, cursor, } = query || {};
        const where = { hospitalId };
        if (employeeId) {
            where.userId = employeeId;
        }
        if (departmentId) {
            where.user = { departmentId };
        }
        if (startDate && endDate) {
            where.attendanceDate = {
                gte: new Date(startDate),
                lte: new Date(endDate),
            };
        }
        if (status) {
            where.status = status;
        }
        if (cursor) {
            where.attendanceDate = {
                ...where.attendanceDate,
                lt: new Date(cursor),
            };
        }
        return this.prisma.attendanceRecord.findMany({
            where,
            skip: cursor ? 0 : skip,
            take,
            orderBy: { attendanceDate: 'desc' },
            include: {
                user: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                    },
                },
            },
        });
    }
    async getEmployeeAttendanceHistory(hospitalId, employeeId, limit = 30, cursor) {
        const where = {
            userId: employeeId,
            hospitalId,
        };
        if (cursor) {
            where.attendanceDate = { lt: new Date(cursor) };
        }
        return this.prisma.attendanceRecord.findMany({
            where,
            take: limit,
            orderBy: { attendanceDate: 'desc' },
            include: {
                user: true,
            },
        });
    }
    async correctAttendance(hospitalId, recordId, dto) {
        const record = await this.getAttendanceRecord(hospitalId, recordId);
        let workingHours = record.workingHours;
        if (dto.correctedCheckInTime && dto.correctedCheckOutTime) {
            workingHours = new client_1.Prisma.Decimal(this.calculateWorkingHours(new Date(dto.correctedCheckInTime), new Date(dto.correctedCheckOutTime)));
        }
        return this.prisma.attendanceRecord.update({
            where: { id: recordId },
            data: {
                status: dto.status,
                checkInTime: dto.correctedCheckInTime
                    ? new Date(dto.correctedCheckInTime)
                    : record.checkInTime,
                checkOutTime: dto.correctedCheckOutTime
                    ? new Date(dto.correctedCheckOutTime)
                    : record.checkOutTime,
                workingHours,
                correctionReason: dto.reason,
                manuallyMarkedBy: dto.approvedBy,
            },
        });
    }
    async getDailySummary(hospitalId, dto) {
        const date = new Date(dto.date || new Date());
        const startOfDay = new Date(date.setHours(0, 0, 0, 0));
        const endOfDay = new Date(new Date(date).setHours(23, 59, 59, 999));
        const where = {
            hospitalId,
            attendanceDate: {
                gte: startOfDay,
                lte: endOfDay,
            },
        };
        if (dto.departmentId) {
            where.user = { departmentId: dto.departmentId };
        }
        const statusGroups = await this.prisma.attendanceRecord.groupBy({
            by: ['status'],
            where,
            _count: { id: true },
        });
        const lateCount = await this.prisma.attendanceRecord.count({
            where: {
                ...where,
                lateByMinutes: { gt: 0 },
            },
        });
        const statsResult = await this.prisma.attendanceRecord.aggregate({
            where,
            _sum: { workingHours: true },
            _avg: { workingHours: true },
        });
        const total = statusGroups.reduce((sum, g) => sum + g._count.id, 0);
        const summary = {
            date,
            total,
            present: statusGroups.find((g) => g.status === client_1.AttendanceStatus.PRESENT)?._count
                .id || 0,
            absent: statusGroups.find((g) => g.status === client_1.AttendanceStatus.ABSENT)?._count
                .id || 0,
            late: lateCount,
            halfDay: statusGroups.find((g) => g.status === client_1.AttendanceStatus.HALF_DAY)
                ?._count.id || 0,
            leave: statusGroups.find((g) => g.status === client_1.AttendanceStatus.ON_LEAVE)
                ?._count.id || 0,
            averageWorkingHours: parseFloat(statsResult._avg.workingHours?.toString() || '0'),
            totalWorkingHours: parseFloat(statsResult._sum.workingHours?.toString() || '0'),
        };
        return summary;
    }
    async getMonthlyAttendance(hospitalId, dto) {
        const startDate = new Date(dto.year, dto.month - 1, 1);
        const endDate = new Date(dto.year, dto.month, 0);
        const where = {
            userId: dto.employeeId,
            hospitalId,
            attendanceDate: {
                gte: startDate,
                lte: endDate,
            },
        };
        const statusGroups = await this.prisma.attendanceRecord.groupBy({
            by: ['status'],
            where,
            _count: { id: true },
        });
        const lateCount = await this.prisma.attendanceRecord.count({
            where: {
                ...where,
                lateByMinutes: { gt: 0 },
            },
        });
        const statsResult = await this.prisma.attendanceRecord.aggregate({
            where,
            _sum: { workingHours: true },
            _avg: { workingHours: true },
            _count: { id: true },
        });
        const total = statsResult._count.id || 0;
        const summary = {
            employeeId: dto.employeeId,
            month: dto.month,
            year: dto.year,
            total,
            present: statusGroups.find((g) => g.status === client_1.AttendanceStatus.PRESENT)?._count
                .id || 0,
            absent: statusGroups.find((g) => g.status === client_1.AttendanceStatus.ABSENT)?._count
                .id || 0,
            late: lateCount,
            halfDay: statusGroups.find((g) => g.status === client_1.AttendanceStatus.HALF_DAY)
                ?._count.id || 0,
            leave: statusGroups.find((g) => g.status === client_1.AttendanceStatus.ON_LEAVE)
                ?._count.id || 0,
            totalWorkingHours: parseFloat(statsResult._sum.workingHours?.toString() || '0'),
            averageWorkingHours: parseFloat(statsResult._avg.workingHours?.toString() || '0'),
        };
        return summary;
    }
    async checkLeaveOnDate(hospitalId, dto) {
        const leave = await this.prisma.leave.findFirst({
            where: {
                userId: dto.employeeId,
                hospitalId,
                startDate: { lte: new Date(dto.date) },
                endDate: { gte: new Date(dto.date) },
                status: 'APPROVED',
            },
        });
        return {
            hasLeave: !!leave,
            leaveType: leave?.leaveTypeId,
            leaveId: leave?.id,
        };
    }
    async bulkMarkAttendance(hospitalId, dto) {
        const results = [];
        for (const employeeId of dto.employeeIds) {
            try {
                const record = await this.markAttendance(hospitalId, {
                    employeeId,
                    attendanceDate: dto.date,
                    checkInTime: dto.date,
                    status: dto.status,
                    notes: dto.notes,
                });
                results.push({ employeeId, success: true, record });
            }
            catch (error) {
                results.push({ employeeId, success: false, error: error.message });
            }
        }
        return {
            total: dto.employeeIds.length,
            successful: results.filter((r) => r.success).length,
            failed: results.filter((r) => !r.success).length,
            results,
        };
    }
    async getAttendanceReport(hospitalId, dto) {
        const where = { hospitalId };
        if (dto.departmentId) {
            where.user = { departmentId: dto.departmentId };
        }
        if (dto.startDate && dto.endDate) {
            where.attendanceDate = {
                gte: new Date(dto.startDate),
                lte: new Date(dto.endDate),
            };
        }
        const records = await this.prisma.attendanceRecord.findMany({
            where,
            include: { user: true },
        });
        const report = {
            period: { startDate: dto.startDate, endDate: dto.endDate },
            totalRecords: records.length,
            byStatus: {
                PRESENT: records.filter((r) => r.status === client_1.AttendanceStatus.PRESENT).length,
                ABSENT: records.filter((r) => r.status === client_1.AttendanceStatus.ABSENT).length,
                HALF_DAY: records.filter((r) => r.status === client_1.AttendanceStatus.HALF_DAY).length,
                ON_LEAVE: records.filter((r) => r.status === client_1.AttendanceStatus.ON_LEAVE).length,
                LATE: records.filter((r) => r.lateByMinutes > 0).length,
            },
            averageWorkingHours: records.length > 0
                ? records.reduce((sum, r) => sum + parseFloat(r.workingHours.toString()), 0) / records.length
                : 0,
            ...(dto.reportType === 'DETAILED' && { records }),
        };
        return report;
    }
    calculateAttendanceStatus(checkInTime, checkOutTime, activeLeave) {
        if (activeLeave) {
            return client_1.AttendanceStatus.ON_LEAVE;
        }
        const gracePeriod = 15;
        const halfDayThreshold = 240;
        const isLate = checkInTime.getHours() * 60 + checkInTime.getMinutes() > gracePeriod;
        if (!checkOutTime) {
            return client_1.AttendanceStatus.PRESENT;
        }
        const workingMinutes = (checkOutTime.getTime() - checkInTime.getTime()) / (1000 * 60);
        if (workingMinutes < halfDayThreshold) {
            return client_1.AttendanceStatus.HALF_DAY;
        }
        return client_1.AttendanceStatus.PRESENT;
    }
    calculateWorkingHours(checkInTime, checkOutTime) {
        if (!checkOutTime)
            return 0;
        const diffMs = checkOutTime.getTime() - checkInTime.getTime();
        const diffHours = diffMs / (1000 * 60 * 60);
        return parseFloat((Math.round(diffHours * 100) / 100).toFixed(2));
    }
    calculateLateMinutes(checkInTime) {
        const gracePeriod = 15;
        const shiftStartMinutes = 8 * 60;
        const checkInMinutes = checkInTime.getHours() * 60 + checkInTime.getMinutes();
        const lateMinutes = checkInMinutes - (shiftStartMinutes + gracePeriod);
        return lateMinutes > 0 ? lateMinutes : 0;
    }
    calculateEarlyMinutes(checkOutTime) {
        const shiftEndMinutes = 16 * 60;
        const checkOutMinutes = checkOutTime.getHours() * 60 + checkOutTime.getMinutes();
        const earlyMinutes = shiftEndMinutes - checkOutMinutes;
        return earlyMinutes > 0 ? earlyMinutes : 0;
    }
    isLate(checkInTime) {
        return this.calculateLateMinutes(checkInTime) > 0;
    }
    isEarlyCheckOut(checkOutTime) {
        return this.calculateEarlyMinutes(checkOutTime) > 0;
    }
    async getDashboardStats(hospitalId, date) {
        const targetDate = date ? new Date(date) : new Date();
        const startOfDay = new Date(targetDate.setHours(0, 0, 0, 0));
        const endOfDay = new Date(new Date(targetDate).setHours(23, 59, 59, 999));
        const where = {
            hospitalId,
            attendanceDate: {
                gte: startOfDay,
                lte: endOfDay,
            },
        };
        const statusGroups = await this.prisma.attendanceRecord.groupBy({
            by: ['status'],
            where,
            _count: { id: true },
        });
        const lateCount = await this.prisma.attendanceRecord.count({
            where: {
                ...where,
                lateByMinutes: { gt: 0 },
            },
        });
        const present = statusGroups.find((g) => g.status === client_1.AttendanceStatus.PRESENT)?._count
            .id || 0;
        const absent = statusGroups.find((g) => g.status === client_1.AttendanceStatus.ABSENT)?._count
            .id || 0;
        const onLeave = statusGroups.find((g) => g.status === client_1.AttendanceStatus.ON_LEAVE)?._count
            .id || 0;
        const total = present + absent + onLeave;
        const attendanceRate = total > 0 ? (present / total) * 100 : 0;
        return {
            present,
            absent,
            onLeave,
            lateArrivals: lateCount,
            attendanceRate: parseFloat(attendanceRate.toFixed(2)),
            lastUpdated: new Date(),
        };
    }
    async getDepartmentBreakdown(hospitalId, date) {
        const targetDate = date ? new Date(date) : new Date();
        const startOfDay = new Date(targetDate.setHours(0, 0, 0, 0));
        const endOfDay = new Date(new Date(targetDate).setHours(23, 59, 59, 999));
        const departments = await this.prisma.department.findMany({
            where: { hospitalId },
            select: { id: true, name: true },
        });
        const breakdown = await Promise.all(departments.map(async (dept) => {
            const where = {
                hospitalId,
                userId: { user: { departmentId: dept.id } },
                attendanceDate: {
                    gte: startOfDay,
                    lte: endOfDay,
                },
            };
            const deptRecords = await this.prisma.attendanceRecord.findMany({
                where: {
                    hospitalId,
                    attendanceDate: {
                        gte: startOfDay,
                        lte: endOfDay,
                    },
                    user: {
                        departmentId: dept.id,
                    },
                },
            });
            const present = deptRecords.filter((r) => r.status === client_1.AttendanceStatus.PRESENT).length;
            const absent = deptRecords.filter((r) => r.status === client_1.AttendanceStatus.ABSENT).length;
            const onLeave = deptRecords.filter((r) => r.status === client_1.AttendanceStatus.ON_LEAVE).length;
            const total = deptRecords.length;
            return {
                departmentId: dept.id,
                departmentName: dept.name,
                present,
                absent,
                onLeave,
                total,
                attendanceRate: total > 0 ? parseFloat(((present / total) * 100).toFixed(2)) : 0,
            };
        }));
        return breakdown.filter((d) => d.total > 0);
    }
    async getDailyTrend(hospitalId, days = 30) {
        const endDate = new Date();
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - days);
        startDate.setHours(0, 0, 0, 0);
        const records = await this.prisma.attendanceRecord.findMany({
            where: {
                hospitalId,
                attendanceDate: {
                    gte: startDate,
                    lte: new Date(endDate.setHours(23, 59, 59, 999)),
                },
            },
        });
        const grouped = new Map();
        records.forEach((record) => {
            const dateKey = record.attendanceDate.toISOString().split('T')[0];
            if (!grouped.has(dateKey)) {
                grouped.set(dateKey, {
                    date: dateKey,
                    present: 0,
                    absent: 0,
                    onLeave: 0,
                });
            }
            const day = grouped.get(dateKey);
            if (record.status === client_1.AttendanceStatus.PRESENT) {
                day.present++;
            }
            else if (record.status === client_1.AttendanceStatus.ABSENT) {
                day.absent++;
            }
            else if (record.status === client_1.AttendanceStatus.ON_LEAVE) {
                day.onLeave++;
            }
        });
        return Array.from(grouped.values()).sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());
    }
    async getWeeklyPattern(hospitalId) {
        const endDate = new Date();
        const startDate = new Date();
        startDate.setDate(startDate.getDate() - 28);
        startDate.setHours(0, 0, 0, 0);
        const records = await this.prisma.attendanceRecord.findMany({
            where: {
                hospitalId,
                attendanceDate: {
                    gte: startDate,
                    lte: new Date(endDate.setHours(23, 59, 59, 999)),
                },
            },
        });
        const weeklyData = [
            { day: 'Sunday', dayOfWeek: 0, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Monday', dayOfWeek: 1, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Tuesday', dayOfWeek: 2, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Wednesday', dayOfWeek: 3, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Thursday', dayOfWeek: 4, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Friday', dayOfWeek: 5, present: 0, absent: 0, onLeave: 0, count: 0 },
            { day: 'Saturday', dayOfWeek: 6, present: 0, absent: 0, onLeave: 0, count: 0 },
        ];
        records.forEach((record) => {
            const dayOfWeek = record.attendanceDate.getDay();
            const dayData = weeklyData[dayOfWeek];
            if (record.status === client_1.AttendanceStatus.PRESENT) {
                dayData.present++;
            }
            else if (record.status === client_1.AttendanceStatus.ABSENT) {
                dayData.absent++;
            }
            else if (record.status === client_1.AttendanceStatus.ON_LEAVE) {
                dayData.onLeave++;
            }
            dayData.count++;
        });
        return weeklyData.map((day) => ({
            ...day,
            attendanceRate: day.count > 0
                ? parseFloat(((day.present / day.count) * 100).toFixed(2))
                : 0,
        }));
    }
    async getRecentCheckIns(hospitalId, limit = 20, departmentId, status) {
        const startOfDay = new Date();
        startOfDay.setHours(0, 0, 0, 0);
        const where = {
            hospitalId,
            attendanceDate: {
                gte: startOfDay,
            },
        };
        if (departmentId) {
            where.user = { departmentId };
        }
        if (status) {
            where.status = status.toUpperCase();
        }
        const records = await this.prisma.attendanceRecord.findMany({
            where,
            take: limit,
            orderBy: { checkInTime: 'desc' },
            include: {
                user: {
                    select: {
                        id: true,
                        fullName: true,
                        email: true,
                        department: {
                            select: { id: true, name: true },
                        },
                    },
                },
            },
        });
        return records.map((record) => ({
            employeeId: record.userId,
            employeeName: record.user.fullName,
            email: record.user.email,
            departmentId: record.user.department?.id,
            departmentName: record.user.department?.name,
            checkInTime: record.checkInTime,
            checkOutTime: record.checkOutTime,
            status: record.status,
            workingHours: parseFloat(record.workingHours.toString()),
            lateByMinutes: record.lateByMinutes,
            notes: record.remarks,
        }));
    }
};
exports.AttendanceRecordsService = AttendanceRecordsService;
exports.AttendanceRecordsService = AttendanceRecordsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], AttendanceRecordsService);


/***/ }),
/* 243 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var AttendanceAutoAbsentService_1;
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceAutoAbsentService = void 0;
const common_1 = __webpack_require__(2);
const schedule_1 = __webpack_require__(9);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
let AttendanceAutoAbsentService = AttendanceAutoAbsentService_1 = class AttendanceAutoAbsentService {
    constructor(prisma) {
        this.prisma = prisma;
        this.logger = new common_1.Logger(AttendanceAutoAbsentService_1.name);
    }
    async markAbsentEmployees() {
        const now = new Date();
        const hospitals = await this.prisma.hospital.findMany({
            select: { id: true },
        });
        for (const hospital of hospitals) {
            await this.processHospital(hospital.id, now);
        }
    }
    async processHospital(hospitalId, now) {
        const users = await this.prisma.user.findMany({
            where: {
                hospitalId,
                status: client_1.UserStatus.ACTIVE,
            },
            select: { id: true },
        });
        if (users.length === 0)
            return;
        const activeShift = await this.prisma.shift.findFirst({
            where: { hospitalId, isActive: true },
            orderBy: { createdAt: 'asc' },
        });
        for (const user of users) {
            const shiftAssignment = await this.prisma.employeeShift.findFirst({
                where: {
                    hospitalId,
                    userId: user.id,
                    effectiveFrom: { lte: now },
                    OR: [{ effectiveTo: null }, { effectiveTo: { gte: now } }],
                },
                orderBy: { effectiveFrom: 'desc' },
                include: { shift: true },
            });
            const shift = shiftAssignment?.shift || activeShift;
            if (!shift)
                continue;
            const shiftWindow = this.getShiftWindow(shift.startTime, shift.endTime, shift.isNightShift, now);
            if (!shiftWindow.shouldMark)
                continue;
            const attendanceDate = shiftWindow.attendanceDate;
            const existingRecord = await this.prisma.attendanceRecord.findFirst({
                where: {
                    hospitalId,
                    userId: user.id,
                    attendanceDate,
                },
                select: { id: true },
            });
            if (existingRecord)
                continue;
            const onLeave = await this.prisma.leave.findFirst({
                where: {
                    hospitalId,
                    userId: user.id,
                    status: 'APPROVED',
                    startDate: { lte: attendanceDate },
                    endDate: { gte: attendanceDate },
                },
                select: { id: true },
            });
            if (onLeave)
                continue;
            await this.prisma.attendanceRecord.create({
                data: {
                    hospitalId,
                    userId: user.id,
                    attendanceDate,
                    status: client_1.AttendanceStatus.ABSENT,
                    isManualEntry: false,
                    remarks: 'Auto-marked absent (no biometric/manual entry)',
                    shiftId: shift.id,
                },
            });
        }
    }
    getShiftWindow(startTime, endTime, isNightShift, now) {
        const start = this.applyTime(new Date(now), startTime);
        let end = this.applyTime(new Date(now), endTime);
        if (isNightShift || end <= start) {
            end = new Date(end.getTime() + 24 * 60 * 60 * 1000);
        }
        if ((isNightShift || end <= start) && now < start) {
            start.setDate(start.getDate() - 1);
            end.setDate(end.getDate() - 1);
        }
        const graceMinutes = 0;
        const cutoff = new Date(end.getTime() + graceMinutes * 60 * 1000);
        return {
            attendanceDate: new Date(start.setHours(0, 0, 0, 0)),
            shouldMark: now >= cutoff,
        };
    }
    applyTime(base, time) {
        const [hour, minute] = time.split(':').map((v) => parseInt(v, 10));
        const result = new Date(base);
        result.setHours(hour || 0, minute || 0, 0, 0);
        return result;
    }
};
exports.AttendanceAutoAbsentService = AttendanceAutoAbsentService;
__decorate([
    (0, schedule_1.Cron)('*/30 * * * *'),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", typeof (_b = typeof Promise !== "undefined" && Promise) === "function" ? _b : Object)
], AttendanceAutoAbsentService.prototype, "markAbsentEmployees", null);
exports.AttendanceAutoAbsentService = AttendanceAutoAbsentService = AttendanceAutoAbsentService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], AttendanceAutoAbsentService);


/***/ }),
/* 244 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceRecordsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const attendance_records_service_1 = __webpack_require__(242);
const attendance_records_dto_1 = __webpack_require__(245);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
let AttendanceRecordsController = class AttendanceRecordsController {
    constructor(recordService) {
        this.recordService = recordService;
    }
    async markAttendance(hospitalId, dto) {
        return this.recordService.markAttendance(hospitalId, dto);
    }
    async queryAttendance(hospitalId, query) {
        return this.recordService.queryAttendanceRecords(hospitalId, query);
    }
    async getAttendanceRecord(hospitalId, recordId) {
        return this.recordService.getAttendanceRecord(hospitalId, recordId);
    }
    async getEmployeeHistory(hospitalId, employeeId, limit) {
        return this.recordService.getEmployeeAttendanceHistory(hospitalId, employeeId, limit ? parseInt(limit) : 30);
    }
    async correctAttendance(hospitalId, recordId, dto) {
        return this.recordService.correctAttendance(hospitalId, recordId, dto);
    }
    async getDailySummary(hospitalId, query) {
        return this.recordService.getDailySummary(hospitalId, query);
    }
    async getMonthlyAttendance(hospitalId, dto) {
        return this.recordService.getMonthlyAttendance(hospitalId, dto);
    }
    async checkLeave(hospitalId, dto) {
        return this.recordService.checkLeaveOnDate(hospitalId, dto);
    }
    async bulkMarkAttendance(hospitalId, dto) {
        return this.recordService.bulkMarkAttendance(hospitalId, dto);
    }
    async generateReport(hospitalId, query) {
        return this.recordService.getAttendanceReport(hospitalId, query);
    }
    async exportReport(hospitalId, query) {
        const report = await this.recordService.getAttendanceReport(hospitalId, query);
        return {
            format: 'csv',
            data: report,
            exportedAt: new Date(),
        };
    }
    async getAttendanceStats(hospitalId, date) {
        const summary = await this.recordService.getDailySummary(hospitalId, {
            date: date || new Date().toISOString(),
        });
        return {
            date: summary.date,
            total: summary.total,
            present: summary.present,
            absent: summary.absent,
            late: summary.late,
            halfDay: summary.halfDay,
            leave: summary.leave,
            presentPercentage: summary.total > 0 ? (summary.present / summary.total) * 100 : 0,
            absentPercentage: summary.total > 0 ? (summary.absent / summary.total) * 100 : 0,
        };
    }
    async deleteAttendance(hospitalId, recordId) {
        await this.recordService.correctAttendance(hospitalId, recordId, {
            status: 'ABSENT',
            reason: 'Record deleted',
        });
    }
    async getDashboardStats(hospitalId, date) {
        return this.recordService.getDashboardStats(hospitalId, date);
    }
    async getDepartmentBreakdown(hospitalId, date) {
        return this.recordService.getDepartmentBreakdown(hospitalId, date);
    }
    async getDailyTrend(hospitalId, days = 30) {
        return this.recordService.getDailyTrend(hospitalId, Math.min(days, 90));
    }
    async getWeeklyPattern(hospitalId) {
        return this.recordService.getWeeklyPattern(hospitalId);
    }
    async getRecentCheckIns(hospitalId, limit = 20, departmentId, status) {
        return this.recordService.getRecentCheckIns(hospitalId, Math.min(limit, 100), departmentId, status);
    }
};
exports.AttendanceRecordsController = AttendanceRecordsController;
__decorate([
    (0, common_1.Post)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    (0, swagger_1.ApiOperation)({ summary: 'Mark attendance for an employee' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Attendance marked successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof attendance_records_dto_1.MarkAttendanceDto !== "undefined" && attendance_records_dto_1.MarkAttendanceDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "markAttendance", null);
__decorate([
    (0, common_1.Get)(),
    (0, swagger_1.ApiOperation)({ summary: 'Query attendance records' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Attendance records retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof attendance_records_dto_1.QueryAttendanceDto !== "undefined" && attendance_records_dto_1.QueryAttendanceDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "queryAttendance", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Get attendance record by ID' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Attendance record retrieved successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getAttendanceRecord", null);
__decorate([
    (0, common_1.Get)('employee/:employeeId'),
    (0, swagger_1.ApiOperation)({ summary: 'Get attendance history for employee' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Attendance history retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __param(2, (0, common_1.Query)('limit')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getEmployeeHistory", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, swagger_1.ApiOperation)({ summary: 'Correct attendance record' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Attendance corrected successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_d = typeof attendance_records_dto_1.CorrectAttendanceDto !== "undefined" && attendance_records_dto_1.CorrectAttendanceDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "correctAttendance", null);
__decorate([
    (0, common_1.Get)('summary/daily'),
    (0, swagger_1.ApiOperation)({ summary: 'Get daily attendance summary' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Daily summary retrieved successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof attendance_records_dto_1.AttendanceSummaryDto !== "undefined" && attendance_records_dto_1.AttendanceSummaryDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getDailySummary", null);
__decorate([
    (0, common_1.Post)('summary/monthly'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Get monthly attendance summary' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Monthly summary retrieved successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof attendance_records_dto_1.MonthlyAttendanceDto !== "undefined" && attendance_records_dto_1.MonthlyAttendanceDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getMonthlyAttendance", null);
__decorate([
    (0, common_1.Post)('check-leave'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    (0, swagger_1.ApiOperation)({ summary: 'Check if employee has approved leave on date' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Leave status checked' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_g = typeof attendance_records_dto_1.AttendanceLeaveCheckDto !== "undefined" && attendance_records_dto_1.AttendanceLeaveCheckDto) === "function" ? _g : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "checkLeave", null);
__decorate([
    (0, common_1.Post)('bulk-mark'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    (0, swagger_1.ApiOperation)({ summary: 'Bulk mark attendance for multiple employees' }),
    (0, swagger_1.ApiResponse)({ status: 201, description: 'Bulk attendance marked successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_h = typeof attendance_records_dto_1.BulkMarkAttendanceDto !== "undefined" && attendance_records_dto_1.BulkMarkAttendanceDto) === "function" ? _h : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "bulkMarkAttendance", null);
__decorate([
    (0, common_1.Get)('report/generate'),
    (0, swagger_1.ApiOperation)({ summary: 'Generate attendance report' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Report generated successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_j = typeof attendance_records_dto_1.AttendanceReportDto !== "undefined" && attendance_records_dto_1.AttendanceReportDto) === "function" ? _j : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "generateReport", null);
__decorate([
    (0, common_1.Get)('report/export'),
    (0, swagger_1.ApiOperation)({ summary: 'Export attendance report' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Report exported successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof attendance_records_dto_1.AttendanceReportDto !== "undefined" && attendance_records_dto_1.AttendanceReportDto) === "function" ? _k : Object]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "exportReport", null);
__decorate([
    (0, common_1.Get)('statistics/count'),
    (0, swagger_1.ApiOperation)({ summary: 'Get attendance count statistics' }),
    (0, swagger_1.ApiResponse)({ status: 200, description: 'Statistics retrieved successfully' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getAttendanceStats", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.NO_CONTENT),
    (0, swagger_1.ApiOperation)({ summary: 'Delete attendance record (soft delete)' }),
    (0, swagger_1.ApiResponse)({ status: 204, description: 'Attendance record deleted' }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "deleteAttendance", null);
__decorate([
    (0, common_1.Get)('dashboard/stats'),
    (0, swagger_1.ApiOperation)({ summary: 'Get dashboard statistics for today or specific date' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Dashboard stats retrieved successfully',
        schema: {
            example: {
                present: 245,
                absent: 12,
                onLeave: 8,
                lateArrivals: 23,
                attendanceRate: 95.2,
                lastUpdated: '2026-02-19T14:30:00Z',
            },
        },
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getDashboardStats", null);
__decorate([
    (0, common_1.Get)('dashboard/department-breakdown'),
    (0, swagger_1.ApiOperation)({ summary: 'Get department-wise attendance breakdown' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Department breakdown retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getDepartmentBreakdown", null);
__decorate([
    (0, common_1.Get)('dashboard/daily-trend'),
    (0, swagger_1.ApiOperation)({ summary: 'Get daily attendance trend for last N days' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Daily trend retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('days')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getDailyTrend", null);
__decorate([
    (0, common_1.Get)('dashboard/weekly-pattern'),
    (0, swagger_1.ApiOperation)({ summary: 'Get weekly attendance pattern (Mon-Sun)' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Weekly pattern retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getWeeklyPattern", null);
__decorate([
    (0, common_1.Get)('dashboard/recent-checkins'),
    (0, swagger_1.ApiOperation)({ summary: 'Get recent check-ins for today' }),
    (0, swagger_1.ApiResponse)({
        status: 200,
        description: 'Recent check-ins retrieved successfully',
        isArray: true,
    }),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('limit')),
    __param(2, (0, common_1.Query)('departmentId')),
    __param(3, (0, common_1.Query)('status')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Number, String, String]),
    __metadata("design:returntype", Promise)
], AttendanceRecordsController.prototype, "getRecentCheckIns", null);
exports.AttendanceRecordsController = AttendanceRecordsController = __decorate([
    (0, swagger_1.ApiTags)('Attendance Records'),
    (0, common_1.Controller)('attendance-records'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    __metadata("design:paramtypes", [typeof (_a = typeof attendance_records_service_1.AttendanceRecordsService !== "undefined" && attendance_records_service_1.AttendanceRecordsService) === "function" ? _a : Object])
], AttendanceRecordsController);


/***/ }),
/* 245 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AttendanceReportDto = exports.BulkMarkAttendanceDto = exports.AttendanceLeaveCheckDto = exports.MonthlyAttendanceDto = exports.AttendanceSummaryDto = exports.QueryAttendanceDto = exports.CorrectAttendanceDto = exports.MarkAttendanceDto = void 0;
const class_validator_1 = __webpack_require__(35);
const swagger_1 = __webpack_require__(3);
const client_1 = __webpack_require__(16);
class MarkAttendanceDto {
}
exports.MarkAttendanceDto = MarkAttendanceDto;
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Employee ID',
        example: 'emp-123456',
    }),
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MarkAttendanceDto.prototype, "employeeId", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Attendance date (ISO 8601 format)',
        example: '2026-02-17',
    }),
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MarkAttendanceDto.prototype, "attendanceDate", void 0);
__decorate([
    (0, swagger_1.ApiProperty)({
        description: 'Check-in time (ISO 8601 format)',
        example: '2026-02-17T09:00:00Z',
    }),
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MarkAttendanceDto.prototype, "checkInTime", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Check-out time (ISO 8601 format)',
        example: '2026-02-17T17:30:00Z',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], MarkAttendanceDto.prototype, "checkOutTime", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Additional notes about attendance',
        example: 'Left early for medical appointment',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], MarkAttendanceDto.prototype, "notes", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Whether this is a manual mark (not from device)',
        example: false,
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], MarkAttendanceDto.prototype, "isManualMark", void 0);
class CorrectAttendanceDto {
}
exports.CorrectAttendanceDto = CorrectAttendanceDto;
__decorate([
    (0, swagger_1.ApiProperty)({
        enum: client_1.AttendanceStatus,
        description: 'Corrected attendance status',
        example: 'PRESENT',
    }),
    (0, class_validator_1.IsEnum)(client_1.AttendanceStatus),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_a = typeof client_1.AttendanceStatus !== "undefined" && client_1.AttendanceStatus) === "function" ? _a : Object)
], CorrectAttendanceDto.prototype, "status", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Corrected check-in time',
        example: '2026-02-17T09:00:00Z',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], CorrectAttendanceDto.prototype, "correctedCheckInTime", void 0);
__decorate([
    (0, swagger_1.ApiPropertyOptional)({
        description: 'Corrected check-out time',
        example: '2026-02-17T17:30:00Z',
    }),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], CorrectAttendanceDto.prototype, "correctedCheckOutTime", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CorrectAttendanceDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], CorrectAttendanceDto.prototype, "approvedBy", void 0);
class QueryAttendanceDto {
}
exports.QueryAttendanceDto = QueryAttendanceDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], QueryAttendanceDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], QueryAttendanceDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], QueryAttendanceDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], QueryAttendanceDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsEnum)(client_1.AttendanceStatus),
    __metadata("design:type", typeof (_b = typeof client_1.AttendanceStatus !== "undefined" && client_1.AttendanceStatus) === "function" ? _b : Object)
], QueryAttendanceDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], QueryAttendanceDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], QueryAttendanceDto.prototype, "take", void 0);
class AttendanceSummaryDto {
}
exports.AttendanceSummaryDto = AttendanceSummaryDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AttendanceSummaryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AttendanceSummaryDto.prototype, "date", void 0);
class MonthlyAttendanceDto {
}
exports.MonthlyAttendanceDto = MonthlyAttendanceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], MonthlyAttendanceDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(12),
    __metadata("design:type", Number)
], MonthlyAttendanceDto.prototype, "month", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(2000),
    __metadata("design:type", Number)
], MonthlyAttendanceDto.prototype, "year", void 0);
class AttendanceLeaveCheckDto {
}
exports.AttendanceLeaveCheckDto = AttendanceLeaveCheckDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AttendanceLeaveCheckDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AttendanceLeaveCheckDto.prototype, "date", void 0);
class BulkMarkAttendanceDto {
}
exports.BulkMarkAttendanceDto = BulkMarkAttendanceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkMarkAttendanceDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkMarkAttendanceDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Array)
], BulkMarkAttendanceDto.prototype, "employeeIds", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_c = typeof client_1.AttendanceStatus !== "undefined" && client_1.AttendanceStatus) === "function" ? _c : Object)
], BulkMarkAttendanceDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], BulkMarkAttendanceDto.prototype, "notes", void 0);
class AttendanceReportDto {
}
exports.AttendanceReportDto = AttendanceReportDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AttendanceReportDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AttendanceReportDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AttendanceReportDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AttendanceReportDto.prototype, "reportType", void 0);


/***/ }),
/* 246 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ShiftsModule = void 0;
const common_1 = __webpack_require__(2);
const shifts_service_1 = __webpack_require__(247);
const shifts_controller_1 = __webpack_require__(248);
let ShiftsModule = class ShiftsModule {
};
exports.ShiftsModule = ShiftsModule;
exports.ShiftsModule = ShiftsModule = __decorate([
    (0, common_1.Module)({
        controllers: [shifts_controller_1.ShiftsController],
        providers: [shifts_service_1.ShiftsService],
        exports: [shifts_service_1.ShiftsService],
    })
], ShiftsModule);


/***/ }),
/* 247 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ShiftsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let ShiftsService = class ShiftsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async createShift(hospitalId, dto) {
        const code = `${dto.name.replace(/\s+/g, '_').toUpperCase()}_${Date.now()}`;
        return this.prisma.shift.create({
            data: {
                hospitalId,
                name: dto.name,
                code: code.substring(0, 50),
                startTime: `${dto.startTime.toString().padStart(2, '0')}:${(dto.startMinute || 0).toString().padStart(2, '0')}`,
                endTime: `${dto.endTime.toString().padStart(2, '0')}:${(dto.endMinute || 0).toString().padStart(2, '0')}`,
                breakDurationMinutes: dto.breakDurationMinutes || 0,
                gracePeriodMinutes: dto.gracePeriodMinutes || 15,
                isActive: dto.isActive ?? true,
                description: dto.description,
            },
        });
    }
    async getShiftById(hospitalId, shiftId) {
        const shift = await this.prisma.shift.findFirst({
            where: {
                id: shiftId,
                hospitalId,
            },
        });
        if (!shift) {
            throw new common_1.NotFoundException(`Shift ${shiftId} not found`);
        }
        return shift;
    }
    async getShifts(hospitalId, query) {
        const { isActive, skip = 0, take = 10 } = query || {};
        return this.prisma.shift.findMany({
            where: {
                hospitalId,
                ...(isActive !== undefined && { isActive }),
            },
            skip,
            take,
            orderBy: { createdAt: 'desc' },
        });
    }
    async updateShift(hospitalId, shiftId, dto) {
        await this.getShiftById(hospitalId, shiftId);
        return this.prisma.shift.update({
            where: { id: shiftId },
            data: {
                ...(dto.name && { name: dto.name }),
                ...(dto.startTime !== undefined && {
                    startTime: `${dto.startTime.toString().padStart(2, '0')}:${(dto.startMinute || 0).toString().padStart(2, '0')}`,
                }),
                ...(dto.endTime !== undefined && {
                    endTime: `${dto.endTime.toString().padStart(2, '0')}:${(dto.endMinute || 0).toString().padStart(2, '0')}`,
                }),
                ...(dto.breakDurationMinutes !== undefined && {
                    breakDurationMinutes: dto.breakDurationMinutes,
                }),
                ...(dto.gracePeriodMinutes !== undefined && {
                    gracePeriodMinutes: dto.gracePeriodMinutes,
                }),
                ...(dto.isActive !== undefined && { isActive: dto.isActive }),
                ...(dto.description && { description: dto.description }),
            },
        });
    }
    async deleteShift(hospitalId, shiftId) {
        await this.getShiftById(hospitalId, shiftId);
        return this.prisma.shift.update({
            where: { id: shiftId },
            data: { isActive: false },
        });
    }
    async assignShiftToEmployee(hospitalId, dto) {
        const employee = await this.prisma.user.findUnique({
            where: { id: dto.employeeId },
        });
        if (!employee || employee.hospitalId !== hospitalId) {
            throw new common_1.NotFoundException(`Employee ${dto.employeeId} not found`);
        }
        await this.getShiftById(hospitalId, dto.shiftId);
        const userId = employee.id;
        const conflict = await this.checkShiftConflict(hospitalId, {
            employeeId: dto.employeeId,
            shiftId: dto.shiftId,
            startDate: dto.effectiveFrom,
            endDate: dto.effectiveTo,
        });
        if (conflict.hasConflict) {
            throw new common_1.ConflictException(`Shift conflict detected: ${conflict.reason}`);
        }
        return this.prisma.employeeShift.create({
            data: {
                hospitalId,
                userId: dto.employeeId,
                shiftId: dto.shiftId,
                effectiveFrom: new Date(dto.effectiveFrom),
                effectiveTo: dto.effectiveTo ? new Date(dto.effectiveTo) : null,
                assignmentReason: dto.notes,
                assignedBy: userId,
                isPermanent: !dto.effectiveTo,
            },
        });
    }
    async bulkAssignShift(hospitalId, dto) {
        const results = [];
        for (const employeeId of dto.employeeIds) {
            try {
                const assignment = await this.assignShiftToEmployee(hospitalId, {
                    employeeId,
                    shiftId: dto.shiftId,
                    effectiveFrom: dto.effectiveFrom,
                    effectiveTo: dto.effectiveTo,
                });
                results.push({ employeeId, success: true, assignment });
            }
            catch (error) {
                results.push({ employeeId, success: false, error: error.message });
            }
        }
        return {
            total: dto.employeeIds.length,
            successful: results.filter((r) => r.success).length,
            failed: results.filter((r) => !r.success).length,
            results,
        };
    }
    async getEmployeeCurrentShift(hospitalId, employeeId) {
        const now = new Date();
        return this.prisma.employeeShift.findFirst({
            where: {
                userId: employeeId,
                hospitalId,
                effectiveFrom: { lte: now },
                OR: [{ effectiveTo: null }, { effectiveTo: { gte: now } }],
            },
            include: { shift: true },
        });
    }
    async getEmployeeShiftHistory(hospitalId, employeeId, query) {
        const { startDate, endDate, skip = 0, take = 10, cursor } = query || {};
        const where = {
            userId: employeeId,
            hospitalId,
            ...(startDate &&
                endDate && {
                effectiveFrom: { gte: new Date(startDate) },
                effectiveTo: { lte: new Date(endDate) },
            }),
        };
        if (cursor) {
            where.effectiveFrom = {
                ...where.effectiveFrom,
                lt: new Date(cursor),
            };
        }
        return this.prisma.employeeShift.findMany({
            where,
            skip: cursor ? 0 : skip,
            take,
            orderBy: { effectiveFrom: 'desc' },
            include: { shift: true },
        });
    }
    async removeEmployeeShift(hospitalId, assignmentId, dto) {
        const assignment = await this.prisma.employeeShift.findFirst({
            where: {
                id: assignmentId,
                hospitalId,
            },
        });
        if (!assignment) {
            throw new common_1.NotFoundException(`Shift assignment ${assignmentId} not found`);
        }
        return this.prisma.employeeShift.update({
            where: { id: assignmentId },
            data: {
                effectiveTo: dto.effectiveFrom ? new Date(dto.effectiveFrom) : new Date(),
                assignmentReason: dto.reason ? `Removed: ${dto.reason}` : 'Removed',
            },
        });
    }
    async checkShiftConflict(hospitalId, dto) {
        const startDate = new Date(dto.startDate);
        const endDate = dto.endDate ? new Date(dto.endDate) : null;
        const overlapping = await this.prisma.employeeShift.findFirst({
            where: {
                userId: dto.employeeId,
                hospitalId,
                NOT: {
                    shiftId: dto.shiftId,
                },
                effectiveFrom: { lte: endDate || startDate },
                OR: [
                    { effectiveTo: null },
                    { effectiveTo: { gte: startDate } },
                ],
            },
        });
        if (overlapping) {
            return {
                hasConflict: true,
                reason: `Employee already assigned to another shift during this period`,
            };
        }
        return { hasConflict: false };
    }
    async getShiftRoster(hospitalId, departmentId, date) {
        const queryDate = new Date(date);
        return this.prisma.employeeShift.findMany({
            where: {
                hospitalId,
                user: { departmentId },
                effectiveFrom: { lte: queryDate },
                OR: [{ effectiveTo: null }, { effectiveTo: { gte: queryDate } }],
            },
            include: {
                user: true,
                shift: true,
            },
        });
    }
    async createShiftRotation(hospitalId, dto) {
        const results = [];
        const startDate = new Date(dto.startDate);
        const endDate = dto.endDate ? new Date(dto.endDate) : new Date(startDate.getTime() + 365 * 24 * 60 * 60 * 1000);
        for (let i = 0; i < dto.employeeIds.length; i++) {
            const employeeId = dto.employeeIds[i];
            let currentDate = new Date(startDate);
            let shiftIndex = 0;
            try {
                while (currentDate <= endDate) {
                    const shiftId = dto.shiftIds[shiftIndex % dto.shiftIds.length];
                    await this.assignShiftToEmployee(hospitalId, {
                        employeeId,
                        shiftId,
                        effectiveFrom: currentDate.toISOString(),
                        effectiveTo: new Date(currentDate.getTime() + dto.rotationIntervalDays * 24 * 60 * 60 * 1000).toISOString(),
                        notes: `Rotation shift ${shiftIndex + 1}/${dto.shiftIds.length}`,
                    });
                    currentDate = new Date(currentDate.getTime() + dto.rotationIntervalDays * 24 * 60 * 60 * 1000);
                    shiftIndex++;
                }
                results.push({ employeeId, success: true });
            }
            catch (error) {
                results.push({ employeeId, success: false, error: error.message });
            }
        }
        return {
            rotationPattern: dto.shiftIds.length,
            rotationInterval: dto.rotationIntervalDays,
            employees: dto.employeeIds.length,
            successful: results.filter((r) => r.success).length,
            failed: results.filter((r) => !r.success).length,
            results,
        };
    }
    async getShiftStatistics(hospitalId) {
        const shifts = await this.prisma.shift.findMany({
            where: { hospitalId },
        });
        const employeeShifts = await this.prisma.employeeShift.findMany({
            where: { hospitalId },
        });
        return {
            totalShifts: shifts.length,
            activeShifts: shifts.filter((s) => s.isActive).length,
            inactiveShifts: shifts.filter((s) => !s.isActive).length,
            totalAssignments: employeeShifts.length,
            shiftDetails: shifts.map((s) => ({
                id: s.id,
                name: s.name,
                startTime: s.startTime,
                endTime: s.endTime,
                employeeCount: employeeShifts.filter((e) => e.shiftId === s.id).length,
            })),
        };
    }
    async getAllShiftAssignments(hospitalId) {
        return this.prisma.employeeShift.findMany({
            where: { hospitalId },
            orderBy: { effectiveFrom: 'desc' },
            include: {
                user: true,
                shift: true,
            },
        });
    }
};
exports.ShiftsService = ShiftsService;
exports.ShiftsService = ShiftsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], ShiftsService);


/***/ }),
/* 248 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ShiftsController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
const shifts_service_1 = __webpack_require__(247);
const shifts_dto_1 = __webpack_require__(249);
let ShiftsController = class ShiftsController {
    constructor(shiftsService) {
        this.shiftsService = shiftsService;
    }
    async createShift(hospitalId, dto) {
        return this.shiftsService.createShift(hospitalId, dto);
    }
    async getShifts(hospitalId, query) {
        return this.shiftsService.getShifts(hospitalId, query);
    }
    async getAllAssignments(hospitalId) {
        return this.shiftsService.getAllShiftAssignments(hospitalId);
    }
    async getShift(hospitalId, shiftId) {
        return this.shiftsService.getShiftById(hospitalId, shiftId);
    }
    async updateShift(hospitalId, shiftId, dto) {
        return this.shiftsService.updateShift(hospitalId, shiftId, dto);
    }
    async deleteShift(hospitalId, shiftId) {
        return this.shiftsService.deleteShift(hospitalId, shiftId);
    }
    async assignShift(hospitalId, dto) {
        return this.shiftsService.assignShiftToEmployee(hospitalId, dto);
    }
    async bulkAssignShift(hospitalId, dto) {
        return this.shiftsService.bulkAssignShift(hospitalId, dto);
    }
    async removeEmployeeShift(hospitalId, assignmentId, dto) {
        return this.shiftsService.removeEmployeeShift(hospitalId, assignmentId, dto);
    }
    async getEmployeeCurrentShift(hospitalId, employeeId) {
        return this.shiftsService.getEmployeeCurrentShift(hospitalId, employeeId);
    }
    async getEmployeeShiftHistory(hospitalId, employeeId, query) {
        return this.shiftsService.getEmployeeShiftHistory(hospitalId, employeeId, query);
    }
    async checkShiftConflict(hospitalId, dto) {
        return this.shiftsService.checkShiftConflict(hospitalId, dto);
    }
    async getShiftRoster(hospitalId, departmentId, date) {
        return this.shiftsService.getShiftRoster(hospitalId, departmentId, date);
    }
    async createShiftRotation(hospitalId, dto) {
        return this.shiftsService.createShiftRotation(hospitalId, dto);
    }
    async getShiftStatistics(hospitalId) {
        return this.shiftsService.getShiftStatistics(hospitalId);
    }
};
exports.ShiftsController = ShiftsController;
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create a new shift' }),
    (0, common_1.Post)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof shifts_dto_1.CreateShiftDto !== "undefined" && shifts_dto_1.CreateShiftDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "createShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get all shifts' }),
    (0, common_1.Get)(),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof shifts_dto_1.QueryShiftsDto !== "undefined" && shifts_dto_1.QueryShiftsDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getShifts", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get all shift assignments' }),
    (0, common_1.Get)('assignments'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getAllAssignments", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get shift by ID' }),
    (0, common_1.Get)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update shift' }),
    (0, common_1.Put)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_d = typeof shifts_dto_1.UpdateShiftDto !== "undefined" && shifts_dto_1.UpdateShiftDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "updateShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Delete/deactivate shift' }),
    (0, common_1.Delete)(':id'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "deleteShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Assign shift to employee' }),
    (0, common_1.Post)('assign/single'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof shifts_dto_1.AssignShiftToEmployeeDto !== "undefined" && shifts_dto_1.AssignShiftToEmployeeDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "assignShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Bulk assign shift to multiple employees' }),
    (0, common_1.Post)('assign/bulk'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof shifts_dto_1.BulkAssignShiftDto !== "undefined" && shifts_dto_1.BulkAssignShiftDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "bulkAssignShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Remove shift assignment from employee' }),
    (0, common_1.Delete)('assignments/:assignmentId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('assignmentId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_g = typeof shifts_dto_1.RemoveEmployeeShiftDto !== "undefined" && shifts_dto_1.RemoveEmployeeShiftDto) === "function" ? _g : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "removeEmployeeShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get employee current shift' }),
    (0, common_1.Get)('employees/:employeeId/current'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getEmployeeCurrentShift", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get employee shift history' }),
    (0, common_1.Get)('employees/:employeeId/history'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __param(2, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_h = typeof shifts_dto_1.QueryEmployeeShiftsDto !== "undefined" && shifts_dto_1.QueryEmployeeShiftsDto) === "function" ? _h : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getEmployeeShiftHistory", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Check shift conflict for employee' }),
    (0, common_1.Post)('check-conflict'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_j = typeof shifts_dto_1.CheckShiftConflictDto !== "undefined" && shifts_dto_1.CheckShiftConflictDto) === "function" ? _j : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "checkShiftConflict", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get shift roster for department on specific date' }),
    (0, common_1.Get)('roster/:departmentId/:date'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('departmentId')),
    __param(2, (0, common_1.Param)('date')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getShiftRoster", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create shift rotation for employees' }),
    (0, common_1.Post)('rotation/create'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof shifts_dto_1.ShiftRotationDto !== "undefined" && shifts_dto_1.ShiftRotationDto) === "function" ? _k : Object]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "createShiftRotation", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get shift statistics' }),
    (0, common_1.Get)('statistics/summary'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], ShiftsController.prototype, "getShiftStatistics", null);
exports.ShiftsController = ShiftsController = __decorate([
    (0, swagger_1.ApiTags)('Shift Management'),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.Controller)('shifts'),
    __metadata("design:paramtypes", [typeof (_a = typeof shifts_service_1.ShiftsService !== "undefined" && shifts_service_1.ShiftsService) === "function" ? _a : Object])
], ShiftsController);


/***/ }),
/* 249 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.RemoveEmployeeShiftDto = exports.CheckShiftConflictDto = exports.ShiftRotationDto = exports.QueryEmployeeShiftsDto = exports.QueryShiftsDto = exports.BulkAssignShiftDto = exports.AssignShiftToEmployeeDto = exports.UpdateShiftDto = exports.CreateShiftDto = void 0;
const class_validator_1 = __webpack_require__(35);
class CreateShiftDto {
}
exports.CreateShiftDto = CreateShiftDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateShiftDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(23),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "startTime", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(59),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "startMinute", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(23),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "endTime", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(59),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "endMinute", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(60),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "breakDurationMinutes", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(30),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], CreateShiftDto.prototype, "gracePeriodMinutes", void 0);
__decorate([
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Array)
], CreateShiftDto.prototype, "applicableDays", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], CreateShiftDto.prototype, "isActive", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateShiftDto.prototype, "description", void 0);
class UpdateShiftDto {
}
exports.UpdateShiftDto = UpdateShiftDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateShiftDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "startTime", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "startMinute", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "endTime", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "endMinute", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "breakDurationMinutes", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    __metadata("design:type", Number)
], UpdateShiftDto.prototype, "gracePeriodMinutes", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)({ each: true }),
    __metadata("design:type", Array)
], UpdateShiftDto.prototype, "applicableDays", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], UpdateShiftDto.prototype, "isActive", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], UpdateShiftDto.prototype, "description", void 0);
class AssignShiftToEmployeeDto {
}
exports.AssignShiftToEmployeeDto = AssignShiftToEmployeeDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AssignShiftToEmployeeDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AssignShiftToEmployeeDto.prototype, "shiftId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AssignShiftToEmployeeDto.prototype, "effectiveFrom", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], AssignShiftToEmployeeDto.prototype, "effectiveTo", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], AssignShiftToEmployeeDto.prototype, "notes", void 0);
class BulkAssignShiftDto {
}
exports.BulkAssignShiftDto = BulkAssignShiftDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkAssignShiftDto.prototype, "shiftId", void 0);
__decorate([
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Array)
], BulkAssignShiftDto.prototype, "employeeIds", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkAssignShiftDto.prototype, "effectiveFrom", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], BulkAssignShiftDto.prototype, "effectiveTo", void 0);
class QueryShiftsDto {
}
exports.QueryShiftsDto = QueryShiftsDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsBoolean)(),
    __metadata("design:type", Boolean)
], QueryShiftsDto.prototype, "isActive", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsString)(),
    __metadata("design:type", String)
], QueryShiftsDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], QueryShiftsDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], QueryShiftsDto.prototype, "take", void 0);
class QueryEmployeeShiftsDto {
}
exports.QueryEmployeeShiftsDto = QueryEmployeeShiftsDto;
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], QueryEmployeeShiftsDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], QueryEmployeeShiftsDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], QueryEmployeeShiftsDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], QueryEmployeeShiftsDto.prototype, "take", void 0);
class ShiftRotationDto {
}
exports.ShiftRotationDto = ShiftRotationDto;
__decorate([
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Array)
], ShiftRotationDto.prototype, "shiftIds", void 0);
__decorate([
    (0, class_validator_1.IsString)({ each: true }),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Array)
], ShiftRotationDto.prototype, "employeeIds", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ShiftRotationDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.Min)(1),
    __metadata("design:type", Number)
], ShiftRotationDto.prototype, "rotationIntervalDays", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], ShiftRotationDto.prototype, "endDate", void 0);
class CheckShiftConflictDto {
}
exports.CheckShiftConflictDto = CheckShiftConflictDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CheckShiftConflictDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CheckShiftConflictDto.prototype, "shiftId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CheckShiftConflictDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.IsDateString)(),
    __metadata("design:type", String)
], CheckShiftConflictDto.prototype, "endDate", void 0);
class RemoveEmployeeShiftDto {
}
exports.RemoveEmployeeShiftDto = RemoveEmployeeShiftDto;
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RemoveEmployeeShiftDto.prototype, "effectiveFrom", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RemoveEmployeeShiftDto.prototype, "reason", void 0);


/***/ }),
/* 250 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LeavesModule = void 0;
const common_1 = __webpack_require__(2);
const leaves_service_1 = __webpack_require__(251);
const leaves_controller_1 = __webpack_require__(253);
let LeavesModule = class LeavesModule {
};
exports.LeavesModule = LeavesModule;
exports.LeavesModule = LeavesModule = __decorate([
    (0, common_1.Module)({
        controllers: [leaves_controller_1.LeavesController],
        providers: [leaves_service_1.LeavesService],
        exports: [leaves_service_1.LeavesService],
    })
], LeavesModule);


/***/ }),
/* 251 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LeavesService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const library_1 = __webpack_require__(107);
const leaves_dto_1 = __webpack_require__(252);
let LeavesService = class LeavesService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async applyForLeave(hospitalId, userId, dto) {
        const startDate = new Date(dto.startDate);
        const endDate = new Date(dto.endDate);
        if (startDate > endDate) {
            throw new common_1.BadRequestException('End date must be after start date');
        }
        const leaveType = await this.prisma.leaveType.findFirst({
            where: { id: dto.leaveTypeId, hospitalId },
        });
        if (!leaveType) {
            throw new common_1.NotFoundException(`Leave type not found`);
        }
        const numberOfDays = Math.ceil((endDate.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24)) + 1;
        const overlapping = await this.prisma.leave.findFirst({
            where: {
                userId,
                hospitalId,
                status: { in: ['PENDING', 'APPROVED'] },
                startDate: { lte: endDate },
                endDate: { gte: startDate },
            },
        });
        if (overlapping) {
            throw new common_1.ConflictException('Overlapping leave request found');
        }
        return this.prisma.leave.create({
            data: {
                hospitalId,
                userId,
                leaveTypeId: dto.leaveTypeId,
                startDate,
                endDate,
                totalDays: new library_1.Decimal(numberOfDays),
                reason: dto.reason,
                attachmentPath: dto.attachmentUrl,
                status: 'PENDING',
            },
        });
    }
    async getLeaveRequestById(hospitalId, leaveId) {
        const leave = await this.prisma.leave.findFirst({
            where: {
                id: leaveId,
                hospitalId,
            },
            include: {
                leaveType: true,
                user: true,
            },
        });
        if (!leave) {
            throw new common_1.NotFoundException(`Leave request not found`);
        }
        return leave;
    }
    async queryLeaveRequests(hospitalId, query) {
        const { status, leaveTypeId, fromDate, toDate, employeeId, skip = 0, take = 10, cursor, } = query || {};
        const where = {
            hospitalId,
            ...(status && { status }),
            ...(leaveTypeId && { leaveTypeId }),
            ...(employeeId && { userId: employeeId }),
            ...(fromDate &&
                toDate && {
                startDate: { gte: new Date(fromDate) },
                endDate: { lte: new Date(toDate) },
            }),
        };
        if (cursor) {
            where.appliedDate = { lt: new Date(cursor) };
        }
        return this.prisma.leave.findMany({
            where,
            skip: cursor ? 0 : skip,
            take,
            orderBy: { appliedDate: 'desc' },
            include: {
                leaveType: true,
                user: true,
            },
        });
    }
    async updateLeaveRequest(hospitalId, leaveId, dto) {
        const leave = await this.getLeaveRequestById(hospitalId, leaveId);
        if (leave.status !== 'PENDING') {
            throw new common_1.BadRequestException('Cannot update non-pending leave requests');
        }
        const startDate = dto.startDate ? new Date(dto.startDate) : leave.startDate;
        const endDate = dto.endDate ? new Date(dto.endDate) : leave.endDate;
        const numberOfDays = Math.ceil((endDate.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24)) + 1;
        return this.prisma.leave.update({
            where: { id: leaveId },
            data: {
                ...(dto.startDate && { startDate }),
                ...(dto.endDate && { endDate }),
                ...(dto.startDate || dto.endDate) && { totalDays: new library_1.Decimal(numberOfDays) },
                ...(dto.reason && { reason: dto.reason }),
            },
        });
    }
    async processLeaveRequest(hospitalId, leaveId, approverUserId, dto) {
        const leave = await this.getLeaveRequestById(hospitalId, leaveId);
        if (leave.status !== 'PENDING') {
            throw new common_1.BadRequestException('Leave request is no longer pending');
        }
        const status = dto.decision === leaves_dto_1.ApprovalDecision.APPROVED ? 'APPROVED' : 'REJECTED';
        const updated = await this.prisma.leave.update({
            where: { id: leaveId },
            data: {
                status,
                reviewedBy: approverUserId,
                reviewedDate: new Date(),
                reviewComments: dto.comments,
            },
        });
        if (status === 'APPROVED') {
            await this.createLeaveAttendanceRecords(hospitalId, leave);
        }
        return updated;
    }
    async cancelLeaveRequest(hospitalId, leaveId, dto) {
        const leave = await this.getLeaveRequestById(hospitalId, leaveId);
        if (!['PENDING', 'APPROVED'].includes(leave.status)) {
            throw new common_1.BadRequestException('Cannot cancel rejected leave');
        }
        const updated = await this.prisma.leave.update({
            where: { id: leaveId },
            data: {
                status: 'REJECTED',
                rejectionReason: dto.reason,
                rejectedAt: new Date(),
            },
        });
        if (leave.status === 'APPROVED') {
            await this.deleteLeaveAttendanceRecords(leaveId);
        }
        return updated;
    }
    async getLeaveHistory(hospitalId, userId, query) {
        const { status, fromDate, toDate, skip = 0, take = 10 } = query || {};
        return this.prisma.leave.findMany({
            where: {
                hospitalId,
                userId,
                ...(status && { status }),
                ...(fromDate && toDate && {
                    startDate: { gte: new Date(fromDate) },
                    endDate: { lte: new Date(toDate) },
                }),
            },
            skip,
            take,
            orderBy: { createdAt: 'desc' },
            include: { leaveType: true },
        });
    }
    async createLeaveType(hospitalId, dto) {
        const code = dto.code || `${dto.name.replace(/\s+/g, '_').toUpperCase()}`;
        return this.prisma.leaveType.create({
            data: {
                hospitalId,
                name: dto.name,
                code: code.substring(0, 50),
                maxDaysPerYear: dto.annualAllowance,
                description: dto.description,
                requiresApproval: dto.requiresApproval ?? true,
                isActive: dto.isActive ?? true,
            },
        });
    }
    async getLeaveType(hospitalId, leaveTypeId) {
        const leaveType = await this.prisma.leaveType.findFirst({
            where: { id: leaveTypeId, hospitalId },
        });
        if (!leaveType) {
            throw new common_1.NotFoundException(`Leave type not found`);
        }
        return leaveType;
    }
    async getLeaveTypes(hospitalId) {
        return this.prisma.leaveType.findMany({
            where: { hospitalId, isActive: true },
            orderBy: { createdAt: 'asc' },
        });
    }
    async updateLeaveType(hospitalId, leaveTypeId, dto) {
        await this.getLeaveType(hospitalId, leaveTypeId);
        return this.prisma.leaveType.update({
            where: { id: leaveTypeId },
            data: {
                ...(dto.name && { name: dto.name }),
                ...(dto.annualAllowance !== undefined && { maxDaysPerYear: dto.annualAllowance }),
                ...(dto.description && { description: dto.description }),
                ...(dto.requiresApproval !== undefined && { requiresApproval: dto.requiresApproval }),
                ...(dto.isActive !== undefined && { isActive: dto.isActive }),
            },
        });
    }
    async createHoliday(hospitalId, dto) {
        return this.prisma.holiday.create({
            data: {
                hospitalId,
                name: dto.name,
                holidayDate: new Date(dto.date),
                holidayType: 'PUBLIC',
                description: dto.description,
            },
        });
    }
    async getHoliday(hospitalId, holidayId) {
        const holiday = await this.prisma.holiday.findFirst({
            where: { id: holidayId, hospitalId },
        });
        if (!holiday) {
            throw new common_1.NotFoundException(`Holiday not found`);
        }
        return holiday;
    }
    async getHolidays(hospitalId, year) {
        const startDate = year ? new Date(`${year}-01-01`) : new Date();
        const endDate = year ? new Date(`${year}-12-31`) : new Date(`${startDate.getFullYear() + 1}-12-31`);
        return this.prisma.holiday.findMany({
            where: {
                hospitalId,
                isActive: true,
                holidayDate: {
                    gte: startDate,
                    lte: endDate,
                },
            },
            orderBy: { holidayDate: 'asc' },
        });
    }
    async updateHoliday(hospitalId, holidayId, dto) {
        await this.getHoliday(hospitalId, holidayId);
        return this.prisma.holiday.update({
            where: { id: holidayId },
            data: {
                ...(dto.name && { name: dto.name }),
                ...(dto.date && { holidayDate: new Date(dto.date) }),
                ...(dto.description && { description: dto.description }),
            },
        });
    }
    async deleteHoliday(hospitalId, holidayId) {
        await this.getHoliday(hospitalId, holidayId);
        return this.prisma.holiday.update({
            where: { id: holidayId },
            data: { isActive: false },
        });
    }
    async bulkProcessLeaveRequests(hospitalId, approverUserId, dto) {
        const results = [];
        for (const item of dto.items) {
            try {
                const result = await this.processLeaveRequest(hospitalId, item.leaveRequestId, approverUserId, {
                    decision: dto.decision,
                    comments: dto.comments,
                });
                results.push({ leaveRequestId: item.leaveRequestId, success: true, result });
            }
            catch (error) {
                results.push({ leaveRequestId: item.leaveRequestId, success: false, error: error.message });
            }
        }
        return {
            total: dto.items.length,
            successful: results.filter((r) => r.success).length,
            failed: results.filter((r) => !r.success).length,
            results,
        };
    }
    async getLeaveStatistics(hospitalId, query) {
        const year = query?.year || new Date().getFullYear();
        const startDate = query?.fromDate ? new Date(query.fromDate) : new Date(`${year}-01-01`);
        const endDate = query?.toDate ? new Date(query.toDate) : new Date(`${year}-12-31`);
        const leaves = await this.prisma.leave.findMany({
            where: {
                hospitalId,
                ...(query?.employeeId && { userId: query.employeeId }),
                startDate: { gte: startDate },
                endDate: { lte: endDate },
            },
            include: { leaveType: true, user: true },
        });
        const approved = leaves.filter((r) => r.status === 'APPROVED');
        const pending = leaves.filter((r) => r.status === 'PENDING');
        const rejected = leaves.filter((r) => r.status === 'REJECTED');
        const approvedDays = approved.reduce((sum, r) => sum + parseFloat(r.totalDays.toString()), 0);
        return {
            year,
            period: { from: startDate, to: endDate },
            total: leaves.length,
            approved: approved.length,
            pending: pending.length,
            rejected: rejected.length,
            totalApprovedDays: approvedDays,
            byType: this.groupByLeaveType(leaves),
            byStatus: { approved: approved.length, pending: pending.length, rejected: rejected.length },
        };
    }
    async createLeaveAttendanceRecords(hospitalId, leave) {
        const currentDate = new Date(leave.startDate);
        while (currentDate <= leave.endDate) {
            const holiday = await this.prisma.holiday.findFirst({
                where: {
                    hospitalId,
                    holidayDate: {
                        gte: new Date(currentDate.getFullYear(), currentDate.getMonth(), currentDate.getDate()),
                        lt: new Date(currentDate.getFullYear(), currentDate.getMonth(), currentDate.getDate() + 1),
                    },
                },
            });
            if (!holiday) {
                await this.prisma.attendanceRecord.create({
                    data: {
                        hospitalId,
                        userId: leave.userId,
                        attendanceDate: new Date(currentDate),
                        status: 'ON_LEAVE',
                        isManualEntry: true,
                        leaveId: leave.id,
                        remarks: `Leave approved`,
                    },
                });
            }
            currentDate.setDate(currentDate.getDate() + 1);
        }
    }
    async deleteLeaveAttendanceRecords(leaveId) {
        await this.prisma.attendanceRecord.deleteMany({
            where: { leaveId },
        });
    }
    groupByLeaveType(leaves) {
        const grouped = {};
        leaves.forEach((leave) => {
            const typeName = leave.leaveType.name;
            if (!grouped[typeName]) {
                grouped[typeName] = { count: 0, days: 0 };
            }
            grouped[typeName].count += 1;
            grouped[typeName].days += parseFloat(leave.totalDays.toString());
        });
        return grouped;
    }
};
exports.LeavesService = LeavesService;
exports.LeavesService = LeavesService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], LeavesService);


/***/ }),
/* 252 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SetLeavePolicyDto = exports.LeaveStatisticsQueryDto = exports.BulkLeaveItem = exports.BulkProcessLeaveDto = exports.UpdateHolidayDto = exports.CreateHolidayDto = exports.UpdateLeaveTypeDto = exports.CreateLeaveTypeDto = exports.UpdateLeaveBalanceDto = exports.GetLeaveBalanceDto = exports.QueryLeaveRequestsDto = exports.CancelLeaveRequestDto = exports.UpdateLeaveRequestDto = exports.ProcessLeaveRequestDto = exports.ApplyLeaveDto = exports.ApprovalDecision = exports.LeaveRequestStatus = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
var LeaveRequestStatus;
(function (LeaveRequestStatus) {
    LeaveRequestStatus["PENDING"] = "PENDING";
    LeaveRequestStatus["APPROVED"] = "APPROVED";
    LeaveRequestStatus["REJECTED"] = "REJECTED";
    LeaveRequestStatus["CANCELLED"] = "CANCELLED";
})(LeaveRequestStatus || (exports.LeaveRequestStatus = LeaveRequestStatus = {}));
var ApprovalDecision;
(function (ApprovalDecision) {
    ApprovalDecision["APPROVED"] = "APPROVED";
    ApprovalDecision["REJECTED"] = "REJECTED";
})(ApprovalDecision || (exports.ApprovalDecision = ApprovalDecision = {}));
class ApplyLeaveDto {
}
exports.ApplyLeaveDto = ApplyLeaveDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "leaveTypeId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "attachmentUrl", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], ApplyLeaveDto.prototype, "isMedicalLeave", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ApplyLeaveDto.prototype, "medicalCertificateUrl", void 0);
class ProcessLeaveRequestDto {
}
exports.ProcessLeaveRequestDto = ProcessLeaveRequestDto;
__decorate([
    (0, class_validator_1.IsEnum)(ApprovalDecision),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ProcessLeaveRequestDto.prototype, "decision", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ProcessLeaveRequestDto.prototype, "comments", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], ProcessLeaveRequestDto.prototype, "approvedUpto", void 0);
class UpdateLeaveRequestDto {
}
exports.UpdateLeaveRequestDto = UpdateLeaveRequestDto;
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveRequestDto.prototype, "startDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveRequestDto.prototype, "endDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveRequestDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], UpdateLeaveRequestDto.prototype, "isMedicalLeave", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveRequestDto.prototype, "medicalCertificateUrl", void 0);
class CancelLeaveRequestDto {
}
exports.CancelLeaveRequestDto = CancelLeaveRequestDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CancelLeaveRequestDto.prototype, "reason", void 0);
class QueryLeaveRequestsDto {
    constructor() {
        this.skip = 0;
        this.take = 10;
    }
}
exports.QueryLeaveRequestsDto = QueryLeaveRequestsDto;
__decorate([
    (0, class_validator_1.IsEnum)(LeaveRequestStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QueryLeaveRequestsDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QueryLeaveRequestsDto.prototype, "leaveTypeId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QueryLeaveRequestsDto.prototype, "fromDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QueryLeaveRequestsDto.prototype, "toDate", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QueryLeaveRequestsDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], QueryLeaveRequestsDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    (0, class_transformer_1.Type)(() => Number),
    __metadata("design:type", Number)
], QueryLeaveRequestsDto.prototype, "take", void 0);
class GetLeaveBalanceDto {
}
exports.GetLeaveBalanceDto = GetLeaveBalanceDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], GetLeaveBalanceDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], GetLeaveBalanceDto.prototype, "year", void 0);
class UpdateLeaveBalanceDto {
}
exports.UpdateLeaveBalanceDto = UpdateLeaveBalanceDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], UpdateLeaveBalanceDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], UpdateLeaveBalanceDto.prototype, "leaveTypeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.Min)(-365),
    (0, class_validator_1.Max)(365),
    __metadata("design:type", Number)
], UpdateLeaveBalanceDto.prototype, "balanceAdjustment", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveBalanceDto.prototype, "reason", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], UpdateLeaveBalanceDto.prototype, "isAdmin", void 0);
class CreateLeaveTypeDto {
}
exports.CreateLeaveTypeDto = CreateLeaveTypeDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateLeaveTypeDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLeaveTypeDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(365),
    __metadata("design:type", Number)
], CreateLeaveTypeDto.prototype, "annualAllowance", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateLeaveTypeDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], CreateLeaveTypeDto.prototype, "requiresApproval", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], CreateLeaveTypeDto.prototype, "isMedicalLeaveType", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], CreateLeaveTypeDto.prototype, "isActive", void 0);
class UpdateLeaveTypeDto {
}
exports.UpdateLeaveTypeDto = UpdateLeaveTypeDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveTypeDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(365),
    __metadata("design:type", Number)
], UpdateLeaveTypeDto.prototype, "annualAllowance", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateLeaveTypeDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], UpdateLeaveTypeDto.prototype, "requiresApproval", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], UpdateLeaveTypeDto.prototype, "isActive", void 0);
class CreateHolidayDto {
}
exports.CreateHolidayDto = CreateHolidayDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateHolidayDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateHolidayDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateHolidayDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], CreateHolidayDto.prototype, "isOptional", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateHolidayDto.prototype, "departmentId", void 0);
class UpdateHolidayDto {
}
exports.UpdateHolidayDto = UpdateHolidayDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateHolidayDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateHolidayDto.prototype, "date", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateHolidayDto.prototype, "description", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], UpdateHolidayDto.prototype, "isOptional", void 0);
class BulkProcessLeaveDto {
}
exports.BulkProcessLeaveDto = BulkProcessLeaveDto;
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => BulkLeaveItem),
    __metadata("design:type", Array)
], BulkProcessLeaveDto.prototype, "items", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(ApprovalDecision),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkProcessLeaveDto.prototype, "decision", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkProcessLeaveDto.prototype, "comments", void 0);
class BulkLeaveItem {
}
exports.BulkLeaveItem = BulkLeaveItem;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], BulkLeaveItem.prototype, "leaveRequestId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BulkLeaveItem.prototype, "notes", void 0);
class LeaveStatisticsQueryDto {
}
exports.LeaveStatisticsQueryDto = LeaveStatisticsQueryDto;
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], LeaveStatisticsQueryDto.prototype, "year", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], LeaveStatisticsQueryDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], LeaveStatisticsQueryDto.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], LeaveStatisticsQueryDto.prototype, "fromDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], LeaveStatisticsQueryDto.prototype, "toDate", void 0);
class SetLeavePolicyDto {
}
exports.SetLeavePolicyDto = SetLeavePolicyDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SetLeavePolicyDto.prototype, "leaveTypeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SetLeavePolicyDto.prototype, "maxConsecutiveDays", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], SetLeavePolicyDto.prototype, "minNoticeRequiredDays", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], SetLeavePolicyDto.prototype, "allowPartialDay", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], SetLeavePolicyDto.prototype, "carryForwardAllowed", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], SetLeavePolicyDto.prototype, "carryForwardPercentage", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SetLeavePolicyDto.prototype, "description", void 0);


/***/ }),
/* 253 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LeavesController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_user_decorator_1 = __webpack_require__(33);
const current_hospital_decorator_1 = __webpack_require__(225);
const leaves_service_1 = __webpack_require__(251);
const leaves_dto_1 = __webpack_require__(252);
let LeavesController = class LeavesController {
    constructor(leavesService) {
        this.leavesService = leavesService;
    }
    async applyForLeave(hospitalId, user, dto) {
        return this.leavesService.applyForLeave(hospitalId, user.id, dto);
    }
    async getLeaveRequest(hospitalId, leaveRequestId) {
        return this.leavesService.getLeaveRequestById(hospitalId, leaveRequestId);
    }
    async queryLeaveRequests(hospitalId, query) {
        return this.leavesService.queryLeaveRequests(hospitalId, query);
    }
    async updateLeaveRequest(hospitalId, leaveRequestId, dto) {
        return this.leavesService.updateLeaveRequest(hospitalId, leaveRequestId, dto);
    }
    async processLeaveRequest(hospitalId, user, leaveRequestId, dto) {
        return this.leavesService.processLeaveRequest(hospitalId, leaveRequestId, user.id, dto);
    }
    async cancelLeaveRequest(hospitalId, leaveRequestId, dto) {
        return this.leavesService.cancelLeaveRequest(hospitalId, leaveRequestId, dto);
    }
    async getLeaveHistory(hospitalId, employeeId, query) {
        return this.leavesService.getLeaveHistory(hospitalId, employeeId, query);
    }
    async getLeaveBalance(hospitalId, employeeId, leaveTypeId) {
        const leaves = await this.leavesService.queryLeaveRequests(hospitalId, {
            employeeId,
            leaveTypeId,
            status: 'APPROVED',
        });
        const totalDays = leaves.reduce((sum, l) => sum + parseFloat(l.totalDays.toString()), 0);
        const leaveType = await this.leavesService.getLeaveType(hospitalId, leaveTypeId);
        return {
            employeeId,
            leaveTypeId,
            leaveType: leaveType.name,
            maxDaysPerYear: leaveType.maxDaysPerYear,
            usedDays: totalDays,
            availableDays: leaveType.maxDaysPerYear - totalDays,
        };
    }
    async createLeaveType(hospitalId, dto) {
        return this.leavesService.createLeaveType(hospitalId, dto);
    }
    async getLeaveType(hospitalId, leaveTypeId) {
        return this.leavesService.getLeaveType(hospitalId, leaveTypeId);
    }
    async getLeaveTypes(hospitalId) {
        return this.leavesService.getLeaveTypes(hospitalId);
    }
    async updateLeaveType(hospitalId, leaveTypeId, dto) {
        return this.leavesService.updateLeaveType(hospitalId, leaveTypeId, dto);
    }
    async createHoliday(hospitalId, dto) {
        return this.leavesService.createHoliday(hospitalId, dto);
    }
    async getHoliday(hospitalId, holidayId) {
        return this.leavesService.getHoliday(hospitalId, holidayId);
    }
    async getHolidays(hospitalId, year) {
        return this.leavesService.getHolidays(hospitalId, year ? parseInt(year) : undefined);
    }
    async updateHoliday(hospitalId, holidayId, dto) {
        return this.leavesService.updateHoliday(hospitalId, holidayId, dto);
    }
    async deleteHoliday(hospitalId, holidayId) {
        return this.leavesService.deleteHoliday(hospitalId, holidayId);
    }
    async bulkProcessLeaves(hospitalId, user, dto) {
        return this.leavesService.bulkProcessLeaveRequests(hospitalId, user.sub, dto);
    }
    async getLeaveStatistics(hospitalId, query) {
        return this.leavesService.getLeaveStatistics(hospitalId, query);
    }
};
exports.LeavesController = LeavesController;
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Apply for leave' }),
    (0, common_1.Post)('requests/apply'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, typeof (_b = typeof leaves_dto_1.ApplyLeaveDto !== "undefined" && leaves_dto_1.ApplyLeaveDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "applyForLeave", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get leave request by ID' }),
    (0, common_1.Get)('requests/:leaveRequestId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('leaveRequestId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveRequest", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Query leave requests with filters' }),
    (0, common_1.Get)('requests'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof leaves_dto_1.QueryLeaveRequestsDto !== "undefined" && leaves_dto_1.QueryLeaveRequestsDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "queryLeaveRequests", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update pending leave request' }),
    (0, common_1.Put)('requests/:leaveRequestId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('leaveRequestId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_d = typeof leaves_dto_1.UpdateLeaveRequestDto !== "undefined" && leaves_dto_1.UpdateLeaveRequestDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "updateLeaveRequest", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Approve or reject leave request' }),
    (0, common_1.Post)('requests/:leaveRequestId/process'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __param(2, (0, common_1.Param)('leaveRequestId')),
    __param(3, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, String, typeof (_e = typeof leaves_dto_1.ProcessLeaveRequestDto !== "undefined" && leaves_dto_1.ProcessLeaveRequestDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "processLeaveRequest", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Cancel leave request' }),
    (0, common_1.Post)('requests/:leaveRequestId/cancel'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('leaveRequestId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_f = typeof leaves_dto_1.CancelLeaveRequestDto !== "undefined" && leaves_dto_1.CancelLeaveRequestDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "cancelLeaveRequest", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get leave history for employee' }),
    (0, common_1.Get)('history/:employeeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __param(2, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_g = typeof leaves_dto_1.QueryLeaveRequestsDto !== "undefined" && leaves_dto_1.QueryLeaveRequestsDto) === "function" ? _g : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveHistory", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get leave balance for employee and type' }),
    (0, common_1.Get)('balance/:employeeId/:leaveTypeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('employeeId')),
    __param(2, (0, common_1.Param)('leaveTypeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveBalance", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create leave type' }),
    (0, common_1.Post)('types'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_h = typeof leaves_dto_1.CreateLeaveTypeDto !== "undefined" && leaves_dto_1.CreateLeaveTypeDto) === "function" ? _h : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "createLeaveType", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get leave type' }),
    (0, common_1.Get)('types/:leaveTypeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('leaveTypeId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveType", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get all leave types' }),
    (0, common_1.Get)('types'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveTypes", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update leave type' }),
    (0, common_1.Put)('types/:leaveTypeId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('leaveTypeId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_j = typeof leaves_dto_1.UpdateLeaveTypeDto !== "undefined" && leaves_dto_1.UpdateLeaveTypeDto) === "function" ? _j : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "updateLeaveType", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Create holiday' }),
    (0, common_1.Post)('holidays'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof leaves_dto_1.CreateHolidayDto !== "undefined" && leaves_dto_1.CreateHolidayDto) === "function" ? _k : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "createHoliday", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get holiday' }),
    (0, common_1.Get)('holidays/:holidayId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('holidayId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getHoliday", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get holidays for year' }),
    (0, common_1.Get)('holidays'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('year')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getHolidays", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update holiday' }),
    (0, common_1.Put)('holidays/:holidayId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('holidayId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_l = typeof leaves_dto_1.UpdateHolidayDto !== "undefined" && leaves_dto_1.UpdateHolidayDto) === "function" ? _l : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "updateHoliday", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Delete holiday' }),
    (0, common_1.Delete)('holidays/:holidayId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('holidayId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "deleteHoliday", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Bulk process leave requests' }),
    (0, common_1.Post)('requests/bulk-process'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, current_user_decorator_1.CurrentUser)()),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object, typeof (_m = typeof leaves_dto_1.BulkProcessLeaveDto !== "undefined" && leaves_dto_1.BulkProcessLeaveDto) === "function" ? _m : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "bulkProcessLeaves", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get leave statistics and reports' }),
    (0, common_1.Get)('statistics/summary'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_o = typeof leaves_dto_1.LeaveStatisticsQueryDto !== "undefined" && leaves_dto_1.LeaveStatisticsQueryDto) === "function" ? _o : Object]),
    __metadata("design:returntype", Promise)
], LeavesController.prototype, "getLeaveStatistics", null);
exports.LeavesController = LeavesController = __decorate([
    (0, swagger_1.ApiTags)('Leave Management'),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.Controller)('leaves'),
    __metadata("design:paramtypes", [typeof (_a = typeof leaves_service_1.LeavesService !== "undefined" && leaves_service_1.LeavesService) === "function" ? _a : Object])
], LeavesController);


/***/ }),
/* 254 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DeviceSyncModule = void 0;
const common_1 = __webpack_require__(2);
const device_sync_service_1 = __webpack_require__(255);
const device_sync_controller_1 = __webpack_require__(256);
let DeviceSyncModule = class DeviceSyncModule {
};
exports.DeviceSyncModule = DeviceSyncModule;
exports.DeviceSyncModule = DeviceSyncModule = __decorate([
    (0, common_1.Module)({
        controllers: [device_sync_controller_1.DeviceSyncController],
        providers: [device_sync_service_1.DeviceSyncService],
        exports: [device_sync_service_1.DeviceSyncService],
    })
], DeviceSyncModule);


/***/ }),
/* 255 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DeviceSyncService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
let DeviceSyncService = class DeviceSyncService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async triggerDeviceSync(hospitalId, dto) {
        const device = await this.prisma.biometricDevice.findFirst({
            where: { id: dto.deviceId, hospitalId },
        });
        if (!device) {
            throw new common_1.NotFoundException(`Device not found`);
        }
        if (device.status !== 'ACTIVE') {
            throw new common_1.BadRequestException(`Device is not active`);
        }
        const syncLog = await this.prisma.deviceSyncLog.create({
            data: {
                hospitalId,
                deviceId: dto.deviceId,
                syncStartTime: new Date(),
                status: 'PENDING',
            },
        });
        return syncLog;
    }
    async getSyncLogById(hospitalId, syncLogId) {
        const syncLog = await this.prisma.deviceSyncLog.findFirst({
            where: {
                id: syncLogId,
                hospitalId,
            },
            include: {
                device: true,
            },
        });
        if (!syncLog) {
            throw new common_1.NotFoundException(`Sync log not found`);
        }
        return syncLog;
    }
    async querySyncLogs(hospitalId, query) {
        const { deviceId, status, fromDate, toDate, skip = 0, take = 10, cursor, } = query || {};
        const where = {
            hospitalId,
            ...(deviceId && { deviceId }),
            ...(status && { status }),
            ...(fromDate &&
                toDate && {
                syncStartTime: { gte: new Date(fromDate) },
                syncEndTime: { lte: new Date(toDate) },
            }),
        };
        if (cursor) {
            where.syncStartTime = {
                ...where.syncStartTime,
                lt: new Date(cursor),
            };
        }
        return this.prisma.deviceSyncLog.findMany({
            where,
            skip: cursor ? 0 : skip,
            take,
            orderBy: { syncStartTime: 'desc' },
            include: { device: true },
        });
    }
    async processAttendanceLogs(hospitalId, dto) {
        const device = await this.prisma.biometricDevice.findFirst({
            where: { id: dto.deviceId, hospitalId },
        });
        if (!device) {
            throw new common_1.NotFoundException(`Device not found`);
        }
        const syncLog = await this.prisma.deviceSyncLog.create({
            data: {
                hospitalId,
                deviceId: dto.deviceId,
                syncStartTime: new Date(),
                logsReceived: dto.logs.length,
                status: 'PENDING',
            },
        });
        let processed = 0;
        let skipped = 0;
        let errors = 0;
        const processedLogIds = [];
        const logTypeMap = {
            'CHECKIN': 'CHECK_IN',
            'CHECK_IN': 'CHECK_IN',
            'CHECKOUT': 'CHECK_OUT',
            'CHECK_OUT': 'CHECK_OUT',
            'ENTRY': 'ENTRY',
            'EXIT': 'EXIT',
            'VERIFY': 'CHECK_IN',
            'UNKNOWN': 'UNKNOWN',
        };
        const enrollments = await this.prisma.biometricEnrollment.findMany({
            where: {
                deviceId: dto.deviceId,
                isActive: true,
            },
            include: { user: true },
        });
        const enrollmentsByType = new Map();
        for (const enrollment of enrollments) {
            const key = enrollment.enrollmentType;
            if (!enrollmentsByType.has(key)) {
                enrollmentsByType.set(key, []);
            }
            enrollmentsByType.get(key).push(enrollment);
        }
        for (const logEntry of dto.logs) {
            try {
                let enrollment = enrollments[0];
                if (!enrollment) {
                    errors++;
                    continue;
                }
                const logDate = new Date(logEntry.timestamp * 1000);
                const existing = await this.prisma.attendanceLog.findFirst({
                    where: {
                        deviceId: dto.deviceId,
                        userId: enrollment.userId,
                        logTime: logDate,
                    },
                });
                if (existing) {
                    skipped++;
                    continue;
                }
                const dbLogType = logTypeMap[logEntry.logType] || 'UNKNOWN';
                let verificationMethod = 'FINGERPRINT';
                if (enrollment.enrollmentType.includes('FACE')) {
                    verificationMethod = 'FACE';
                }
                else if (enrollment.enrollmentType.includes('RFID')) {
                    verificationMethod = 'RFID';
                }
                const attendanceLog = await this.prisma.attendanceLog.create({
                    data: {
                        hospitalId,
                        deviceId: dto.deviceId,
                        userId: enrollment.userId,
                        enrollmentId: enrollment.id,
                        logTime: logDate,
                        logType: dbLogType,
                        verificationMethod,
                        verificationScore: 95,
                        isProcessed: false,
                    },
                });
                processedLogIds.push(attendanceLog.id);
                processed++;
                await this.prisma.biometricDevice.update({
                    where: { id: dto.deviceId },
                    data: { lastSyncTime: new Date() },
                });
            }
            catch (error) {
                errors++;
            }
        }
        const syncEndTime = new Date();
        const durationMs = syncEndTime.getTime() - syncLog.syncStartTime.getTime();
        const updatedSyncLog = await this.prisma.deviceSyncLog.update({
            where: { id: syncLog.id },
            data: {
                syncEndTime,
                durationMs,
                logsProcessed: processed,
                logsSkipped: skipped,
                logsErrors: errors,
                status: errors > 0 && processed === 0 ? 'FAILURE' : (errors > 0 ? 'PARTIAL' : 'SUCCESS'),
                lastLogTime: dto.logs.length > 0 ? new Date(Math.max(...dto.logs.map((l) => l.timestamp * 1000))) : null,
            },
        });
        return {
            syncLogId: updatedSyncLog.id,
            total: dto.logs.length,
            processed,
            skipped,
            errors,
            processedLogIds,
            duration: durationMs,
            status: updatedSyncLog.status,
        };
    }
    async getAttendanceLogsFromSync(hospitalId, syncLogId) {
        const syncLog = await this.getSyncLogById(hospitalId, syncLogId);
        return this.prisma.attendanceLog.findMany({
            where: {
                hospitalId,
                deviceId: syncLog.deviceId,
                logTime: {
                    gte: syncLog.syncStartTime,
                    lte: syncLog.syncEndTime || new Date(),
                },
            },
            orderBy: { logTime: 'asc' },
        });
    }
    async configureSyncSettings(hospitalId, dto) {
        await this.prisma.biometricDevice.findFirstOrThrow({
            where: { id: dto.deviceId, hospitalId },
        });
        const configEntries = [];
        if (dto.syncIntervalMinutes !== undefined) {
            configEntries.push({
                hospitalId,
                configKey: `sync_interval_${dto.deviceId}`,
                configValue: dto.syncIntervalMinutes.toString(),
                dataType: 'number',
                description: `Sync interval for device ${dto.deviceId} in minutes`,
            });
        }
        if (dto.enableAutoSync !== undefined) {
            configEntries.push({
                hospitalId,
                configKey: `auto_sync_${dto.deviceId}`,
                configValue: dto.enableAutoSync.toString(),
                dataType: 'boolean',
                description: `Auto sync enabled for device ${dto.deviceId}`,
            });
        }
        if (dto.maxRetries !== undefined) {
            configEntries.push({
                hospitalId,
                configKey: `max_retries_${dto.deviceId}`,
                configValue: dto.maxRetries.toString(),
                dataType: 'number',
                description: `Max retries for device ${dto.deviceId}`,
            });
        }
        for (const entry of configEntries) {
            await this.prisma.attendanceConfig.upsert({
                where: {
                    hospitalId_configKey: {
                        hospitalId: entry.hospitalId,
                        configKey: entry.configKey,
                    },
                },
                create: entry,
                update: { configValue: entry.configValue },
            });
        }
        return { success: true, message: 'Sync settings configured', deviceId: dto.deviceId };
    }
    async retrySync(hospitalId, dto) {
        const syncLog = await this.getSyncLogById(hospitalId, dto.syncLogId);
        if (syncLog.status !== 'FAILURE') {
            throw new common_1.BadRequestException('Only failed syncs can be retried');
        }
        const newSyncLog = await this.prisma.deviceSyncLog.create({
            data: {
                hospitalId,
                deviceId: syncLog.deviceId,
                syncStartTime: new Date(),
                status: 'PENDING',
            },
        });
        return newSyncLog;
    }
    async resolveSyncError(hospitalId, dto) {
        const syncLog = await this.getSyncLogById(hospitalId, dto.syncLogId);
        const updated = await this.prisma.deviceSyncLog.update({
            where: { id: syncLog.id },
            data: {
                status: 'SUCCESS',
                errorMessage: dto.resolution,
            },
        });
        if (dto.retrySync) {
            await this.retrySync(hospitalId, { syncLogId: dto.syncLogId, reason: dto.resolution });
        }
        return updated;
    }
    async batchDeviceSync(hospitalId, dto) {
        const results = [];
        for (const deviceId of dto.deviceIds) {
            try {
                const syncLog = await this.triggerDeviceSync(hospitalId, {
                    deviceId,
                    fullSync: dto.fullSync,
                    reason: dto.reason,
                });
                results.push({ deviceId, success: true, syncLogId: syncLog.id });
            }
            catch (error) {
                results.push({ deviceId, success: false, error: error.message });
            }
        }
        return {
            total: dto.deviceIds.length,
            successful: results.filter((r) => r.success).length,
            failed: results.filter((r) => !r.success).length,
            results,
        };
    }
    async getSyncStatistics(hospitalId, query) {
        const fromDate = query?.fromDate ? new Date(query.fromDate) : new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
        const toDate = query?.toDate ? new Date(query.toDate) : new Date();
        const syncLogs = await this.prisma.deviceSyncLog.findMany({
            where: {
                hospitalId,
                ...(query?.deviceId && { deviceId: query.deviceId }),
                syncStartTime: { gte: fromDate, lte: toDate },
            },
            include: { device: true },
        });
        const successful = syncLogs.filter((s) => s.status === 'SUCCESS').length;
        const failed = syncLogs.filter((s) => s.status === 'FAILURE').length;
        const pending = syncLogs.filter((s) => s.status === 'PENDING').length;
        const totalLogsProcessed = syncLogs.reduce((sum, s) => sum + s.logsProcessed, 0);
        const totalLogsSkipped = syncLogs.reduce((sum, s) => sum + s.logsSkipped, 0);
        const totalLogsErrors = syncLogs.reduce((sum, s) => sum + s.logsErrors, 0);
        const avgDuration = syncLogs.filter((s) => s.durationMs).reduce((sum, s) => sum + (s.durationMs || 0), 0) / syncLogs.filter((s) => s.durationMs).length || 0;
        return {
            period: { from: fromDate, to: toDate },
            totalSyncs: syncLogs.length,
            successful,
            failed,
            pending,
            totalLogsProcessed,
            totalLogsSkipped,
            totalLogsErrors,
            averageDurationMs: Math.round(avgDuration),
            successRate: syncLogs.length > 0 ? Math.round((successful / syncLogs.length) * 100) : 0,
            byDevice: this.groupByDevice(syncLogs),
        };
    }
    async verifyLogIntegrity(hospitalId, dto) {
        const fromDate = new Date(dto.fromDate);
        const toDate = new Date(dto.toDate);
        const logs = await this.prisma.attendanceLog.findMany({
            where: {
                hospitalId,
                deviceId: dto.deviceId,
                logTime: { gte: fromDate, lte: toDate },
            },
        });
        const unprocessed = logs.filter((l) => !l.isProcessed);
        const processed = logs.filter((l) => l.isProcessed);
        return {
            deviceId: dto.deviceId,
            period: { from: fromDate, to: toDate },
            totalLogs: logs.length,
            processedLogs: processed.length,
            unprocessedLogs: unprocessed.length,
            integrity: unprocessed.length === 0 ? 'OK' : 'PENDING_PROCESSING',
            unprocessedLogIds: unprocessed.map((l) => l.id),
        };
    }
    async updateDeviceSyncStatus(hospitalId, dto) {
        const device = await this.prisma.biometricDevice.findFirst({
            where: { id: dto.deviceId, hospitalId },
        });
        if (!device) {
            throw new common_1.NotFoundException(`Device not found`);
        }
        const statusMap = {
            'PENDING': 'ACTIVE',
            'SUCCESS': 'ACTIVE',
            'FAILURE': 'OFFLINE',
            'PARTIAL': 'ACTIVE',
            'TIMEOUT': 'OFFLINE',
        };
        const mappedStatus = statusMap[dto.status];
        if (!mappedStatus) {
            throw new common_1.BadRequestException(`Invalid device operation status`);
        }
        return this.prisma.biometricDevice.update({
            where: { id: dto.deviceId },
            data: {
                status: mappedStatus,
            },
        });
    }
    async getUnprocessedLogs(hospitalId, deviceId) {
        return this.prisma.attendanceLog.findMany({
            where: {
                hospitalId,
                isProcessed: false,
                ...(deviceId && { deviceId }),
            },
            orderBy: { logTime: 'asc' },
            take: 1000,
        });
    }
    async markLogsAsProcessed(logIds) {
        const result = await this.prisma.attendanceLog.updateMany({
            where: { id: { in: logIds } },
            data: { isProcessed: true },
        });
        return { processed: result.count };
    }
    groupByDevice(syncLogs) {
        const grouped = {};
        syncLogs.forEach((log) => {
            const deviceId = log.deviceId;
            if (!grouped[deviceId]) {
                grouped[deviceId] = { syncs: 0, logsProcessed: 0, errors: 0 };
            }
            grouped[deviceId].syncs += 1;
            grouped[deviceId].logsProcessed += log.logsProcessed;
            grouped[deviceId].errors += log.logsErrors;
        });
        return grouped;
    }
};
exports.DeviceSyncService = DeviceSyncService;
exports.DeviceSyncService = DeviceSyncService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], DeviceSyncService);


/***/ }),
/* 256 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DeviceSyncController = void 0;
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const jwt_auth_guard_1 = __webpack_require__(29);
const current_hospital_decorator_1 = __webpack_require__(225);
const device_sync_service_1 = __webpack_require__(255);
const device_sync_dto_1 = __webpack_require__(257);
let DeviceSyncController = class DeviceSyncController {
    constructor(deviceSyncService) {
        this.deviceSyncService = deviceSyncService;
    }
    async triggerSync(hospitalId, dto) {
        return this.deviceSyncService.triggerDeviceSync(hospitalId, dto);
    }
    async getSyncLog(hospitalId, syncLogId) {
        return this.deviceSyncService.getSyncLogById(hospitalId, syncLogId);
    }
    async querySyncLogs(hospitalId, query) {
        return this.deviceSyncService.querySyncLogs(hospitalId, query);
    }
    async processLogs(hospitalId, dto) {
        return this.deviceSyncService.processAttendanceLogs(hospitalId, dto);
    }
    async getAttendanceLogs(hospitalId, syncLogId) {
        return this.deviceSyncService.getAttendanceLogsFromSync(hospitalId, syncLogId);
    }
    async configureSyncSettings(hospitalId, dto) {
        return this.deviceSyncService.configureSyncSettings(hospitalId, dto);
    }
    async retrySync(hospitalId, dto) {
        return this.deviceSyncService.retrySync(hospitalId, dto);
    }
    async resolveError(hospitalId, dto) {
        return this.deviceSyncService.resolveSyncError(hospitalId, dto);
    }
    async batchSync(hospitalId, dto) {
        return this.deviceSyncService.batchDeviceSync(hospitalId, dto);
    }
    async getSyncStatistics(hospitalId, query) {
        return this.deviceSyncService.getSyncStatistics(hospitalId, query);
    }
    async verifyIntegrity(hospitalId, dto) {
        return this.deviceSyncService.verifyLogIntegrity(hospitalId, dto);
    }
    async updateDeviceSyncStatus(hospitalId, deviceId, dto) {
        return this.deviceSyncService.updateDeviceSyncStatus(hospitalId, dto);
    }
    async getUnprocessedLogs(hospitalId, deviceId) {
        return this.deviceSyncService.getUnprocessedLogs(hospitalId, deviceId);
    }
    async markLogsAsProcessed(dto) {
        return this.deviceSyncService.markLogsAsProcessed(dto.logIds);
    }
};
exports.DeviceSyncController = DeviceSyncController;
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Trigger device synchronization' }),
    (0, common_1.Post)('trigger'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_b = typeof device_sync_dto_1.TriggerDeviceSyncDto !== "undefined" && device_sync_dto_1.TriggerDeviceSyncDto) === "function" ? _b : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "triggerSync", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get sync log by ID' }),
    (0, common_1.Get)('logs/:syncLogId'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('syncLogId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "getSyncLog", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Query sync logs with filtering' }),
    (0, common_1.Get)('logs'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof device_sync_dto_1.QuerySyncLogsDto !== "undefined" && device_sync_dto_1.QuerySyncLogsDto) === "function" ? _c : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "querySyncLogs", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Process attendance logs from device' }),
    (0, common_1.Post)('process-logs'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof device_sync_dto_1.ProcessAttendanceLogsDto !== "undefined" && device_sync_dto_1.ProcessAttendanceLogsDto) === "function" ? _d : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "processLogs", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get attendance logs from sync' }),
    (0, common_1.Get)('logs/:syncLogId/logs'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('syncLogId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "getAttendanceLogs", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Configure device sync settings' }),
    (0, common_1.Post)('configure'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof device_sync_dto_1.ConfigureSyncSettingsDto !== "undefined" && device_sync_dto_1.ConfigureSyncSettingsDto) === "function" ? _e : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "configureSyncSettings", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Retry failed sync' }),
    (0, common_1.Post)('retry'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_f = typeof device_sync_dto_1.RetrySyncDto !== "undefined" && device_sync_dto_1.RetrySyncDto) === "function" ? _f : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "retrySync", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Resolve sync error' }),
    (0, common_1.Post)('resolve-error'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_g = typeof device_sync_dto_1.ResolveSyncErrorDto !== "undefined" && device_sync_dto_1.ResolveSyncErrorDto) === "function" ? _g : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "resolveError", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Batch synchronize multiple devices' }),
    (0, common_1.Post)('batch-sync'),
    (0, common_1.HttpCode)(common_1.HttpStatus.CREATED),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_h = typeof device_sync_dto_1.BatchDeviceSyncDto !== "undefined" && device_sync_dto_1.BatchDeviceSyncDto) === "function" ? _h : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "batchSync", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get sync statistics' }),
    (0, common_1.Get)('statistics/summary'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_j = typeof device_sync_dto_1.SyncStatisticsQueryDto !== "undefined" && device_sync_dto_1.SyncStatisticsQueryDto) === "function" ? _j : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "getSyncStatistics", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Verify log integrity' }),
    (0, common_1.Post)('verify-integrity'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_k = typeof device_sync_dto_1.VerifyLogIntegrityDto !== "undefined" && device_sync_dto_1.VerifyLogIntegrityDto) === "function" ? _k : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "verifyIntegrity", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Update device sync status' }),
    (0, common_1.Put)('devices/:deviceId/sync-status'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Param)('deviceId')),
    __param(2, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, typeof (_l = typeof device_sync_dto_1.UpdateDeviceSyncStatusDto !== "undefined" && device_sync_dto_1.UpdateDeviceSyncStatusDto) === "function" ? _l : Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "updateDeviceSyncStatus", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Get unprocessed attendance logs' }),
    (0, common_1.Get)('unprocessed-logs'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, current_hospital_decorator_1.CurrentHospital)()),
    __param(1, (0, common_1.Query)('deviceId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "getUnprocessedLogs", null);
__decorate([
    (0, swagger_1.ApiOperation)({ summary: 'Mark logs as processed' }),
    (0, common_1.Post)('mark-processed'),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Body)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Object]),
    __metadata("design:returntype", Promise)
], DeviceSyncController.prototype, "markLogsAsProcessed", null);
exports.DeviceSyncController = DeviceSyncController = __decorate([
    (0, swagger_1.ApiTags)('Device Synchronization'),
    (0, swagger_1.ApiBearerAuth)('access-token'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard),
    (0, common_1.Controller)('device-sync'),
    __metadata("design:paramtypes", [typeof (_a = typeof device_sync_service_1.DeviceSyncService !== "undefined" && device_sync_service_1.DeviceSyncService) === "function" ? _a : Object])
], DeviceSyncController);


/***/ }),
/* 257 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a, _b;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateDeviceSyncStatusDto = exports.VerifyLogIntegrityDto = exports.SyncStatisticsQueryDto = exports.BatchDeviceSyncDto = exports.ResolveSyncErrorDto = exports.RetrySyncDto = exports.ConfigureSyncSettingsDto = exports.QuerySyncLogsDto = exports.AttendanceLogEntry = exports.ProcessAttendanceLogsDto = exports.TriggerDeviceSyncDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
const client_1 = __webpack_require__(16);
class TriggerDeviceSyncDto {
}
exports.TriggerDeviceSyncDto = TriggerDeviceSyncDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], TriggerDeviceSyncDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], TriggerDeviceSyncDto.prototype, "fullSync", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], TriggerDeviceSyncDto.prototype, "syncFromTime", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], TriggerDeviceSyncDto.prototype, "reason", void 0);
class ProcessAttendanceLogsDto {
}
exports.ProcessAttendanceLogsDto = ProcessAttendanceLogsDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ProcessAttendanceLogsDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.ValidateNested)({ each: true }),
    (0, class_transformer_1.Type)(() => AttendanceLogEntry),
    __metadata("design:type", Array)
], ProcessAttendanceLogsDto.prototype, "logs", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ProcessAttendanceLogsDto.prototype, "syncTime", void 0);
class AttendanceLogEntry {
}
exports.AttendanceLogEntry = AttendanceLogEntry;
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Number)
], AttendanceLogEntry.prototype, "logId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AttendanceLogEntry.prototype, "employeeId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", Number)
], AttendanceLogEntry.prototype, "timestamp", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], AttendanceLogEntry.prototype, "logType", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], AttendanceLogEntry.prototype, "temperature", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AttendanceLogEntry.prototype, "biometricType", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], AttendanceLogEntry.prototype, "deviceSN", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Number)
], AttendanceLogEntry.prototype, "workCode", void 0);
class QuerySyncLogsDto {
    constructor() {
        this.skip = 0;
        this.take = 10;
    }
}
exports.QuerySyncLogsDto = QuerySyncLogsDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QuerySyncLogsDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.DeviceOperationStatus),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", typeof (_a = typeof client_1.DeviceOperationStatus !== "undefined" && client_1.DeviceOperationStatus) === "function" ? _a : Object)
], QuerySyncLogsDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QuerySyncLogsDto.prototype, "fromDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], QuerySyncLogsDto.prototype, "toDate", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(0),
    __metadata("design:type", Number)
], QuerySyncLogsDto.prototype, "skip", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(100),
    __metadata("design:type", Number)
], QuerySyncLogsDto.prototype, "take", void 0);
class ConfigureSyncSettingsDto {
}
exports.ConfigureSyncSettingsDto = ConfigureSyncSettingsDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ConfigureSyncSettingsDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(1440),
    __metadata("design:type", Number)
], ConfigureSyncSettingsDto.prototype, "syncIntervalMinutes", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], ConfigureSyncSettingsDto.prototype, "enableAutoSync", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], ConfigureSyncSettingsDto.prototype, "enableErrorNotification", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(1),
    (0, class_validator_1.Max)(10),
    __metadata("design:type", Number)
], ConfigureSyncSettingsDto.prototype, "maxRetries", void 0);
__decorate([
    (0, class_validator_1.IsNumber)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Min)(30),
    (0, class_validator_1.Max)(300),
    __metadata("design:type", Number)
], ConfigureSyncSettingsDto.prototype, "retryIntervalSeconds", void 0);
class RetrySyncDto {
}
exports.RetrySyncDto = RetrySyncDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], RetrySyncDto.prototype, "syncLogId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], RetrySyncDto.prototype, "reason", void 0);
class ResolveSyncErrorDto {
}
exports.ResolveSyncErrorDto = ResolveSyncErrorDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ResolveSyncErrorDto.prototype, "syncLogId", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], ResolveSyncErrorDto.prototype, "resolution", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], ResolveSyncErrorDto.prototype, "retrySync", void 0);
class BatchDeviceSyncDto {
}
exports.BatchDeviceSyncDto = BatchDeviceSyncDto;
__decorate([
    (0, class_validator_1.IsArray)(),
    (0, class_validator_1.IsUUID)('all', { each: true }),
    __metadata("design:type", Array)
], BatchDeviceSyncDto.prototype, "deviceIds", void 0);
__decorate([
    (0, class_validator_1.IsBoolean)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", Boolean)
], BatchDeviceSyncDto.prototype, "fullSync", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], BatchDeviceSyncDto.prototype, "reason", void 0);
class SyncStatisticsQueryDto {
}
exports.SyncStatisticsQueryDto = SyncStatisticsQueryDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SyncStatisticsQueryDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SyncStatisticsQueryDto.prototype, "fromDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], SyncStatisticsQueryDto.prototype, "toDate", void 0);
class VerifyLogIntegrityDto {
}
exports.VerifyLogIntegrityDto = VerifyLogIntegrityDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], VerifyLogIntegrityDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], VerifyLogIntegrityDto.prototype, "fromDate", void 0);
__decorate([
    (0, class_validator_1.IsDateString)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], VerifyLogIntegrityDto.prototype, "toDate", void 0);
class UpdateDeviceSyncStatusDto {
}
exports.UpdateDeviceSyncStatusDto = UpdateDeviceSyncStatusDto;
__decorate([
    (0, class_validator_1.IsUUID)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], UpdateDeviceSyncStatusDto.prototype, "deviceId", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.DeviceOperationStatus),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", typeof (_b = typeof client_1.DeviceOperationStatus !== "undefined" && client_1.DeviceOperationStatus) === "function" ? _b : Object)
], UpdateDeviceSyncStatusDto.prototype, "status", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], UpdateDeviceSyncStatusDto.prototype, "notes", void 0);


/***/ }),
/* 258 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LicenseModule = void 0;
const common_1 = __webpack_require__(2);
const schedule_1 = __webpack_require__(9);
const license_service_1 = __webpack_require__(259);
let LicenseModule = class LicenseModule {
};
exports.LicenseModule = LicenseModule;
exports.LicenseModule = LicenseModule = __decorate([
    (0, common_1.Global)(),
    (0, common_1.Module)({
        imports: [schedule_1.ScheduleModule.forRoot()],
        providers: [license_service_1.LicenseService],
        exports: [license_service_1.LicenseService],
    })
], LicenseModule);


/***/ }),
/* 259 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var LicenseService_1;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LicenseService = void 0;
const common_1 = __webpack_require__(2);
const schedule_1 = __webpack_require__(9);
const license_validator_1 = __webpack_require__(260);
let LicenseService = LicenseService_1 = class LicenseService {
    constructor() {
        this.logger = new common_1.Logger(LicenseService_1.name);
        this.cachedPayload = null;
    }
    onApplicationBootstrap() {
        const result = (0, license_validator_1.validateLicense)();
        if (result.status === 'VALID') {
            this.cachedPayload = result.payload ?? null;
            this.logger.log(`License active — client: "${this.cachedPayload?.clientName}", ` +
                `expires: ${this.cachedPayload?.expiresAt ?? 'perpetual'}`);
        }
    }
    async periodicReValidation() {
        this.logger.debug('Running periodic license re-validation…');
        const result = (0, license_validator_1.validateLicense)();
        if (result.status !== 'VALID') {
            this.logger.error(`Periodic license check FAILED: ${result.status} — ${result.message}`);
            await new Promise((r) => setTimeout(r, 2000));
            process.exit(1);
        }
        this.logger.debug('Periodic license check passed.');
    }
    getLicenseInfo() {
        return {
            clientName: this.cachedPayload?.clientName ?? 'Unknown',
            licensedTo: this.cachedPayload?.licensedTo ?? 'Unknown',
            issuedAt: this.cachedPayload?.issuedAt,
            expiresAt: this.cachedPayload?.expiresAt ?? 'perpetual',
            features: this.cachedPayload?.features ?? [],
        };
    }
    getMachineInfo() {
        const fp = (0, license_validator_1.collectMachineFingerprint)();
        return {
            machineId: fp.machineId,
            macAddresses: fp.macAddresses,
            hostname: fp.hostname,
            platform: fp.platform,
        };
    }
};
exports.LicenseService = LicenseService;
__decorate([
    (0, schedule_1.Cron)(schedule_1.CronExpression.EVERY_6_HOURS),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], LicenseService.prototype, "periodicReValidation", null);
exports.LicenseService = LicenseService = LicenseService_1 = __decorate([
    (0, common_1.Injectable)()
], LicenseService);


/***/ }),
/* 260 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.collectMachineFingerprint = collectMachineFingerprint;
exports.encryptLicense = encryptLicense;
exports.validateLicense = validateLicense;
exports.enforeceLicenseOrExit = enforeceLicenseOrExit;
const crypto = __webpack_require__(19);
const fs = __webpack_require__(261);
const os = __webpack_require__(262);
const path = __webpack_require__(263);
const child_process_1 = __webpack_require__(264);
const _k1 = 'M1MS';
const _k2 = 'H05P1T4L';
const _k3 = 'PR0T3CT3D';
const _k4 = '2026-K3Y';
const EMBEDDED_MASTER_KEY = process.env.LICENSE_MASTER_KEY ||
    `${_k1}-${_k2}-${_k3}-${_k4}`;
const LICENSE_FILE_PATHS = [
    path.join(process.cwd(), 'license.key'),
    path.join(path.dirname(process.execPath ?? ''), 'license.key'),
    '/etc/mims/license.key',
    path.join(os.homedir(), '.mims', 'license.key'),
];
const AUDIT_LOG_PATH = path.join(process.cwd(), 'license-audit.log');
function deriveKey(masterKey, salt) {
    return crypto.scryptSync(masterKey, salt, 32);
}
function auditLog(message) {
    const entry = `[${new Date().toISOString()}] ${message}\n`;
    try {
        fs.appendFileSync(AUDIT_LOG_PATH, entry, 'utf8');
    }
    catch {
    }
    console.error(`[LICENSE] ${message}`);
}
function getMachineId() {
    try {
        if (process.platform === 'linux') {
            const files = ['/etc/machine-id', '/var/lib/dbus/machine-id'];
            for (const f of files) {
                if (fs.existsSync(f)) {
                    return fs.readFileSync(f, 'utf8').trim();
                }
            }
        }
        if (process.platform === 'darwin') {
            const raw = (0, child_process_1.execSync)("ioreg -rd1 -c IOPlatformExpertDevice | awk '/IOPlatformUUID/ { print $3; }'", { encoding: 'utf8', timeout: 5000 });
            return raw.trim().replace(/["]/g, '');
        }
        if (process.platform === 'win32') {
            const raw = (0, child_process_1.execSync)('wmic csproduct get uuid', {
                encoding: 'utf8',
                timeout: 5000,
            });
            const lines = raw
                .split('\n')
                .map((l) => l.trim())
                .filter((l) => l && !l.toUpperCase().startsWith('UUID'));
            return lines[0] ?? '';
        }
    }
    catch {
    }
    return '';
}
function getMacAddresses() {
    const ifaces = os.networkInterfaces();
    const macs = new Set();
    for (const iface of Object.values(ifaces)) {
        if (!iface)
            continue;
        for (const addr of iface) {
            if (!addr.internal && addr.mac && addr.mac !== '00:00:00:00:00:00') {
                macs.add(addr.mac.toLowerCase());
            }
        }
    }
    return [...macs];
}
function collectMachineFingerprint() {
    return {
        machineId: getMachineId(),
        macAddresses: getMacAddresses(),
        hostname: os.hostname(),
        platform: process.platform,
    };
}
function encryptLicense(payload, masterKey) {
    const key = masterKey ?? EMBEDDED_MASTER_KEY;
    const salt = crypto.randomBytes(16);
    const iv = crypto.randomBytes(12);
    const derivedKey = deriveKey(key, salt);
    const cipher = crypto.createCipheriv('aes-256-gcm', derivedKey, iv);
    const plaintext = JSON.stringify(payload);
    const ciphertext = Buffer.concat([
        cipher.update(plaintext, 'utf8'),
        cipher.final(),
    ]);
    const authTag = cipher.getAuthTag();
    return Buffer.concat([salt, iv, authTag, ciphertext]).toString('hex');
}
function decryptLicense(hex, masterKey) {
    const key = masterKey ?? EMBEDDED_MASTER_KEY;
    const raw = Buffer.from(hex.trim(), 'hex');
    const salt = raw.subarray(0, 16);
    const iv = raw.subarray(16, 28);
    const authTag = raw.subarray(28, 44);
    const ciphertext = raw.subarray(44);
    const derivedKey = deriveKey(key, salt);
    const decipher = crypto.createDecipheriv('aes-256-gcm', derivedKey, iv);
    decipher.setAuthTag(authTag);
    const plaintext = decipher.update(ciphertext).toString('utf8') + decipher.final('utf8');
    return JSON.parse(plaintext);
}
function computeSignature(payload, masterKey) {
    const { signature: _, ...rest } = payload;
    const data = JSON.stringify(rest, Object.keys(rest).sort());
    return crypto
        .createHmac('sha256', masterKey)
        .update(data)
        .digest('hex');
}
function findLicenseFile() {
    for (const p of LICENSE_FILE_PATHS) {
        if (fs.existsSync(p))
            return p;
    }
    return null;
}
function checkExpiry(expiresAt) {
    if (!expiresAt)
        return true;
    return new Date(expiresAt) > new Date();
}
function machineMatches(fingerprint, payload) {
    const idMatch = payload.machineIds.length === 0 ||
        payload.machineIds.includes(fingerprint.machineId);
    const currentMacs = new Set(fingerprint.macAddresses);
    const macMatch = payload.macAddresses.length === 0 ||
        payload.macAddresses.some((m) => currentMacs.has(m.toLowerCase()));
    return idMatch && macMatch;
}
function validateLicense() {
    const licenseFile = findLicenseFile();
    if (!licenseFile) {
        return {
            status: 'NOT_FOUND',
            message: 'License file not found. Expected location: ' +
                LICENSE_FILE_PATHS.join(', '),
        };
    }
    let payload;
    try {
        const raw = fs.readFileSync(licenseFile, 'utf8');
        payload = decryptLicense(raw);
    }
    catch {
        return { status: 'CORRUPT', message: 'License file is corrupt or tampered.' };
    }
    const expectedSig = computeSignature(payload, EMBEDDED_MASTER_KEY);
    if (payload.signature && payload.signature !== expectedSig) {
        return { status: 'INVALID_SIGNATURE', message: 'License signature is invalid.' };
    }
    if (!checkExpiry(payload.expiresAt)) {
        return {
            status: 'EXPIRED',
            message: `License expired on ${payload.expiresAt}. Contact your vendor to renew.`,
        };
    }
    const fingerprint = collectMachineFingerprint();
    if (!machineMatches(fingerprint, payload)) {
        return {
            status: 'MACHINE_MISMATCH',
            message: `This license is bound to a different machine. ` +
                `Current machine-id: "${fingerprint.machineId}", ` +
                `MACs: [${fingerprint.macAddresses.join(', ')}]. ` +
                `Contact your vendor to re-issue the license.`,
        };
    }
    return {
        status: 'VALID',
        payload,
        message: `License valid — client: "${payload.clientName}", expires: ${payload.expiresAt ?? 'perpetual'}`,
    };
}
async function enforeceLicenseOrExit() {
    if (process.env.SKIP_LICENSE_CHECK === 'true' &&
        process.env.NODE_ENV !== 'production') {
        console.warn('⚠️  [LICENSE] SKIP_LICENSE_CHECK=true — license validation bypassed (development only).');
        return {};
    }
    const result = validateLicense();
    if (result.status !== 'VALID') {
        const host = os.hostname();
        const fingerprint = collectMachineFingerprint();
        auditLog(`UNAUTHORIZED START ATTEMPT | status=${result.status} | host=${host} | ` +
            `machine-id="${fingerprint.machineId}" | MACs=[${fingerprint.macAddresses.join(',')}] | ` +
            `message="${result.message}"`);
        console.error('\n╔══════════════════════════════════════════════════╗');
        console.error('║           M-IMS LICENSE VALIDATION FAILED         ║');
        console.error('╠══════════════════════════════════════════════════╣');
        console.error(`║  Status  : ${result.status.padEnd(38)} ║`);
        console.error(`║  Reason  : ${result.message.substring(0, 38).padEnd(38)} ║`);
        console.error('║                                                    ║');
        console.error('║  Contact your vendor to obtain a valid license.    ║');
        console.error('╚══════════════════════════════════════════════════╝\n');
        await new Promise((r) => setTimeout(r, 3000));
        process.exit(1);
    }
    console.log(`✅ [LICENSE] ${result.message}`);
    return result.payload;
}


/***/ }),
/* 261 */
/***/ ((module) => {

module.exports = require("fs");

/***/ }),
/* 262 */
/***/ ((module) => {

module.exports = require("os");

/***/ }),
/* 263 */
/***/ ((module) => {

module.exports = require("path");

/***/ }),
/* 264 */
/***/ ((module) => {

module.exports = require("child_process");

/***/ }),
/* 265 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HospitalsModule = void 0;
const common_1 = __webpack_require__(2);
const hospitals_controller_1 = __webpack_require__(266);
const hospitals_service_1 = __webpack_require__(267);
const database_module_1 = __webpack_require__(52);
let HospitalsModule = class HospitalsModule {
};
exports.HospitalsModule = HospitalsModule;
exports.HospitalsModule = HospitalsModule = __decorate([
    (0, common_1.Module)({
        imports: [database_module_1.DatabaseModule],
        controllers: [hospitals_controller_1.HospitalsController],
        providers: [hospitals_service_1.HospitalsService],
        exports: [hospitals_service_1.HospitalsService],
    })
], HospitalsModule);


/***/ }),
/* 266 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HospitalsController = void 0;
const common_1 = __webpack_require__(2);
const hospitals_service_1 = __webpack_require__(267);
const dto_1 = __webpack_require__(268);
const jwt_auth_guard_1 = __webpack_require__(29);
const roles_decorator_1 = __webpack_require__(32);
const roles_guard_1 = __webpack_require__(31);
const client_1 = __webpack_require__(16);
let HospitalsController = class HospitalsController {
    constructor(hospitalsService) {
        this.hospitalsService = hospitalsService;
    }
    async findAll() {
        return this.hospitalsService.findAll();
    }
    async findOne(id) {
        return this.hospitalsService.findOne(id);
    }
    async create(createHospitalDto, req) {
        const userId = req.user.id;
        return this.hospitalsService.create(createHospitalDto, userId);
    }
    async update(id, updateHospitalDto, req) {
        const userId = req.user.id;
        return this.hospitalsService.update(id, updateHospitalDto, userId);
    }
    async remove(id, req) {
        const userId = req.user.id;
        return this.hospitalsService.remove(id, userId);
    }
    async findHospitalUsers(id, role, departmentId, subDepartmentId) {
        return this.hospitalsService.findHospitalUsers(id, {
            role,
            departmentId,
            subDepartmentId,
        });
    }
    async addUser(id, createUserDto, req) {
        const createdByUserId = req.user.id;
        return this.hospitalsService.addUser(id, createUserDto, createdByUserId);
    }
    async assignAdmin(id, userId, req) {
        const assignedByUserId = req.user.id;
        return this.hospitalsService.assignAdmin(id, userId, assignedByUserId);
    }
};
exports.HospitalsController = HospitalsController;
__decorate([
    (0, common_1.Get)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "findAll", null);
__decorate([
    (0, common_1.Get)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "findOne", null);
__decorate([
    (0, common_1.Post)(),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN),
    __param(0, (0, common_1.Body)()),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [typeof (_b = typeof dto_1.CreateHospitalDto !== "undefined" && dto_1.CreateHospitalDto) === "function" ? _b : Object, Object]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "create", null);
__decorate([
    (0, common_1.Put)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_c = typeof dto_1.UpdateHospitalDto !== "undefined" && dto_1.UpdateHospitalDto) === "function" ? _c : Object, Object]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "update", null);
__decorate([
    (0, common_1.Delete)(':id'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN),
    (0, common_1.HttpCode)(common_1.HttpStatus.OK),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, Object]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "remove", null);
__decorate([
    (0, common_1.Get)(':id/users'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN, client_1.UserRole.RECEPTIONIST, client_1.UserRole.REGISTRATION_STAFF),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Query)('role')),
    __param(2, (0, common_1.Query)('departmentId')),
    __param(3, (0, common_1.Query)('subDepartmentId')),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_d = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _d : Object, String, String]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "findHospitalUsers", null);
__decorate([
    (0, common_1.Post)(':id/users'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN, client_1.UserRole.HOSPITAL_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)()),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, typeof (_e = typeof dto_1.CreateHospitalUserDto !== "undefined" && dto_1.CreateHospitalUserDto) === "function" ? _e : Object, Object]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "addUser", null);
__decorate([
    (0, common_1.Post)(':id/assign-admin'),
    (0, roles_decorator_1.Roles)(client_1.UserRole.SUPER_ADMIN),
    __param(0, (0, common_1.Param)('id')),
    __param(1, (0, common_1.Body)('userId')),
    __param(2, (0, common_1.Request)()),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String, Object]),
    __metadata("design:returntype", Promise)
], HospitalsController.prototype, "assignAdmin", null);
exports.HospitalsController = HospitalsController = __decorate([
    (0, common_1.Controller)('hospitals'),
    (0, common_1.UseGuards)(jwt_auth_guard_1.JwtAuthGuard, roles_guard_1.RolesGuard),
    __metadata("design:paramtypes", [typeof (_a = typeof hospitals_service_1.HospitalsService !== "undefined" && hospitals_service_1.HospitalsService) === "function" ? _a : Object])
], HospitalsController);


/***/ }),
/* 267 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HospitalsService = void 0;
const common_1 = __webpack_require__(2);
const prisma_service_1 = __webpack_require__(15);
const client_1 = __webpack_require__(16);
const argon2 = __webpack_require__(43);
let HospitalsService = class HospitalsService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    async findAll() {
        return this.prisma.hospital.findMany({
            select: {
                id: true,
                name: true,
                code: true,
                address: true,
                phone: true,
                email: true,
                status: true,
                createdAt: true,
                updatedAt: true,
                _count: {
                    select: {
                        users: true,
                        pharmacies: true,
                        patients: true,
                    },
                },
            },
            orderBy: {
                createdAt: 'desc',
            },
        });
    }
    async findOne(id) {
        const hospital = await this.prisma.hospital.findUnique({
            where: { id },
            include: {
                _count: {
                    select: {
                        users: true,
                        pharmacies: true,
                        patients: true,
                        medicines: true,
                        prescriptions: true,
                        issueTransactions: true,
                    },
                },
            },
        });
        if (!hospital) {
            throw new common_1.NotFoundException(`Hospital with ID ${id} not found`);
        }
        return hospital;
    }
    async create(createHospitalDto, userId) {
        const existingHospital = await this.prisma.hospital.findUnique({
            where: { code: createHospitalDto.code },
        });
        if (existingHospital) {
            throw new common_1.ConflictException(`Hospital with code ${createHospitalDto.code} already exists`);
        }
        const hospital = await this.prisma.hospital.create({
            data: createHospitalDto,
            select: {
                id: true,
                name: true,
                code: true,
                address: true,
                phone: true,
                email: true,
                status: true,
                createdAt: true,
                updatedAt: true,
            },
        });
        if (userId) {
            await this.prisma.auditLog.create({
                data: {
                    hospitalId: hospital.id,
                    userId,
                    entityType: 'HOSPITAL',
                    entityId: hospital.id,
                    action: 'CREATE',
                    beforeState: null,
                    afterState: JSON.parse(JSON.stringify(createHospitalDto)),
                    ipAddress: '0.0.0.0',
                    userAgent: 'API',
                },
            });
        }
        return hospital;
    }
    async update(id, updateHospitalDto, userId) {
        const existingHospital = await this.findOne(id);
        if (updateHospitalDto.code && updateHospitalDto.code !== existingHospital.code) {
            const codeExists = await this.prisma.hospital.findUnique({
                where: { code: updateHospitalDto.code },
            });
            if (codeExists) {
                throw new common_1.ConflictException(`Hospital with code ${updateHospitalDto.code} already exists`);
            }
        }
        const hospital = await this.prisma.hospital.update({
            where: { id },
            data: updateHospitalDto,
            select: {
                id: true,
                name: true,
                code: true,
                address: true,
                phone: true,
                email: true,
                status: true,
                createdAt: true,
                updatedAt: true,
            },
        });
        if (userId) {
            await this.prisma.auditLog.create({
                data: {
                    hospitalId: id,
                    userId,
                    entityType: 'HOSPITAL',
                    entityId: id,
                    action: 'UPDATE',
                    beforeState: JSON.parse(JSON.stringify(existingHospital)),
                    afterState: JSON.parse(JSON.stringify(updateHospitalDto)),
                    ipAddress: '0.0.0.0',
                    userAgent: 'API',
                },
            });
        }
        return hospital;
    }
    async remove(id, userId) {
        await this.findOne(id);
        const hospital = await this.prisma.hospital.update({
            where: { id },
            data: {
                status: 'INACTIVE',
            },
            select: {
                id: true,
                name: true,
                code: true,
                status: true,
            },
        });
        if (userId) {
            await this.prisma.auditLog.create({
                data: {
                    hospitalId: id,
                    userId,
                    entityType: 'HOSPITAL',
                    entityId: id,
                    action: 'DELETE',
                    beforeState: JSON.parse(JSON.stringify(hospital)),
                    afterState: { status: 'INACTIVE' },
                    ipAddress: '0.0.0.0',
                    userAgent: 'API',
                },
            });
        }
        return hospital;
    }
    async findHospitalUsers(hospitalId, filters) {
        await this.findOne(hospitalId);
        const where = { hospitalId };
        if (filters?.role) {
            where.role = filters.role;
        }
        if (filters?.departmentId) {
            where.departmentId = filters.departmentId;
        }
        if (filters?.subDepartmentId) {
            where.subDepartmentId = filters.subDepartmentId;
        }
        return this.prisma.user.findMany({
            where,
            select: {
                id: true,
                email: true,
                fullName: true,
                role: true,
                phone: true,
                status: true,
                lastLogin: true,
                createdAt: true,
                departmentId: true,
                subDepartmentId: true,
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                    },
                },
            },
            orderBy: {
                createdAt: 'desc',
            },
        });
    }
    async addUser(hospitalId, createUserDto, createdBy) {
        await this.findOne(hospitalId);
        if (createUserDto.role === client_1.UserRole.SUPER_ADMIN) {
            throw new common_1.BadRequestException('Cannot create SUPER_ADMIN users via hospital user endpoint');
        }
        const allowedRoles = [
            client_1.UserRole.HOSPITAL_ADMIN,
            client_1.UserRole.DEPARTMENT_ADMIN,
            client_1.UserRole.MAIN_PHARMACY_MANAGER,
            client_1.UserRole.SUB_PHARMACY_MANAGER,
            client_1.UserRole.DOCTOR,
            client_1.UserRole.DOCTOR_ASSISTANT,
            client_1.UserRole.REGISTRATION_STAFF,
            client_1.UserRole.PHARMACY_STAFF,
            client_1.UserRole.AUDITOR,
            client_1.UserRole.LAB_TECHNICIAN,
            client_1.UserRole.RADIOLOGIST,
            client_1.UserRole.NURSE,
            client_1.UserRole.BILLING_STAFF,
            client_1.UserRole.RECEPTIONIST,
        ];
        if (!allowedRoles.includes(createUserDto.role)) {
            throw new common_1.BadRequestException(`Invalid role. Allowed roles: ${allowedRoles.join(', ')}`);
        }
        const existingUser = await this.prisma.user.findUnique({
            where: { email: createUserDto.email },
        });
        if (existingUser) {
            throw new common_1.ConflictException(`User with email ${createUserDto.email} already exists`);
        }
        const pharmacyRoles = [
            client_1.UserRole.MAIN_PHARMACY_MANAGER,
            client_1.UserRole.SUB_PHARMACY_MANAGER,
            client_1.UserRole.PHARMACY_STAFF,
        ];
        if (pharmacyRoles.includes(createUserDto.role)) {
            if (!createUserDto.pharmacyId) {
                throw new common_1.BadRequestException(`Pharmacy ID is required for ${createUserDto.role} role`);
            }
            const pharmacy = await this.prisma.pharmacy.findFirst({
                where: {
                    id: createUserDto.pharmacyId,
                    hospitalId,
                },
            });
            if (!pharmacy) {
                throw new common_1.BadRequestException(`Pharmacy with ID ${createUserDto.pharmacyId} not found in this hospital`);
            }
            if (createUserDto.role === client_1.UserRole.MAIN_PHARMACY_MANAGER &&
                pharmacy.type !== 'MAIN') {
                throw new common_1.BadRequestException('MAIN_PHARMACY_MANAGER can only be assigned to MAIN pharmacy');
            }
            if (createUserDto.role === client_1.UserRole.SUB_PHARMACY_MANAGER &&
                pharmacy.type !== 'SUB') {
                throw new common_1.BadRequestException('SUB_PHARMACY_MANAGER can only be assigned to SUB pharmacy');
            }
        }
        const departmentRoles = [
            client_1.UserRole.DEPARTMENT_ADMIN,
            client_1.UserRole.DOCTOR,
            client_1.UserRole.DOCTOR_ASSISTANT,
            client_1.UserRole.REGISTRATION_STAFF,
            client_1.UserRole.AUDITOR,
            client_1.UserRole.LAB_TECHNICIAN,
            client_1.UserRole.RADIOLOGIST,
            client_1.UserRole.NURSE,
            client_1.UserRole.BILLING_STAFF,
            client_1.UserRole.RECEPTIONIST,
        ];
        if (departmentRoles.includes(createUserDto.role)) {
            if (!createUserDto.departmentId) {
                throw new common_1.BadRequestException(`Department ID is required for ${createUserDto.role} role`);
            }
            const department = await this.prisma.department.findFirst({
                where: {
                    id: createUserDto.departmentId,
                    hospitalId,
                },
            });
            if (!department) {
                throw new common_1.BadRequestException(`Department with ID ${createUserDto.departmentId} not found in this hospital`);
            }
            if (createUserDto.subDepartmentId) {
                const subDepartment = await this.prisma.subDepartment.findFirst({
                    where: {
                        id: createUserDto.subDepartmentId,
                        departmentId: createUserDto.departmentId,
                    },
                });
                if (!subDepartment) {
                    throw new common_1.BadRequestException(`Sub-department with ID ${createUserDto.subDepartmentId} not found for the given department`);
                }
            }
        }
        const passwordHash = await argon2.hash(createUserDto.password);
        const user = await this.prisma.user.create({
            data: {
                email: createUserDto.email,
                passwordHash,
                fullName: createUserDto.fullName,
                phone: createUserDto.phone,
                role: createUserDto.role,
                hospitalId,
                pharmacyId: createUserDto.pharmacyId,
                departmentId: createUserDto.departmentId,
                subDepartmentId: createUserDto.subDepartmentId,
                status: 'ACTIVE',
            },
            select: {
                id: true,
                email: true,
                fullName: true,
                role: true,
                phone: true,
                status: true,
                hospitalId: true,
                pharmacyId: true,
                departmentId: true,
                subDepartmentId: true,
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
                pharmacy: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                        type: true,
                    },
                },
                createdAt: true,
            },
        });
        if (createdBy) {
            await this.prisma.auditLog.create({
                data: {
                    hospitalId,
                    userId: createdBy,
                    entityType: 'USER',
                    entityId: user.id,
                    action: 'CREATE_USER',
                    beforeState: null,
                    afterState: {
                        email: user.email,
                        fullName: user.fullName,
                        role: user.role,
                        hospitalId: user.hospitalId,
                        pharmacyId: user.pharmacyId,
                        departmentId: user.departmentId,
                        subDepartmentId: user.subDepartmentId,
                    },
                    ipAddress: '0.0.0.0',
                    userAgent: 'API',
                },
            });
        }
        return user;
    }
    async assignAdmin(hospitalId, userId, assignedByUserId) {
        await this.findOne(hospitalId);
        const user = await this.prisma.user.findUnique({
            where: { id: userId },
        });
        if (!user) {
            throw new common_1.NotFoundException(`User with ID ${userId} not found`);
        }
        if (user.role !== client_1.UserRole.HOSPITAL_ADMIN) {
            throw new common_1.BadRequestException('User must have HOSPITAL_ADMIN role to be assigned');
        }
        const updatedUser = await this.prisma.user.update({
            where: { id: userId },
            data: {
                hospitalId,
            },
            select: {
                id: true,
                email: true,
                fullName: true,
                role: true,
                hospitalId: true,
                hospital: {
                    select: {
                        id: true,
                        name: true,
                        code: true,
                    },
                },
            },
        });
        if (assignedByUserId) {
            await this.prisma.auditLog.create({
                data: {
                    hospitalId,
                    userId: assignedByUserId,
                    entityType: 'USER',
                    entityId: userId,
                    action: 'ASSIGN_HOSPITAL_ADMIN',
                    beforeState: { hospitalId: user.hospitalId },
                    afterState: { hospitalId },
                    ipAddress: '0.0.0.0',
                    userAgent: 'API',
                },
            });
        }
        return updatedUser;
    }
};
exports.HospitalsService = HospitalsService;
exports.HospitalsService = HospitalsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [typeof (_a = typeof prisma_service_1.PrismaService !== "undefined" && prisma_service_1.PrismaService) === "function" ? _a : Object])
], HospitalsService);


/***/ }),
/* 268 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateHospitalUserDto = exports.UpdateHospitalDto = exports.CreateHospitalDto = void 0;
var create_hospital_dto_1 = __webpack_require__(269);
Object.defineProperty(exports, "CreateHospitalDto", ({ enumerable: true, get: function () { return create_hospital_dto_1.CreateHospitalDto; } }));
var update_hospital_dto_1 = __webpack_require__(270);
Object.defineProperty(exports, "UpdateHospitalDto", ({ enumerable: true, get: function () { return update_hospital_dto_1.UpdateHospitalDto; } }));
var create_user_dto_1 = __webpack_require__(271);
Object.defineProperty(exports, "CreateHospitalUserDto", ({ enumerable: true, get: function () { return create_user_dto_1.CreateHospitalUserDto; } }));


/***/ }),
/* 269 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateHospitalDto = void 0;
const class_validator_1 = __webpack_require__(35);
const class_transformer_1 = __webpack_require__(36);
class CreateHospitalDto {
}
exports.CreateHospitalDto = CreateHospitalDto;
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MinLength)(3),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "name", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MinLength)(2),
    (0, class_validator_1.MaxLength)(10),
    (0, class_transformer_1.Transform)(({ value }) => (typeof value === 'string' ? value.trim().toUpperCase() : value)),
    (0, class_validator_1.Matches)(/^[A-Z0-9-]+$/, { message: 'Hospital code must be uppercase alphanumeric' }),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "code", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MinLength)(5),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "address", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)(),
    (0, class_validator_1.MinLength)(10),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "phone", void 0);
__decorate([
    (0, class_validator_1.IsEmail)(),
    (0, class_validator_1.IsNotEmpty)(),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "email", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(['ACTIVE', 'INACTIVE']),
    __metadata("design:type", String)
], CreateHospitalDto.prototype, "status", void 0);


/***/ }),
/* 270 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UpdateHospitalDto = void 0;
const mapped_types_1 = __webpack_require__(58);
const create_hospital_dto_1 = __webpack_require__(269);
class UpdateHospitalDto extends (0, mapped_types_1.PartialType)(create_hospital_dto_1.CreateHospitalDto) {
}
exports.UpdateHospitalDto = UpdateHospitalDto;


/***/ }),
/* 271 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var _a;
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateHospitalUserDto = void 0;
const class_validator_1 = __webpack_require__(35);
const client_1 = __webpack_require__(16);
class CreateHospitalUserDto {
}
exports.CreateHospitalUserDto = CreateHospitalUserDto;
__decorate([
    (0, class_validator_1.IsEmail)({}, { message: 'Please provide a valid email address' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Email is required' }),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "email", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Password is required' }),
    (0, class_validator_1.MinLength)(8, { message: 'Password must be at least 8 characters' }),
    (0, class_validator_1.Matches)(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&#])[A-Za-z\d@$!%*?&#]/, {
        message: 'Password must contain at least one uppercase letter, one lowercase letter, one number, and one special character',
    }),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "password", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsNotEmpty)({ message: 'Full name is required' }),
    (0, class_validator_1.MinLength)(2, { message: 'Full name must be at least 2 characters' }),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "fullName", void 0);
__decorate([
    (0, class_validator_1.IsString)(),
    (0, class_validator_1.IsOptional)(),
    (0, class_validator_1.Matches)(/^\+?[1-9]\d{1,14}$/, {
        message: 'Please provide a valid phone number',
    }),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "phone", void 0);
__decorate([
    (0, class_validator_1.IsEnum)(client_1.UserRole, { message: 'Invalid user role' }),
    (0, class_validator_1.IsNotEmpty)({ message: 'Role is required' }),
    __metadata("design:type", typeof (_a = typeof client_1.UserRole !== "undefined" && client_1.UserRole) === "function" ? _a : Object)
], CreateHospitalUserDto.prototype, "role", void 0);
__decorate([
    (0, class_validator_1.IsUUID)('4', { message: 'Invalid pharmacy ID' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "pharmacyId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)('4', { message: 'Invalid department ID' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "departmentId", void 0);
__decorate([
    (0, class_validator_1.IsUUID)('4', { message: 'Invalid sub-department ID' }),
    (0, class_validator_1.IsOptional)(),
    __metadata("design:type", String)
], CreateHospitalUserDto.prototype, "subDepartmentId", void 0);


/***/ }),
/* 272 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
const config_1 = __webpack_require__(4);
exports["default"] = (0, config_1.registerAs)('database', () => ({
    url: process.env.DATABASE_URL,
    host: process.env.DB_HOST || 'localhost',
    port: parseInt(process.env.DB_PORT, 10) || 5432,
    username: process.env.DB_USERNAME || 'postgres',
    password: process.env.DB_PASSWORD || 'password',
    database: process.env.DB_NAME || 'mims_db',
    schema: process.env.DB_SCHEMA || 'public',
    ssl: process.env.DB_SSL === 'true',
}));


/***/ }),
/* 273 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
const config_1 = __webpack_require__(4);
exports["default"] = (0, config_1.registerAs)('app', () => ({
    env: process.env.NODE_ENV || 'development',
    port: parseInt(process.env.PORT, 10) || 3001,
    frontendUrl: process.env.FRONTEND_URL || 'http://localhost:3000',
    apiPrefix: process.env.API_PREFIX || 'api/v1',
    jwtSecret: process.env.JWT_SECRET || 'mims-jwt-secret-key-change-in-production',
    jwtExpiresIn: process.env.JWT_EXPIRES_IN || '24h',
    jwtRefreshSecret: process.env.JWT_REFRESH_SECRET || 'mims-refresh-secret-key',
    jwtRefreshExpiresIn: process.env.JWT_REFRESH_EXPIRES_IN || '7d',
    redisHost: process.env.REDIS_HOST || 'localhost',
    redisPort: parseInt(process.env.REDIS_PORT, 10) || 6379,
    redisPassword: process.env.REDIS_PASSWORD || '',
    storageProvider: process.env.STORAGE_PROVIDER || 'local',
    storageEndpoint: process.env.STORAGE_ENDPOINT || 'http://localhost:9000',
    storageAccessKey: process.env.STORAGE_ACCESS_KEY || 'minioadmin',
    storageSecretKey: process.env.STORAGE_SECRET_KEY || 'minioadmin',
    storageBucket: process.env.STORAGE_BUCKET || 'mims-files',
    hospitalTimeZone: process.env.HOSPITAL_TIMEZONE || 'UTC',
    hospitalLocale: process.env.HOSPITAL_LOCALE || 'en-US',
    fifoAllocationEnabled: process.env.FIFO_ALLOCATION_ENABLED !== 'false',
    autoRedistributionEnabled: process.env.AUTO_REDISTRIBUTION_ENABLED !== 'false',
    redistributionIntervalMinutes: parseInt(process.env.REDISTRIBUTION_INTERVAL_MINUTES, 10) || 60,
    offlineSyncEnabled: process.env.OFFLINE_SYNC_ENABLED !== 'false',
    syncIntervalMinutes: parseInt(process.env.SYNC_INTERVAL_MINUTES, 10) || 15,
}));


/***/ }),
/* 274 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.GlobalExceptionFilter = void 0;
const common_1 = __webpack_require__(2);
let GlobalExceptionFilter = class GlobalExceptionFilter {
    catch(exception, host) {
        const ctx = host.switchToHttp();
        const response = ctx.getResponse();
        const request = ctx.getRequest();
        let statusCode = common_1.HttpStatus.INTERNAL_SERVER_ERROR;
        let message = 'Internal Server Error';
        let error = 'InternalServerError';
        let validationErrors = undefined;
        console.error('[EXCEPTION_FILTER]', {
            exception,
            url: request.url,
            method: request.method,
            timestamp: new Date().toISOString(),
        });
        if (exception instanceof common_1.BadRequestException) {
            const response = exception.getResponse();
            statusCode = common_1.HttpStatus.BAD_REQUEST;
            const responseMessage = response?.message;
            if (Array.isArray(responseMessage)) {
                message = responseMessage.length === 1 ? responseMessage[0] : 'Validation Error';
            }
            else if (typeof responseMessage === 'string') {
                message = responseMessage;
            }
            else {
                message = 'Validation Error';
            }
            error = 'BadRequest';
            validationErrors = responseMessage;
        }
        else if (exception instanceof common_1.UnauthorizedException) {
            statusCode = common_1.HttpStatus.UNAUTHORIZED;
            message = 'Invalid credentials or token';
            error = 'Unauthorized';
        }
        else if (exception instanceof common_1.ForbiddenException) {
            statusCode = common_1.HttpStatus.FORBIDDEN;
            message = 'Access denied';
            error = 'Forbidden';
        }
        else if (exception instanceof common_1.NotFoundException) {
            statusCode = common_1.HttpStatus.NOT_FOUND;
            message = 'Resource not found';
            error = 'NotFound';
        }
        else if (exception instanceof common_1.ConflictException) {
            statusCode = common_1.HttpStatus.CONFLICT;
            const exceptionMessage = exception.getResponse()?.message;
            message = exceptionMessage || 'Conflict - Resource already exists';
            error = 'Conflict';
        }
        else if (exception instanceof common_1.HttpException) {
            statusCode = exception.getStatus();
            const response = exception.getResponse();
            if (typeof response === 'object' && 'message' in response) {
                message = response.message || 'An error occurred';
                error = response.error || 'Error';
            }
            else if (typeof response === 'string') {
                message = response;
                error = 'Error';
            }
        }
        else if (exception instanceof Error) {
            console.error('[UNHANDLED_ERROR]', exception.message, exception.stack);
            message = 'Internal Server Error';
            error = 'InternalServerError';
            statusCode = common_1.HttpStatus.INTERNAL_SERVER_ERROR;
        }
        const errorResponse = {
            statusCode,
            message: this.sanitizeMessage(message, statusCode),
            error: this.sanitizeError(error, statusCode),
            timestamp: new Date().toISOString(),
            path: request.url,
        };
        if (validationErrors) {
            if (Array.isArray(validationErrors) && validationErrors.length === 1) {
                errorResponse.details = validationErrors[0];
            }
            else {
                errorResponse.details = validationErrors;
            }
        }
        response.status(statusCode).json(errorResponse);
    }
    sanitizeMessage(message, statusCode) {
        if (statusCode >= 500) {
            return 'An unexpected error occurred. Please try again later.';
        }
        if (statusCode === 401) {
            return 'Authentication failed. Please check your credentials.';
        }
        if (statusCode === 403) {
            return 'You do not have permission to access this resource.';
        }
        if (statusCode === 404) {
            return 'The requested resource was not found.';
        }
        if (statusCode === 409) {
            return message;
        }
        if (statusCode === 400) {
            return message;
        }
        return message;
    }
    sanitizeError(error, statusCode) {
        const errorMap = {
            400: 'BadRequest',
            401: 'Unauthorized',
            403: 'Forbidden',
            404: 'NotFound',
            409: 'Conflict',
            429: 'TooManyRequests',
            500: 'InternalServerError',
            502: 'BadGateway',
            503: 'ServiceUnavailable',
        };
        return errorMap[statusCode] || error;
    }
};
exports.GlobalExceptionFilter = GlobalExceptionFilter;
exports.GlobalExceptionFilter = GlobalExceptionFilter = __decorate([
    (0, common_1.Catch)()
], GlobalExceptionFilter);


/***/ })
/******/ 	]);
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
var exports = __webpack_exports__;

Object.defineProperty(exports, "__esModule", ({ value: true }));
const core_1 = __webpack_require__(1);
const common_1 = __webpack_require__(2);
const swagger_1 = __webpack_require__(3);
const config_1 = __webpack_require__(4);
const helmet_1 = __webpack_require__(5);
const compression = __webpack_require__(6);
const app_module_1 = __webpack_require__(7);
const global_exception_filter_1 = __webpack_require__(274);
const license_validator_1 = __webpack_require__(260);
async function bootstrap() {
    await (0, license_validator_1.enforeceLicenseOrExit)();
    const app = await core_1.NestFactory.create(app_module_1.AppModule);
    const configService = app.get(config_1.ConfigService);
    app.useGlobalFilters(new global_exception_filter_1.GlobalExceptionFilter());
    app.use((0, helmet_1.default)());
    app.use(compression({
        threshold: 1024,
        level: 6,
    }));
    app.enableCors({
        origin: configService.get('FRONTEND_URL') || 'http://localhost:3000',
        credentials: true,
    });
    app.useGlobalPipes(new common_1.ValidationPipe({
        whitelist: true,
        forbidNonWhitelisted: true,
        transform: true,
    }));
    app.setGlobalPrefix('api/v1');
    const config = new swagger_1.DocumentBuilder()
        .setTitle('Hospital Medicine IMS - Attendance Module API')
        .setDescription('Comprehensive REST API for Hospital Attendance Management System with biometric device integration, employee enrollment, shift management, and leave tracking')
        .setVersion('1.0.0')
        .setContact('Hospital IT Support', '', 'support@hospital.local')
        .setLicense('Proprietary', '')
        .addBearerAuth({
        type: 'http',
        scheme: 'bearer',
        bearerFormat: 'JWT',
        description: 'Enter your JWT token',
    }, 'access-token')
        .addTag('Biometric Devices', 'Device registration, management, and monitoring')
        .addTag('Device Status', 'Real-time device status and connectivity monitoring')
        .addTag('Biometric Enrollments', 'Employee biometric enrollment and template management')
        .addTag('Enrollment Templates', 'Fingerprint, face, and iris template storage')
        .addTag('Attendance Records', 'Mark attendance, corrections, and history')
        .addTag('Attendance Reports', 'Attendance analytics and reporting')
        .addTag('Shift Management', 'Shift creation, assignment, and roster management')
        .addTag('Shift Templates', 'Reusable shift patterns and rotations')
        .addTag('Leave Management', 'Leave applications, approvals, and balance tracking')
        .addTag('Leave Types', 'Leave type definitions and policies')
        .addTag('Device Synchronization', 'Real-time device sync and batch operations')
        .addTag('Sync Logs', 'Device synchronization logs and statistics')
        .addTag('System Configuration', 'Attendance configuration and system settings')
        .build();
    const document = swagger_1.SwaggerModule.createDocument(app, config);
    swagger_1.SwaggerModule.setup('api-docs', app, document);
    const port = configService.get('PORT') || 3001;
    await app.listen(port);
    console.log(`🚀 M-IMS Backend API running on: http://localhost:${port}`);
    console.log(`📚 API Documentation: http://localhost:${port}/api/docs`);
}
bootstrap();

})();

/******/ })()
;